# B1 高密度列表范式验收报告

## 基本信息

- 验收日期：2026-06-23
- 验收范围：VXE Table 接入、通用列表组件、6 个样板列表、列表 stub API、列设置、列过滤、列宽拖动、表头拖拽换列、回车查询、勾选、批量确认、同单据锁定
- 测试账号：本地 stub 用户 `本地管理员 / 系统管理员`
- 验收结论：B1 核心列表范式已可用，仍需继续补空态/错误态/无权限态和列偏好持久化
- 等级说明：达到 A1 列表可操作；不是 A2/A3 业务闭环，真实保存/审核/库存回写尚未进入本批

## 通过项

- 已接入 `vxe-table` 和 `xe-utils`，并在 Vue 入口全局注册。
- 新增通用列表组件 `frontend/src/components/DataListPage.vue`，承载筛选、工具栏、VXE 表格、分页、列设置、批量动作确认。
- 新增列表 API service `frontend/src/services/listApi.ts`。
- 新增后端 stub API `GET /api/lists/{listKey}`，统一返回 `page/pageSize/total/rows`。
- 已落 6 个样板列表：`商品资料`、`客户`、`供应商`、`销售订单列表`、`采购入库单`、`库存查询`。
- 列设置入口已可用，支持列显隐和固定方向选择。
- VXE 表格已启用列宽鼠标拖动、固定列、横向表格结构、复选框列。
- 每列表头已提供靠右过滤按钮，浮层按金蝶样式提供 `包含/不包含/等于/不等于/以……开始/以……结束/为空/不为空` 操作符、输入框、重置和确定。
- 支持鼠标按住列标题任意空白/文字区域后拖拽到其它列左侧/右侧，实现列顺序调整；该交互独立于列宽拖动。
- 关键字搜索支持输入后按回车直接查询。
- 列表支持勾选、已选中数量、批量删除/审核确认弹窗。
- 销售订单列表在同一单据已打开时，审核/删除/批量操作锁定并提示。
- 库存查询页面文案明确：不做业务结果缓存，后续真实库存必须走数据库余额口径。

## 验证命令

```bash
cd /Users/linzhenyue/Projects/JDY\ 推理补完/frontend
npm run build

cd /Users/linzhenyue/Projects/JDY\ 推理补完/backend
export JAVA_HOME=/opt/homebrew/opt/openjdk@21/libexec/openjdk.jdk/Contents/Home
export PATH=/opt/homebrew/opt/openjdk@21/bin:$PATH
./mvnw test
```

## 自动化验收

- 前端：`npm run build` 通过。
- 后端：`./mvnw test` 通过，`Tests run: 1, Failures: 0, Errors: 0, Skipped: 0`。
- Playwright：通过 session、商品资料列表、列设置、列显隐、列过滤、列宽拖动、表头拖拽换列、回车查询、勾选、批量确认、销售订单同单据锁定、库存查询 1920 截图。
- 表头拖拽换列验证：拖动 `商品名称` 到 `规格型号` 右侧后，表头顺序由 `商品编码|商品名称|规格型号|...` 变为 `商品编码|规格型号|商品名称|...`；补充验证首个业务列 `单据编号` 可通过表头空白区域拖到第三列位置，列筛选按钮到列右边线距离为 `0px`。
- 6 个样板列表打开验证通过：商品资料、客户、供应商、销售订单列表、采购入库单、库存查询。

## 截图路径

- `/Users/linzhenyue/Projects/JDY 推理补完/output/playwright/b1-list-vxe-1366.png`
- `/Users/linzhenyue/Projects/JDY 推理补完/output/playwright/b1-inventory-list-1920.png`
- `/Users/linzhenyue/Projects/JDY 推理补完/output/playwright/b1-column-filter-1366.png`
- `/Users/linzhenyue/Projects/JDY 推理补完/output/playwright/b1-kingdee-filter-and-column-drag-1366.png`
- `/Users/linzhenyue/Projects/JDY 推理补完/output/playwright/b1-filter-right-and-first-column-drag-1366.png`
- `/Users/linzhenyue/Projects/JDY 推理补完/output/playwright/b1-filter-button-flush-right-header-drag-1366.png`

## 失败项

- 无当前阻断失败项。

## 阻塞项

- 无。

## 剩余风险

- 列设置目前只在当前组件内生效，尚未持久化到用户偏好。
- 空态、错误态、无权限态已有结构预留不足，下一步需要做成可视化可验收状态。
- 当前列表数据仍是 stub，不是真实数据库业务查询。
- VXE Table 首次接入后前端包体变大，后续可按模块做动态加载或拆 chunk。
- 列宽拖动和表头拖拽换列均已做 Playwright 断言；当前手动换列会优先尊重顺序并解除参与换位列的固定状态，后续列偏好持久化时需要补固定列跨区策略和恢复断言。

## 下一步

继续完成 B1 尾项：空态/错误态/无权限态、列设置持久化、列表 API 的排序字段结构，然后再滚动到下一批。
