# D 真实主数据第一批验收报告

## 基本信息

- 验收日期：2026-06-23
- 验收范围：商品、客户、供应商、仓库基础资料真实表读取；库存余额真实口径读取；客户新增写库验收
- 验收结论：D 阶段第一批已从 stub 推进到真实数据库读写，但编辑/停用/删除策略和表单主数据选择器仍待补

## 通过项

- 新增迁移 `V2__master_data_seed.sql`：
  - `md_product` 增加 `category` 字段。
  - 新增 `md_customer`、`md_supplier`。
  - 初始化商品、客户、供应商、仓库和库存余额样例数据。
- `product-master-list`、`customer-master-list`、`supplier-master-list`、`warehouse-master-list` 从数据库读取。
- `inventory-query-list` 从 `inv_stock_balance` 读取现存量和可用量。
- 新增 `POST /api/master-data/{type}`，支持 `product/customer/supplier/warehouse` 新增。
- 前端列表“新增”对商品、客户、供应商、仓库打开标准主数据新增弹窗。
- 客户新增已通过浏览器和 API 双重验证。

## 验证命令

```bash
cd /Users/linzhenyue/Projects/JDY\ 推理补完/frontend
npm run build

cd /Users/linzhenyue/Projects/JDY\ 推理补完/backend
export JAVA_HOME=/opt/homebrew/opt/openjdk@21/libexec/openjdk.jdk/Contents/Home
export PATH=/opt/homebrew/opt/openjdk@21/bin:$PATH
./mvnw test
```

## 验证证据

- Flyway 已应用到 schema version `2`。
- API 验证：
  - `GET /api/lists/product-master-list` 返回数据库商品。
  - `GET /api/lists/customer-master-list` 返回数据库客户。
  - `GET /api/lists/inventory-query-list` 返回数据库库存余额。
- Playwright 验证：
  - 新增客户 `KH-T982097`。
  - 列表关键字查询可见该客户。
  - API 按编码查询 `total=1`。

## 截图路径

- `/Users/linzhenyue/Projects/JDY 推理补完/output/playwright/d-master-customer-create-1366.png`

## 剩余风险

- 当前只完成新增，编辑、停用、删除/归档策略还未做。
- 唯一编码冲突已返回 `409`，但前端错误提示仍是通用文案。
- 销售订单、采购订单尚未引用真实客户/供应商/商品选择器。
- 真实主数据仍是第一批字段，不含价格、税率、分类树、客户信用等扩展资料。
