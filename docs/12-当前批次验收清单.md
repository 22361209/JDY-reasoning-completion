# 当前批次验收清单

**覆盖更新，随批次滚动**——一次只放当前批次，做完归档进交接报告后换下一批，不按批次堆积文件。
通用门禁见 `docs/11`；规格以 `复刻-local/02_复刻规划` + `01_金蝶调研/截图` 为准；铁律见 `CLAUDE.md`。

---

## 当前批次：A5 销售订单下推销售出库闭环

目标：在 A4 销售订单新增可用的基础上，把已审核销售订单推进到销售出库单，完成保存、审核、库存出库和源订单出库状态反写。

### 1. 下推入口

- [x] 销售订单列表工具栏新增 `销售出库` 动作。
- [x] 仅单选、已审核、未全部出库的销售订单允许下推。
- [x] 草稿、未选中、多选、已全部出库时下推按钮不可用。

### 2. 下推预填

- [x] 新增 `GET /api/sales-orders/{billNo}` 详情接口，返回单头、客户编码和明细。
- [x] 下推时打开 `销售出库单` 表单页签。
- [x] 销售出库单自动生成新 `XSCK` 单号。
- [x] 自动带入源销售订单号、客户编码、业务日期、部门、经办人。
- [x] 自动带入源销售订单明细的商品、仓库、数量、单价和金额。

### 3. 保存审核与反写

- [x] 下推出库草稿可保存。
- [x] 下推出库单可审核。
- [x] 审核后销售出库单状态显示 `已审核`。
- [x] 审核后源销售订单出库状态从 `未出库` 反写为 `全部出库`。
- [x] 销售出库列表可查到新销售出库单。

### 4. 验收

- [x] `frontend npm run build` 通过。
- [x] `backend ./mvnw test` 通过。
- [x] API 验证销售订单详情接口返回客户编码 `KH-001` 和明细。
- [x] Playwright 从销售订单列表选中 `XSDD-20260624-095543633-ZP0` 并下推出库。
- [x] Playwright 完成销售出库保存、审核，页面显示 `已审核` 和 `审核成功`。
- [x] Playwright 截图：`output/playwright/a5-sales-order-push-sales-out-1366.png`。
- [x] API 验证源销售订单出库状态为 `全部出库`。
- [x] API 验证销售出库单 `XSCK-20260624-100200351-EQI` 列表回显为 `已审核 / 86.00`。

### 5. 不可触碰

- [x] 不重跑 `build-jdy-feature-approval.mjs`。
- [x] 不手改任何生成物（`docs/01/02/03`、`config/approved-feature-scope.json`、`app/feature-data.js`、`frontend/src/app/featureScope.ts`）。
- [x] 本批无范围例外变化。

### 6. 下一批建议

- [ ] 销售订单列表点击单据编号打开详情。
- [ ] 表单从后端读取已有单据详情，而不是只承载当前前端表单对象。
- [ ] 销售出库单已审核后反审核/红冲的前端验收补齐。
- [ ] 销售订单部分出库场景的前端下推数量控制。
