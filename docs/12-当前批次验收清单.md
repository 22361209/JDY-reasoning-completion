---
taskKind: governance
roadmapItemIds: []
roadmapGateId: null
taskState: in_progress
taskId: A175
title: A23/A25/A128 历史夹具分页、日期与清理治理
timeBudgetMinutes: 240
goalTokenBudget: 12000000
maxCommits: 4
maxConcurrentSubagents: 0
maxTotalSubagents: 0
maxFullGateRuns: 1
fullGateAuthorization: user_authorized_a174_a175_autonomous_completion_2026-08-03
outOfScopePolicy: record-and-stop
---

# 当前任务包

## 目标

修复 A174 有效 full 的两项历史夹具失败，且不修改产品行为：

JDY 不作为本回归治理任务的需求、交互或视觉参考。

- A25 必须以稳定、精确、可清理的方式构造或检索两个同名物料候选，确保粘贴冲突选择器及键盘选择实际被覆盖；不得通过增大固定 `pageSize` 或删除共享物料恢复偶然通过。
- A128 的“本月”期望必须从运行时业务日期推导，并明确时区/跨月边界；页面行为正确时不能再因写死 2026-07 失败。
- A23 与 A25 使用的候选、销售单和物料 cleanup 只读核验；仅当两者确实共享同一污染或分页根因时，才以最小改动纳入。

## 允许路径

- `scripts/a23-*`、`scripts/a25-*`、`scripts/a128-*` 及其直接复用的 selector、日期、fixture、cleanup helper。
- 仅为上述夹具新增或加强精确 ownership/cleanup 证据的回归 helper 与合同。
- 当前任务包、交接、验收报告和归档索引。

## 明确不做

- 不修改生产计划、物料选择器、列表日期快捷过滤等产品行为来迁就脚本。
- 不批量清理共享业务资料、不处理 A126 旧 cleanup 债务、不修改 migration、权限 catalog、正式角色或 tenant 语义。
- 不 push、不操作生产环境；无明确修复和定向证据不得运行 full。

## 验收

- A25 单项可重复通过，真实出现多候选冲突并完成键盘选择；所有 run-owned 物料、单据、审计/会话残留为零。
- A128 单项在运行时当前月通过，并以显式时区规则覆盖月初/月末推导；不依赖硬编码月份。
- A23 仅在受本批共享根因影响时一并通过，且不会扩大候选查询或污染共享数据。
- 相关 `node --check`、`git diff --check`、定向回归、独立 review 通过；最终一次 full 143/143、0 跳过、postflight 通过，才提交收口。

## 停止条件

- 达到 240 分钟、12,000,000 Goal tokens、4 个提交，或发现 3 个范围外失败时停止并报告。
- 需要改变产品逻辑、正式数据/权限/tenant、或重新解释 A174 有效 full 时立即停止。
