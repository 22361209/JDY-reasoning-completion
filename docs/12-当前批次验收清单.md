# 当前批次验收清单

**覆盖更新，随批次滚动**——一次只放当前批次，做完归档进交接报告后换下一批，不按批次堆积文件。
通用门禁见 `docs/11`；规格以 `复刻-local/02_复刻规划` + `01_金蝶调研/截图` 为准；铁律见 `CLAUDE.md`。

---

## 当前批次：A2 采购/出入库业务闭环

目标：把采购订单、采购入库、销售出库从壳层推进到真实数据库单据，并让审核动作复用统一库存过账服务，形成“订单 -> 出入库 -> 库存流水/余额 -> 列表状态”的最小闭环。

### 1. 采购订单

- [x] 建立 `purchase_order / purchase_order_line` 表结构。
- [x] 支持采购订单保存草稿接口。
- [x] 前端采购订单保存接入后端。
- [x] 采购订单列表读取真实数据库。
- [x] 采购订单审核接口可用。

### 2. 采购入库

- [x] 建立 `purchase_in / purchase_in_line` 表结构。
- [x] 支持采购入库单保存草稿接口。
- [x] 前端采购入库单页面可保存。
- [x] 采购入库审核后调用库存过账服务增加库存。
- [x] 采购入库单列表读取真实数据库。
- [x] 采购入库审核后采购订单入库状态更新为全部入库。

### 3. 销售出库

- [x] 建立 `sales_out / sales_out_line` 表结构。
- [x] 支持销售出库单保存草稿接口。
- [x] 前端销售出库单页面可保存。
- [x] 销售出库审核后调用库存过账服务减少库存。
- [x] 销售出库单列表读取真实数据库。
- [x] 销售出库审核后销售订单出库状态更新为全部出库。

### 4. 库存与状态

- [x] 采购入库和销售出库复用 `InventoryPostingService`。
- [x] 库存查询继续只读 `inv_stock_balance`。
- [x] 入库/出库审核写入 `inv_stock_txn` 流水。
- [x] 销售出库继续防止负库存。

### 5. 验收

- [x] `frontend npm run build` 通过。
- [x] `backend ./mvnw test` 通过。
- [x] Flyway schema version 升至 `4`。
- [x] API 验证采购订单保存/审核。
- [x] API 验证采购入库保存/审核，库存增加并写 `PURCHASE_IN` 流水。
- [x] API 验证销售出库保存/审核，库存减少并写 `SALES_OUT` 流水。
- [x] API 验证采购订单列表显示 `全部入库`。
- [x] API 验证销售订单列表显示 `全部出库`。
- [x] Playwright 验证采购订单页面保存/审核。
- [x] Playwright 验证采购入库单页面保存/审核。
- [x] Playwright 验证销售出库单页面保存/审核。

### 6. 不可触碰

- [x] 不重跑 `build-jdy-feature-approval.mjs`。
- [x] 不手改任何生成物（`docs/01/02/03`、`config/approved-feature-scope.json`、`app/feature-data.js`、`frontend/src/app/featureScope.ts`）。
- [ ] 如范围例外变化，只改 `config/implementation-overrides.json`，再按生成链重跑：`export-approved-scope.mjs`、`generate-frontend-scope.mjs`，必要时再跑 `build-app-data.mjs`。
