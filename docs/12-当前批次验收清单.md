# 当前批次验收清单

**覆盖更新，随批次滚动**——一次只放当前批次，做完归档进交接报告后换下一批，不按批次堆积文件。
通用门禁见 `docs/11`；规格以 `复刻-local/02_复刻规划` + `01_金蝶调研/截图` 为准；铁律见 `CLAUDE.md`。

---

## 当前批次：A8 表单明细多行真实渲染

目标：把销售订单、销售出库、采购订单、采购入库表单从“数据已读入但界面主要显示第一行”推进到员工可编辑多行明细，支持新增、删除、逐行选择商品/仓库，并保持保存/审核/打开详情一致。A8 主体已完成；A2-A7 多行/多状态硬回归已补验通过，不再用单行最小样例证明闭环。

### 1. 明细表格

- [x] 四类核心表单使用同一套明细行渲染范式。
- [x] 每一行显示商品编码、商品名称、规格型号、仓库、数量、单价、金额。
- [x] 多行数据打开详情后全部可见，不只显示第一行。
- [x] 金额随数量、单价即时计算。

### 2. 行操作

- [x] 支持新增明细行。
- [x] 支持删除未保存或草稿单据的明细行。
- [x] 已审核、已反审核、已作废、已红冲状态下明细行不可编辑。
- [x] 商品/仓库选择器可按当前行回填，不串行。

### 3. 保存与详情一致性

- [x] 保存草稿时提交全部明细行。
- [x] 审核后再次从列表打开，明细行数量和金额与保存时一致。
- [x] 销售出库、采购入库保留源订单号和全部明细。
- [x] 查询列表打开详情不标记为未保存。

### 4. 验收

- [x] `frontend npm run build` 通过。
- [x] `backend ./mvnw test` 通过。
- [x] Playwright 新建一张至少 3 行销售订单，保存后列表打开详情验证 3 行均可见且合计金额正确。
- [x] Playwright 新建一张至少 3 行采购订单，保存后列表打开详情验证 3 行均可见且合计金额正确。
- [x] Playwright 新建一张至少 3 行销售出库单，保存后列表打开详情验证 3 行均可见且合计金额正确。
- [x] Playwright 新建一张至少 3 行采购入库单，保存后列表打开详情验证 3 行均可见且合计金额正确。
- [x] Playwright 截图：`verification/playwright/a8-sales-order-multiline-1366.png`。
- [x] Playwright 截图：`verification/playwright/a8-purchase-order-multiline-1366.png`。
- [x] Playwright 截图：`verification/playwright/a8-sales-out-multiline-1366.png`。
- [x] Playwright 截图：`verification/playwright/a8-purchase-in-multiline-1366.png`。

### 4.1 A2-A7 多行回归硬门禁

- [x] A8 完成后，准备真实多行 + 多状态回归数据，不得使用自挑单行最小样例。
- [x] 销售订单、销售出库、采购订单、采购入库、生产领料、产品入库每类至少 1 张 >=3 行明细的单据。
- [x] 覆盖适用状态：草稿、已审核、已反审核、红冲；不适用的状态必须在报告中写明原因。
- [x] 列表打开详情后，全部明细行与状态章正确显示。
- [x] Playwright 断言每类单据 `行数 = 3`，且 `合计金额 = 各行金额之和`。
- [x] 输出 `docs/验收报告/验收报告-A2-A7-多行回归-20260624.md`，包含每类单据截图、行数断言和合计断言结果。
- [x] 截图统一输出到 `verification/playwright/`，命名以 `a2-a7-regression-` 开头。

### 5. 不可触碰

- [x] 不重跑 `build-jdy-feature-approval.mjs`。
- [x] 不手改任何生成物（`docs/01/02/03`、`config/approved-feature-scope.json`、`app/feature-data.js`、`frontend/src/app/featureScope.ts`）。
- [ ] 本批如需范围例外，只改 `config/implementation-overrides.json` 并按生成链重跑。

### 6. 后续建议

- [ ] 销售订单部分出库时按剩余可出数量下推。
- [ ] 采购订单部分入库时按剩余可入数量下推。
- [ ] 表格明细键盘操作、行复制、批量填仓库。

---

## 批次坐标映射（固定区）

本表用于把实现侧临时批次挂回复刻规划的 B0-B6 与 277 入口坐标。以后新批次命名必须优先使用 B 体系；若继续使用 A/C/D/E/F/G 临时批号，必须在本表补对应 B 批次与入口。

| 实现批次 | 对应 B 批次 | 复刻-local 入口/样板锚点 | 说明 |
| --- | --- | --- | --- |
| B0 页面外壳 | B0 页面外壳 | 首页/工作台、全站模块入口、基础资料功能面板、库存管理功能面板、页签上限提示 | ERP 容器、左侧导航、功能面板、内部页签、顶部账套期间 |
| B1 高密度列表范式 | B1 高密度列表 | 商品列表、供应商列表、销售订单列表、采购入库列表、设置列表 | 列表工具栏、筛选、表格、分页、列设置、空态/错误态 |
| C 表单范式与主数据选择器 | B2 单据新增/详情；B3 选择器与分录 | 商品选择器、客户选择器、供应商选择器、仓库选择器；销售订单/采购订单分录 | 单头字段、分录表格、选择器回填、未保存关闭确认 |
| D 真实主数据第一批 | B1 高密度列表；B3 选择器与分录 | 236 商品信息、251 客户、252 供应商、255 仓库 | 主数据新增、列表、参照数据源 |
| D2 主数据编辑启停策略 | B1 高密度列表；B3 选择器与分录；B6 设置/权限/工作流 | 236 商品信息、251 客户、252 供应商、255 仓库 | 启用/禁用、引用保护、选择器默认过滤 |
| E 销售订单草稿闭环 | B2 单据新增/详情；B3 选择器与分录 | 037 销售订单 | 销售订单保存草稿、分录、主数据参照 |
| E2 销售订单动作闭环 | B2 单据新增/详情 | 037 销售订单 | 审核、删除、引出、打印和动作状态 |
| F 库存流水与余额闭环 | B2 单据新增/详情；B4 查询报表 | 038 销售出库、072 采购入库、104 库存查询表、107 商品收发明细表 | 审核后库存流水和余额解释 |
| F2 出入库复用库存过账 | B2 单据新增/详情；B4 查询报表 | 038 销售出库、072 采购入库、100/181 生产领料单、102/185 产品入库单 | 出入库共用库存过账服务 |
| G 视觉手感专项 | B0/B1/B2/B3 横向复验 | B1 列表页、销售订单表单、主导航、模块浮层 | 像素和交互专项修正 |
| A2 采购/出入库业务闭环 | B2 单据新增/详情；B3 选择器与分录；B4 查询报表 | 071 采购订单、072 采购入库、038 销售出库、104 库存查询表 | 采购、入库、销售出库最小闭环 |
| A3 七方向业务闭环补完 | B2 单据新增/详情；B4 查询报表；B6 设置/权限/工作流 | 037 销售订单、038 销售出库、071 采购订单、072 采购入库、131/140 收付款、180 生产任务单、181/100 生产领料单、185/102 产品入库单 | 多业务方向状态和流水补齐 |
| A4 销售订单新增可用化 | B2 单据新增/详情；B3 选择器与分录 | 037 销售订单 | 员工可用的新建销售订单 |
| A5 销售订单下推销售出库闭环 | B2 单据新增/详情；B4 查询报表 | 037 销售订单、038 销售出库 | 从销售订单下推销售出库、审核扣库存、回写出库状态 |
| A6 销售订单列表打开详情 | B1 高密度列表；B2 单据新增/详情 | 037 销售订单 | 销售订单列表单据编号打开真实详情 |
| A7 多单据列表打开详情 | B1 高密度列表；B2 单据新增/详情 | 038 销售出库、071 采购订单、072 采购入库 | 多核心单据列表打开详情 |
| A8 表单明细多行真实渲染 | B2 单据新增/详情；B3 选择器与分录 | 037 销售订单、038 销售出库、071 采购订单、072 采购入库、100/181 生产领料单、102/185 产品入库单 | 多行分录显示、编辑、保存和 A2-A7 多行回归门禁 |
