---
taskKind: governance
roadmapItemIds: []
roadmapGateId: null
taskState: in_progress
taskId: A176
title: 采购计划到采购订单闭环
timeBudgetMinutes: 300
goalTokenBudget: 6000000
maxCommits: 3
maxConcurrentSubagents: 0
maxTotalSubagents: 0
maxFullGateRuns: 1
outOfScopePolicy: record-and-stop
---

# 当前任务包

## 目标

修复现场演练发现的采购链断点：已审核采购计划会占用来源采购申请，但采购订单既不能引用采购计划，也不能再从该申请选择同一行，导致“采购计划 → 采购订单 → 采购入库”无法闭环。

本批将采购计划确立为采购订单的正式来源：

- 已审核采购计划可下推生成同供应商的采购订单草稿，并保留计划、采购申请和生产计划来源追溯。
- 采购订单审核按采购计划剩余可订数量校验并写入计划行已订数量；采购申请的已计划占用不得被重复计入已订购占用。
- 采购订单作废、反审核或其他会撤销已审核订单占用的既有生命周期，必须同步恢复计划行可订数量；已有采购入库时保持既有不可逆边界。
- 采购入库继续只从已审核采购订单选源，审核后正常写库存；生产领料必须在采购入库后才能消耗该采购件。

JDY：不作为本批实现参考；以当前 ERP 的采购生命周期、来源追溯、库存过账和 tenant 隔离规范为准。

## 允许路径

- `backend/src/main/java/com/jdy/erp/purchase/application/PurchasePlanAppService.java`、`PurchaseOrderAppService.java` 及直接相关 controller、DTO、查询适配器。
- `frontend/src/modules/purchase/purchase-plan/**`、`purchase-order/**`、直接相关 service、类型和入口装配。
- 本批必需的 forward-only Flyway 迁移、账套 provisioning / migration 校验，以及采购来源选择器契约。
- `scripts/a176-purchase-plan-order-closure-regression.mjs`、既有 A170/A171/A172/A129 采购回归的必要调整、`config/regression-manifest.json` 与其校验。
- `config/feature-delivery-status.json` 中 F032/F033/F034 的已知缺口状态、A176 验收报告、报告索引、交接快照和本文档。

## 明确不做

- 不修改 A166 的列表布局、物料名称、BOM 母件选择或期初录入问题；A166 保持独立、暂停状态。
- 不实现 MRP、库存净需求、提前期、批量策略、供应商绩效、采购预测、成本核算、预付款或跨币种结算。
- 不改变采购入库、生产领料、产品入库的库存与财务过账规则，只补足它们上游的可追溯采购来源。
- 不弱化已审核采购申请、采购计划、采购订单、供应商、仓库和 tenant 的后端校验；不得以让采购申请重新可选的方式绕过已计划占用。
- 不 push；不修改历史任务包、历史验收报告或已关闭迁移。

## 验收

- 采购计划审核后，计划页存在明确的“下推采购订单”动作；一张计划只带入其所属供应商、计划行、仓库、数量、交期和价格快照，不混入其他供应商或计划。
- 下推产生采购订单草稿。草稿不改变采购申请 `ordered_qty`，也不增加采购计划行已订数量；审核订单后，计划行已订数量增加，且不能超过计划数量。
- 采购申请的 `planned_qty` 与已审核采购计划合计一致；由采购计划生成的已审核订单不对同一申请行重复计入 `ordered_qty`。未计划的申请剩余量仍可保留既有“采购申请 → 采购订单”直选流程。
- 采购计划或订单发生会释放来源占用的既有合法撤销动作时，计划行可订数量和申请占用正确恢复；已有已审核采购入库时，不允许破坏来源和库存一致性的撤销。
- 采购订单审核后，采购入库单只可选择该订单的剩余可入数量；采购入库审核后库存增加。计划来源、订单来源与入库来源在单据明细中可追溯。
- 并发或重复审核不会超订：同一计划行并发下推/审核只有合计不超过计划数量的事务可以成功，失败操作不留下部分占用。
- 在隔离测试账套，用两组 run-owned 单据通过可见 Chrome 界面完成：生产计划生成采购申请和生产任务 → 采购计划审核 → 采购订单 → 采购入库审核 → 生产领料 → 产品完工入库。两组均须先完成采购入库再领料；不把旧的“仅生成采购申请”演练计入通过。
- 运行一次前端构建、后端针对性测试、`smoke`、`area:purchase`、A170/A171/A172/A176 定向回归、迁移/provisioning 校验、文档治理检查；所有针对性门禁通过后，最终最多运行一次 full。每项门禁仅在对应改动阶段运行一次。

## 停止条件

- 到达 Goal token、提交或 full 任一有限预算的 70%，或同一任务发生 3 次上下文压缩时，停止实现并汇报已完成范围与剩余项。
- 出现 3 个范围外失败，或发现需要改变生产、库存、财务、权限、租户隔离或历史迁移语义时，记录复现证据并停止，不顺手修复。
- Flyway 迁移不能同时满足公共 schema、现有测试账套和新账套 provisioning，或采购/库存账实核对不一致时，立即停止并报告；不得绕过校验或手工修正业务数据。
