# 当前批次验收清单

**覆盖更新，随批次滚动**——一次只放当前批次，做完归档进交接报告后换下一批，不按批次堆积文件。
通用门禁见 `docs/11`；规格以 `复刻-local/02_复刻规划` + `01_金蝶调研/截图` 为准；铁律见 `CLAUDE.md`。

---

## 当前批次：A6 销售订单列表打开详情

目标：把销售订单列表从“只能查询和下推”推进到员工日常可用的列表-详情路径，单据编号点击后读取后端真实单据并回填表单。

### 1. 列表入口

- [x] 销售订单列表的 `单据编号` 列渲染为可点击入口。
- [x] 点击单据编号不勾选行、不触发批量动作。
- [x] 列表被锁定时单据编号入口禁用，避免覆盖已打开表单。

### 2. 后端详情读取

- [x] 复用 `GET /api/sales-orders/{billNo}` 读取单头和明细。
- [x] 详情返回客户编码、业务日期、部门、经办人、状态和明细行。
- [x] 前端根据后端 `DRAFT / AUDITED / REVERSED / VOID / RED_REVERSED` 映射表单状态。

### 3. 表单回填

- [x] 点击列表单号后打开 `销售订单` 表单页签。
- [x] 表单回填单据编号、客户编码、业务日期、部门、经办人。
- [x] 表单回填商品、仓库、数量、单价和金额。
- [x] 已审核单据打开后显示 `已审核`，保存/审核/删除不可用。
- [x] 打开已有单据不标记为未保存。

### 4. 验收

- [x] `frontend npm run build` 通过。
- [x] `backend ./mvnw test` 通过。
- [x] Playwright 从销售订单列表点击 `XSDD-20260624-095543633-ZP0` 单据编号。
- [x] Playwright 验证表单显示 `已审核`、客户 `KH-001`、日期 `2026-06-24`、明细 `CP-001 / CK-001 / 1 / 86`。
- [x] Playwright 截图：`output/playwright/a6-sales-order-open-detail-1366.png`。

### 5. 不可触碰

- [x] 不重跑 `build-jdy-feature-approval.mjs`。
- [x] 不手改任何生成物（`docs/01/02/03`、`config/approved-feature-scope.json`、`app/feature-data.js`、`frontend/src/app/featureScope.ts`）。
- [x] 本批无范围例外变化。

### 6. 下一批建议

- [ ] 销售出库列表点击单据编号打开详情。
- [ ] 采购订单、采购入库列表点击单据编号打开详情。
- [ ] 表单支持多行明细真实渲染，而不是只显示第一行。
- [ ] 销售订单部分出库时按剩余可出数量下推。
