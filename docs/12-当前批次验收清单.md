---
taskId: null
title: 无活动任务
taskKind: governance
taskState: idle
roadmapItemIds: []
roadmapGateId: null
---

# 当前任务包

当前没有活动任务。A144–A153 的 6E 合同已归档至
`docs/archive/2026-07-16-A144-A153-6E-关闭任务合同.md`；roadmap 0–6E 已关闭，不能通过恢复该合同继续追加工作。

开始任何新任务前，必须用该任务完整替换本文件，并同时更新 `docs/09-交接清单.md`。任务包至少声明：

- `taskId`、标题、`taskKind`（`roadmap` 或 `governance`）和任务状态；
- 唯一 roadmap 坐标，或治理任务使用 `roadmapItemIds: []` 与 `roadmapGateId: null`；
- 业务目标、JDY 参考（如适用）、允许路径和明确不做事项；
- `timeBudgetMinutes`、`goalTokenBudget`、`maxCommits`、`maxConcurrentSubagents`、`maxTotalSubagents`、`maxFullGateRuns`、`outOfScopePolicy`；
- 目标门禁、人工验收、停止条件和独立提交计划。

默认预算及扩展规则见 `AGENTS.md` 与 `docs/guides/codex-execution-policy.md`。没有完整任务包不得开始实现，也不得把历史夹具债务、已关闭功能或 JDY 未确认功能混入新批次。
