# 当前批次验收清单

**覆盖更新，随批次滚动**——一次只放当前批次，做完归档进交接报告后换下一批，不按批次堆积文件。
通用门禁见 `docs/11`；规格以 `复刻-local/02_复刻规划` + `01_金蝶调研/截图` 为准；铁律见 `CLAUDE.md`。

---

## 当前批次：C/D 表单范式与真实主数据第一批

目标：在 B1 高密度列表范式基础上，把基础资料从 stub 推进到真实数据库读写，并沉淀销售/采购类单据表单范式。先完成主数据真实化，再让单据表单引用真实主数据。

### 1. 真实主数据

- [x] 商品、客户、供应商、仓库具备数据库表或迁移。
- [x] 商品、客户、供应商、仓库列表从数据库读取，不再依赖 Java 固定 seed。
- [x] 库存查询从 `inv_stock_balance` 余额口径读取，不做业务结果缓存。
- [x] 商品、客户、供应商、仓库支持新增写入真实表。
- [x] 支持编辑、启用/禁用、删除/停用策略。
- [x] 唯一编码、必填项、状态字段的前后端校验完整展示。

### 2. 表单范式

- [x] 销售订单标准表单：单据头、分录、金额计算、保存草稿。
- [ ] 销售订单审核、删除、引出、打印。
- [x] 采购订单标准表单：复用销售订单布局规则，字段换成供应商口径。
- [x] 未保存提醒和页签关闭确认覆盖新增/编辑场景。
- [x] 单据头字段、分录表格、底部合计在 `1366x768` 下不遮挡。
- [ ] 表单按钮只保留有明确语义和实现路径的动作。

### 3. 主数据与表单衔接

- [x] 销售订单保存时使用真实客户、商品、仓库编码校验，并拒绝禁用资料。
- [x] 表单客户/供应商/商品选择器使用可搜索选择控件。
- [x] 分录商品选择后带出名称、规格、单位。

### 4. 库存流水与余额

- [x] 提供库存调整接口，统一写 `inv_stock_txn` 流水并更新 `inv_stock_balance` 余额。
- [x] 库存调整不允许把现存量/可用量调成负数。
- [x] 库存查询继续只读余额表。
- [x] 销售出库、采购入库复用库存调整服务。

### 5. 验收

- [x] `frontend npm run build` 通过。
- [x] `backend ./mvnw test` 通过。
- [x] Playwright 验证新增客户写入真实表并可在客户列表查询。
- [x] Playwright 验证商品/供应商/仓库新增。
- [x] API 验证客户编辑、启用、禁用、删除即停用完整链路。
- [x] Playwright 验证客户查询后选中编辑，弹窗带出当前筛选行。
- [x] Playwright 验证销售订单表单保存草稿，并在销售订单列表查询可见。
- [x] API 验证库存调整后余额变化，并确认库存流水写入。
- [x] API 验证采购入库增加库存、销售出库减少库存，并确认流水类型。
- [x] Playwright 验证采购订单供应商选择器、销售订单商品选择器和商品带出。
- [x] Playwright 验证表单修改后关闭页签触发未保存确认。
- [x] Playwright 视觉/DOM 检查：导航无首字图标化残留，页面无负坐标/溢出元素。
- [x] Playwright 截图验证列表筛选弹层、销售订单表单在 `1366x768` 和 `1920x1080` 下不遮挡。

### 6. 视觉手感专项

- [x] 左侧主导航取消首字放大图标，保持金蝶式紧凑文字导航。
- [x] 列表分页规格使用 `200/500/1000`。
- [x] 列头筛选按钮靠列标题右侧显示，筛选弹层具备重置、确定、输入框。
- [x] 模块导航浮层关闭时不再参与布局和可访问树测量。
- [x] 列顺序拖拽升级为点住列标题任意位置都可拖动。
- [x] 表单主数据字段升级为可搜索选择器。

### 7. 不可触碰

- [x] 不重跑 `build-jdy-feature-approval.mjs`。
- [x] 不手改任何生成物（`docs/01/02/03`、`config/approved-feature-scope.json`、`app/feature-data.js`、`frontend/src/app/featureScope.ts`）。
- [ ] 如范围例外变化，只改 `config/implementation-overrides.json`，再按生成链重跑：`export-approved-scope.mjs`、`generate-frontend-scope.mjs`，必要时再跑 `build-app-data.mjs`。
