---
taskKind: governance
roadmapItemIds: []
roadmapGateId: null
taskState: in_progress
taskId: A182
title: A181 full失败回归夹具定向收口与真实UI终验
timeBudgetMinutes: unbounded
goalTokenBudget: 15000000
maxCommits: 3
maxConcurrentSubagents: 3
maxTotalSubagents: unbounded
maxFullGateRuns: 0
outOfScopePolicy: record-and-stop
---

# A182 当前任务包

## 目标

在不改生产行为、不重跑 full 的前提下，修复 A181 唯一 full 暴露的 A115、A118、A119 三个回归测试合同，并以三条定向回归证明关闭。随后回到 BLD-TEST，通过 Chrome 可见界面完成 A181 十项缺陷的真实用户终验。

JDY 不作为本治理任务的需求、交互或视觉参考；本批只校准当前 ERP 的既有测试合同与已确认生产行为。

## 显式范围

1. A115 按当前 `DataListPage` 的真实结构校准列表右侧工具区静态断言；如果当前产品结构不符合既定合同，则停止而不是用测试绕过产品问题。
2. A118 为 run-owned 委外物料创建并审核对应“物料名称”，保持原委外、领料、收货、调整、注销和 cleanup 覆盖不变。
3. A119 把 tenant CHECK 拓扑预期从 `118` 校准为 current-head `119`，为委外、报表和销售隔离测试的物料创建已审核“物料名称”，并使委外母件夹具满足 BOM 已有的 `isProduce=true` 前置；保持 tenant 隔离、权限、编号、库存、报表和 cleanup 断言不变。
4. 三条定向回归通过并完成独立 review、commit 后，以 Chrome 可见 UI 逐项复验 A181 十项生产行为。

## 允许路径

- `scripts/a115-metadata-definition-regression.mjs`。
- `scripts/a118-outsourcing-chain-regression.mjs`。
- `backend/src/test/java/**/AccountSetManagementServiceTest.java`、`TenantOutsourcingChainIsolationTest.java`、`TenantReportingIsolationTest.java`、`TenantSalesChainIsolationTest.java`。
- `docs/09-交接清单.md`、本任务包、A181/A182 归档任务包、A182 验收报告和验收报告索引。

## 明确不做

- 不修改 `backend/src/main/**`、`frontend/src/**`、正式 Flyway migration、权限 catalog、tenant 路由、业务状态机或生产数据。
- 不修改共享回归 helper、回归执行器或 `config/regression-manifest.json`；若发现必须修改，先停止并重新评估范围和门禁。
- 不把 A181 历史 full 改写为 147/147，不运行 smoke、area 或 full，不清理与三个失败无关的历史夹具。
- 不 push，不操作生产环境，不直接修改数据库业务数据。

## 验收矩阵

- `node --check scripts/a115-metadata-definition-regression.mjs scripts/a118-outsourcing-chain-regression.mjs` 通过。
- A115、A118 定向执行并通过；A119 首轮在更深层旧夹具缺口处失败，补齐后只重跑 A119 并通过。A119 自身覆盖相关 Java 隔离测试和 current-head 拓扑。
- `git diff --check`、治理文档检查和独立只读 review 通过；review 报告 P0–P3，所有本批发现处理或明确记录。
- 本任务 `maxFullGateRuns: 0`：测试夹具/断言改动不触及生产或共享核心逻辑，按现行分层门禁只做定向回归。
- Chrome 终验仅使用可见界面、视觉、鼠标和键盘；不得调用接口、查看日志、读代码、运行脚本或直接操作数据库。账套仅限 BLD-TEST，测试数据前缀为 `UAT-20260810-序号`。

## 停止条件与预算

- 墙钟时间不限；本批测试收口预计 Codex 墙钟 25–50 分钟，Chrome 十项终验另计；Goal token 上限 15,000,000；最多 3 个提交；并发子代理最多 3 个、累计不限；full 次数为 0。
- Goal token、提交任一有限预算达到 70%，出现 3 个新增范围外失败、3 次上下文压缩或 3 个提交时停止并报告。
- 子代理全部只读；脚本、Java 测试、docs 和最终提交仅由主执行者写入。
- Chrome 终验发现问题先按同样可见步骤复现一次，确认后只记录、不绕过、不修复；继续可独立项并最终汇总。

## 已有基线

- A181 实现提交：`9183c1a1`，未 push。
- A181 唯一 full：147/147 已执行，144 通过、A115/A118/A119 三个脚本失败、0 跳过；postflight 通过且工作区干净。
- A181 十项核心定向脚本中，A87 调拨、A88 盘点、A150 财务报表、A160 主数据发布、A163 资金转账、A167 迁移与多型号生产、A176 UAT 缺陷等均在唯一 full 中通过。

## 定向验收证据

- `node --check` 覆盖 A115、A118 两个变更脚本；`docs-governance-check` 返回 `ok: true`；`git diff --check` 通过。
- A115 定向通过：当前 `DataListPage.vue` 的工具组父子层级、三个按钮顺序及 `base.css` 的 `margin-left: auto` 均被有界断言覆盖。
- A118 最终定向通过：run `1E766BFCD269`，委外加工单 `WWJG000044`、发料单 `WWFL000052`、收货单 `WWRK000052`、退货单 `WWTH000052`、报废单 `WWBF000052`；两条 run-owned 物料名称均创建并审核，BOM 展开、审核/反审核、库存事实和来源边界全过，最终 `productNames/products/documents/transactions/balances/logs/locks/session/users` 残留均为 `0`。
- A119 首轮为 19 项中 18 项通过，唯一错误是委外母件旧夹具缺少 `isProduce=true`，在物料名称前置修复后才走到 BOM 合同并被正确拒绝。只补该测试字段后，最终 8 个隔离套件、19 项测试全部通过，耗时 `30.752s`；临时 tenant schema 均关闭并删除。
- 最终只读 review 的 P0/P1/P3 为 `0`；一项 P2 指出产品编码碰撞行可能消费本轮已接受物料名称，已补为“存在任何外部消费者即停止 cleanup”，并以新 run `1E766BFCD269` 重跑成功路径。最终无未关闭 P0–P3。
- 本批未修改生产代码、正式迁移、共享 helper、runner 或 manifest；未运行 smoke、area 或 full。
