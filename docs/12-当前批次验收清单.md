# 当前批次验收清单

**覆盖更新，随批次滚动**——一次只放当前批次，做完归档进交接报告后换下一批，不按批次堆积文件。
通用门禁见 `docs/11`；规格以 `复刻-local/02_复刻规划` + `01_金蝶调研/截图` 为准；铁律见 `CLAUDE.md`。

---

## 当前批次：A4 销售订单新增可用化

目标：把“销售订单直达新增”从可见壳层推进到员工可实际使用的最小闭环，覆盖入口生成新单、保存、审核、列表回显和已审核保护。

### 1. 前端入口与表单状态

- [x] 首页“销售订单 / 直达新增”打开表单时自动生成新单号，不再复用 `XSDD-00001`。
- [x] 表单内“新增”按钮可重新生成新草稿号、默认客户、日期、部门、经办人和一行明细。
- [x] 单据状态徽标从硬编码“草稿”改为跟随表单状态显示。
- [x] 审核成功后状态显示 `已审核`。
- [x] 审核成功后 `保存 / 审核 / 删除` 置灰，避免覆盖已审核单据。
- [x] 草稿状态仍可保存、审核、删除。

### 2. 后端状态保护

- [x] `POST /api/sales-orders/draft` 禁止覆盖非草稿销售订单。
- [x] 已审核销售订单重复保存返回 `409 Conflict`。
- [x] `POST /api/sales-orders/{billNo}/audit` 只允许草稿审核。
- [x] `DELETE /api/sales-orders/{billNo}` 只允许删除草稿。
- [x] `/error` 放行，业务异常能正常返回 409/400，不再被安全链误拦成 403。

### 3. 验收

- [x] `frontend npm run build` 通过。
- [x] `backend ./mvnw test` 通过。
- [x] Playwright 从首页点击销售订单直达新增，生成新单号 `XSDD-20260624-095543633-ZP0`。
- [x] Playwright 完成保存、审核，页面显示 `已审核` 和 `审核成功`。
- [x] Playwright 截图：`output/playwright/a4-sales-new-save-audit-1366.png`。
- [x] API 列表按新单号查询返回 1 条，状态 `已审核`，金额 `86.00`。
- [x] API 验证同单号再次保存返回 `409 Conflict`。

### 4. 不可触碰

- [x] 不重跑 `build-jdy-feature-approval.mjs`。
- [x] 不手改任何生成物（`docs/01/02/03`、`config/approved-feature-scope.json`、`app/feature-data.js`、`frontend/src/app/featureScope.ts`）。
- [x] 本批无范围例外变化。

### 5. 下一批建议

- [ ] 销售订单列表打开详情。
- [ ] 销售订单下推销售出库。
- [ ] 销售出库审核后反写销售订单出库状态。
- [ ] 销售订单表单从后端读取已存在单据，而不是只承载当前前端表单对象。
