# 当前批次验收清单

**覆盖更新，随批次滚动**——一次只放当前批次，做完归档进交接报告后换下一批，不按批次堆积文件。
通用门禁见 `docs/11`；规格以 `复刻-local/02_复刻规划` + `01_金蝶调研/截图` 为准；铁律见 `CLAUDE.md`。

---

## 当前批次：B1 高密度列表范式

目标：把 B0 外壳里的占位表格升级成可复用的 ERP 高密度列表范式。先做“列表共同骨架”，再落到商品/客户/供应商/销售订单/采购入库/库存查询样板页。

对照规格：`首版页面实现批次与组件复用矩阵-1880收口版.md`、`交互组件复刻规范.md`、`像素级布局复刻规格-1221证据版.md`、列表/查询类截图。

### 1. 表格与列表组件

- [x] 接入 `VXE Table`，不要继续扩写原生表格内核。
- [x] 沉淀标准列表组件：工具栏、筛选区、表格区、分页区、批量动作区。
- [x] 支持列宽、固定列、列显隐、横向滚动、单元格省略提示。
- [x] 支持鼠标按住列标题任意空白/文字区域拖拽换列，区别于列宽拖动；首个业务列也可参与换列。
- [x] 支持勾选、全选、禁用勾选、已选数量提示。
- [ ] 支持加载态、空态、错误态、无权限态。

### 2. 标准列表交互

- [x] 查询条件可展开/收起，常用条件默认一行可扫。
- [x] 查询、重置、刷新、引出、打印、列设置按钮位置稳定。
- [x] 关键字输入后按回车可查询。
- [x] 每列表头有靠右过滤按钮，并按金蝶样式提供操作符、输入框、重置、确定。
- [x] 批量审核/删除等危险动作必须有确认。
- [x] 同一单据已在其它页签打开时，列表审核/删除/批量操作锁定并提示。
- [x] 分页、每页条数、总数展示稳定，不因数据变化跳动。

### 3. 样板页面

- [x] `商品资料` 列表：编码、名称、规格型号、类别、单位、状态、最近更新时间。
- [x] `客户` 列表：客户编码、客户名称、联系人、电话、地区、状态。
- [x] `供应商` 列表：供应商编码、供应商名称、联系人、电话、状态。
- [x] `销售订单` 列表：单据编号、客户、日期、状态、金额、经办人；保留同单据锁定行为。
- [x] `采购入库单` 列表：单据编号、供应商、日期、状态、金额、仓库。
- [x] `库存查询` 列表：商品编码、商品名称、规格型号、仓库、现存量、可用量；不得读取缓存旧库存。

### 4. 后端 stub 与数据边界

- [x] 提供列表查询 stub API，返回分页结构、总数、示例行、状态字段。
- [ ] 列表 API 返回结构统一，预留真实查询条件和排序字段。
- [x] 业务数据仍可用 stub，但不得写成前端硬编码孤岛；至少通过 service 层取数。
- [x] 库存查询 stub 文档注明：第一版真实库存必须走数据库余额表，不允许业务结果缓存。

### 5. 视觉与 A 级门禁

- [x] `1366x768` 下列表工具栏、筛选区、表头、分页不遮挡。
- [x] `1920x1080` 下表格不过度拉散，列宽与密度仍像 ERP 工作台。
- [ ] 文案沿用 `业务术语与文案口径草案`，不自造销售/库存/采购术语。
- [x] Playwright 覆盖：导航进入列表 → 查询/重置 → 列筛选 → 列宽拖动 → 表头拖拽换列 → 勾选/批量动作确认 → 页签切换 → 同单据锁定。
- [x] 输出 B1 验收报告，包含截图路径、通过/失败/阻塞、剩余风险。

### 6. 不可触碰

- [x] 不重跑 `build-jdy-feature-approval.mjs`。
- [x] 不手改任何生成物（`docs/01/02/03`、`config/approved-feature-scope.json`、`app/feature-data.js`、`frontend/src/app/featureScope.ts`）。
- [ ] 如范围例外变化，只改 `config/implementation-overrides.json`，再按生成链重跑：`export-approved-scope.mjs`、`generate-frontend-scope.mjs`，必要时再跑 `build-app-data.mjs`。
