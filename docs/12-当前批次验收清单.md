---
taskKind: governance
roadmapItemIds: []
roadmapGateId: null
taskState: in_progress
taskId: A180
title: 业务单据仓库选择器初始筛选修复
timeBudgetMinutes: unbounded
goalTokenBudget: 15000000
maxCommits: 3
maxConcurrentSubagents: 0
maxTotalSubagents: 0
maxFullGateRuns: 1
outOfScopePolicy: record-and-stop
---

# A180 当前任务包

## 目标

修复 BLD-TEST 真实业务单据仓库整列表选择器的初始筛选行为。当前行已有 `CK-001` 时，打开整列表会把该值作为搜索关键字，用户继续输入 `UAT-20260809-009` 后实际查询 `CK-001UAT-20260809-009`，造成“暂无可选资料”；未主动搜索时也只显示当前值匹配项。修复后，仓库弹窗打开即以空关键字加载当前账套全部“已审核且启用”候选，现有行值不得变成筛选条件。

## 先复现证据

- 当前工作树为 `/Users/linzhenyue/Projects/JDY 推理补完`、分支 `main`，包含 A179 实现提交 `41723c09`。
- Chrome 现场为 `http://127.0.0.1:5173/`、账套 `博莱德机械测试账套 / BLD-TEST`。
- 现场弹窗关键字为 `CK-001UAT-20260809-009`，结果 `0` 条；明确覆盖为 `UAT-20260809-009` 后，实际请求 `/api/lists/warehouse-master-selector?keyword=UAT-20260809-009&page=1&pageSize=100` 返回 `200` 和 `1` 条。
- 因此 A179 的 tenant/审核/启用接口已生效，本任务只修复弹窗把当前仓库值当作初始筛选的前端缺陷。

## 允许路径

- `frontend/src/modules/documents/useDocumentModule.ts`
- `frontend/src/modules/sales/sales-out/useSalesOutDocument.ts`
- 与该行为直接相关的前端回归脚本、回归 manifest、当前任务/交接文档和 A180 验收报告

## 明确不做

- 不修改仓库主数据、tenant 数据、审核/启用状态、数据库迁移或后端候选 SQL。
- 不删除或重命名 CK-001–CK-008 默认仓库，不用 CK-001 绕过 UAT。
- 不保存、审核或删除用户当前销售报价草稿；采购—库存—销售—资金闭环继续由独立验收任务处理。
- 不顺手修复 A115 或其他既有范围外失败，不 push。

## 验收与门禁

1. 失败优先回归必须证明：仓库整列表打开时关键字为空，当前行仓库值只保留在业务行内，不参与候选过滤。
2. 销售报价、销售订单、采购订单、采购入库及销售出库共享路径均满足该规则。
3. 重启实际监听 `5173` 的前端后，在 Chrome 的 BLD-TEST 使用 `UAT-20260809-009` 回归销售报价和采购订单：打开弹窗可直接看到或按编码/名称查到并回填。
4. 运行 A180 定向回归、前端构建、`area:sales`、`area:purchase`、manifest 与文档治理检查；full 最多一次，仅在共享核心风险需要时运行。

## 停止条件

- Goal token、提交或 full 有限预算达到 70%，出现 3 个范围外失败、3 次上下文压缩或 10 个提交时停止并报告。
- 每阶段只运行一次针对性门禁；代码未变化时不重复运行。累计子代理和墙钟时间不设停止上限，本任务不启用子代理。
