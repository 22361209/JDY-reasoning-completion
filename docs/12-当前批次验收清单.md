# 当前批次验收清单

**覆盖更新，随批次滚动**——一次只放当前批次，做完归档进交接报告后换下一批，不按批次堆积文件。
通用门禁见 `docs/11`；规格以 `复刻-local/02_复刻规划` + `01_金蝶调研/截图` 为准；铁律见 `CLAUDE.md`。

---

## 当前批次：A3 七方向业务闭环补完

目标：在 A2 的订单/出入库基础上，把用户列出的 1-7 个方向推进到本机可验收的最小闭环，覆盖状态、流水、列表入口和基础前端交互。

### 1. 部分出入库

- [x] `purchase_order_line.received_qty` 累计采购入库数量。
- [x] `sales_order_line.shipped_qty` 累计销售出库数量。
- [x] 采购订单列表可显示 `未入库 / 部分入库 / 全部入库`。
- [x] 销售订单列表可显示 `未出库 / 部分出库 / 全部出库`。
- [x] API 验证采购 4/10 入库为 `部分入库`，10/10 后为 `全部入库`。
- [x] API 验证销售 3/10 出库为 `部分出库`，10/10 后为 `全部出库`。

### 2. 反审核、作废、冲销流水

- [x] 采购入库支持 `POST /api/purchase-ins/{billNo}/reverse`。
- [x] 销售出库支持 `POST /api/sales-outs/{billNo}/reverse`。
- [x] 反审核写入反向库存流水并回算订单入库/出库状态。
- [x] 采购入库草稿支持作废并显示 `已作废`。
- [x] 销售出库草稿支持作废并显示 `已作废`。
- [x] 采购入库支持 `POST /api/purchase-ins/{billNo}/red-reverse` 生成红字单据。
- [x] 销售出库支持 `POST /api/sales-outs/{billNo}/red-reverse` 生成红字单据。
- [x] 红冲写反向库存流水、红字单金额为负，并回算源订单状态。

### 3. 打印与引出

- [x] 通用单据 CSV 输出：`/api/documents/{documentType}/{billNo}/export.csv`。
- [x] 通用单据 HTML 打印页：`/api/documents/{documentType}/{billNo}/print.html`。
- [x] 覆盖销售订单、采购订单、采购入库单、销售出库单。
- [x] 前端表单 `引出 / 打印` 按钮改为所有已接入单据可用。
- [x] API 验证销售订单 CSV 与采购订单 HTML 内容包含单据号和单据标题。

### 4. 应收应付

- [x] 建立 `ar_receivable / ar_receipt / ap_payable / ap_payment`。
- [x] 已审核销售订单可生成应收单。
- [x] 应收收款支持部分核销和已核销状态。
- [x] 已审核采购订单可生成应付单。
- [x] 应付付款支持已核销状态。
- [x] 应收单/应付单列表读取真实数据库并显示核销状态。

### 5. 生产

- [x] 建立 `prod_bom / prod_bom_line / production_task / production_material_issue / production_completion`。
- [x] 支持保存 BOM。
- [x] 支持创建生产任务。
- [x] 生产领料按 BOM 消耗物料库存。
- [x] 完工入库增加成品库存。
- [x] 生产任务列表显示计划数、已领料数、完工数和状态。

### 6. 权限与审计

- [x] 建立 `sys_role / sys_user_role / sys_permission`。
- [x] V6 初始化 `admin` 用户、`ADMIN/WAREHOUSE/FINANCE` 角色和最小权限码。
- [x] 审核、反审核、作废、生成应收应付、核销、生产动作写入 `sys_operation_log`。
- [x] 用户角色列表读取真实角色和权限。
- [x] 操作日志列表读取真实审计日志。

### 7. 高级交互与列表体验

- [x] 主数据选择器支持 `ArrowDown / ArrowUp / Enter / Escape`。
- [x] 出入库表单增加 `反审核 / 作废` 按钮。
- [x] 新增应收、应付、生产任务、BOM、角色、操作日志列表列配置。
- [x] VXE 列表清理无效 `loading/show-overflow` 参数，Playwright 新会话不再出现该控制台错误。
- [x] 分页保持 `200 / 500 / 1000`。

### 8. 验收

- [x] `frontend npm run build` 通过。
- [x] `backend ./mvnw test` 通过。
- [x] Flyway schema version 升至 `6`。
- [x] API 验证 A3 七方向脚本通过，批次号 `792848`。
- [x] API 验证显式红冲专项通过，批次号 `230205`。
- [x] Playwright 截图：`output/playwright/a3-selector-keyboard-1366.png`。
- [x] Playwright 截图：`output/playwright/a3-receivable-list-clean-1366.png`。
- [x] Playwright 截图：`output/playwright/a3-production-task-list-1366.png`。
- [x] Playwright 截图：`output/playwright/a3-red-reverse-button-1366.png`。

### 9. 不可触碰

- [x] 不重跑 `build-jdy-feature-approval.mjs`。
- [x] 不手改任何生成物（`docs/01/02/03`、`config/approved-feature-scope.json`、`app/feature-data.js`、`frontend/src/app/featureScope.ts`）。
- [x] 本批无范围例外变化；如后续范围例外变化，只改 `config/implementation-overrides.json`，再按生成链重跑：`export-approved-scope.mjs`、`generate-frontend-scope.mjs`，必要时再跑 `build-app-data.mjs`。
