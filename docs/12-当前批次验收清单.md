---
taskKind: governance
roadmapItemIds: []
roadmapGateId: null
taskState: in_progress
taskId: A174
title: A173 回归会话与历史门禁复核修复
timeBudgetMinutes: 180
goalTokenBudget: 8000000
maxCommits: 3
maxConcurrentSubagents: 3
maxTotalSubagents: 3
maxFullGateRuns: 1
outOfScopePolicy: record-and-stop
---

# 当前任务包

## 目标

修复 A173 二次 review 确认的回归治理缺口，使回归执行器本身不污染共享管理员会话，权限拒绝测试不会在 401 时升级为 ADMIN，角色/权限直接 SQL 变更会触发即时共享状态复验，Redis 清理证据不允许空快照；补齐已声明但因客户端依赖缺失而未生效的 Redis HTTP session 运行依赖；隔离身份必须在删除前跨获授权 schema 清零自身审计日志，成功证据只能在 cleanup 后落盘；同时补齐 tenant 测试的隔离身份与整条业务链动态日期，以及已发布迁移的固定 checksum 合同。

本批只修回归基础设施和测试夹具，不修改产品业务行为，不以放宽权限、tenant、迁移、生命周期或安全规则换取通过。

JDY 不作为本治理任务的需求、交互或视觉参考。

## 允许路径与负责人

- 主执行者独占：`docs/09-交接清单.md`、`docs/12-当前批次验收清单.md`、归档任务包、验收报告及报告索引。
- 会话与权限回归负责人：`scripts/helpers/regression-preflight.mjs`、`scripts/helpers/regression-auth.mjs`、A44、A56、A162、A163；修复共享 ADMIN 会话、显式 Cookie 续期、共享状态 mutator 声明和 Redis 非空证据。
- 主执行者独占 `backend/pom.xml`：补齐已由 `application.yml` 声明的 Redis session 所需 Boot Redis starter；同时仅在 Maven Surefire 进程排除 `SessionAutoConfiguration`，保留既有 `MockHttpSession` 集成夹具语义，不改变运行态会话策略、权限规则或产品接口。
- tenant 测试负责人：`TenantSalesChainIsolationTest`、`TenantReportingIsolationTest`；使用隔离测试身份并统一动态业务日期。
- 迁移合同负责人：`scripts/helpers/current-migration-head.mjs` 及 A141/A142/A143/A146/A151 migration regression；固定并验证已发布 V105–V113 checksum，不修改任何 migration SQL。
- 主执行者负责串行整合、目标门禁、最终 full、独立 review 和提交。

## 明确不做

- 不修改产品业务逻辑、数据库 migration SQL、权限 catalog、角色正式授权、tenant 路由或安全策略；Redis starter 仅使既有 `spring.session.store-type: redis` 配置真实生效。
- 不处理 A173 已记录的 A23/A25 分页、多候选和 A128 本月日期失败；该候选顺延为 A175。
- 不清理 A126 或其他历史业务数据，不扩大到非本轮 review 发现。
- 不手改生成范围或交付状态，不 push。

## 验收

- 静态/单元：改动脚本 `node --check`，回归 manifest 与文档治理通过；checksum helper 对仓库 V105–V113 给出固定匹配结果。
- 目标后端：`TenantSalesChainIsolationTest,TenantReportingIsolationTest` 通过，并验证回归专用身份及 session/scope 清理；至少两组既有 `MockHttpSession` 集成用例在 Surefire 测试边界通过。
- 目标脚本：运行态必须返回 Redis `SESSION` Cookie 且能精确定位非空 Redis 主会话；A44 正常与故障注入、A56、A162、A163 以及共享状态检测合同通过；显式低权限 Cookie 的 401 不得以 ADMIN 重试；回归结束后隔离用户、scope、Redis 会话与 public/tenant 审计孤儿必须全部为 0，cleanup 失败不得留下成功 artifact。
- 迁移目标脚本按本批改动执行一次，确认 upgrade/fresh/repeat 与固定 checksum 合同。
- 代码完成后独立 review；无 P0/P1 后提交候选。
- 最终只运行一次 full；范围外既有失败按 `record-and-stop` 记录，不在本批修复。
- full 后 postflight 必须确认角色/权限、安全设置、工作区及本批拥有的回归 session/Redis 残留均符合合同。

## 停止条件

- 任一预算达到 70%、实际耗时超过 360 分钟、出现 3 个范围外失败、经历 3 次上下文压缩或产生 3 个提交时停止并汇报。
- 同一目标门禁在代码未变化时不重复运行；full 配额用完后不得再次运行。
- 发现需要修改产品业务逻辑、正式 migration、权限 catalog 或正式角色授权时立即停止并请求用户确认。
