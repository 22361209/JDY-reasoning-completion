---
taskId: A133
title: G0 生产合同阻塞修复
baseCommit: df5c7cea41b3540eb0930ebb49cb9f72caec28c9
predecessorChangeSet: A132 已由 df5c7cea41b3540eb0930ebb49cb9f72caec28c9 固化阻塞证据，尚未达到 A4
scopeIds: []
roadmapItemIds: []
roadmapGateId: G0-regression-baseline-before-2
engineeringScope:
  - controlled-production-contract-repair
  - shared-ui-density-and-traceability
  - sales-order-draft-audit-quantity-contract
  - red-reverse-operation-audit
  - isolated-preset-fixture-recovery
changeClass: shared-core-and-test-infrastructure
requiredTier:
  - governance-contract
  - backend-fast-tests
  - targeted-regressions
  - smoke
  - full
  - manual-visual
targetAcceptance: A4
manualVisualRequired: true
allowedPaths:
  - docs/09-交接清单.md
  - docs/12-当前批次验收清单.md
  - docs/guides/business-rules.md
  - docs/验收报告/验收报告-A133-G0生产合同阻塞修复-20260712.md
  - frontend/src/modules/sales/sales-order/useSalesOrderDocument.ts
  - frontend/src/modules/sales/sales-out/useSalesOutDocument.ts
  - frontend/src/modules/documents/useDocumentModule.ts
  - frontend/src/components/DocumentDialogs.vue
  - frontend/src/components/TableCore.vue
  - frontend/src/components/DataListPage.vue
  - frontend/src/styles/base.css
  - backend/src/main/java/com/jdy/erp/shared/application/ValidationService.java
  - backend/src/main/java/com/jdy/erp/sales/application/SalesOrderAppService.java
  - backend/src/main/java/com/jdy/erp/sales/application/SalesOutAppService.java
  - backend/src/main/java/com/jdy/erp/purchase/application/PurchaseInAppService.java
  - backend/src/test/java/com/jdy/erp/shared/application/CoreBusinessFastIntegrationTest.java
  - backend/src/test/java/com/jdy/erp/shared/application/RedReverseOperationLogIntegrationTest.java
  - scripts/a30-entry-zero-value-confirm-regression.mjs
  - scripts/a31-zero-value-reason-remark-regression.mjs
  - scripts/a50-operation-log-role-scoped-preset-regression.mjs
  - scripts/a51-operation-log-user-preset-override-regression.mjs
  - scripts/a112-table-core-scroll-regression.mjs
runtimeArtifacts:
  - verification/a133-preset-recovery-before.json
  - verification/a133-preset-recovery-after.json
  - verification/regression-tier-smoke-latest.json
  - verification/regression-tier-full-continue-latest.json
  - verification/**/*-regression.json
  - verification/playwright/**
pendingUserDecisions:
  - F021/F031/发货通知范围归属沿用 A130 待决项，本批不得触碰
---

# 当前批次验收清单

本文件一次只保存一个当前任务。A132 的历史事实、命令和阻塞证据已固化在 `docs/验收报告/验收报告-A132-回归夹具可信基线-20260712.md`。

## 当前批次：A133 G0 生产合同阻塞修复

### 目标

- 只修 A132 真实暴露的七类生产合同阻塞，以及直接导致 A49/A50/A51 不可重复的 preset 夹具隔离问题。
- 让 A132 的 17 个目标失败全部转绿，并恢复 A49、A50、A51 可独立、串行和 full 重复运行。
- 关闭 `G0-regression-baseline-before-2` 的回归基线门；未达到本清单 A4 硬验收前，不进入路线问题 2。
- 以独立合同、生产代码、夹具/测试、验收文档提交收口，不夹带范围外修改。

### 用户已确认的业务决策

- 本批获得七类生产阻塞的完整修复授权，并允许对 BLD-TEST preset 做完整快照后的定向恢复。
- 销售订单草稿数量允许为 `0`，但不得为负数；数量为 `0` 不需要填写原因，也不得把“零数量原因”作为保存条件写入分录备注。
- 销售订单审核时每行数量必须大于 `0`；零数量草稿审核必须以正式业务原因失败且保持草稿。
- 现有“单价为 0”的提醒、原因选择和备注语义不在上述决策内，不得顺带删除。
- preset 只允许恢复被 A50/A51 历史失败污染的默认语义；不得重建账套、清空整表或修改无关 preset。

### 本批只处理

1. A12/A22/A33：销售订单已执行数量、剩余数量和下游追溯入口恢复渲染。
2. A15/A19/A98：销售出库来源追溯打开精确销售订单，同时保留销售订单与发货通知两层来源。
3. A30/A31：草稿数量非负、审核数量为正；数量 `0` 不采集原因，单价 `0` 原合同不变。
4. A37/A41–A45：销售出库和采购入库红字审核成功后，在同一事务写入对称 `RED_REVERSE` 领域日志。
5. A49/A50/A51：夹具在成功和失败路径都精确恢复调用前 preset 快照，并对现存污染做一次可审计定向恢复。
6. A99：正式列表/选择器行高为 `20–22.5px`，单据分录继续保持 `30–33px`。
7. A112：稀疏一行表体填满列表可用区；密集场景改用至少 17 行，在同一 `710px` 视口内验证真实纵向滚动，避免与 A99 的 `20–22.5px` 行高合同形成数学冲突。

### 明确不做

- 不处理权限 fail-open、库存直调入口、tenant 外键、主数据稀疏更新、未知列表 fallback 或任何路线问题 2–6/6A–6E 实现。
- 不修改 migration、范围配置、生成物、编号规则、库存/财务事实或审批范围。
- 不处理 A32、A38、A57、A63、A101、A116、A118 的原范围外失败。
- 不改变销售订单以外单据的草稿数量校验；不把本批规则扩散为全仓生命周期重构。
- 不删除 `CREATE_RED_DRAFT` 或共享 `AUDIT`；不得以改脚本查询条件掩盖缺失的 `RED_REVERSE`。
- 不清理历史业务单据、库存调整或操作日志；preset 恢复不得影响业务事实。
- 不用空白 padding、伪造 `scrollHeight` 或缩短密集场景视口让 A112 换绿；只允许增加真实密集数据行数。

### 实施与验收顺序

1. **合同检查点**：固化用户决策、允许路径、数据恢复边界与 A4 退出条件，先跑治理门禁并独立提交。
2. **前端合同**：分别修执行列、来源追溯、数量零值交互、列表密度和稀疏填充；前端 build 与专项通过后独立 review。
3. **后端合同**：以销售订单最小范围实现草稿非负/审核为正；红字反向事实全部成功后才写领域日志；后端快测通过后独立 review。
4. **夹具隔离**：A50/A51 在 `try/finally` 中保存并恢复精确调用前状态，保留主错误；不得先改数据库迎合脚本。
5. **定向恢复**：保存 BLD-TEST 完整前快照，只修受影响 preset 的默认/残留状态，再保存后快照并证明无关行逐字段不变。
6. **整体验收**：重启到当前构建，先专项，再 smoke/full；输出报告、独立 review、拆分提交和清洁审计。

### RED_REVERSE 精确合同

- 创建红字草稿成功：只写 `CREATE_RED_DRAFT`，不动账，不写 `RED_REVERSE`。
- 审核红字草稿：共享生命周期保留 `AUDIT`。
- 红字校验、库存、来源和财务反向事实全部成功：同一事务再写 `SALES/RED_REVERSE` 或 `PURCHASE/RED_REVERSE`，target 为红字单 ID。
- 失败审核、普通单审核、重复审核不得留下错误或重复的 `RED_REVERSE`；事务回滚必须同时回滚业务事实和领域日志。

### BLD-TEST preset 恢复边界

- 恢复前保存完整逐字段快照，至少包含 `id/list_key/name/role_code/user_name/query/column_filters/shared/is_default/read_only/updated_at`。
- 仅允许处理 `operation-log-list` 下由 A50/A51 创建的 `A50仓库不可见-*`、`A51本人默认-*` 残留，以及恢复既有 `系统默认-红冲审计` 的 ADMIN 只读角色默认语义。
- 保留原系统 preset ID、只读属性、查询条件和角色范围；不得用删后重建替代恢复。
- finally 恢复后必须逐字段等于调用前快照；清理失败不能覆盖主失败，且必须让脚本最终失败并输出两类错误。
- 恢复后保存完整后快照，证明无关 preset 零变化；A49、A50、A51 连跑两轮仍通过。

### 必跑命令

```bash
git diff --check
node scripts/effective-scope-contract-check.mjs
node scripts/feature-delivery-status-scan.mjs
node scripts/validate-regression-manifest.mjs
cd backend && ./mvnw test
cd frontend && npm run build
node scripts/a12-execution-columns-regression.mjs
node scripts/a15-source-trace-regression.mjs
node scripts/a19-source-line-focus-regression.mjs
node scripts/a22-source-line-downstream-regression.mjs
node scripts/a30-entry-zero-value-confirm-regression.mjs
node scripts/a31-zero-value-reason-remark-regression.mjs
node scripts/a33-downstream-impact-chain-regression.mjs
node scripts/a37-operation-log-regression.mjs
node scripts/a41-operation-log-composite-filter-regression.mjs
node scripts/a42-operation-log-operator-target-filter-regression.mjs
node scripts/a43-operation-log-export-regression.mjs
node scripts/a44-operation-log-filter-preset-regression.mjs
node scripts/a45-operation-log-shared-preset-regression.mjs
node scripts/a49-operation-log-default-readonly-preset-regression.mjs
node scripts/a50-operation-log-role-scoped-preset-regression.mjs
node scripts/a51-operation-log-user-preset-override-regression.mjs
node scripts/a98-line-level-source-regression.mjs
node scripts/a99-density-regression.mjs
node scripts/a112-table-core-scroll-regression.mjs
node scripts/run-regression-tier.mjs smoke --continue-on-failure
node scripts/run-regression-tier.mjs full --continue-on-failure
```

### A4 硬验收

- A132 的 17 个目标脚本全部通过；A49/A50/A51 可重复运行且调用前后 preset 精确一致。
- 销售订单草稿 `qty=0` 可保存且不要求原因、不写零数量备注；`qty<0` 保存失败；含零数量草稿审核失败并保持 `DRAFT`。
- 销售和采购红字单成功审核各保留 `CREATE_RED_DRAFT + AUDIT + RED_REVERSE` 三层事件；失败、普通和重复审核无错误/重复领域事件。
- A99 实测列表/选择器 `20–22.5px`、单据分录 `30–33px`；A112 在同一视口下以一行验证稀疏填充、以至少 17 行验证真实密集滚动，并有真实浏览器截图。
- 前端 build、后端全测试、治理门禁均通过；smoke `10/10`，full `102/109`，失败集合必须精确为 A32、A38、A57、A63、A101、A116、A118，且 `skipped=0`。
- diff 只包含 `allowedPaths`；migration、范围配置、生成物和无关数据库行零变化。
- 验收报告记录前后快照摘要、专项结果、smoke/full、视觉证据、剩余 7 项、独立 review 和提交边界；工作区最终干净。

### 停止条件

- 任一修复需要触碰本任务明确排除的路线问题、范围决策、migration 或业务数据清理。
- preset 无法在保留既有系统 ID 和无关行零变化的前提下恢复。
- 红字领域日志不能与反向业务事实保持同一事务，或失败/重复审核留下审计污染。
- full 出现不属于七个既知范围外脚本的新失败，或目标结果只能靠放宽断言、跳过脚本、固定 sleep 获得。

命中停止条件时保留可复现证据并报告，不扩大 `allowedPaths`，不进入路线问题 2。
