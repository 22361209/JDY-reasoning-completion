# 交接快照

每个会话第一份读的文件。**保持简短，覆盖更新，不追加历史。**
铁律见 `CLAUDE.md`；规格见 `复刻-local`；架构底线见 `docs/04/06/07`。

## 当前快照（每次收尾覆盖）

- 更新时间：2026-06-24
- 当前批次/模块：**A64 生产红字单来源原单前端跳转完成**
- 进度：B0/B1/C/D/E/F/G/A2-A64 已完成验收。A8 已补多行明细和 A2-A7 多行/多状态回归。A9-A20 已补订单剩余数量下推、分录效率、下推确认、订单执行列、行排序插入、批量调整、源单追踪、源行号追溯、Excel 多行粘贴、源行号定位、名称规格智能粘贴。A21-A37 已补双方向下推回归、下游执行反查、粘贴候选、仓库应用、键盘流、保存校验、零值原因、打印/引出备注、反审核/红冲风险提示、红字来源追踪、操作日志红冲检索。A38-A53 已补六类单据 PDF/HTML 输出、默认套打模板、打印模板维护、复制、角色默认、生产红冲来源打印和打印纸张/方向/边距/联次。A54-A62 已补权限目录、权限矩阵、前端权限驱动、后端权限拦截、本地 HTTP session 用户切换、用户维护页、正式登录与退出、员工自助改密、会话超时提示、登录失败锁定、安全审计和管理员解除锁定入口，前后端权限按同一登录用户生效。A64 已补生产红字单详情中的来源原单可点击跳转。
- 可跑命令：`cd infra && docker compose up -d`；`cd backend && ./mvnw spring-boot:run`；`cd frontend && npm run dev -- --host 127.0.0.1 --port 5173`
- 本地服务地址：前端 `http://127.0.0.1:5173/`；后端 `http://127.0.0.1:8080/`；PostgreSQL `localhost:5432`；Redis `localhost:6379`
- 数据库状态：Docker context `desktop-linux`；`jdy-erp-postgres`、`jdy-erp-redis` 均 healthy；Flyway schema version `22`
- 最近通过的测试：`node --check scripts/a64-production-red-source-ui-regression.mjs` 通过；`JAVA_HOME=/opt/homebrew/opt/openjdk@21 backend ./mvnw test` 通过；`frontend npm run build` 通过；A64 浏览器回归通过，生产领料/产品入库红字单详情均显示来源原单按钮并能切回原单。
- 当前验收等级：A64 补齐生产红冲前端审计链，红字单详情可从 UI 回到来源原单。
- 已知阻塞：无
- 最近报告：全部归档到 `docs/验收报告/`；最近为 `docs/验收报告/验收报告-A64-生产红字单来源原单前端跳转-20260624.md`、`docs/验收报告/验收报告-A63-打印参数预览与联次设置-20260624.md`、`docs/验收报告/验收报告-A62-锁定解除管理入口-20260624.md`、`docs/验收报告/验收报告-A61-登录失败锁定与安全审计-20260624.md`、`docs/验收报告/验收报告-A60-密码修改与会话超时提示-20260624.md`、`docs/验收报告/验收报告-A59-正式登录与退出-20260624.md`。
- 列表 API 契约：`docs/13-列表API契约.md`
- 批次坐标：`docs/12-当前批次验收清单.md` 固定区维护 A/C/D/E/F/G/A2-A64 ↔ B0-B6 ↔ 复刻-local 入口映射，含 `A2-A7 多行/多状态回归`。
- 验证证据：A64 结构化结果见 `verification/a64-production-red-source-ui-regression.json`；截图见 `verification/playwright/a64-material-issue-bill-no-red-source-visible-20260624145027.png`、`verification/playwright/a64-material-issue-bill-no-source-opened-20260624145027.png`、`verification/playwright/a64-product-in-bill-no-red-source-visible-20260624145027.png`、`verification/playwright/a64-product-in-bill-no-source-opened-20260624145027.png`。
- 验证产物目录：`verification/`；审批 Excel 目录仍为 `outputs/`，不要改。
- 下一步：建议补忘记密码外部链路，或把历史 A57/A58 回归脚本从顶栏切换器迁移到正式登录流。

## 开工前必读（廉价起步）

1. 本快照
2. 当前批次验收清单 `docs/12-当前批次验收清单.md`
3. 目标入口的 `复刻-local` 截图 + 对应 B 批次组件规格
4. 本任务相关代码文件

不要重读全部 docs；规格按需点开 `复刻-local/02_复刻规划`。

## 收尾必更（一行带过即可）

- 高风险逻辑（库存/资金/审核/权限）是否动过、是否测过
- 是否有临时实现/桩、未验证推理补完
- 是否完成截图视觉验收、达到的验收等级（A0-A4）
- 范围有无调整（调整走 `config/implementation-overrides.json` + 重跑生成脚本，勿手改 01/02/03）
