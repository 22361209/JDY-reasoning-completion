# 交接快照

每个会话第一份读的文件。保持简短、覆盖更新、不追加历史。铁律见 `CLAUDE.md`，当前任务见 `docs/12-当前批次验收清单.md`，规格仍以 `JDY-复刻-local` 为准。

## 当前快照

- 更新时间：2026-07-12
- A131 基线提交：`40c4f797132f3b71000a524f5b3a7b1d60b1bcf7`（`40c4f79 建立 A130 项目防偏移治理基线`）
- 当前分支：`codex/review-cleanup-20260627`
- 前序批次：**A130 已由独立提交 `40c4f79` 收口，尚未推送**。有效范围 104 项；交付状态覆盖 93 个 non-exclude 功能，56 个 catalog 入口为 `48 owner + 8 exception`，六类 capability checks 全量登记；回归脚本为 `109 full + 21 exemption`。
- 当前唯一批次：**A131 可复现测试与 Flyway 基线，已完成本地 A4 验收，由本提交独立收口，尚未推送**。
- 当前结果：V90/V91 从未进入 Git，已撤回临时恢复文件；共享 source 保持 93 个 migration、最高 V95。当前库多余历史经备份和隔离试验后由 Flyway 11.7.2 repair 标记删除；后端测试夹具服从自动编号、规范车间和既有生产任务审核规则；正式业务代码零 diff。
- 当前任务包：`docs/12-当前批次验收清单.md`
- 服务实测：后端 `8080`、前端 `5173` 均监听且 ready；PostgreSQL、Redis 容器 healthy。
- 数据库实测：当前库保留原 V90/V91 SQL history，并由 repair 追加 installed_rank 96/97 的 `DELETE` 审计记录；V94/V95 未修改。变更前备份已验证；已发布 V95 库和全新空库均为 93 条成功 history、无 90/91，验收后全部临时库已删除。
- 最近真实门禁：修复后当前库、由 `4037847` 创建的已发布 V95 库、全新空库三种拓扑 `clean test` 均为 48/48；A119 八组租户隔离、effective scope、delivery status、regression manifest、A124、A128、前端 build、`git diff --check` 全部通过。smoke 仍为 4 通过/6 失败且集合与 A130 一致；tracked full 的最近完整基线仍为 38 通过/71 失败/0 skipped。
- 当前范围待确认：`F021` 销售报价单（原 later、已有实现）、`F031` 采购申请（原 exclude、已有实现）、发货通知（已有实现但原审批无 scope ID）。在确认并写入 override/addition 前，不继续扩展这些业务。
- 已确认高风险待办：写接口权限 fail-open、库存直调入口、tenant schema 缺外键、主数据稀疏更新丢字段、操作日志操作人缺失、未知列表错误回落销售数据。
- 最近报告：`docs/验收报告/验收报告-A131-可复现测试与Flyway基线-20260712.md`。
- CI 状态：`.github/workflows/project-gates.yml` 已随 A130 提交；backend job 对应命令已在三种数据库拓扑本地转绿。A130/A131 尚未推送，因此没有远端 workflow 记录。
- 当前阻塞：整个 ERP 未通过发布门禁。smoke 仍有 6 项旧失败，最近 full 仍有 71 项旧失败；三个范围决策继续阻塞对应业务扩展。
- 当前唯一下一步：另开 A132 回归夹具基线批次，先处理 JavaScript 脚本的手工单号与仓库状态，再独立诊断 UI 超时和财务红冲；不得与权限、库存直调或范围决策混做。

## 开工协议

1. 先读本快照和 `docs/12-当前批次验收清单.md`。
2. 只打开当前任务列明的规格锚点和代码；实际改动不得超出 `allowedPaths`。
3. `pendingUserDecisions` 非空时，只允许完成不依赖该决策的治理/诊断，不得推进对应业务实现。
4. 范围只认 `config/approved-feature-scope.json + config/implementation-overrides.json` 的推导结果。
5. 不运行 `build-jdy-feature-approval.mjs`，不手改生成物。

## 收尾协议

- 按改动类型执行 `docs/11` 规定的 smoke / area / full 门禁，并记录真实失败项。
- 输出 `docs/验收报告/` 报告，更新交付状态和本快照。
- 审计 `git status --short --untracked-files=all`、实际 diff 和允许路径，移除任何夹带改动。
- 运行产物留在 ignored `verification/`；关键结论和精选证据进入验收报告。
