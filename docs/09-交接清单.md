# 交接快照

每个会话第一份读的文件。**保持简短，覆盖更新，不追加历史。**
铁律见 `CLAUDE.md`；规格见 `复刻-local`；架构底线见 `docs/04/06/07`。

## 当前快照（每次收尾覆盖）

- 更新时间：2026-06-24
- 当前批次/模块：**A55 权限驱动前端入口与动作完成**
- 进度：B0/B1/C/D/E/F/G/A2/A3/A4/A5/A6/A7/A8/A9/A10/A11/A12/A13/A14/A15/A16/A17/A18/A19/A20 已完成验收；A8 已补多行明细和 A2-A7 多行/多状态回归。A9 已补剩余数量下推和超量审核保护。A10-A20 已补分录效率、下推确认、订单执行列、行排序插入、下推数量批量调整、源单追踪入口、源行号追溯、分录 Excel 多行粘贴、下推确认选中行局部调整、源行号定位源单分录、分录名称规格智能粘贴。A21 已补销售/采购下推确认双方向同级回归。A22 已补源订单分录已执行数量反查下游执行单据。A23 已补分录粘贴同名多规格商品候选选择，并修正真实主数据列表不再被 1000 行演示扩展污染。A24 已补下推确认弹窗按选中行应用仓库，销售/采购下游草稿均继承确认仓库。A25 已补粘贴冲突候选弹窗键盘上下选择、Enter 确认和 Esc 取消。A26 已补主数据选择器选中后自动跳到下一录入字段。A27 已补数量 Enter 跳单价，单价 Enter 新增下一行并聚焦商品编码。A28 已补按钮新增行和单价 Enter 新增行继承上一行仓库。A29 已补保存前移除空白分录、重复商品+仓库阻止保存并提示行号。A30 已补零数量/零单价保存确认。A31 已补零值原因写入分录备注。A32 已补 CSV 引出和打印 HTML 输出分录备注。A33 已补源订单分录下游执行弹窗的反审核/红冲影响链提示。A34 已补销售出库/采购入库反审核、红冲二次确认，并修正红冲后前端加载真实红字单。A35 已补红冲原单反查红字单入口。A36 已补红字单详情反向显示来源原单号入口，并加固为 `red_source_bill_id` 显式关联。A37 已补操作日志按业务单号检索红冲动作。A38 已补单据 PDF 打印输出，前端打印按钮生成 PDF 文件。A39 已补销售订单、采购订单、销售出库、采购入库四类核心单据 PDF 回归门禁。A40 已补生产领料、产品入库 PDF 输出和前端打印门禁。A41 已补操作日志按关键字、状态、模块、动作、起止日期组合筛选。A42 已补操作日志按操作人和对象类型继续收窄。A43 已补操作日志筛选结果 CSV 引出。A44 已补操作日志筛选条件本机预设。A45 已补操作日志后端持久化共享预设，支持空本机缓存加载、应用和删除同步。A46 已补六类单据 PDF/HTML 默认套打模板，含公司抬头、模板名、页脚、签字栏和公司章区域，并用 PDF 预览图复验无明显裁切。A47 已补系统设置/打印模板维护页，保存后真实影响 HTML/PDF 输出，脚本回归后恢复默认模板。A48 已补打印模板另存副本、同单据多模板下拉和默认模板切换，HTML/PDF 按当前默认模板输出，脚本回归后恢复 `STANDARD` 默认。A49 已补操作日志系统默认只读预设，打开操作日志自动应用红冲审计条件，后端拒绝误删/覆盖只读预设。A50 已补操作日志预设按角色下发，当前 `ADMIN` 只加载通用和 ADMIN 预设，不显示 `WAREHOUSE` 预设。A51 已补操作日志用户级预设覆盖角色默认，本人默认优先应用且不影响角色默认。A52 已补打印模板按角色默认，当前 `ADMIN` 可用角色模板覆盖通用模板，HTML/PDF 输出按角色模板渲染。A53 已补生产领料/产品入库红冲来源原单显式关联，详情接口和 HTML/PDF 打印显示来源原单号。A54 已补权限目录、角色权限矩阵管理页和后端保存接口，当前 session 从用户/角色表读取。A55 已补 session 权限码驱动前端功能入口、快捷入口、表单审核、列表关键按钮和系统设置维护动作。
- 可跑命令：`cd infra && docker compose up -d`；`cd backend && ./mvnw spring-boot:run`；`cd frontend && npm run dev -- --host 127.0.0.1 --port 5173`
- 本地服务地址：前端 `http://127.0.0.1:5173/`；后端 `http://127.0.0.1:8080/`；PostgreSQL `localhost:5432`；Redis `localhost:6379`
- 数据库状态：Docker context `desktop-linux`；`jdy-erp-postgres`、`jdy-erp-redis` 均 healthy；Flyway schema version `19`
- 最近通过的测试：`node --check scripts/a55-permission-driven-ui-regression.mjs` 通过；`backend ./mvnw test` 通过；`frontend npm run build` 通过；A55 权限驱动 UI 回归通过，临时移除 ADMIN 权限后 session 和前端入口同步收敛，脚本结束恢复原权限。
- 当前验收等级：A55 补齐前端权限驱动入口过滤和关键动作禁用，均已验证。
- 已知阻塞：无
- 最近报告：全部归档到 `docs/验收报告/`；最近为 `docs/验收报告/验收报告-A55-权限驱动前端入口与动作-20260624.md`、`docs/验收报告/验收报告-A54-权限矩阵管理页-20260624.md`、`docs/验收报告/验收报告-A53-生产红冲来源打印-20260624.md`、`docs/验收报告/验收报告-A52-打印模板按角色默认-20260624.md`、`docs/验收报告/验收报告-A51-操作日志用户级预设覆盖-20260624.md`、`docs/验收报告/验收报告-四项收口-20260624.md`。
- 列表 API 契约：`docs/13-列表API契约.md`
- 批次坐标：`docs/12-当前批次验收清单.md` 固定区维护 A/C/D/E/F/G/A2-A55 ↔ B0-B6 ↔ 复刻-local 入口映射，含 `A2-A7 多行/多状态回归`。
- 截图证据：A55 权限驱动 UI 见 `verification/playwright/a55-permission-driven-ui-20260624131309.png`；结构化结果见 `verification/a55-permission-driven-ui-regression.json`。
- 验证产物目录：`verification/`；审批 Excel 目录仍为 `outputs/`，不要改。
- 下一步：建议把后端 API 也接入统一权限拦截，或补打印参数预览、纸张/边距/联次设置。

## 开工前必读（廉价起步）

1. 本快照
2. 当前批次验收清单 `docs/12-当前批次验收清单.md`
3. 目标入口的 `复刻-local` 截图 + 对应 B 批次组件规格
4. 本任务相关代码文件

不要重读全部 docs；规格按需点开 `复刻-local/02_复刻规划`。

## 收尾必更（一行带过即可）

- 高风险逻辑（库存/资金/审核/权限）是否动过、是否测过
- 是否有临时实现/桩、未验证推理补完
- 是否完成截图视觉验收、达到的验收等级（A0-A4）
- 范围有无调整（调整走 `config/implementation-overrides.json` + 重跑生成脚本，勿手改 01/02/03）
