---
roadmapItemIds:
  - "2"
roadmapGateId: null
---

# 交接快照

每个会话第一份读的文件。保持简短、覆盖更新、不追加历史。铁律见 `CLAUDE.md`，当前任务见 `docs/12-当前批次验收清单.md`，规格仍以 `JDY-复刻-local` 为准。

## 当前快照

- 更新时间：2026-07-12
- 当前分支：`codex/review-cleanup-20260627`；A134 实现收口 `18a3a08a`，未获推送授权。
- 当前唯一批次：**A134 已达到 A4 并关闭路线问题 2**；完整事实见 `docs/验收报告/验收报告-A134-权限与库存写接口闭环-20260712.md`，不得再把本报告范围当作未完成任务重复实现。
- 最终门禁：后端 `113/113`、前端 build、smoke `10/10`、治理门禁通过；full `104/110`、`skipped=0`，失败精确为 A32/A38/A63/A101/A116/A118。
- 写边界：删除两个历史库存旁路后，148 个真实写 handler 100% 唯一分类；8 个非固定权限例外使用 3/3/2 具名 policy 和启动期真实 Handler 扫描，未声明写接口运行时 403。
- 生命周期：16 种 type 共用唯一权限表，单据锁不再维护重复类型集合；`productionTask` 真实 HTTP 为 ADMIN 200、WAREHOUSE 403、未知 type 404。
- 员工/preset：登录前不再请求 `/api/system/users`；PERSONAL 使用 role + username。A49/A50/A51 两轮后 138 行 preset 与 A133 保存快照逐字段一致。
- 库存：生产环境无管理员例外；旧 sales-out/purchase-in 路由 404，adjustment 只允许 local/test/regression + 显式开关 + BLD-TEST + 管理员权限。
- 运行身份：当前 backend health fingerprint 为 `6f49b2985170...` 且测试能力 true；dev-up 已验证 8080 构建身份和 5173 cwd 后复用。
- A134 安全回归每轮 44 请求、9 组断言、五类精确 cleanup；FINANCE 正向收付款、密码找回防枚举和生产误配置均有真实证据。
- 当前任务包：`docs/12-当前批次验收清单.md` 已写入最终状态；A133/A134 历史事实不再回写。
- 当前范围仍待确认：F021 销售报价单、F031 采购申请、发货通知范围归属；A134 不触碰。
- 既有安全债务：公开 session/account-set DTO 仍暴露内部路由元数据；密码找回申请尚无限流/冷却/去重；136 条 display-name 旧 PERSONAL preset 未猜测迁移。
- 路线下一候选为问题 3“操作日志真实 actor”；尚未建立 A135 合同，也未授权实施。
- 服务为 A134 最终构建：后端 `8080`、前端 `5173`、PostgreSQL、Redis healthy。
- CI 状态：本地提交均未推送，因此没有对应远端 workflow 记录。

## 下一批开工协议

1. 先读本快照、A134 报告和当前任务包；不要重做已关闭的路线问题 2。
2. 若继续路线问题 3，先建立独立 A135 合同并把 docs/09/docs/12 的 roadmap 坐标一起切换；未立合同前只允许只读审计。
3. A135 开工前请用户决定旧 display-name preset 迁移、公开 DTO 收窄和密码找回限流的批次归属，不得静默夹带。
4. 保留 A134 的具名 policy、生产库存 fail-closed、dev fingerprint 和 preset 精确恢复门禁；后续任务不得为方便测试回退。
5. 不运行 `build-jdy-feature-approval.mjs`，不手改 scope/status 生成物；F021/F031/发货通知归属仍需用户确认。

## 下一批收尾协议

- 新任务仍须先专项、后端/前端和治理门禁，再跑受影响 area、smoke、full。
- 失败安全必须比较真实副作用；路线问题 3 应补真实 actor 与失败审计，而不是回写固定“本地管理员”。
- 最终审计实际 diff、allowedPaths、服务构建身份和数据恢复；独立 review finding 必须修复或明确记录，不能只报通过。
