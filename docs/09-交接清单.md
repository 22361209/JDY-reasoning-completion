---
roadmapItemIds:
  - "6E"
roadmapGateId: null
---

# 交接快照

每个会话第一份读的文件。保持简短、覆盖更新、不追加历史。铁律见 `CLAUDE.md`，当前任务见 `docs/12-当前批次验收清单.md`，规格仍以 `JDY-复刻-local` 为准。

## 当前快照

- 更新时间：2026-07-14。
- 当前分支：`codex/review-cleanup-20260627`；本地领先远端且尚未 push，精确提交数以 `git status` 为准。
- 当前批次：**A144 6E 报表与当前错误入口合同**；roadmap 6E，范围精确为 F029/F042/F061/F091/F093，基线为 A143 治理关单提交 `9fd55cc7`。A144 只冻结合同和验收矩阵，不实现业务。
- 6E 拆分为 A144-A153 十个独立 change set：A145 报表 SQL 底座、A146 编码规则、A147 库存来源、A148 商品收发明细、A149 销售报表、A150 应收应付报表、A151 材料报废后端、A152 页面与统计、A153 全量与治理发布。
- 金额报表必须按 CNY/USD 分组展示和合计，禁止无汇率依据的跨币种总计；F042/F061 是纯数量报表，不伪造 currency，也不跨单位合计。
- F042 只读真实 `inv_stock_txn`；生产环境不开放库存调整接口。测试夹具仅允许 `local/test/regression` + 显式开关 + 测试账套 allowlist，管理员权限不得绕过环境门禁。
- F061 草稿 `scrapQty=0` 可保存且无需原因，负数禁止；审核必须数量大于 0 且填写原因。报废审核不重复扣减领料库存；`isStockIn=false` 零库存流水，`true` 必须另行整单入报废仓，反审核前先反入库；`reissueQty` 首版仅记录且满足 `0<=reissueQty<=scrapQty`。
- A145-A150 已在独立 worktree 完成实现、测试与提交，尚未整合到主线；必须先提交 A144，再按依赖顺序串行合并共享 report 文件。A151-A153 尚未完成。
- A143 已由提交 `9fd55cc7` 完成治理收口；F008 为 `verified / A4 / published`。其浏览器、门禁和残留证据见 `docs/验收报告/验收报告-A143-基础资料Excel导入-20260714.md`。
- A137 的精确数据删除授权和 A139 的“按建议删除”均已执行完毕，不得泛化为其他数据、历史收付款记录或功能删除。
- `a126-sales-daily-usability-regression.mjs` 缺业务 cleanup/finally，存在历史测试夹具债务；不得模糊删除或简单回减库存，必须另立可审计清理任务，不与 A142、6D 或 6E 混做。

## A144 合同边界

1. 报表底座只负责受控 registry、固定 SQL、分页、totals、CSV、权限与 tenant scope；不得成为跨领域万能查询器。
2. 当前错误入口只能绑定真实 report key 或保持隐藏，不得恢复未知 list key 的销售订单 fallback。
3. F042 库存来源与 F061 正式材料报废先达到 A4，相关报表才允许按 A3 发布；“有统计页”不能替代底层业务闭环。
4. F029/F091 的金额口径按币种隔离，F042/F061 的数量口径按商品与单位快照隔离；页面、分页、合计与 CSV 必须共享同一 predicate。
5. A144-A153 每个 change set 独立 review、测试和 commit；共享 registry、类型、catalog 与 UI 只能按依赖顺序串行整合。
6. 6E 最终发布前必须完成双视口浏览器、迁移、area、smoke、full、治理回写与数据库残留检查。

## 下一步

1. 完成 A144 正式门禁、独立 review 与提交；合同提交不得夹带 A145-A153 业务实现。
2. 串行整合 A145-A150，并在每一步解决共享 report 文件冲突和复跑对应门禁。
3. 在 A145/A146/A147 基线上实施 A151，再由 A152 完成页面与材料报废统计。
4. A153 统一完成真实浏览器、全量门禁和治理发布；在此之前 F042/F061 不提前发布 catalog。
5. 用户未要求 push，当前继续保持本地提交。
