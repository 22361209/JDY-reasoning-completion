# 交接快照

每个会话第一份读的文件。**保持简短，覆盖更新，不追加历史。**
铁律见 `CLAUDE.md`；规格见 `复刻-local`；架构底线见 `docs/04/06/07`。

## 当前快照（每次收尾覆盖）

- 更新时间：2026-06-26
- 当前批次/模块：**A101 库存预警已完成**。库存预警是非单据查询+配置：新增安全库存阈值表 `inv_safety_stock_setting`，按商品+仓库维护最低安全量和可选库存上限；预警列表直查 `inv_stock_balance + inv_safety_stock_setting`，显示低于安全库存和高于库存上限的商品仓库；前端库存管理新增“库存预警查询表”和“安全库存设置”入口，走 `DataListPage`，不上审核/过账/单据骨架。
- 进度：B0/B1/C/D/E/F/G/A2-A78 已完成验收。A8 已补多行明细和 A2-A7 多行/多状态回归。A9-A20 已补订单剩余数量下推、分录效率、旧下推确认退役、订单执行列、行排序插入、源单追踪、源行号追溯、Excel 多行粘贴、源行号定位、名称规格智能粘贴。A21-A37 已补双方向旧下推确认退役、下游执行反查、粘贴候选、仓库应用退役、键盘流、保存校验、零值原因、打印/引出备注、反审核/红冲风险提示、红字来源追踪、操作日志红冲检索。A38-A53 已补六类单据 PDF/HTML 输出、默认套打模板、打印模板维护、复制、角色默认、生产红冲来源打印和打印纸张/方向/边距/联次。A54-A62 已补权限目录、权限矩阵、前端权限驱动、后端权限拦截、本地 HTTP session 用户切换、用户维护页、正式登录与退出、员工自助改密、会话超时提示、登录失败锁定、安全审计和管理员解除锁定入口，前后端权限按同一登录用户生效。A64-A77 已补生产红字来源跳转、找回密码、跨标签/重复登录、安全策略、会话超时、密码策略、通知 outbox、重试、回执和供应商设置。A78-A82 已完成前端模块化收口；A83 已建立后端单据骨架并迁销售出库；A84 已把其余 5 张核心单据迁到骨架；A85 已把销售出库/采购入库财务过账接到审核；A86 已证明库存新单广度生产线；A87 已补其他出库和直接调拨；A88 盘点链保留；A89 已修复销售核心流并全量回归；A90 已补单据编号；A91 已修共享前端 UX；A92 已补销售单据字段；A93 已重做销售出库下推直达和直接建单选源流；A94 已补销售/采购税务金额模型并把财务过账改为含税总额；A95 已把反审核语义对齐金蝶 `AUDITED -> DRAFT` 并补工作台建单自动取号；A96 已补主数据选择器弹窗；A97 已补表体通用 UX；A98 已完成源单模型行级化和下推追溯/重复下推守卫；A99 已补全局密度；A100 已完成采购下推直连和下推弹窗机制删除；A101 已完成库存预警。
- 可跑命令：`cd infra && docker compose up -d`；`cd backend && ./mvnw spring-boot:run`；`cd frontend && npm run dev -- --host 127.0.0.1 --port 5173`
- 本地服务地址：前端 `http://127.0.0.1:5173/`；后端 `http://127.0.0.1:8080/`；PostgreSQL `localhost:5432`；Redis `localhost:6379`
- 数据库状态：Docker context `desktop-linux`；`jdy-erp-postgres`、`jdy-erp-redis` 均 healthy；Flyway schema version `45`
- 最近通过的测试：`npm --prefix frontend run build` 通过；`cd backend && JAVA_HOME=/opt/homebrew/opt/openjdk@21 ./mvnw test` 通过；`node scripts/a101-stock-alert-regression.mjs` 通过；A2-A101 全量回归 `verification/a2-a101-regression-latest.json` 为 `ok=true`、`total=89`、`expectedTotal=89`、`badCount=0`、`skipped=0`，当前 mtime `2026-06-26 13:33:50 +0800`（提交后若继续要求 mtime 晚于最终提交，需要以最终 HEAD 再跑同名聚合脚本）。
- 当前验收等级：A101 达到 A3 查询/配置链路回归。动到共享 `DataListPage` 和库存权限/列表数据源，已用 A101 专项、A2-A101 全量、Maven/前端 build 验证；未改库存过账、财务、税务、编号。
- 已知阻塞：无
- 最近报告：全部归档到 `docs/验收报告/`；最近为 `docs/验收报告/验收报告-A101-库存预警-20260626.md`、`docs/验收报告/验收报告-A100-采购下推收尾-20260626.md`、`docs/验收报告/验收报告-A98-源单模型-20260626.md`。
- 列表 API 契约：`docs/13-列表API契约.md`
- 批次坐标：`docs/12-当前批次验收清单.md` 固定区维护 A/C/D/E/F/G/A2-A101 ↔ B0-B6 ↔ 复刻-local 入口映射。
- 验证证据：A101 见 `docs/验收报告/验收报告-A101-库存预警-20260626.md`、`verification/a101-stock-alert-regression.json`、`verification/a2-a101-regression-latest.json`；A100/A99/A98/A97/A96/A95/A94/A93/A92/A91/A90/A89/A88 回归仍保留。
- 验证产物目录：`verification/`；审批 Excel 目录仍为 `outputs/`，不要改。
- 下一步：库存模块广度可继续呆滞料/库存报表尾巴，或转资金/报表；勿顺手改税务、编号或 feature scope 生成物。

## 开工前必读（廉价起步）

1. 本快照
2. 当前批次验收清单 `docs/12-当前批次验收清单.md`
3. 目标入口的 `复刻-local` 截图 + 对应 B 批次组件规格
4. 本任务相关代码文件

不要重读全部 docs；规格按需点开 `复刻-local/02_复刻规划`。

## 收尾必更（一行带过即可）

- 高风险逻辑（库存/资金/审核/权限）是否动过、是否测过
- 是否有临时实现/桩、未验证推理补完
- 是否完成截图视觉验收、达到的验收等级（A0-A4）
- 范围有无调整（调整走 `config/implementation-overrides.json` + 重跑生成脚本，勿手改 01/02/03）
