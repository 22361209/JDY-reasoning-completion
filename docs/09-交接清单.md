# 交接快照

每个会话第一份读的文件。**保持简短，覆盖更新，不追加历史。**
铁律见 `CLAUDE.md`；规格见 `复刻-local`；架构底线见 `docs/04/06/07`。

## 当前快照（每次收尾覆盖）

- 更新时间：2026-06-25
- 当前批次/模块：**A75 通知自动重试调度完成**
- 进度：B0/B1/C/D/E/F/G/A2-A75 已完成验收。A8 已补多行明细和 A2-A7 多行/多状态回归。A9-A20 已补订单剩余数量下推、分录效率、下推确认、订单执行列、行排序插入、批量调整、源单追踪、源行号追溯、Excel 多行粘贴、源行号定位、名称规格智能粘贴。A21-A37 已补双方向下推回归、下游执行反查、粘贴候选、仓库应用、键盘流、保存校验、零值原因、打印/引出备注、反审核/红冲风险提示、红字来源追踪、操作日志红冲检索。A38-A53 已补六类单据 PDF/HTML 输出、默认套打模板、打印模板维护、复制、角色默认、生产红冲来源打印和打印纸张/方向/边距/联次。A54-A62 已补权限目录、权限矩阵、前端权限驱动、后端权限拦截、本地 HTTP session 用户切换、用户维护页、正式登录与退出、员工自助改密、会话超时提示、登录失败锁定、安全审计和管理员解除锁定入口，前后端权限按同一登录用户生效。A64 已补生产红字单详情中的来源原单可点击跳转。A65 已补未登录找回申请、管理员处理和审计闭环。A66 已把 A57/A58 历史回归迁移到正式登录流。A67 已补跨标签登录态失效同步。A68 已补同账号重复登录后登录踢旧登录、旧会话后端失效和审计追溯。A69 已补系统设置/安全设置，可配置重复登录策略为允许并发或后登录踢旧登录。A70 已补管理员配置会话超时分钟数，并让后端 HttpSession 按配置真实执行。A71 已补密码复杂度参数配置，前端改密提示和后端校验同源。A72 已补安全设置保存当前管理员密码二次确认，错误密码不写设置且不误登出。A73 已补找回密码重置/驳回通知 outbox、用户维护页最近通知和通知审计。A74 已补通知失败筛选、重试次数、失败原因、管理员手动重发和重发审计。A75 已补通知 outbox 本地自动重试调度和自动重试审计。
- 可跑命令：`cd infra && docker compose up -d`；`cd backend && ./mvnw spring-boot:run`；`cd frontend && npm run dev -- --host 127.0.0.1 --port 5173`
- 本地服务地址：前端 `http://127.0.0.1:5173/`；后端 `http://127.0.0.1:8080/`；PostgreSQL `localhost:5432`；Redis `localhost:6379`
- 数据库状态：Docker context `desktop-linux`；`jdy-erp-postgres`、`jdy-erp-redis` 均 healthy；Flyway schema version `29`
- 最近通过的测试：`node --check scripts/a75-notification-auto-retry-regression.mjs` 通过；`JAVA_HOME=/opt/homebrew/opt/openjdk@21 ./mvnw test` 通过；`npm --prefix frontend run build` 通过；A75 浏览器回归通过，失败通知无需人工点击即可由调度器自动恢复为 `SENT`，重试次数递增、失败原因清理和 `AUTO_RETRY_NOTIFICATION` 审计均通过；A74 历史回归通过。
- 当前验收等级：A75 补齐通知 outbox 本地自动重试、重试状态回写和操作日志追溯。
- 已知阻塞：无
- 最近报告：全部归档到 `docs/验收报告/`；最近为 `docs/验收报告/验收报告-A75-通知自动重试调度-20260625.md`、`docs/验收报告/验收报告-A74-通知失败筛选与手动重发-20260625.md`、`docs/验收报告/验收报告-A73-找回密码通知outbox-20260625.md`、`docs/验收报告/验收报告-A72-安全设置变更二次确认-20260625.md`、`docs/验收报告/验收报告-A71-密码复杂度参数配置-20260625.md`、`docs/验收报告/验收报告-A70-会话超时分钟数配置-20260625.md`。
- 列表 API 契约：`docs/13-列表API契约.md`
- 批次坐标：`docs/12-当前批次验收清单.md` 固定区维护 A/C/D/E/F/G/A2-A75 ↔ B0-B6 ↔ 复刻-local 入口映射，含 `A2-A7 多行/多状态回归`。
- 验证证据：A75 结构化结果见 `verification/a75-notification-auto-retry-regression.json`；截图见 `verification/playwright/a75-notification-auto-retry-20260624171737.png`；A74 手动重发链已刷新 `verification/a74-notification-retry-regression.json`。
- 验证产物目录：`verification/`；审批 Excel 目录仍为 `outputs/`，不要改。
- 下一步：建议基于 `sys_notification_outbox` 补供应商回执同步/调度参数设置页，或继续扩展 OTP 二次验证/设备信任策略。

## 开工前必读（廉价起步）

1. 本快照
2. 当前批次验收清单 `docs/12-当前批次验收清单.md`
3. 目标入口的 `复刻-local` 截图 + 对应 B 批次组件规格
4. 本任务相关代码文件

不要重读全部 docs；规格按需点开 `复刻-local/02_复刻规划`。

## 收尾必更（一行带过即可）

- 高风险逻辑（库存/资金/审核/权限）是否动过、是否测过
- 是否有临时实现/桩、未验证推理补完
- 是否完成截图视觉验收、达到的验收等级（A0-A4）
- 范围有无调整（调整走 `config/implementation-overrides.json` + 重跑生成脚本，勿手改 01/02/03）
