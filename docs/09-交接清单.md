# 交接快照

每个会话第一份读的文件。**保持简短，覆盖更新，不追加历史。**
铁律见 `CLAUDE.md`；规格见 `复刻-local`；架构底线见 `docs/04/06/07`。

## 当前快照（每次收尾覆盖）

- 更新时间：2026-06-24
- 当前批次/模块：**A10 分录操作效率完成**
- 进度：B0/B1/C/D/E/F/G/A2/A3/A4/A5/A6/A7/A8/A9 已完成验收；A8 已补多行明细和 A2-A7 多行/多状态回归。A9 已补销售/采购剩余数量下推和超量审核保护。A10 已补单据分录复制行、批量填仓库、Enter/方向键同列移动。
- 可跑命令：`cd infra && docker compose up -d`；`cd backend && ./mvnw spring-boot:run`；`cd frontend && npm run dev -- --host 127.0.0.1 --port 5173`
- 本地服务地址：前端 `http://127.0.0.1:5173/`；后端 `http://127.0.0.1:8080/`；PostgreSQL `localhost:5432`；Redis `localhost:6379`
- 数据库状态：Docker context `desktop-linux`；`jdy-erp-postgres`、`jdy-erp-redis` 均 healthy；Flyway schema version `7`
- 最近通过的测试：`backend ./mvnw test` 通过；`frontend npm run build` 通过；Playwright A10 分录效率回归通过，复制行后 2 行、复制内容保留 `CP-001` 和数量 `2`、批量仓库回填两行 `CK-002`、Enter/ArrowUp 焦点移动正确。
- 当前验收等级：A10 让销售/采购单据录入更接近日常员工可用；草稿分录已具备复制行、批量仓库和基础键盘移动。
- 已知阻塞：无
- 最近报告：全部归档到 `docs/验收报告/`；最近为 `docs/验收报告/验收报告-A10-分录操作效率-20260624.md`、`docs/验收报告/验收报告-A9-订单剩余数量下推-20260624.md`、`docs/验收报告/验收报告-A2-A7-多行回归-20260624.md`、`docs/验收报告/验收报告-A8-表单明细多行真实渲染-20260624.md`、`docs/验收报告/验收报告-A7-多单据列表打开详情-20260624.md`、`docs/验收报告/验收报告-A6-销售订单列表打开详情-20260624.md`。
- 列表 API 契约：`docs/13-列表API契约.md`
- 批次坐标：`docs/12-当前批次验收清单.md` 固定区维护 A/C/D/E/F/G/A2-A10 ↔ B0-B6 ↔ 复刻-local 入口映射。
- 截图证据：`verification/playwright/a10-entry-efficiency-20260624064015.png`、`verification/playwright/a9-sales-remaining-pushdown-20260624063328.png`、`verification/playwright/a9-purchase-remaining-pushdown-20260624063328.png`、`verification/playwright/a2-a7-regression-sales-20260624062534.png`、`verification/playwright/a2-a7-regression-sales-out-20260624062534.png`、`verification/playwright/a2-a7-regression-purchase-20260624062534.png`
- 验证产物目录：`verification/`；审批 Excel 目录仍为 `outputs/`，不要改。
- 下一步：建议做 A11 下推数量确认弹窗，把“剩余数量”给员工二次确认；或者补单据详情执行数量/剩余数量只读列。

## 开工前必读（廉价起步）

1. 本快照
2. 当前批次验收清单 `docs/12-当前批次验收清单.md`
3. 目标入口的 `复刻-local` 截图 + 对应 B 批次组件规格
4. 本任务相关代码文件

不要重读全部 docs；规格按需点开 `复刻-local/02_复刻规划`。

## 收尾必更（一行带过即可）

- 高风险逻辑（库存/资金/审核/权限）是否动过、是否测过
- 是否有临时实现/桩、未验证推理补完
- 是否完成截图视觉验收、达到的验收等级（A0-A4）
- 范围有无调整（调整走 `config/implementation-overrides.json` + 重跑生成脚本，勿手改 01/02/03）
