# 交接快照

每个会话第一份读的文件。**保持简短，覆盖更新，不追加历史。**
铁律见 `CLAUDE.md`；规格见 `复刻-local`；架构底线见 `docs/04/06/07`。

## 当前快照（每次收尾覆盖）

- 更新时间：2026-06-25
- 当前批次/模块：**A70 会话超时分钟数配置完成**
- 进度：B0/B1/C/D/E/F/G/A2-A70 已完成验收。A8 已补多行明细和 A2-A7 多行/多状态回归。A9-A20 已补订单剩余数量下推、分录效率、下推确认、订单执行列、行排序插入、批量调整、源单追踪、源行号追溯、Excel 多行粘贴、源行号定位、名称规格智能粘贴。A21-A37 已补双方向下推回归、下游执行反查、粘贴候选、仓库应用、键盘流、保存校验、零值原因、打印/引出备注、反审核/红冲风险提示、红字来源追踪、操作日志红冲检索。A38-A53 已补六类单据 PDF/HTML 输出、默认套打模板、打印模板维护、复制、角色默认、生产红冲来源打印和打印纸张/方向/边距/联次。A54-A62 已补权限目录、权限矩阵、前端权限驱动、后端权限拦截、本地 HTTP session 用户切换、用户维护页、正式登录与退出、员工自助改密、会话超时提示、登录失败锁定、安全审计和管理员解除锁定入口，前后端权限按同一登录用户生效。A64 已补生产红字单详情中的来源原单可点击跳转。A65 已补未登录找回申请、管理员处理和审计闭环。A66 已把 A57/A58 历史回归迁移到正式登录流。A67 已补跨标签登录态失效同步。A68 已补同账号重复登录后登录踢旧登录、旧会话后端失效和审计追溯。A69 已补系统设置/安全设置，可配置重复登录策略为允许并发或后登录踢旧登录。A70 已补管理员配置会话超时分钟数，并让后端 HttpSession 按配置真实执行。
- 可跑命令：`cd infra && docker compose up -d`；`cd backend && ./mvnw spring-boot:run`；`cd frontend && npm run dev -- --host 127.0.0.1 --port 5173`
- 本地服务地址：前端 `http://127.0.0.1:5173/`；后端 `http://127.0.0.1:8080/`；PostgreSQL `localhost:5432`；Redis `localhost:6379`
- 数据库状态：Docker context `desktop-linux`；`jdy-erp-postgres`、`jdy-erp-redis` 均 healthy；Flyway schema version `26`
- 最近通过的测试：`node --check scripts/a70-session-timeout-settings-regression.mjs` 通过；`JAVA_HOME=/opt/homebrew/opt/openjdk@21 ./mvnw test` 通过；`npm --prefix frontend run build` 通过；A70 浏览器回归通过，设置 7 分钟后 `/api/system/security-settings` 返回 420 秒，`/api/system/session.security.sessionMaxInactiveSeconds` 返回 420，回归结束恢复 30 分钟/1800 秒。
- 当前验收等级：A70 补齐安全设置页会话超时分钟数、权限保护、后端真实 HttpSession 执行和操作日志追溯。
- 已知阻塞：无
- 最近报告：全部归档到 `docs/验收报告/`；最近为 `docs/验收报告/验收报告-A70-会话超时分钟数配置-20260625.md`、`docs/验收报告/验收报告-A69-重复登录策略配置-20260625.md`、`docs/验收报告/验收报告-A68-重复登录单一活动会话-20260624.md`、`docs/验收报告/验收报告-A67-跨标签登录态失效同步-20260624.md`、`docs/验收报告/验收报告-A66-A57A58正式登录回归迁移-20260624.md`、`docs/验收报告/验收报告-A65-忘记密码申请与管理员处理-20260624.md`。
- 列表 API 契约：`docs/13-列表API契约.md`
- 批次坐标：`docs/12-当前批次验收清单.md` 固定区维护 A/C/D/E/F/G/A2-A70 ↔ B0-B6 ↔ 复刻-local 入口映射，含 `A2-A7 多行/多状态回归`。
- 验证证据：A70 结构化结果见 `verification/a70-session-timeout-settings-regression.json`；截图见 `verification/playwright/a70-session-timeout-7-minutes-20260624162707.png`、`verification/playwright/a70-session-timeout-reset-30-minutes-20260624162707.png`。
- 验证产物目录：`verification/`；审批 Excel 目录仍为 `outputs/`，不要改。
- 下一步：建议补真实短信/邮箱发送网关，或继续扩展安全设置页（密码复杂度参数、二次验证/设备信任策略）。

## 开工前必读（廉价起步）

1. 本快照
2. 当前批次验收清单 `docs/12-当前批次验收清单.md`
3. 目标入口的 `复刻-local` 截图 + 对应 B 批次组件规格
4. 本任务相关代码文件

不要重读全部 docs；规格按需点开 `复刻-local/02_复刻规划`。

## 收尾必更（一行带过即可）

- 高风险逻辑（库存/资金/审核/权限）是否动过、是否测过
- 是否有临时实现/桩、未验证推理补完
- 是否完成截图视觉验收、达到的验收等级（A0-A4）
- 范围有无调整（调整走 `config/implementation-overrides.json` + 重跑生成脚本，勿手改 01/02/03）
