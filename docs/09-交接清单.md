---
roadmapItemIds:
  - "2"
roadmapGateId: null
---

# 交接快照

每个会话第一份读的文件。保持简短、覆盖更新、不追加历史。铁律见 `CLAUDE.md`，当前任务见 `docs/12-当前批次验收清单.md`，规格仍以 `JDY-复刻-local` 为准。

## 当前快照

- 更新时间：2026-07-12
- 当前分支：`codex/review-cleanup-20260627`；A134 开工基线 `e55601f91b18243bd5582f7e4bf30fe898ffc796`，基线相对远端 ahead 12，未获推送授权。
- 当前唯一批次：**A134 权限 fail-closed、生命周期授权与库存直调收口**；路线坐标为问题 2，目标 A4，任务合同正在独立提交，生产代码尚未修改。
- 前置状态：A133 已达到 A4，19 个专项、后端 `64/64`、前端 build、smoke `10/10` 通过；full `102/109`，失败为 A32/A38/A57/A63/A101/A116/A118。
- A134 审计基线：150 个后端写 handler 中 118 个有固定权限，32 个无显式语义；缺口为 3 个库存直写、9 个共享生命周期动作、11 个草稿保存和 9 个公共、自助或作用域写入。
- 用户已确认：生产环境禁止任何角色绕正式单据直调库存，不提供生产管理员例外；本地测试入口必须 fail-closed、多重受限。
- A134 固定安全口径：写接口只允许固定权限、按单据动态权限、请求/资源动态权限、已认证自助、明确公开五类；未分类写接口运行时拒绝且自动测试失败。
- 员工侧写口径：取消登录前 `/api/system/users` 匿名枚举和候选 datalist；保留账号自由输入，用户管理仅管理员可访问。
- 库存口径：删除无调用者的 `/api/inventory/sales-out`、`/api/inventory/purchase-in`；adjustments 只允许 local/test/regression + 显式开关 + BLD-TEST allowlist + 管理员权限。
- 生命周期口径：16 种 type 按现有模块权限唯一映射，头级、行级和单据锁获取都在写业务事实前检查。
- A134 必须让 A57 转绿；最终 full 失败只允许 A32/A38/A63/A101/A116/A118 子集，`skipped=0`。
- 当前任务包：`docs/12-当前批次验收清单.md`；A133 历史报告不再回写。
- 当前范围仍待确认：F021 销售报价单、F031 采购申请、发货通知范围归属；A134 不触碰。
- 服务仍是 A133 最终构建：后端 `8080`、前端 `5173`，PostgreSQL、Redis healthy；A134 生产代码落地后必须停旧监听并按受限测试配置重启。
- CI 状态：本地提交均未推送，因此没有对应远端 workflow 记录。

## 开工协议

1. 先读本快照和 `docs/12-当前批次验收清单.md`，只按 A134 allowedPaths 工作。
2. 合同提交通过治理门禁后，按访问语义基础、缺口分类、fail-closed 激活、前端/运行器、验收拆分独立提交。
3. fail-closed 必须在剩余 30 个写 handler 完成唯一分类、两个历史库存 handler 删除后才启用，不能先把生命周期、测试造数、登录或切账套链路打断。
4. 库存测试入口必须同时校验登录、管理员权限、profile、显式开关和 BLD-TEST；不得以自由文本 environment 替代账套 allowlist。
5. 不运行 `build-jdy-feature-approval.mjs`，不手改 scope/status 生成物，不推进路线问题 3–6/6A–6E。
6. A49/A50/A51 如涉及 preset，必须保存完整前快照、在 `finally` 精确恢复并核对完整指纹。

## 收尾协议

- 先跑专项和后端/前端门禁，再跑 security/lifecycle/inventory/finance area、smoke 和 full。
- 低权限/默认关闭验收必须比较请求前后的真实状态、版本、库存、资金、预设和成功业务日志，不能只看 HTTP 状态；失败安全审计留给路线问题 3。
- 人工核对登录页不再公开员工候选、管理员用户管理页仍正常，并保存 1366×768 截图。
- 最终审计 `git status --short --untracked-files=all`、实际 diff 和 allowedPaths；移除夹带改动，输出 A134 报告与独立 review。
