# 交接快照

每个会话第一份读的文件。**保持简短，覆盖更新，不追加历史。**
铁律见 `CLAUDE.md`；规格见 `复刻-local`；架构底线见 `docs/04/06/07`。

## 当前快照（每次收尾覆盖）

- 更新时间：2026-06-30
- 当前批次/模块：**A124-L BLD 单据生命周期统一规则收口已完成**。本批承接 A124 动作规则，把保存/审核/反审核/作废/关闭/冻结/红冲/选源/下推的生命周期口径收成统一协议，并在前后端使用同一份单据能力分类。
- 进度：B0/B1/C/D/E/F/G/A2-A124-L 已完成验收。A124-L 在独立工作树 `/Users/linzhenyue/Projects/JDY-lifecycle-unification`、分支 `codex/a124-lifecycle-unification` 上完成。A121 已提交 `7a4a02e refactor: split data list page internals`，`DataListPage.vue` 从 2580 行降到 1621 行；A122 已提交 `b8880c4 refactor: split entry table internals`，`EntryTable.vue` 从 1636 行降到 1338 行；A123 已新增 `FieldRenderer.vue` 和字段协议类型；A124 已把单据表头字段接入 FieldRenderer；A124-L 已新增单据生命周期协议与前后端能力表。
- 可跑命令：`./scripts/dev-up.sh`；`./scripts/dev-status.sh`；`./scripts/dev-down.sh`
- 本地服务地址：前端 `http://127.0.0.1:5173/`；后端 `http://127.0.0.1:8080/`；PostgreSQL `localhost:5432`；Redis `localhost:6379`
- 数据库状态：Docker context `desktop-linux`；`jdy-erp-postgres`、`jdy-erp-redis` 均 running；Flyway schema version `79`
- 最近通过的测试：A124-L 已通过 `git diff --check`、`npm --prefix frontend run build`、`cd backend && JAVA_HOME=/opt/homebrew/opt/openjdk@21 ./mvnw test`（37/37）、`node scripts/run-regression-tier.mjs smoke`（10/10）、`node scripts/run-regression-tier.mjs full --continue-on-failure`（99/99，0 skipped，结果 `verification/regression-tier-full-continue-latest.json`）。回归是在新工作树服务上跑的；本地 ignored 的 Playwright 依赖和历史 full 清单只作为运行依赖，不进 Git。
- 当前验收等级：A124-L 已完成验收。未重跑 `build-jdy-feature-approval.mjs`，未手改 `featureScope.ts`。
- 已知阻塞：无
- 最近报告：全部归档到 `docs/验收报告/`；最近为 `docs/验收报告/验收报告-A124-L-单据生命周期统一规则-20260630.md`、`docs/验收报告/验收报告-A124-BLD页面协议表头与动作规则收口-20260630.md`、`docs/验收报告/验收报告-A121-A123-BLD页面协议内部收口-20260630.md`、`docs/验收报告/验收报告-A120-BLD页面统一协议落地-20260630.md`、`docs/验收报告/全项目Review与回归报告-20260630.md`。
- 列表 API 契约：`docs/13-列表API契约.md`
- 协作指南目录：`docs/guides/`，包含业务规则、动作按钮规则、`docs/guides/bld-page-unification-protocol.md`（BLD 页面统一协议 + 动作按钮注册表 + 表格/表体硬约束）、`docs/guides/bill-lifecycle-unification-protocol.md`（单据生命周期统一协议）、仓库文件地图、`docs/guides/bill-metadata-roadmap.md`、`docs/guides/new-project-migration-blueprint.md` 和 `docs/guides/account-set-architecture.md`。业务规则开头已记录云星空业务骨架优先级，并明确本项目第一版生产链走轻量路径。采购规则已补：采购退货源自采购入库，非作废退货占用可退数量，采购汇总只统计已审核采购订单/入库/退货。
- 批次坐标：`docs/12-当前批次验收清单.md` 固定区维护 A/C/D/E/F/G/A2-A101 ↔ B0-B6 ↔ 复刻-local 入口映射。
- 验证证据：历史 `verification/` 运行产物已归档到 `/Users/linzhenyue/Projects/JDY-资料库/verification-archive-20260627`；关键结论仍以 `docs/验收报告/` 为准。
- 验证产物目录：`verification/` 为本地临时产物，默认不进 Git，只保留 `.gitkeep` 目录骨架；审批 Excel 目录仍为 `outputs/`，不要改。A118 浏览器验收截图在 `verification/playwright/a118-outsourcing-source-picker-20260629131010.png`，关键结论已写入 `docs/验收报告/验收报告-A118-委外加工闭环-20260629.md`。
- Git 历史：2026-06-27 已重写本地历史剔除 `verification/**`，`.git/` 从约 1.7G 降至约 5.7M；重写前 bundle 备份在 `/Users/linzhenyue/Projects/JDY-资料库/git-backups/20260627-174203/`。
- 下一步：从 A124-L 提交继续推进动作按钮的权限/可见性细化，或按新需求继续迁移生产/委外/BOM 的页面协议。所有单据保存只落草稿，审核后才可被选源、下推、影响库存或参与后续链路；关闭/冻结只属于连续执行承诺单据，红冲只属于库存/执行事实单据；勿重跑 `build-jdy-feature-approval.mjs` 或手改 `featureScope.ts`。

## 开工前必读（廉价起步）

1. 本快照
2. 当前批次验收清单 `docs/12-当前批次验收清单.md`
3. 目标入口的 `复刻-local` 截图 + 对应 B 批次组件规格
4. 本任务相关代码文件

不要重读全部 docs；规格按需点开 `复刻-local/02_复刻规划`。

## 收尾必更（一行带过即可）

- 高风险逻辑（库存/资金/审核/权限）是否动过、是否测过
- 是否有临时实现/桩、未验证推理补完
- 是否完成截图视觉验收、达到的验收等级（A0-A4）
- 范围有无调整（调整走 `config/implementation-overrides.json` + 重跑生成脚本，勿手改 01/02/03）
