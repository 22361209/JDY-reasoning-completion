---
taskKind: governance
roadmapItemIds: []
roadmapGateId: null
---

# 交接快照

每个会话第一份读的文件。它只记录当前状态；历史合同、验收证据和旧路线分别进入 `docs/archive/`、`docs/验收报告/` 与 Git 历史。

## 当前状态

- 更新时间：2026-08-10。
- 当前工作区位于 `main`；A183 实现提交为 `b64d9ce9`，full 后测试清理修复提交为 `4ef4b6b2`。当前没有活动任务，A183 未 push，也未操作生产环境。
- 项目阶段：已从 JDY 复刻骨架转入当前 ERP 产品化完善。用户决策、数据一致性、安全、tenant、权限和生命周期规范优先于 JDY 参考；详见 `AGENTS.md` 与 ADR-015。
- roadmap 0、1、2、3、4、5、6、6A、6B、6C、6D、6E 已全部关闭。6E 的唯一最终历史引用是 `docs/验收报告/验收报告-A144-6E报表与当前错误入口-20260714.md`。
- A156–A162 均已归档。A161 已精确恢复五批 A118 fail-closed 夹具；A162 已以采购 area 18/18 与唯一 full 136/136 完成采购订单—入库—退货 A3 收口。F010/F012/F013/F014/F015/F018、F033/F034/F035 均为 `verified/published/A3`，分别以 A161、A162 验收报告为准。
- A163 已完成归档。P1-3 的 F087 已以 `docs/验收报告/验收报告-A163-P1-3资金账户与同币种转账发布闭环-20260716.md` 收口为 `verified/published/A3`，最终有效 full 为 137/137。
- A167 已完成归档。生产计划母件已稳定关联物料，一张计划可包含多个型号并逐行下推生产任务；生产领料、产品入库、库存流水和任务进度已在隔离真实页面与自动回归中跑通。F056/F057/F059 保持 `verified/published/A3`，证据见 `docs/验收报告/验收报告-A167-生产计划多型号与生产执行闭环-20260802.md`。
- A168 已完成归档。生产计划序号格已复用分录加减行工具，输入框使用通用整格尺寸；母件编码支持输入联想、键盘选择和完整选择器兜底，选中时即时校验并回填当前 BOM。A168 两型号计划 `SCJH000146` 已保存成功，本地 `5173` 已切换到 A168 前端并连接 A167 后端 `18080`；证据见 `docs/验收报告/验收报告-A168-生产计划分录录入效率收口-20260802.md`。
- A169 已完成归档。标准业务列表数据行已从 `22px` 对齐分录基线到 `32px`，字号保持双方原有的 `13px`；销售、采购、库存、生产和物料资料五类列表均通过真实浏览器验收。本地 `5173` 已重启到 A169 前端并连接 A167 后端 `18080`；证据见 `docs/验收报告/验收报告-A169-列表表格密度与分录对齐-20260802.md`。
- A170 已完成归档。生产计划分录可逐行选择多层 BOM 任务与采购申请；自制子件任务保留父子树和冻结用料，非自制叶子进入可删行、改数量和补供应商的采购申请，再按供应商拆分采购计划。F031/F032 已发布为 `verified/published/A3`；本地 `5173` 已切换到 A170 前端并连接后端 `18080`，证据见 `docs/验收报告/验收报告-A170-多层生产任务与采购规划-20260802.md`。
- A171 已完成归档。采购计划草稿不再占用采购申请，审核时占用、反审核时释放；显式子 BOM 仅允许已审核、启用且当前版本，采购需求继承 BOM 分录发料仓。V112 已把既有 `planned_qty` 重算为已审核采购计划汇总，隔离后端 `18080` 正运行本批版本；证据见 `docs/验收报告/验收报告-A171-多层生产采购计划生命周期修复-20260802.md`。
- A172 已完成归档。采购订单审核与采购申请反审核统一按来源申请头、分录 UUID 确定顺序加锁，锁后按已审核下游重算占用；已审核采购订单不得被并发保存改回草稿。草稿采购计划可删除后按未覆盖剩余行重新下推，删除有不可恢复提示和操作日志。V113 与恢复收尾会将历史 backup schema 的 `planned_qty` 统一校正为已审核计划汇总；失效 BOM 发料仓会在下推前整体阻断。证据见 `docs/验收报告/验收报告-A172-采购规划并发与恢复边界修复-20260802.md`。
- A173 已完成归档。full 现在会在登录前校验干净工作区、当前 worktree 的 5173 进程、精确 8080 代理与同一构建 fingerprint，并在执行前后核对 `BLD-TEST/public` 的 28 项权限全集、完整角色矩阵和安全设置。权限/会话、生产母件、确定性滚动、迁移 V113 与日期夹具均已收口；唯一 full 完整执行 143 项，141 通过、2 项历史夹具失败、0 跳过，且无共享状态漂移；证据见 `docs/验收报告/验收报告-A173-回归基线与历史夹具治理-20260802.md`。
- A174 已完成归档。回归使用 run-unique ADMIN 与 Redis `SESSION`，显式低权限 Cookie 的 401 不升级，直接 SQL 权限 mutator 会即时复验共享状态；跨 schema 审计清理、cleanup 后成功证据、tenant 动态日期和 V105–V113 固定 checksum 均已收口。唯一 full 完整执行 143 项，140 通过、3 失败；其中 A146 旧 closure 静态断言已在 full 后针对性修复并通过 117 条断言，剩余 A25/A128 仍归 A175。隔离用户/scope/Redis/public+tenant 审计孤儿最终均为 0；当前本地 `5173`/`8080` 运行 A174 候选 `7b250720`，构建 fingerprint 为 `66745ed2df50a67ef6e02fd5e2e3b013c2c3659fa67730857e876a9c6efd2fba`；证据见 `docs/验收报告/验收报告-A174-A173回归会话与历史门禁复核修复-20260802.md`。
- A174-2 已完成并归档。终局 review 的 A43 在途请求传输边界已收口为“仅固定唯一键仓库创建可重查、其余业务写不重试”，A118 注销响应读取也只在幂等清理路径重试。有效 full 于 2026-08-07 完整执行 143/143：141 通过、A25/A128 两项已登记历史夹具失败、0 跳过，preflight/postflight 均通过，工作区干净；终局 review 无 P0–P2，最终提交为 `90bd3c66`。证据见 `docs/验收报告/验收报告-A174-2-Full回归终局隔离-20260807.md`。
- A176 已完成并归档。采购计划可按剩余数量生成采购订单，保留计划头/行、价格和税率快照；订单审核占用计划数量，反审核释放并在已有已审核入库时阻断。两遍真实 UI 已跑通采购计划—订单—入库—生产领料—产品入库。UAT-DEF-001 的密码规则错误改为用户可读提示，UAT-DEF-002 的物料名称新建采用“保存并审核”并自动返回来源物料表单。`smoke` 11/11、`area:purchase` 23/23 通过；唯一 full 执行 101/145，100 通过，范围外 A115 列表工具区旧静态断言失败，44 项按 fail-fast 跳过。证据见 `docs/验收报告/验收报告-A176-采购计划到采购订单闭环与UAT缺陷收口-20260809.md`。
- A177 已完成并归档。用户指定普通仓 `400041371 / UAT-20260809-005` 在 BLD-TEST 为启用、已审核，销售报价仓库整列表按编码返回 1 条；新建并审核的普通仓 `400041372 / A177-UAT-ORD-20260809` 和原料仓 `400041373 / A177-UAT-RAW-20260809` 也均能立即检索、回填。本轮未确认缺陷，未修改业务代码；证据见 `docs/验收报告/验收报告-A177-销售报价仓库选择器缺陷确认-20260809.md`。
- A178 已完成并归档。同一组 UAT 主数据完成采购入库、库存、销售出库、销售/采购退货和同币种收付款审核核销；库存按 `0 → 10 → 6 → 7 → 6` 变化，应收和应付期末净余额均为 `0`。数量 `7`、可用库存 `6` 的发货通知审核被明确阻断；物料名称回填和无小写字母密码提示各回归一次并通过。总账/凭证等不在有效目录的能力标为不适用，F055/F089 保持 deferred。销售、采购、库存 area 全过；资金 area 的既有 A141 迁移夹具形状断言失败已独立记录，未修改业务代码。证据见 `docs/验收报告/验收报告-A178-采购库存销售退货与收付款核销验收能力-20260809.md`。
- A179 已完成并归档。仓库维护列表与业务候选已分离，销售报价、销售订单、采购订单、采购入库及同类分录统一读取当前 tenant 已审核且启用的实时仓库，不再合并静态 CK 候选。`400041390 / UAT-20260809-009` 在销售报价、采购订单中按编码和名称均返回 1 条并成功回填；`area:sales 23/23`、`area:purchase 24/24` 及 postflight 全通过。证据见 `docs/验收报告/验收报告-A179-业务单据仓库选择器实时账套候选修复-20260809.md`。
- A180 已完成并归档。Chrome 现场确认用户操作正常，缺陷是当前行 `CK-001` 被整列表弹窗误作初始关键字；修复后销售报价和采购订单均以空关键字显示 17 条当前 tenant 已审核启用仓库，按编码、名称检索 `UAT-20260809-009` 均返回 1 条并回填。销售 area 24/24 通过；采购 area 因共享管理员 Redis 会话漂移在首项 fail-fast，未出现仓库断言失败且未重复门禁。证据见 `docs/验收报告/验收报告-A180-业务单据仓库选择器初始筛选修复-20260809.md`。
- A181 十项生产缺陷已在提交 `9183c1a1` 完成实现和 review。唯一 full 完整执行 147/147：144 通过、A115/A118/A119 三个脚本失败、0 跳过，postflight 通过；失败归因为旧静态断言、缺少已审核物料名称的测试夹具，以及 A119 的 `118→119` 拓扑测试合同遗漏。A181 full 不重跑，后续由 A182 定向关闭三个测试合同并完成 Chrome 十项终验。
- A182 已完成并归档。A115、A118、A119 均定向通过，最终 review 无未关闭 P0–P3，且未运行第二次 full。Chrome 十项终验为 8 项通过、2 项失败/阻断：账户资料可见已审核启用的 CNY 转出/转入账户，但资金转账单无法选值；盘点单表格和列设置没有可见“系统库存/账面数量”。其余采购应付、BOM 自引用、调拨候选、物料名称、采购备注、销售报价文案和应收/应付事件名称均通过；证据见 `docs/验收报告/验收报告-A182-A181回归夹具收口与真实UI终验-20260810.md`。
- A183 已完成并归档。资金转账候选加载、错误/空态/重试、仅转账权限最小读取和盘点“实盘数量/系统库存/差异”必显列均已收口；真实 Chrome 转账 `ZJZZ000090` 完成两轮审核/反审核并最终回滚。唯一 full 完整执行 147/147：146 通过、A136 1 项失败、0 跳过；失败由 full 前两次新版 A163 留下 14 条 run-owned actor 孤儿造成，full 后现场合计 21 条，已用显式 allowlist 恢复并以 A136、修后 A163 定向和全局 actor 孤儿为 0 闭合，未运行第二次 full。证据见 `docs/验收报告/验收报告-A183-资金转账候选与盘点系统库存真实主链修复-20260810.md`。

## 已关闭能力与保留边界

- F029、F042、F091、F093 为 `verified/published/A3`，F061 为 `verified/published/A4`；以 `config/feature-delivery-status.json` 和 A153 报告为准。
- 金额报表按 CNY/USD 分组、分页、合计和 CSV，禁止无汇率依据的跨币种汇总；F042/F061 是纯数量能力，不伪造 currency，也不跨单位合计。
- 生产环境不开放库存调整接口。测试夹具只允许 `local/test/regression`、显式开关和测试账套 allowlist，管理员权限不得绕过环境门禁。
- F061 草稿 `scrapQty=0` 可保存且无需原因，负数禁止；审核必须数量大于 0 并填写原因。

## 已知独立事项

- A126 历史回归夹具存在 cleanup 债务，必须新建可审计任务包；不得回填或扩大 A144–A153。
- A175 已完成并归档。A23/A25 使用稳定前页的 run-owned 物料编码并在任意结果后精确删除销售单和物料；A128 以 `Asia/Shanghai` 推导当前月、上月与上季度并清理测试单据。有效 full 于 2026-08-07 完整执行 143/143：143 通过、0 失败、0 跳过，postflight 通过；证据见 `docs/验收报告/验收报告-A175-历史夹具分页日期与清理治理-20260807.md`。
- A183 保留两个独立候选：`GET /api/cash-transfers/{billNo}` 未声明资金转账权限的既有 P1；账户候选 offset 分页无法识别页间“删 1、增 1且 total 不变”的低风险一致性 P3。前者可能泄露同 tenant 转账详情，后者由保存/审核复验以 409 阻断且不会错账；均须新建任务包，不得顺手混修。
- 6E 已排除的销售排行/利润、库存成本金额、往来预警、实际退款、预收预付、其他往来、汇率折算、编号自定义类型等，均需按产品价值单独立项。

## 当前任务与后续顺序

1. 当前没有活动任务；处理 A183 保留的详情读取 P1 或分页一致性 P3 前，必须分别建立新任务包。
2. A166《列表页布局优化与物料-BOM录入可用性修复》继续待确认，不与已归档 A179/A183 混改。
3. A165《BOM 页面工作区高度收口》已归档：BOM 维护页已使用受限 flex 工作区，外层页面纵向滚动已消除；验收见 `docs/验收报告/验收报告-A165-BOM页面工作区高度收口-20260720.md`。材料报废仍是独立候选，不得与 A165 混改。
4. P1-3 已关闭，仍不得借其既有转账能力扩展余额初始化、汇率、跨币种转账、预收预付、退款、自动对账或银行直连；此类需求必须建立新任务包。

## 本批验证基线

```bash
node scripts/effective-scope-contract-check.mjs
node scripts/feature-delivery-status-scan.mjs
node scripts/validate-regression-manifest.mjs
node scripts/docs-governance-check.mjs
```
