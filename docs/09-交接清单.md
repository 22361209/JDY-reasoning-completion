---
roadmapItemIds:
  - "6"
roadmapGateId: null
---

# 交接快照

每个会话第一份读的文件。保持简短、覆盖更新、不追加历史。铁律见 `CLAUDE.md`，当前任务见 `docs/12-当前批次验收清单.md`，规格仍以 `JDY-复刻-local` 为准。

## 当前快照

- 更新时间：2026-07-13。
- 当前分支：`codex/review-cleanup-20260627`；远端仍停在 A135 `c53f2b78`，本分支领先 17 个独立提交且尚未 push。
- 当前唯一批次：**A139 未知列表 Fail-Closed 与错误入口收口**；roadmap item 6，开工基线 `cf1756e2`，目标 A4。
- 用户“按建议删除”的授权只覆盖 A137 中已列明的 7 条 A136 自动化孤儿行。完整 JSON 已先隔离，V98 按五字段完整 tuple 与父记录仍缺失条件精确删除，三处 scope 的相关残留均为 0；该授权不得泛化。
- A138 已落地字段白名单 PATCH：absent 保持、显式 null 只清可空列、version CAS、已审核拒绝、引用整笔回滚和真实错误反馈；四类旧 PUT 返回 405，三个范围外旧 PUT 类型不受影响。
- A138 后端 `179/179`、定向 `14/14`、`area:master-data 21/21`、smoke `10/10` 通过；full 为 `109/114`，失败仅 A32/A38/A63/A101/A118，比 A137 基线少 A116 且没有新增失败。
- A138 浏览器批次 `20260713060102` 的四页保存/重开与双端 CAS 冲突均通过并人工复核；独立 review 最终 APPROVE，无剩余 P0/P1。
- A139 已确认前端 fallback、Registry 通配/default、provider default 和 source-selector 空集共同导致未知 key 不 fail-closed；三个生产测试后门、六个错误入口、三个盘点 header、两个报表权限和 `*-report-list` 错误路由均纳入本批。
- 6A–6E 继续只读预审，不与 A139 夹带提交。

## A139 固定边界

1. query/export 固定为精确 key 404 → 真实权限 403 → adapter/provider/JDBC；未知管理员和低权限用户都先 404。
2. Registry 只认可精确 key；provider default 不返回 salesRows，selector default 不返回 200 空集，前端不保留 sales-order fallbackDefinition。
3. `standard-list/error-list/permission-denied-list` 从 main 删除；测试只断言它们作为未知 key 返回 404。
4. 隐藏六个未交付入口，不实现报表；为三类盘点补真实 header，为采购汇总和任务跟踪补现有权限码。
5. 报表入口名称与“查询”都必须保留原 key，不拼 `*-report-list`。
6. 同步 delivery status 的 entry 归属，但不自动晋级 feature；补 item 2–6 的真实 historyRefs。
7. A115/A124/A128/A139、backend、build、area:table/inventory/system、smoke、full 和独立 review 全部通过后才关闭。

## 并行执行方式

- 主线程：合同、集成、真实 API/浏览器验收、分层回归、review 与拆分提交。
- 后端线程：精确 Registry、404/403 顺序、provider/selector fail-fast、盘点 header 与 Java 测试。
- 前端线程：删除 fallback/六入口、报表路由与权限；治理线程：status/roadmap、A128 迁移、A139 门禁与 manifest。
- 浏览器登录型脚本和正式 tier 串行，避免单活 session 与共享夹具互相污染。

## 下一步

1. 独立提交 A139 合同。
2. 三路并行实现后端、前端和治理/回归；主线程审查交叉边界并串行跑真实 API/浏览器与 tier。
3. 按后端、前端、测试/治理、文档拆分提交；保持不 push，除非用户明确要求。
