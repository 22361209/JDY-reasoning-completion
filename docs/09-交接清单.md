# 交接快照

每个会话第一份读的文件。**保持简短，覆盖更新，不追加历史。**
铁律见 `CLAUDE.md`；规格见 `复刻-local`；架构底线见 `docs/04/06/07`。

## 当前快照（每次收尾覆盖）

- 更新时间：2026-06-29
- 当前批次/模块：**A117 账套上下文、初始化、期初库存、编号规则**。本批把系统从单测试账套推进到多账套第一层：登录选择账套，登录后管理员可切换账套和初始化本账套；库存期初、库存余额/流水、单据编号规则开始按当前账套隔离。长期架构见 `docs/guides/account-set-architecture.md`。
- 进度：B0/B1/C/D/E/F/G/A2-A78 已完成验收。A8 已补多行明细和 A2-A7 多行/多状态回归。A9-A106 详见历史报告与 `docs/15`；A107-A112d 已完成共享表格内核、横滑、表格高度和用户眼验收口；A113a 已完成销售链前端范式；A113b 已完成单价批量填充 + 价格来源统计接口；销售报价单/源单链路已在提交 `bfbebd3` 收口。A114 已补生产计划、生产任务用料快照和生产计划入口。A115-7 已完成主数据定义统一维护、物料类别/计量单位真实落表、物料单位/净重/毛重快照。A115-9 已完成生产链定义化和轻量委外表面处理。A116B 已完成生产/采购闭环收口。A117 已完成：`sys_account_set`、登录页账套选择、右上角账套切换与账套管理/初始化、库存期初数页面、账套级编号规则页面；当前仍是单数据库过渡，不是完整 `platform_db + tenant_*` 路由。
- 可跑命令：`./scripts/dev-up.sh`；`./scripts/dev-status.sh`；`./scripts/dev-down.sh`
- 本地服务地址：前端 `http://127.0.0.1:5173/`；后端 `http://127.0.0.1:8080/`；PostgreSQL `localhost:5432`；Redis `localhost:6379`
- 数据库状态：Docker context `desktop-linux`；`jdy-erp-postgres`、`jdy-erp-redis` 均 running；Flyway schema version `75`
- 最近通过的测试：`npm --prefix frontend run build` 通过；`cd backend && JAVA_HOME=/opt/homebrew/opt/openjdk@21 ./mvnw test` 通过（16/16）；浏览器轻验通过登录页账套选择、右上角账号菜单账套切换入口、账套管理/初始化页、库存期初数页、单据编号规则页。
- 当前验收等级：A117 API/构建/浏览器轻验通过；待用户肉眼复查多账套入口、初始化风险提示和期初库存录入体验。未重跑 `build-jdy-feature-approval.mjs`，未手改 `featureScope.ts`。
- 已知阻塞：无
- 最近报告：全部归档到 `docs/验收报告/`；最近为 `docs/验收报告/验收报告-A116B-生产采购闭环收口-20260629.md`、`docs/验收报告/验收报告-A116-生产计划采购申请第一段-20260629.md`、`docs/验收报告/验收报告-A114-生产计划与用料快照-20260628.md`、`docs/验收报告/验收报告-Git历史瘦身-20260627.md`、`docs/验收报告/验收报告-仓库资料分离-20260627.md`。
- 列表 API 契约：`docs/13-列表API契约.md`
- 协作指南目录：`docs/guides/`，包含业务规则、动作按钮规则、仓库文件地图、`docs/guides/bill-metadata-roadmap.md`、`docs/guides/new-project-migration-blueprint.md` 和 `docs/guides/account-set-architecture.md`。业务规则开头已记录云星空业务骨架优先级，并明确本项目第一版生产链走轻量路径。采购规则已补：采购退货源自采购入库，非作废退货占用可退数量，采购汇总只统计已审核采购订单/入库/退货。
- 批次坐标：`docs/12-当前批次验收清单.md` 固定区维护 A/C/D/E/F/G/A2-A101 ↔ B0-B6 ↔ 复刻-local 入口映射。
- 验证证据：历史 `verification/` 运行产物已归档到 `/Users/linzhenyue/Projects/JDY-资料库/verification-archive-20260627`；关键结论仍以 `docs/验收报告/` 为准。
- 验证产物目录：`verification/` 为本地临时产物，默认不进 Git，只保留 `.gitkeep` 目录骨架；审批 Excel 目录仍为 `outputs/`，不要改。A115-7 临时浏览器截图在 `verification/playwright/a115-material-unit-pages-20260628.png`，关键验收截图后续归档到 `docs/验收报告/` 下稳定路径。
- Git 历史：2026-06-27 已重写本地历史剔除 `verification/**`，`.git/` 从约 1.7G 降至约 5.7M；重写前 bundle 备份在 `/Users/linzhenyue/Projects/JDY-资料库/git-backups/20260627-174203/`。
- 下一步：让用户在浏览器肉眼检查登录页账套选择、右上角账号菜单、账套初始化、库存期初数和编号规则；随后决定是否进入完整 `platform_db + tenant_*` 数据库路由、附件按账套分目录/桶、Redis key 前缀和权限细化。后续按 `CLAUDE.md`/`docs/11` 的表现/行为/共享核心逻辑判定选择 `smoke`、`area:<module>` 或 `full`；勿重跑 `build-jdy-feature-approval.mjs` 或手改 `featureScope.ts`。

## 开工前必读（廉价起步）

1. 本快照
2. 当前批次验收清单 `docs/12-当前批次验收清单.md`
3. 目标入口的 `复刻-local` 截图 + 对应 B 批次组件规格
4. 本任务相关代码文件

不要重读全部 docs；规格按需点开 `复刻-local/02_复刻规划`。

## 收尾必更（一行带过即可）

- 高风险逻辑（库存/资金/审核/权限）是否动过、是否测过
- 是否有临时实现/桩、未验证推理补完
- 是否完成截图视觉验收、达到的验收等级（A0-A4）
- 范围有无调整（调整走 `config/implementation-overrides.json` + 重跑生成脚本，勿手改 01/02/03）
