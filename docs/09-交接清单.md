# 交接快照

每个会话第一份读的文件。**保持简短，覆盖更新，不追加历史。**
铁律见 `CLAUDE.md`；规格见 `复刻-local`；架构底线见 `docs/04/06/07`。

## 当前快照（每次收尾覆盖）

- 更新时间：2026-06-24
- 当前批次/模块：**A28 新增行继承上一行仓库完成**
- 进度：B0/B1/C/D/E/F/G/A2/A3/A4/A5/A6/A7/A8/A9/A10/A11/A12/A13/A14/A15/A16/A17/A18/A19/A20 已完成验收；A8 已补多行明细和 A2-A7 多行/多状态回归。A9 已补剩余数量下推和超量审核保护。A10-A20 已补分录效率、下推确认、订单执行列、行排序插入、下推数量批量调整、源单追踪入口、源行号追溯、分录 Excel 多行粘贴、下推确认选中行局部调整、源行号定位源单分录、分录名称规格智能粘贴。A21 已补销售/采购下推确认双方向同级回归。A22 已补源订单分录已执行数量反查下游执行单据。A23 已补分录粘贴同名多规格商品候选选择，并修正真实主数据列表不再被 1000 行演示扩展污染。A24 已补下推确认弹窗按选中行应用仓库，销售/采购下游草稿均继承确认仓库。A25 已补粘贴冲突候选弹窗键盘上下选择、Enter 确认和 Esc 取消。A26 已补主数据选择器选中后自动跳到下一录入字段。A27 已补数量 Enter 跳单价、单价 Enter 新增下一行并聚焦商品编码。A28 已补按钮新增行和单价 Enter 新增行继承上一行仓库。
- 可跑命令：`cd infra && docker compose up -d`；`cd backend && ./mvnw spring-boot:run`；`cd frontend && npm run dev -- --host 127.0.0.1 --port 5173`
- 本地服务地址：前端 `http://127.0.0.1:5173/`；后端 `http://127.0.0.1:8080/`；PostgreSQL `localhost:5432`；Redis `localhost:6379`
- 数据库状态：Docker context `desktop-linux`；`jdy-erp-postgres`、`jdy-erp-redis` 均 healthy；Flyway schema version `8`
- 最近通过的测试：`backend ./mvnw test` 通过；`frontend npm run build` 通过；Playwright A28 新增行继承上一行仓库通过，销售订单 `XSDD-20260624-170758473-SJK` 验证按钮新增行继承 `CK-002`、单价 Enter 新增行继承 `CK-T413874`、三行金额 `60.00/24.00/8.00`、合计 `92.00`、后端保存一致；A27 连续录入回归通过。
- 当前验收等级：A28 提高高频录单键盘手感，同仓库连续录入减少重复输入。
- 已知阻塞：无
- 最近报告：全部归档到 `docs/验收报告/`；最近为 `docs/验收报告/验收报告-A28-新增行继承上一行仓库-20260624.md`、`docs/验收报告/验收报告-A27-分录数量单价连续录入-20260624.md`、`docs/验收报告/验收报告-A26-主数据选择器选后跳格-20260624.md`、`docs/验收报告/验收报告-A25-粘贴冲突候选键盘选择-20260624.md`、`docs/验收报告/验收报告-A24-下推确认选中行应用仓库-20260624.md`、`docs/验收报告/验收报告-A23-分录粘贴冲突候选选择-20260624.md`。
- 列表 API 契约：`docs/13-列表API契约.md`
- 批次坐标：`docs/12-当前批次验收清单.md` 固定区维护 A/C/D/E/F/G/A2-A28 ↔ B0-B6 ↔ 复刻-local 入口映射，含 `A2-A7 多行/多状态回归`。
- 截图证据：`verification/playwright/a28-entry-new-line-inherit-warehouse-form-20260624090757.png`、`verification/playwright/a27-entry-enter-next-line-form-20260624090733.png`
- 验证产物目录：`verification/`；审批 Excel 目录仍为 `outputs/`，不要改。
- 下一步：建议补分录保存前空白行/重复行提示；或者扩展下游执行单据弹窗的反审核/红冲影响链。

## 开工前必读（廉价起步）

1. 本快照
2. 当前批次验收清单 `docs/12-当前批次验收清单.md`
3. 目标入口的 `复刻-local` 截图 + 对应 B 批次组件规格
4. 本任务相关代码文件

不要重读全部 docs；规格按需点开 `复刻-local/02_复刻规划`。

## 收尾必更（一行带过即可）

- 高风险逻辑（库存/资金/审核/权限）是否动过、是否测过
- 是否有临时实现/桩、未验证推理补完
- 是否完成截图视觉验收、达到的验收等级（A0-A4）
- 范围有无调整（调整走 `config/implementation-overrides.json` + 重跑生成脚本，勿手改 01/02/03）
