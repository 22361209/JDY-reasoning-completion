---
roadmapItemIds:
  - "6D"
roadmapGateId: null
---

# 交接快照

每个会话第一份读的文件。保持简短、覆盖更新、不追加历史。铁律见 `CLAUDE.md`，当前任务见 `docs/12-当前批次验收清单.md`，规格仍以 `JDY-复刻-local` 为准。

## 当前快照

- 更新时间：2026-07-14。
- 当前分支：`codex/review-cleanup-20260627`；本地领先远端且尚未 push，精确提交数以 `git status` 为准。
- 当前实施批次：**A143 基础资料 Excel 导入**；roadmap 6D，F008，开工基线 `16000fc1`，目标 A4。A142/6C 已收口，证据见 `docs/验收报告/验收报告-A142-销售退货单-20260714.md`。
- A143 已冻结：仅 `.xlsx`；一文件一类主数据；只新增；任一重复/错误整批不可提交；最大 10 MiB/5,000 行；导入后为 DRAFT/version=0；账户币种支持 CNY/USD；八类范围和完整验收见 `docs/12-当前批次验收清单.md`。
- A142 正式最终 runId `20260713203255-a592f154`，330 断言、165 请求、20 类 coverage 全 true；public/tenant 结构残留 0，30 条本批库存流水与完整库存快照精确恢复。
- A142 migration `ok: true`：V102→V103、repeat、tenant sync `81/81` 两次、backup/restore API 200、fresh V1→V103 101 条 migration/81 表，随机库、Redis 和临时进程清理 0。
- backend `249/249`、frontend build `207` modules、独立 backend/frontend review APPROVE、清理审计 PASS。
- area sales/finance/lifecycle/system/table 为 `19/19`、`16/16`、`16/16`、`21/21`、`21/21`，smoke `10/10`；inventory 仅冻结 A101 失败。full `115/120`、skip 0，失败精确等于冻结 A32/A38/A63/A101/A118，A142 无新增失败。
- A137 的精确数据删除授权和 A139 的“按建议删除”均已执行完毕，不得泛化为其他数据、历史收付款记录或功能删除。
- `a126-sales-daily-usability-regression.mjs` 缺业务 cleanup/finally，存在历史测试夹具债务；不得模糊删除或简单回减库存，必须另立可审计清理任务，不与 A142、6D 或 6E 混做。

## A142 已收口边界

1. F024 是正式“销售退货单”，不是退货申请、换货或实际退款单；现有 catalog 错误文案必须纠正。
2. 只选普通、已审核、未红冲的销售出库行；同一客户、同一币种可多源，CNY/USD 继承来源且不换算，首版退回原出库仓。
3. 客户端只提交来源行、退货数量和备注；商品、仓库、价格、税率、客户、币种全部由后端复制，正式可退量由审核事务计算。
4. 退货先冲实时未收应收，超出部分形成待退款事实；真实收款与退货冲销分栏保存，6C 不自动出款。
5. 审核回补库存，反审核精确扣回；超退、并发、红字互斥、库存不足、已退款下游均 fail-closed 且零半截数据。
6. 权限复用第一版模块级 `sales.out.audit`；sales 不直接写财务关系，必须进入 finance application service。

## 下一步

1. A142 运行证据、截图、frontend dist 和 backend target 保持 ignored，不补入治理提交。
2. 向用户确认 6D 的三个开工决定后，同时切换 `docs/09` 与 `docs/12` 为新 A143 合同；在此之前不写 Excel 导入业务代码。
3. 6E 报表与错误入口继续只读预审，不得夹带实现。
4. 用户未要求 push，当前继续保持本地提交。
