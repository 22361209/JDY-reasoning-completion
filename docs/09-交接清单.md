---
roadmapItemIds:
  - "5"
roadmapGateId: null
---

# 交接快照

每个会话第一份读的文件。保持简短、覆盖更新、不追加历史。铁律见 `CLAUDE.md`，当前任务见 `docs/12-当前批次验收清单.md`，规格仍以 `JDY-复刻-local` 为准。

## 当前快照

- 更新时间：2026-07-13。
- 当前分支：`codex/review-cleanup-20260627`；远端仍停在 A135 `c53f2b78`，A136–A138 尚未 push。
- 当前收口批次：**A138 主数据稀疏 PATCH 与并发保护**；roadmap item 5 已达到 A4，开工基线 `a476ecba`。
- A137/item 4 已达到 A4：两个 tenant 各 72 张受管表、PK 72、UK 64、有效 FK 153、CHECK 11；backend、A119、A137 topology、area:system、smoke 通过，full 仅保留冻结的 A32/A38/A63/A101/A116/A118 六项失败；最终独立 review APPROVE。
- 用户“按建议删除”的授权只覆盖 A137 中已列明的 7 条 A136 自动化孤儿行。完整 JSON 已先隔离，V98 按五字段完整 tuple 与父记录仍缺失条件精确删除，三处 scope 的相关残留均为 0；该授权不得泛化。
- A138 已落地字段白名单 PATCH：absent 保持、显式 null 只清可空列、version CAS、已审核拒绝、引用整笔回滚和真实错误反馈；四类旧 PUT 返回 405，三个范围外旧 PUT 类型不受影响。
- A138 后端 `179/179`、定向 `14/14`、`area:master-data 21/21`、smoke `10/10` 通过；full 为 `109/114`，失败仅 A32/A38/A63/A101/A118，比 A137 基线少 A116 且没有新增失败。
- A138 浏览器批次 `20260713060102` 的四页保存/重开与双端 CAS 冲突均通过并人工复核；独立 review 最终 APPROVE，无剩余 P0/P1。
- feature delivery status 保持零改动；item 6、6A–6E 按 predecessor 顺序继续，不与 A138 夹带提交。

## A138 固定边界

1. 只处理物料、客户、供应商、仓库四类编辑更新；不改变创建、审核、反审核、启禁用的业务规则。
2. `code/systemNo/version/auditStatus/status/enabled` 不属于编辑白名单；生命周期状态只能走正式动作。
3. 字段缺失逐列保持，显式 `null` 只清可空列；不使用创建默认值补更新。
4. 更新必须在一个事务中完成字段校验、引用解析和 `version + DRAFT` CAS；任何错误零半截写入。
5. 前端只提交相对原始快照的字段差异；409 保留用户输入并显示真实原因。
6. 8 个旧 PUT 夹具必须迁移到“查询版本 → 必要时正式反审核 → PATCH → 必要时审核”，不开放测试专用后门。
7. 四类页面都要完成保存、重开和冲突态浏览器验收；backend、A138 专项、area:master-data、smoke、full 和独立 review 后才可关闭。

## 并行执行方式

- 主线程：合同、集成设计、共享回归、review、拆分提交与用户决策。
- 后端线程：字段白名单、引用事务、CAS 与集成测试；前端线程：差异提交、状态守卫、错误反馈与页面回归。
- 数据库写入型专项、浏览器脚本和正式 tier 串行，避免单活 session 与共享夹具互相污染。

## 下一步

1. 按后端、前端、测试/脚本/manifest、文档四个边界完成 A138 独立提交；保持不 push，除非用户明确要求。
2. 建立 A139/item 6“未知列表 fail-closed”合同：删除三个测试后门和未知列表通配兜底，query/export 在权限与数据层之前精确 404。
3. 隐藏六个未定义入口，为三个已发布盘点列表补精确表头/数据合同，并补真实权限和目录—定义—后端注册集合一致性门禁。
