# 交接快照

每个会话第一份读的文件。**保持简短，覆盖更新，不追加历史。**
铁律见 `CLAUDE.md`；规格见 `复刻-local`；架构底线见 `docs/04/06/07`。

## 当前快照（每次收尾覆盖）

- 更新时间：2026-06-27
- 当前批次/模块：**整体 Review / 工作区清洁与方向固化已完成**。本批不新增业务功能；已固化代码/资料边界、验证产物规则、A109 采购在途口径、原生共享表格内核路线，并移除前端 VXE 运行依赖；`verification/` 历史产物已迁出到资料库归档。
- 进度：B0/B1/C/D/E/F/G/A2-A78 已完成验收。A8 已补多行明细和 A2-A7 多行/多状态回归。A9-A106 详见历史报告与 `docs/15`；A107-A112d 已完成共享表格内核、横滑、表格高度和用户眼验收口；A113a 已完成销售链前端范式；A113b 已完成单价批量填充 + 价格来源统计接口。本批完成方向复核后，可继续销售链或先清理提交边界。
- 可跑命令：`./scripts/dev-up.sh`；`./scripts/dev-status.sh`；`./scripts/dev-down.sh`
- 本地服务地址：前端 `http://127.0.0.1:5173/`；后端 `http://127.0.0.1:8080/`；PostgreSQL `localhost:5432`；Redis `localhost:6379`
- 数据库状态：Docker context `desktop-linux`；`jdy-erp-postgres`、`jdy-erp-redis` 均 healthy；Flyway schema version `50`
- 最近通过的测试：`./scripts/dev-status.sh` 通过；`bash -n scripts/dev-up.sh scripts/dev-status.sh scripts/dev-down.sh` 通过；`npm --prefix frontend run build` 通过。上一业务批证据仍为：`JAVA_HOME=/opt/homebrew/Cellar/openjdk@21/21.0.11/libexec/openjdk.jdk/Contents/Home ./mvnw test` 通过；`node scripts/a113b-unit-price-bulk-fill-regression.mjs` 通过；`node scripts/run-regression-tier.mjs smoke` 通过（10/10）。
- 当前验收等级：整体 Review 批达到文档/工具收口。未重跑 `build-jdy-feature-approval.mjs`，未手改 `featureScope.ts`；本批不改单据保存/审核/库存/财务业务规则。
- 已知阻塞：无
- 最近报告：全部归档到 `docs/验收报告/`；最近为 `docs/验收报告/验收报告-仓库资料分离-20260627.md`、`docs/验收报告/验收报告-整体Review-工作区清洁与方向复核-20260627.md`、`docs/验收报告/验收报告-A113b-单价批量填充-20260627.md`、`docs/验收报告/验收报告-A113a-销售链前端范式-20260627.md`。
- 列表 API 契约：`docs/13-列表API契约.md`
- 批次坐标：`docs/12-当前批次验收清单.md` 固定区维护 A/C/D/E/F/G/A2-A101 ↔ B0-B6 ↔ 复刻-local 入口映射。
- 验证证据：历史 `verification/` 运行产物已归档到 `/Users/linzhenyue/Projects/JDY-资料库/verification-archive-20260627`；关键结论仍以 `docs/验收报告/` 为准。
- 验证产物目录：`verification/` 为本地临时产物，默认不进 Git，只保留 `.gitkeep` 目录骨架；审批 Excel 目录仍为 `outputs/`，不要改。关键验收截图归档到 `docs/验收报告/` 下稳定路径。
- 下一步：由用户决定先清理提交边界，还是继续销售链后续批次。后续批次按 `CLAUDE.md`/`docs/11` 的表现/行为/共享核心逻辑判定选择 `smoke`、`area:<module>` 或 `full`；勿重跑 `build-jdy-feature-approval.mjs` 或手改 `featureScope.ts`。

## 开工前必读（廉价起步）

1. 本快照
2. 当前批次验收清单 `docs/12-当前批次验收清单.md`
3. 目标入口的 `复刻-local` 截图 + 对应 B 批次组件规格
4. 本任务相关代码文件

不要重读全部 docs；规格按需点开 `复刻-local/02_复刻规划`。

## 收尾必更（一行带过即可）

- 高风险逻辑（库存/资金/审核/权限）是否动过、是否测过
- 是否有临时实现/桩、未验证推理补完
- 是否完成截图视觉验收、达到的验收等级（A0-A4）
- 范围有无调整（调整走 `config/implementation-overrides.json` + 重跑生成脚本，勿手改 01/02/03）
