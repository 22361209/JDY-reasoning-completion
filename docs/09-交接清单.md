---
roadmapItemIds:
  - "4"
roadmapGateId: null
---

# 交接快照

每个会话第一份读的文件。保持简短、覆盖更新、不追加历史。铁律见 `CLAUDE.md`，当前任务见 `docs/12-当前批次验收清单.md`，规格仍以 `JDY-复刻-local` 为准。

## 当前快照

- 更新时间：2026-07-13。
- 当前分支：`codex/review-cleanup-20260627`；A137 开工基线 `bf931e1a`。A136 的 6 个独立提交尚未 push，远端仍停在 A135 `c53f2b78`。
- 当前唯一批次：**A137 Tenant Schema 约束与结构一致性**；roadmap item 4，目标 A4。
- A136 已达到 A4并关闭 item 3；完整证据见 `docs/验收报告/验收报告-A136-操作日志真实主体与状态追溯-20260713.md`。
- A137 只读基线：两个 tenant 各 72 张受管表；PK 72、UK 64、CHECK 11 与 public 参考匹配，但 FK 为 0，对应参考为 157。
- 两个 tenant 各缺 21 个后续迁移列，`production_plan.status` 默认值仍为旧 `AUDITED`；各保留 7 个 V86 已退役且当前表行数为 0 的 `is_tax_inclusive` 列。
- FK 预检发现 `tenant_a119ops_49f5546b.stock_count_line` 7 行 A136 自动化遗留，每行同时缺盘点父单和物料父记录，共 14 个孤儿关系；另一 tenant 未发现孤儿。
- 7 行在当前 tenant、public 和另一 tenant 均无父记录，无法真实重建；已在 docs/12 登记精确 ID、证据和推荐处置。
- 当前命中 item 4 硬停止条件：等待用户决定是否备份完整 JSON 后按精确 UUID 删除这 7 条已证明的 A136 测试残留。确认前不改数据库、不写 V98。
- item 5、6、6A–6E 仅做提前只读审计，生产实现仍按 predecessor 顺序推进。

## A137 固定边界

1. 参考 DDL 只认 public 受管表；不从任意 tenant 反推或复制第二份手工约束清单。
2. 现有 tenant 和新建账套必须复用同一版本化同步计划。
3. tenant 业务 FK 指向同一 tenant；平台父表才明确指向 public。
4. 缺 schema、缺表、列类型漂移、未知额外数据列、孤儿或非安全 schema 名均 fail-closed。
5. 不删除未知孤儿、不置空引用、不创建伪父记录；已证明的测试残留也必须先取得用户确认。
6. 不修改已应用 migration；A137 使用新 V98，并验证 fresh/upgrade/failure 拓扑。
7. item 4 达到 A4前不切换 item 5，也不借 A137 修改后续生产文件。

## 并行执行方式

- 主线程：A137 合同、用户决策、迁移集成、共享数据库回归、review 与独立提交。
- 审计线程：item 4 schema/孤儿清单，以及 item 5/6、6A–6E 的只读缺口盘点。
- migration、创建/删除临时账套、A119/A137、smoke/area/full 串行，避免共享 schema 与单活 session 互相污染。

## 下一步

1. 等待用户确认 7 条 A136 孤儿夹具的精确删除方案。
2. 确认后先固化隔离证据，再实现 V98 共享 DDL 同步和 provisioning 复用。
3. 完成现有 tenant、新建 tenant、非法 FK/UK/CHECK、两账套同码同号隔离、fresh/upgrade/fail-closed 拓扑验收。
4. A137 独立 review/commit 达到 A4 后，再建立 item 5 的唯一任务合同。
