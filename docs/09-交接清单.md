---
roadmapItemIds:
  - "6D"
roadmapGateId: null
---

# 交接快照

每个会话第一份读的文件。保持简短、覆盖更新、不追加历史。铁律见 `CLAUDE.md`，当前任务见 `docs/12-当前批次验收清单.md`，规格仍以 `JDY-复刻-local` 为准。

## 当前快照

- 更新时间：2026-07-14。
- 当前分支：`codex/review-cleanup-20260627`；本地领先远端且尚未 push，精确提交数以 `git status` 为准。
- 当前收口批次：**A143 基础资料 Excel 导入**；roadmap 6D，F008，开工基线 `16000fc1`，已达到 A4。冻结合同与实现范围见 `docs/12-当前批次验收清单.md`，共享协议见 `docs/guides/master-data-import-protocol.md`，最终证据见 `docs/验收报告/验收报告-A143-基础资料Excel导入-20260714.md`。
- A143 已实现八类预置 `.xlsx`、multipart 预检、结构化行错误/回执、create-only 原子确认、任务结果、过期 payload 清理、统一入口与 tenant/owner 隔离；八类为物料类别、计量单位、客户、供应商、仓库、员工、账户资料和物料。
- 固定边界仍为：只接受 `.xlsx`，一文件一类资料，只新增；任一重复/错误整批不可确认；最大 10 MiB/5,000 行；确认后为 DRAFT/version=0；账户币种仅 CNY/USD；不自动审核、不覆盖、不部分成功。
- F008 已更新为 `verified / A4 / published`，保持 `surface=shared` 与空 `catalogEntryIds`；A17/A43 证据保留，并由 A143 验收报告承接本批证据。
- A143 自动化门禁已完成：目标测试 25/25、backend 277/277、frontend 212 modules、静态 527、migration 125、area 24/24+26/26+23/23+22/22、smoke 10/10；full 117/122，失败严格为冻结集合 A32/A38/A63/A101/A118，A143 新增失败 0。
- 浏览器 runId=`a143-playwright-20260714`：合法客户工作簿原子写入 1 条未审核草稿，重复编码工作簿两行均被拦截且确认禁用；`f8841011` 修复错误表长文本覆盖后，1366×768 与 1920×1080 页面无横向溢出、14/14 数据单元格不越界，窄屏表内横滚可达结果列。验收客户、三个 batch 和四条导入日志已精确清理，最终剩余 `0/0/0`。
- A137 的精确数据删除授权和 A139 的“按建议删除”均已执行完毕，不得泛化为其他数据、历史收付款记录或功能删除。
- `a126-sales-daily-usability-regression.mjs` 缺业务 cleanup/finally，存在历史测试夹具债务；不得模糊删除或简单回减库存，必须另立可审计清理任务，不与 A142、6D 或 6E 混做。

## A143 收口边界

1. F008 是八个既有主数据列表的共享能力，不新增独立 catalog owner、自定义模板设计器或通用空壳入口。
2. 预检只写 `md_import_batch`，业务资料零写入；前端不解析文件、不 Base64 化，也不决定 `canConfirm`。
3. 确认锁 batch 并全批复检，统一调用 `MasterDataCreateService`；ImportService 不得直接 INSERT/UPDATE 八类业务表。
4. 任一行、约束或成功日志失败整批回滚；同 job 并发/重放只创建一次。类别父环必须覆盖导入节点与既有父链。
5. 所有 JDBC 走 routed tenant 数据源；任务绑定 tenant 与创建者，跨 tenant/其他用户按 404，后台过期清理不拼 schema、不写 public。
6. 成功或过期清空可提交 payload；恢复备份不得复活未提交 token。批次日志禁止记录 payload、电话、邮箱、账号或银行信息原文。

## 下一步

1. A143 运行 JSON、模板渲染、截图、frontend dist 和 backend target 继续保持 ignored，不补入治理提交。
2. A143 报告、browser、最终门禁与残留审计均已通过；本治理收口提交后 6D 完成。
3. 下一提交切换到 A144/roadmap 6E 合同；不得用销售订单 fallback 或伪数据冒充库存、资金、生产报表。
4. 用户未要求 push，当前继续保持本地提交。
