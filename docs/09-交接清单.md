---
roadmapItemIds:
  - "2"
roadmapGateId: null
---

# 交接快照

每个会话第一份读的文件。保持简短、覆盖更新、不追加历史。铁律见 `CLAUDE.md`，已完成批次见 `docs/12-当前批次验收清单.md`，规格仍以 `JDY-复刻-local` 为准。

## 当前快照

- 更新时间：2026-07-12。
- 当前分支：`codex/review-cleanup-20260627`；A135 开工基线 `1c634333`，未获推送授权，本地没有对应远端 workflow 记录。
- 当前完成批次：**A135 公开身份面与密码找回安全尾项**。本批仍属于路线问题 2 的独立 review 尾项，已达到 A4；下一批候选 A136 / 路线问题 3 尚未开工。
- A135 实现与验收提交：`6715fbd0`、`c6a81b5a`、`edab2a7a`、`20f807b3`、`7e8af899`、`2ac1ce86`；最终业务规范、报告和本快照由独立治理提交收口。
- 匿名 `/api/system/session` 现在精确为 `{"authenticated":false}`；匿名 `/api/system/account-sets` 每项只含 `code/name`。登录成功后前端重拉当前用户授权账套，认证 DTO 与管理员管理 DTO 保持既有路由字段。
- 密码找回复用 Redis 原子 global/source/account 三层门禁；未知账号、限流、重复、PENDING 满额和 Redis 故障均返回同一固定 200 body。只有真实启用用户在门禁通过后进入 JDBC 事务，未知账号和 Redis fail-closed 路径对 request/log/outbox 零写。
- V96 已在当前 V95 回放、已发布 V95 拓扑和全新空库通过；未修改 V1–V95。当前主库 PENDING 为 0，既有 1013 条 DONE/REJECTED 及数据 hash 在升级时不变；定向拓扑中的旧未知/重复 PENDING 夹具按确定性规则转 `EXPIRED`。
- preset 严格可证明迁移数为 0；136 条 A44/A45 legacy 的 ID SHA-256 仍为 `5a87f5cafd4690d7e88b708d0e1e11494e52efedc6f859ee5d8743b3fd63d89a`，完整行 SHA-256 仍为 `dd2ad764fa86a4a41932471e5d3d8f95cf8fb8b7ff8580bd11066c5fef440b3e`。新建 preset 写 immutable user UUID 到 `created_by`，更新旧行不回填。
- 最终门禁：backend `137/137`；frontend build 通过；governance 通过（effective scope 104、roadmap current item 2、manifest full 111 / security membership 13）；`area:security 22/22`、smoke `10/10`；full 达到合同门槛 `105/111`、`skipped=0`，失败精确为 A32/A38/A63/A101/A116/A118。
- A135 真实回归精确比较 167 个固定响应；default、production、`local,production` 三组放宽配置均拒绝启动；SIGTERM 实测以 143 退出且清除进程组、数据库夹具和 Redis key。最新浏览器证据 console/失败网络均为 0。
- 最终数据审计：A135 临时 user/request/preset/Redis key 为 0，全库 PENDING 为 0；A49/A50/A51 临时残留为 0；隔离端口和临时数据库/worktree 已删除。
- 当前服务：后端 `8080`、前端 `5173`、PostgreSQL、Redis healthy；后端 fingerprint 为 `53c229f91df2cab820afe9de56ab007d1eb2c65fcef39dc28592fcdca397624f`，测试库存能力只对 BLD-TEST 开启。

## 固定边界

1. 密码找回限流器当前只支持直接 standalone Redis host/port 的原始 RESP 连接，不支持 AUTH、TLS、Sentinel、Cluster、非 0 DB 或连接池；不兼容时 fail-closed，不构成安全绕过，但生产可用性部署需另立适配。
2. 136 条 legacy preset 不迁移、不删除、不回填；是否按固定 ID 清理 A44/A45 回归夹具必须另立合同。
3. 不把操作日志真实 actor 伪装成 A135 已解决；路线问题 3–6/6A–6E 继续按 `config/remediation-roadmap.json` 原顺序保留。
4. F021/F031/发货通知范围归属继续沿用 A130 待决项；不得在后续批次中顺带改 scope/status。

## 下一步

1. 另立 A136 合同后，才把 `docs/09` 与 `docs/12` 的 roadmap frontmatter 同步切到 item `3`，处理“操作日志缺真实操作人并伪装成本地管理员”。
2. A136 开工前先做 actor 写入、展示、导出、历史空值与系统任务身份的只读基线，不复用 display name 作为不可变身份。
3. 保留 full 的 6 个既有失败为各自独立任务，不用放宽断言换取全绿。
