# 交接快照

每个会话第一份读的文件。**保持简短，覆盖更新，不追加历史。**
铁律见 `CLAUDE.md`；规格见 `复刻-local`；架构底线见 `docs/04/06/07`。

## 当前快照（每次收尾覆盖）

- 更新时间：2026-06-28
- 当前批次/模块：**销售链源单链路、销售报价单与共享表格/外壳机制收口**。已统一“选源单”文案；新增销售报价单入口、表、接口、列表、打印/引出输出；销售订单可从有效期内销售报价单选源单，发货通知单可从销售订单选源单，销售出库/采购入库保留统一“选源单”入口；分录表列头按钮/拖宽热区/默认交期按共享规则收口；列顺序拖拽已抽到 `useColumnReorder`；单据标题+动作条已抽为 `DocumentCommandHeader`，分录“操作”列全局废弃，顶部栏与首页工作台做轻量化。
- 进度：B0/B1/C/D/E/F/G/A2-A78 已完成验收。A8 已补多行明细和 A2-A7 多行/多状态回归。A9-A106 详见历史报告与 `docs/15`；A107-A112d 已完成共享表格内核、横滑、表格高度和用户眼验收口；A113a 已完成销售链前端范式；A113b 已完成单价批量填充 + 价格来源统计接口。本批补齐销售报价单、报价启用/禁用口径和源单链路。
- 可跑命令：`./scripts/dev-up.sh`；`./scripts/dev-status.sh`；`./scripts/dev-down.sh`
- 本地服务地址：前端 `http://127.0.0.1:5173/`；后端 `http://127.0.0.1:8080/`；PostgreSQL `localhost:5432`；Redis `localhost:6379`
- 数据库状态：Docker context `desktop-linux`；`jdy-erp-postgres`、`jdy-erp-redis` 均 healthy；Flyway schema version `54`
- 最近通过的测试：`npm --prefix frontend run build` 通过；`JAVA_HOME=/opt/homebrew/Cellar/openjdk@21/21.0.11/libexec/openjdk.jdk/Contents/Home ./backend/mvnw -f backend/pom.xml test` 通过（13/13）；`node scripts/run-regression-tier.mjs smoke` 通过（10/10）；`node scripts/a10-entry-efficiency-regression.mjs`、`node scripts/a13-entry-row-reorder-regression.mjs`、`node scripts/a110-table-display-brand-regression.mjs --phase=after` 通过。
- 当前验收等级：销售链行为改动已过后端测试和 `smoke`；销售报价单有效期/取价口径、共享分录表视觉交互、顶部外壳轻量化和分录操作列废弃已做局部专项验证。本批改动涉及销售报价单生命周期、销售订单报价来源保存、销售价格记忆取价规则和共享表格表头/列顺序拖拽/默认交期；未改库存/资金过账规则，未重跑 `build-jdy-feature-approval.mjs`，未手改 `featureScope.ts`。`area:sales` 本轮未跑，提交前若要更稳可补跑。
- 已知阻塞：无
- 最近报告：全部归档到 `docs/验收报告/`；最近为 `docs/验收报告/验收报告-Git历史瘦身-20260627.md`、`docs/验收报告/验收报告-仓库资料分离-20260627.md`、`docs/验收报告/验收报告-整体Review-工作区清洁与方向复核-20260627.md`、`docs/验收报告/验收报告-A113b-单价批量填充-20260627.md`、`docs/验收报告/验收报告-A113a-销售链前端范式-20260627.md`。
- 列表 API 契约：`docs/13-列表API契约.md`
- 协作指南目录：`docs/guides/`，包含业务规则、动作按钮规则、仓库文件地图。
- 批次坐标：`docs/12-当前批次验收清单.md` 固定区维护 A/C/D/E/F/G/A2-A101 ↔ B0-B6 ↔ 复刻-local 入口映射。
- 验证证据：历史 `verification/` 运行产物已归档到 `/Users/linzhenyue/Projects/JDY-资料库/verification-archive-20260627`；关键结论仍以 `docs/验收报告/` 为准。
- 验证产物目录：`verification/` 为本地临时产物，默认不进 Git，只保留 `.gitkeep` 目录骨架；审批 Excel 目录仍为 `outputs/`，不要改。关键验收截图归档到 `docs/验收报告/` 下稳定路径。
- Git 历史：2026-06-27 已重写本地历史剔除 `verification/**`，`.git/` 从约 1.7G 降至约 5.7M；重写前 bundle 备份在 `/Users/linzhenyue/Projects/JDY-资料库/git-backups/20260627-174203/`。
- 下一步：用户确认后提交当前批次；后续可继续补采购/销售更多单据的同源弹窗复用抽象，或继续工作台/导航视觉细调。后续批次按 `CLAUDE.md`/`docs/11` 的表现/行为/共享核心逻辑判定选择 `smoke`、`area:<module>` 或 `full`；勿重跑 `build-jdy-feature-approval.mjs` 或手改 `featureScope.ts`。

## 开工前必读（廉价起步）

1. 本快照
2. 当前批次验收清单 `docs/12-当前批次验收清单.md`
3. 目标入口的 `复刻-local` 截图 + 对应 B 批次组件规格
4. 本任务相关代码文件

不要重读全部 docs；规格按需点开 `复刻-local/02_复刻规划`。

## 收尾必更（一行带过即可）

- 高风险逻辑（库存/资金/审核/权限）是否动过、是否测过
- 是否有临时实现/桩、未验证推理补完
- 是否完成截图视觉验收、达到的验收等级（A0-A4）
- 范围有无调整（调整走 `config/implementation-overrides.json` + 重跑生成脚本，勿手改 01/02/03）
