# 交接快照

每个会话第一份读的文件。**保持简短，覆盖更新，不追加历史。**
铁律见 `CLAUDE.md`；规格见 `复刻-local`；架构底线见 `docs/04/06/07`。

## 当前快照（每次收尾覆盖）

- 更新时间：2026-06-24
- 当前批次/模块：**A13 分录行排序插入完成**
- 进度：B0/B1/C/D/E/F/G/A2/A3/A4/A5/A6/A7/A8/A9/A10/A11/A12 已完成验收；A8 已补多行明细和 A2-A7 多行/多状态回归。A9 已补剩余数量下推和超量审核保护。A10 已补分录复制行、批量填仓库、键盘移动。A11 已补下推数量确认。A12 已补订单详情已执行/剩余列。A13 已补分录行后插入、键盘插入和拖拽排序。
- 可跑命令：`cd infra && docker compose up -d`；`cd backend && ./mvnw spring-boot:run`；`cd frontend && npm run dev -- --host 127.0.0.1 --port 5173`
- 本地服务地址：前端 `http://127.0.0.1:5173/`；后端 `http://127.0.0.1:8080/`；PostgreSQL `localhost:5432`；Redis `localhost:6379`
- 数据库状态：Docker context `desktop-linux`；`jdy-erp-postgres`、`jdy-erp-redis` 均 healthy；Flyway schema version `7`
- 最近通过的测试：`backend ./mvnw test` 通过；`frontend npm run build` 通过；Playwright A13 分录行排序插入通过，按钮插入后 `CP-001 / CP-T413874`，键盘插入后 `CP-001 / CP-T413874 / PJ-014`，拖拽排序后 `PJ-014 / CP-001 / CP-T413874`。
- 当前验收等级：A13 让草稿单据分录具备行后插入、键盘插入和拖拽排序，更接近大量录单场景。
- 已知阻塞：无
- 最近报告：全部归档到 `docs/验收报告/`；最近为 `docs/验收报告/验收报告-A13-分录行排序插入-20260624.md`、`docs/验收报告/验收报告-A12-订单执行剩余列-20260624.md`、`docs/验收报告/验收报告-A11-下推数量确认-20260624.md`、`docs/验收报告/验收报告-A10-分录操作效率-20260624.md`、`docs/验收报告/验收报告-A9-订单剩余数量下推-20260624.md`、`docs/验收报告/验收报告-A2-A7-多行回归-20260624.md`。
- 列表 API 契约：`docs/13-列表API契约.md`
- 批次坐标：`docs/12-当前批次验收清单.md` 固定区维护 A/C/D/E/F/G/A2-A13 ↔ B0-B6 ↔ 复刻-local 入口映射。
- 截图证据：`verification/playwright/a13-entry-row-reorder-20260624070538.png`、`verification/playwright/a12-sales-execution-columns-20260624065833.png`、`verification/playwright/a12-purchase-execution-columns-20260624065833.png`、`verification/playwright/a11-sales-push-confirm-dialog-20260624065050.png`、`verification/playwright/a11-sales-push-confirm-20260624065050.png`、`verification/playwright/a10-entry-efficiency-20260624064015.png`
- 验证产物目录：`verification/`；审批 Excel 目录仍为 `outputs/`，不要改。
- 下一步：建议做 A14 下推确认弹窗“清零/全剩余/按比例”批量调整；或者做销售出库/采购入库详情的源单追踪入口。

## 开工前必读（廉价起步）

1. 本快照
2. 当前批次验收清单 `docs/12-当前批次验收清单.md`
3. 目标入口的 `复刻-local` 截图 + 对应 B 批次组件规格
4. 本任务相关代码文件

不要重读全部 docs；规格按需点开 `复刻-local/02_复刻规划`。

## 收尾必更（一行带过即可）

- 高风险逻辑（库存/资金/审核/权限）是否动过、是否测过
- 是否有临时实现/桩、未验证推理补完
- 是否完成截图视觉验收、达到的验收等级（A0-A4）
- 范围有无调整（调整走 `config/implementation-overrides.json` + 重跑生成脚本，勿手改 01/02/03）
