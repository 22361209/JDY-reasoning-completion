---
roadmapItemIds:
  - "2"
roadmapGateId: null
---

# 交接快照

每个会话第一份读的文件。保持简短、覆盖更新、不追加历史。铁律见 `CLAUDE.md`，当前任务见 `docs/12-当前批次验收清单.md`，规格仍以 `JDY-复刻-local` 为准。

## 当前快照

- 更新时间：2026-07-12
- 当前分支：`codex/review-cleanup-20260627`；A135 开工基线 `1c634333`，未获推送授权。
- 当前唯一批次：**A135 公开身份面与密码找回安全尾项**；仍属路线问题 2 的独立 review 尾项，不重做 A134，不夹带问题 3。
- A134 已在 `1c634333` 达到 A4：后端 `113/113`、前端 build、smoke `10/10`；full `104/110`，仅 A32/A38/A63/A101/A116/A118 失败。
- 公开 DTO 基线：匿名 `/api/system/session` 暴露 database/schema、附件/Redis 前缀、期间与安全策略；匿名 `/api/system/account-sets` 暴露全部启用账套路由字段。登录页实际只需 `code/name`。
- 授权列表基线：前端登录前加载全量账套，登录后未重拉当前用户授权集；WAREHOUSE/FINANCE 会残留无权账套菜单，虽后端切换仍 403。
- 密码找回基线：已知/未知账号对外同为 200 且 body 一致，但每次都无条件新增 request + operation log，无限流、冷却、去重、待处理上限或清理。当前库 1013 条均已处理。
- preset 证据基线：138 行中 136 行为 `ADMIN / 本地管理员` 旧 PERSONAL，`created_by` 全空且无 preset 日志，可自动证明归属为 0。136 行可精确证明为 A44/A45 回归夹具，本批不迁成 admin 个人数据。
- 用户决策：只迁移可唯一证明归属的旧 preset，歧义数据保持不动；公开 DTO 和找回密码防滥用独立先修。
- A135 固定口径：旧 preset 实际迁移 0 行，136 行指纹必须不变；新建 preset 记录 creator，不回填历史空值。
- 服务仍为 A134 构建：后端 `8080`、前端 `5173`、PostgreSQL、Redis healthy；A135 代码落地后必须停旧监听并验证 fingerprint。
- CI 状态：本地提交未推送，没有对应远端 workflow 记录。

## 开工协议

1. 只按 A135 `allowedPaths` 工作；先提交合同，再按公开 DTO、找回密码、preset 来源、回归与报告拆分独立提交。
2. 匿名 session 精确返回 `authenticated=false`；匿名账套只返回 `code/name`。认证 session 和管理 DTO 本批不改字段，避免破坏既有租户路由与回归。
3. 密码找回必须先过 Redis 全局/来源/账号三层 TTL 门禁；Redis 故障 fail-closed 不落库，但对外仍返回完全相同的 200 body。
4. 只为真实启用账号创建找回申请；单账号最多一条 PENDING，重复不覆盖首次联系信息。未知账号、重复、限流和故障不逐条写 request/log/outbox。
5. 不自动删除或迁移 136 条 legacy preset，不用当前 display name 唯一性冒充历史证据；回归夹具清理如需执行，必须另立固定 ID/指纹合同。
6. 不运行 `build-jdy-feature-approval.mjs`，不手改 scope/status 生成物，不实施路线问题 3–6/6A–6E。

## 收尾协议

- 后端快测必须覆盖公开 DTO 精确 shape、Redis 原子限流/故障、PENDING 并发唯一、过期/保留和并发处理。
- 真实 HTTP 必须证明已知/未知/重复/限流响应不可区分，且数据库仅有一条有效申请和一条成功日志。
- 迁移必须在当前 V95 升级副本、已发布 V95 拓扑和全新空库验证；不修改旧 migration 或 Flyway history。
- 前端必须用真实浏览器验证登录页仅有公开账套摘要，WAREHOUSE/FINANCE 登录后只保留授权账套，ADMIN 管理页仍完整。
- 最终跑 backend 全测、frontend build、governance、area:security、smoke、full；新增 A135 脚本后预期 security `13/13`、full `105/111`，失败只允许 A32/A38/A63/A101/A116/A118。
- 最终审计实际 diff、allowedPaths、服务构建身份、Redis key 和数据恢复；独立 review finding 必须修复或明确记录。
