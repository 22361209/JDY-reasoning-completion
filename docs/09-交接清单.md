---
roadmapItemIds: []
roadmapGateId: G0-regression-baseline-before-2
---

# 交接快照

每个会话第一份读的文件。保持简短、覆盖更新、不追加历史。铁律见 `CLAUDE.md`，当前任务见 `docs/12-当前批次验收清单.md`，规格仍以 `JDY-复刻-local` 为准。

## 当前快照

- 更新时间：2026-07-12
- 已收口本地提交：A130 `40c4f79`、A131 `2eed8b5`、长期路线合同 `0f1162b`；均尚未推送。
- 当前分支：`codex/review-cleanup-20260627`
- 前序批次：**A130 已由独立提交 `40c4f79` 收口，尚未推送**。有效范围 104 项；交付状态覆盖 93 个 non-exclude 功能，56 个 catalog 入口为 `48 owner + 8 exception`，六类 capability checks 全量登记；回归脚本为 `109 full + 21 exemption`。
- 当前唯一批次：**A132 回归夹具可信基线，已命中停止条件；路线 gate `G0-regression-baseline-before-2` 保持未关闭**。
- 当前结果：A132 允许范围内的仓库、系统编号、UI 等待和财务红冲夹具已收敛；smoke 10/10，full 83/109，63 个目标脚本 46/63。17 个目标脚本已越过旧夹具失败并暴露正式产品/共享状态阻塞；生产代码、migration 和范围配置零 diff。
- 当前任务包：`docs/12-当前批次验收清单.md`
- 当前路线坐标：`G0-regression-baseline-before-2 / A132`，位于已完成的问题 1 与待办问题 2 之间。
- 长期收口路线：`config/remediation-roadmap.json` 固定保存 `0–6、6A–6E` 的顺序、边界和硬退出条件，不保存范围、完成度或当前批次状态；`docs/09` 只指向当前位置，`docs/12` 仍只保存一个当前任务。
- 服务实测：后端 `8080`、前端 `5173` 均监听且 ready；PostgreSQL、Redis 容器 healthy。
- 数据库实测：当前库保留原 V90/V91 SQL history，并由 repair 追加 installed_rank 96/97 的 `DELETE` 审计记录；V94/V95 未修改。变更前备份已验证；已发布 V95 库和全新空库均为 93 条成功 history、无 90/91，验收后全部临时库已删除。
- 最近真实门禁：A132 smoke 10/10；full 83/109，63 个目标脚本为 46/63。A119 账套切换与 full isolation 均通过。后端三拓扑仍为 48/48；effective scope、delivery status、regression manifest、A124、A128 和 `git diff --check` 通过。
- 当前范围待确认：`F021` 销售报价单（原 later、已有实现）、`F031` 采购申请（原 exclude、已有实现）、发货通知（已有实现但原审批无 scope ID）。在确认并写入 override/addition 前，不继续扩展这些业务。
- 已确认高风险待办：写接口权限 fail-open、库存直调入口、tenant schema 缺外键、主数据稀疏更新丢字段、操作日志操作人缺失、未知列表错误回落销售数据。
- 最近报告：`docs/验收报告/验收报告-A132-回归夹具可信基线-20260712.md`。
- CI 状态：`.github/workflows/project-gates.yml` 已随 A130 提交；backend job 对应命令已在三种数据库拓扑本地转绿。A130/A131/路线合同均未推送；A132 阻塞检查点即本交接所在的本地提交，亦未推送，因此没有远端 workflow 记录。
- 当前阻塞：full 共 26 个失败，结构为 `17 个 A132 目标 + 7 个原范围外 + A50/A51 2 个共享状态失败`。17 个目标脚本已稳定暴露执行列未渲染、销售出库来源追溯类型错误、零值确认与后端校验冲突、销售红冲缺 `RED_REVERSE` 领域日志、A49 preset 基线污染、高密度列表实测 32px、稀疏列表 body 仅 33px；A50/A51 单独记为新增共享状态失败，不重复计入 17 个目标。
- 当前唯一下一步：等待用户确认是否另立受控生产修复批次；未授权前不改生产代码、不清理历史业务/共享配置数据、不关闭 G0，也不进入问题 2。

## 开工协议

1. 先读本快照和 `docs/12-当前批次验收清单.md`。
2. 只打开当前任务列明的规格锚点和代码；实际改动不得超出 `allowedPaths`。
3. `pendingUserDecisions` 非空时，只允许完成不依赖该决策的治理/诊断，不得推进对应业务实现。
4. 范围只认 `config/approved-feature-scope.json + config/implementation-overrides.json` 的推导结果。
5. 不运行 `build-jdy-feature-approval.mjs`，不手改生成物。

## 收尾协议

- 按改动类型执行 `docs/11` 规定的 smoke / area / full 门禁，并记录真实失败项。
- 输出 `docs/验收报告/` 报告，更新交付状态和本快照。
- 审计 `git status --short --untracked-files=all`、实际 diff 和允许路径，移除任何夹带改动。
- 运行产物留在 ignored `verification/`；关键结论和精选证据进入验收报告。
