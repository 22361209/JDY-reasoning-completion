---
roadmapItemIds: []
roadmapGateId: G0-regression-baseline-before-2
---

# 交接快照

每个会话第一份读的文件。保持简短、覆盖更新、不追加历史。铁律见 `CLAUDE.md`，当前任务见 `docs/12-当前批次验收清单.md`，规格仍以 `JDY-复刻-local` 为准。

## 当前快照

- 更新时间：2026-07-12
- 当前分支：`codex/review-cleanup-20260627`；最终治理提交后本地相对远端 ahead 12，未获推送授权。
- 当前唯一批次：**A133 G0 生产合同阻塞修复已达到 A4**；`G0-regression-baseline-before-2` 可以关闭，问题 2 尚未创建实施批次。
- A133 结果：19 个专项全部通过；后端 `64/64`；前端 build 通过；smoke `10/10`；full `102/109`、`skipped=0`，失败精确为 A32/A38/A57/A63/A101/A116/A118。
- 已收口提交：A130 `40c4f79`、A131 `2eed8b5`、路线合同 `0f1162b`、A132 `df5c7ce`，以及 A133 合同/夹具/后端/前端/回归 `5280920`、`0d6a9f5`、`dc8142f`、`b4b9b44`、`ffb3f92`、`b8db94a`、`d7e6b53`；均尚未推送。
- 用户确认的数量规则：销售订单草稿数量允许 `0`、禁止负数，数量 `0` 不要求原因；审核时数量必须大于 `0`。单价 `0` 的既有提醒/原因合同不随本批删除。
- BLD-TEST preset 已定向恢复：A50/A51 残留均为 0，系统 preset 保留原 ID 且为 ADMIN/共享/只读/默认；最终完整指纹 `138|275758f62a20a7354e150e1d14454297`。A49/A50/A51 在两轮 full 中均通过。
- 当前任务包：`docs/12-当前批次验收清单.md`，状态为已验收；最近报告为 `docs/验收报告/验收报告-A133-G0生产合同阻塞修复-20260712.md`。
- 下一路线项：问题 2“权限 fail-open、生命周期和库存直调接口可绕过”；开工前必须另立任务、声明 allowedPaths，并保持 7 个范围外 full 失败不被顺带混改。
- 当前范围仍待确认：`F021` 销售报价单、`F031` 采购申请、发货通知范围归属；A133 未触碰。
- 服务状态：后端 `8080`、前端 `5173` ready，PostgreSQL、Redis healthy，运行于最终提交代码。
- CI 状态：本地提交均未推送，因此没有对应远端 workflow 记录。

## 开工协议

1. 先读本快照和 `docs/12-当前批次验收清单.md`。
2. A133 已完成；继续业务实现前先把 `docs/12` 替换为问题 2 的新任务合同，不得直接沿用 A133 `allowedPaths`。
3. `pendingUserDecisions` 非空时，只允许完成不依赖该决策的内容；不得推进 F021/F031/发货通知范围。
4. 范围只认 `config/approved-feature-scope.json + config/implementation-overrides.json` 的推导结果。
5. 不运行 `build-jdy-feature-approval.mjs`，不手改生成物。
6. 任何数据库恢复先完整快照、再最小修改、后逐字段比对；不得重新清理已稳定的 operation-log preset 基线。

## 收尾协议

- 问题 2 必须重新按 `docs/11` 声明后端快测、前端 build、专项、smoke 和 full 验收集合。
- 不回写 A133 已固化的验收结果；新事实进入新批次报告。
- 审计 `git status --short --untracked-files=all`、实际 diff 和允许路径，移除夹带改动。
- 运行产物留在 ignored `verification/`；关键结论和精选证据进入验收报告。
