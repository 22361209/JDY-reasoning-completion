# 交接快照

每个会话第一份读的文件。保持简短、覆盖更新、不追加历史。铁律见 `CLAUDE.md`，当前任务见 `docs/12-当前批次验收清单.md`，规格仍以 `JDY-复刻-local` 为准。

## 当前快照

- 更新时间：2026-07-12
- 起始基线：`40378478e48f5012777363002f11093b0a3bc87b`（`4037847 收敛生产车间主数据`）
- 当前分支：`codex/review-cleanup-20260627`
- 当前唯一批次：**A130 项目防偏移与可复现治理基线，已完成本地 A4 验收，由本提交收口，尚未推送**。
- 当前结果：有效范围 104 项；交付状态覆盖 93 个 non-exclude 功能，`35 verified / 38 unverified / 20 deferred`，56 个 catalog 入口为 `48 owner + 8 exception`，六类 capability checks 全量登记；回归脚本为 `109 full + 21 exemption`，不再依赖 ignored 清单。
- 当前任务包：`docs/12-当前批次验收清单.md`
- 服务实测：后端 `8080`、前端 `5173` 均监听且 ready；PostgreSQL、Redis 容器 healthy。
- 数据库实测：`flyway_schema_history` 最高版本为 `95`；当前本地历史同时存在 `90/91` 与 `94/95` 两组同主题迁移。A130 不处理；下一独立批次必须先备份、核对 Git 历史及本地/已发布/全新库三种拓扑，不能直接补回低版本迁移。
- 最近真实门禁：effective scope、delivery status、regression manifest、前端 build、A115、A124、A128、`git diff --check` 通过；后端 48 项中 9 个 error；smoke 10 项中 4 通过/6 失败；tracked full 109 项中 38 通过/71 失败/0 skipped。
- 当前范围待确认：`F021` 销售报价单（原 later、已有实现）、`F031` 采购申请（原 exclude、已有实现）、发货通知（已有实现但原审批无 scope ID）。在确认并写入 override/addition 前，不继续扩展这些业务。
- 已确认高风险待办：写接口权限 fail-open、库存直调入口、tenant schema 缺外键、主数据稀疏更新丢字段、操作日志操作人缺失、未知列表错误回落销售数据。
- 最近报告：`docs/验收报告/验收报告-A130-项目防偏移与可复现治理基线-20260712.md`。
- CI 状态：`.github/workflows/project-gates.yml` 已建立，本地治理、构建命令已验证；A130 快照中的 backend job 预计为红。当前提交尚未推送，因此没有远端 workflow 记录。
- 当前阻塞：整个 ERP 未通过发布门禁；三个范围决策继续阻塞对应业务扩展。
- 当前唯一下一步：另开 A131 可复现测试与 Flyway 基线批次；先备份并对账 V90/V91/V94/V95 及已发布升级拓扑，再修自动编号与默认车间测试夹具，不修改正式业务规则。

## 开工协议

1. 先读本快照和 `docs/12-当前批次验收清单.md`。
2. 只打开当前任务列明的规格锚点和代码；实际改动不得超出 `allowedPaths`。
3. `pendingUserDecisions` 非空时，只允许完成不依赖该决策的治理/诊断，不得推进对应业务实现。
4. 范围只认 `config/approved-feature-scope.json + config/implementation-overrides.json` 的推导结果。
5. 不运行 `build-jdy-feature-approval.mjs`，不手改生成物。

## 收尾协议

- 按改动类型执行 `docs/11` 规定的 smoke / area / full 门禁，并记录真实失败项。
- 输出 `docs/验收报告/` 报告，更新交付状态和本快照。
- 审计 `git status --short --untracked-files=all`、实际 diff 和允许路径，移除任何夹带改动。
- 运行产物留在 ignored `verification/`；关键结论和精选证据进入验收报告。
