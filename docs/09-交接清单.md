# 交接快照

每个会话第一份读的文件。**保持简短，覆盖更新，不追加历史。**
铁律见 `CLAUDE.md`；规格见 `复刻-local`；架构底线见 `docs/04/06/07`。

## 当前快照（每次收尾覆盖）

- 更新时间：2026-06-28
- 当前批次/模块：**A115-P0 新项目可行性与迁移蓝图**。A114 已完成生产计划与生产用料快照；A115-0 已记录元数据驱动路线。用户提出是否新开干净项目更高效，当前先做决策蓝图，不继续写 A115 业务代码。蓝图见 `docs/guides/new-project-migration-blueprint.md`，元数据路线见 `docs/guides/bill-metadata-roadmap.md`。
- 进度：B0/B1/C/D/E/F/G/A2-A78 已完成验收。A8 已补多行明细和 A2-A7 多行/多状态回归。A9-A106 详见历史报告与 `docs/15`；A107-A112d 已完成共享表格内核、横滑、表格高度和用户眼验收口；A113a 已完成销售链前端范式；A113b 已完成单价批量填充 + 价格来源统计接口；销售报价单/源单链路已在提交 `bfbebd3` 收口。A114 已补生产计划、生产任务用料快照和生产计划入口，生产领料已改为按任务快照，不再运行时直接读标准 BOM。
- 可跑命令：`./scripts/dev-up.sh`；`./scripts/dev-status.sh`；`./scripts/dev-down.sh`
- 本地服务地址：前端 `http://127.0.0.1:5173/`；后端 `http://127.0.0.1:8080/`；PostgreSQL `localhost:5432`；Redis `localhost:6379`
- 数据库状态：Docker context `desktop-linux`；`jdy-erp-postgres`、`jdy-erp-redis` 均 healthy；Flyway schema version `58`
- 最近通过的测试：`npm --prefix frontend run build` 通过；`JAVA_HOME=/opt/homebrew/opt/openjdk@21/libexec/openjdk.jdk/Contents/Home ./mvnw test` 通过（13/13，Flyway 58）；`node scripts/a114-production-plan-snapshot-regression.mjs` 通过；`node scripts/run-regression-tier.mjs area:production` 通过（15/15）。
- 当前验收等级：A115-P0 只做新项目可行性与迁移蓝图，不改业务代码。推荐结论是“新项目孵化，当前项目保留为可运行参考实现”，先用销售报价单 → 销售订单样板做 2-3 天验证，再决定是否继续迁移。未重跑 `build-jdy-feature-approval.mjs`，未手改 `featureScope.ts`。
- 已知阻塞：无
- 最近报告：全部归档到 `docs/验收报告/`；最近为 `docs/验收报告/验收报告-A114-生产计划与用料快照-20260628.md`、`docs/验收报告/验收报告-Git历史瘦身-20260627.md`、`docs/验收报告/验收报告-仓库资料分离-20260627.md`、`docs/验收报告/验收报告-整体Review-工作区清洁与方向复核-20260627.md`、`docs/验收报告/验收报告-A113b-单价批量填充-20260627.md`。
- 列表 API 契约：`docs/13-列表API契约.md`
- 协作指南目录：`docs/guides/`，包含业务规则、动作按钮规则、仓库文件地图、`docs/guides/bill-metadata-roadmap.md` 和 `docs/guides/new-project-migration-blueprint.md`。业务规则开头已记录云星空业务骨架优先级（物料、BOM、销售订单、库存、生产计划/任务、生产用料快照、生产执行、采购订单），并明确本项目第一版生产链走“自发生产计划 → 生产任务 → 审核 → 生产领料 → 产品入库”的轻量路径，不默认从销售订单下推生产任务。
- 批次坐标：`docs/12-当前批次验收清单.md` 固定区维护 A/C/D/E/F/G/A2-A101 ↔ B0-B6 ↔ 复刻-local 入口映射。
- 验证证据：历史 `verification/` 运行产物已归档到 `/Users/linzhenyue/Projects/JDY-资料库/verification-archive-20260627`；关键结论仍以 `docs/验收报告/` 为准。
- 验证产物目录：`verification/` 为本地临时产物，默认不进 Git，只保留 `.gitkeep` 目录骨架；审批 Excel 目录仍为 `outputs/`，不要改。关键验收截图归档到 `docs/验收报告/` 下稳定路径。
- Git 历史：2026-06-27 已重写本地历史剔除 `verification/**`，`.git/` 从约 1.7G 降至约 5.7M；重写前 bundle 备份在 `/Users/linzhenyue/Projects/JDY-资料库/git-backups/20260627-174203/`。
- 下一步：等待用户决定是否启动新项目孵化。若启动，建议新目录 `/Users/linzhenyue/Projects/BLD-ERP`，第一阶段只做物料/客户基础资料、销售报价单、销售订单样板；当前项目只修阻断问题，不再大扩展。若不启动，则恢复 A115-1~3 旧项目内轻量定义层样板。后续批次按 `CLAUDE.md`/`docs/11` 的表现/行为/共享核心逻辑判定选择 `smoke`、`area:<module>` 或 `full`；勿重跑 `build-jdy-feature-approval.mjs` 或手改 `featureScope.ts`。

## 开工前必读（廉价起步）

1. 本快照
2. 当前批次验收清单 `docs/12-当前批次验收清单.md`
3. 目标入口的 `复刻-local` 截图 + 对应 B 批次组件规格
4. 本任务相关代码文件

不要重读全部 docs；规格按需点开 `复刻-local/02_复刻规划`。

## 收尾必更（一行带过即可）

- 高风险逻辑（库存/资金/审核/权限）是否动过、是否测过
- 是否有临时实现/桩、未验证推理补完
- 是否完成截图视觉验收、达到的验收等级（A0-A4）
- 范围有无调整（调整走 `config/implementation-overrides.json` + 重跑生成脚本，勿手改 01/02/03）
