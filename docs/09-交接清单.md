# 交接快照

每个会话第一份读的文件。**保持简短，覆盖更新，不追加历史。**
铁律见 `CLAUDE.md`；规格见 `复刻-local`；架构底线见 `docs/04/06/07`。

## 当前快照（每次收尾覆盖）

- 更新时间：2026-06-24
- 当前批次/模块：**A8 表单多行明细完成；A2-A7 多行/多状态回归通过**
- 进度：B0/B1/C/D/E/F/G/A2/A3/A4/A5/A6/A7 已完成首轮验收；A8 已把销售订单、销售出库、采购订单、采购入库四类核心表单推进到多行明细渲染、增删、逐行选择器和合计金额，并补齐生产领料/产品入库的真实多行详情。A2-A7 已使用 >=3 行、多状态真实数据回验通过，旧单行最小样例门禁已补深。
- 可跑命令：`cd infra && docker compose up -d`；`cd backend && ./mvnw spring-boot:run`；`cd frontend && npm run dev -- --host 127.0.0.1 --port 5173`
- 本地服务地址：前端 `http://127.0.0.1:5173/`；后端 `http://127.0.0.1:8080/`；PostgreSQL `localhost:5432`；Redis `localhost:6379`
- 数据库状态：Docker context `desktop-linux`；`jdy-erp-postgres`、`jdy-erp-redis` 均 healthy；Flyway schema version `7`
- 最近通过的测试：`backend ./mvnw test` 通过；`frontend npm run build` 通过；Playwright A2-A7 多行回归通过，销售订单、销售出库、采购订单、采购入库、生产领料、产品入库均从列表打开详情，断言行数 = 3、合计金额正确、状态章正确。
- 当前验收等级：A8 表单多行明细和 A2-A7 多行/多状态回归达到本机员工试用的最小闭环；生产领料、产品入库已补真实明细和列表打开详情。
- 已知阻塞：无
- 最近报告：全部归档到 `docs/验收报告/`；最近为 `docs/验收报告/验收报告-A2-A7-多行回归-20260624.md`、`docs/验收报告/验收报告-A8-表单明细多行真实渲染-20260624.md`、`docs/验收报告/验收报告-A7-多单据列表打开详情-20260624.md`、`docs/验收报告/验收报告-A6-销售订单列表打开详情-20260624.md`、`docs/验收报告/验收报告-A5-销售订单下推销售出库闭环-20260624.md`、`docs/验收报告/验收报告-A4-销售订单新增可用化-20260624.md`。
- 列表 API 契约：`docs/13-列表API契约.md`
- 批次坐标：`docs/12-当前批次验收清单.md` 固定区维护 A/C/D/E/F/G/A2-A8 ↔ B0-B6 ↔ 复刻-local 入口映射。
- 截图证据：`verification/playwright/a2-a7-regression-sales-20260624062534.png`、`verification/playwright/a2-a7-regression-sales-out-20260624062534.png`、`verification/playwright/a2-a7-regression-purchase-20260624062534.png`、`verification/playwright/a2-a7-regression-purchase-in-20260624062534.png`、`verification/playwright/a2-a7-regression-material-issue-20260624062534.png`、`verification/playwright/a2-a7-regression-product-in-20260624062534.png`、`verification/playwright/a8-sales-order-multiline-1366.png`、`verification/playwright/a8-sales-out-multiline-1366.png`、`verification/playwright/a8-purchase-order-multiline-1366.png`、`verification/playwright/a8-purchase-in-multiline-1366.png`
- 验证产物目录：`verification/`；审批 Excel 目录仍为 `outputs/`，不要改。
- 下一步：滚动 `docs/12-当前批次验收清单.md` 到下一批；建议优先做销售/采购订单部分出入库按剩余数量下推，或补单据分录键盘操作/行复制/批量填仓库。

## 开工前必读（廉价起步）

1. 本快照
2. 当前批次验收清单 `docs/12-当前批次验收清单.md`
3. 目标入口的 `复刻-local` 截图 + 对应 B 批次组件规格
4. 本任务相关代码文件

不要重读全部 docs；规格按需点开 `复刻-local/02_复刻规划`。

## 收尾必更（一行带过即可）

- 高风险逻辑（库存/资金/审核/权限）是否动过、是否测过
- 是否有临时实现/桩、未验证推理补完
- 是否完成截图视觉验收、达到的验收等级（A0-A4）
- 范围有无调整（调整走 `config/implementation-overrides.json` + 重跑生成脚本，勿手改 01/02/03）
