# 交接快照

每个会话第一份读的文件。**保持简短，覆盖更新，不追加历史。**
铁律见 `CLAUDE.md`；规格见 `复刻-local`；架构底线见 `docs/04/06/07`。

## 当前快照（每次收尾覆盖）

- 更新时间：2026-06-24
- 当前批次/模块：**A16 源单行号追溯完成**
- 进度：B0/B1/C/D/E/F/G/A2/A3/A4/A5/A6/A7/A8/A9/A10/A11/A12/A13/A14/A15 已完成验收；A8 已补多行明细和 A2-A7 多行/多状态回归。A9 已补剩余数量下推和超量审核保护。A10-A15 已补分录效率、下推确认、订单执行列、行排序插入、下推数量批量调整、源单追踪入口。A16 已补下游出入库分录源行号显示和按源行号回写源订单执行数量。
- 可跑命令：`cd infra && docker compose up -d`；`cd backend && ./mvnw spring-boot:run`；`cd frontend && npm run dev -- --host 127.0.0.1 --port 5173`
- 本地服务地址：前端 `http://127.0.0.1:5173/`；后端 `http://127.0.0.1:8080/`；PostgreSQL `localhost:5432`；Redis `localhost:6379`
- 数据库状态：Docker context `desktop-linux`；`jdy-erp-postgres`、`jdy-erp-redis` 均 healthy；Flyway schema version `8`
- 最近通过的测试：`backend ./mvnw test` 通过；`frontend npm run build` 通过；Playwright A16 源单行号追溯通过，销售出库 `XSCK-A16-20260624073003` 显示源行号 `#3 / #1` 且源销售订单已出库数量 `4 / 0 / 2`，采购入库 `CGRK-A16-20260624073003` 显示源行号 `#3 / #1` 且源采购订单已入库数量 `5 / 0 / 3`。
- 当前验收等级：A16 让下游出入库分录具备真实源行追溯，删除/重排行后仍能正确回写源订单执行数量。
- 已知阻塞：无
- 最近报告：全部归档到 `docs/验收报告/`；最近为 `docs/验收报告/验收报告-A16-源单行号追溯-20260624.md`、`docs/验收报告/验收报告-A15-源单追踪入口-20260624.md`、`docs/验收报告/验收报告-A14-下推数量批量调整-20260624.md`、`docs/验收报告/验收报告-A13-分录行排序插入-20260624.md`、`docs/验收报告/验收报告-A12-订单执行剩余列-20260624.md`、`docs/验收报告/验收报告-A11-下推数量确认-20260624.md`。
- 列表 API 契约：`docs/13-列表API契约.md`
- 批次坐标：`docs/12-当前批次验收清单.md` 固定区维护 A/C/D/E/F/G/A2-A16 ↔ B0-B6 ↔ 复刻-local 入口映射。
- 截图证据：`verification/playwright/a16-sales-out-source-line-20260624073003.png`、`verification/playwright/a16-purchase-in-source-line-20260624073003.png`、`verification/playwright/a15-sales-out-source-trace-20260624072009.png`、`verification/playwright/a15-purchase-in-source-trace-20260624072009.png`、`verification/playwright/a14-push-confirm-tools-dialog-20260624071154.png`、`verification/playwright/a14-push-confirm-tools-form-20260624071154.png`
- 验证产物目录：`verification/`；审批 Excel 目录仍为 `outputs/`，不要改。
- 下一步：建议做 A17 分录行批量复制/粘贴 Excel 多行；或者做源单追踪从源行号定位到源单具体分录。

## 开工前必读（廉价起步）

1. 本快照
2. 当前批次验收清单 `docs/12-当前批次验收清单.md`
3. 目标入口的 `复刻-local` 截图 + 对应 B 批次组件规格
4. 本任务相关代码文件

不要重读全部 docs；规格按需点开 `复刻-local/02_复刻规划`。

## 收尾必更（一行带过即可）

- 高风险逻辑（库存/资金/审核/权限）是否动过、是否测过
- 是否有临时实现/桩、未验证推理补完
- 是否完成截图视觉验收、达到的验收等级（A0-A4）
- 范围有无调整（调整走 `config/implementation-overrides.json` + 重跑生成脚本，勿手改 01/02/03）
