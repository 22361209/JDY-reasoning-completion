---
roadmapItemIds:
  - "6E"
roadmapGateId: null
---

# 交接快照

每个会话第一份读的文件。保持简短、覆盖更新、不追加历史。铁律见 `CLAUDE.md`，当前任务见 `docs/12-当前批次验收清单.md`，规格仍以 `JDY-复刻-local` 为准。

## 当前快照

- 更新时间：2026-07-15。
- 当前分支：`codex/review-cleanup-20260627`；本地领先远端且尚未 push，A153 最终差异等待独立 staged review 与本地提交。
- 当前批次：**A153 6E 最终验收与治理发布已通过**。A144 冻结的 F029/F042/F061/F091/F093 合同已由 A145-A152 分项实现，并由 A153 终稿、双视口浏览器证据、全量门禁、精确清理和永久 closure gate 收口。
- roadmap **0、1、2、3、4、5、6、6A、6B、6C、6D、6E 已全部收口**。delivery status、roadmap historyRef、catalog 与 A153 报告保持同一治理快照；后续需求必须新建任务包，不能继续扩大 A144-A153 合同。
- 6E 最终等级为：F029=`A3`、F042=`A3`（底层库存来源 A4）、F061=`A4`、F091=`A3`、F093=`A3`；五项均登记为 `verified/published/catalog`。
- 金额报表必须按 CNY/USD 分组展示和合计，禁止无汇率依据的跨币种总计；F042/F061 是纯数量报表，不伪造 currency，也不跨单位合计。
- F042 只读真实 `inv_stock_txn`；生产环境不开放库存调整接口。测试夹具仅允许 `local/test/regression` + 显式开关 + 测试账套 allowlist，管理员权限不得绕过环境门禁。
- F061 草稿 `scrapQty=0` 可保存且无需原因，负数禁止；审核必须数量大于 0 且填写原因。报废审核不重复扣减领料库存；`isStockIn=false` 零库存流水，`true` 必须另行整单入报废仓，反审核前先反入库；`reissueQty` 首版仅记录且满足 `0<=reissueQty<=scrapQty`。
- 已知产品边界保留在 delivery status：F029 不含排行、利润或无可信成本/毛利分析；F042 不含成本/金额，LEGACY 不可定位流水不钻取；F091 不含预警、实际退款、预收/预付、其他应收/其他应付或汇率/折算；F093 固定 27 种类型，不开放自定义类型或删除规则；F061 无已知交付缺口。
- A137 的精确数据删除授权和 A139 的“按建议删除”均已执行完毕，不得泛化为其他数据、历史收付款记录或功能删除。A126 历史夹具债务仍须另立新任务包，不能回填到已关闭的 6E。
- `a126-sales-daily-usability-regression.mjs` 缺业务 cleanup/finally，存在历史测试夹具债务；不得模糊删除或简单回减库存，必须另立可审计清理任务，不与 A142、6D 或 6E 混做。

## 6E 冻结边界

1. 报表底座只负责受控 registry、固定 SQL、分页、totals、CSV、权限与 tenant scope；不得成为跨领域万能查询器。
2. 当前错误入口只能绑定真实 report key 或保持隐藏，不得恢复未知 list key 的销售订单 fallback。
3. F042 库存来源与 F061 正式材料报废先达到 A4，相关报表才允许按 A3 发布；“有统计页”不能替代底层业务闭环。
4. F029/F091 的金额口径按币种隔离，F042/F061 的数量口径按商品与单位快照隔离；页面、分页、合计与 CSV 必须共享同一 predicate。
5. A144-A153 每个 change set 均保持独立 review、测试和 commit；最终 catalog、状态、roadmap、报告与永久门禁只由 A153 串行发布。
6. A153 最终报告是 roadmap 6E 唯一 `historyRef`；历史领域报告只保留在对应 feature evidence，不增加第二条 6E 历史引用。

## 下一步

1. 对 A153 明确路径执行独立 staged review；若结论不是 CLEAN，修复后从 closure 开始重跑最终序列。
2. staged review 为 CLEAN 后，严格串行复核 closure、十个 area、smoke、full、浏览器证据消费、清理和治理状态，再创建 A153 独立本地提交。
3. 用户未再次要求 push；当前只提交，不推送。
4. A126 历史夹具清理以及 6E 已明确排除的扩展能力，均另立新任务包。
