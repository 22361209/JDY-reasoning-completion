# 交接快照

每个会话第一份读的文件。**保持简短，覆盖更新，不追加历史。**
铁律见 `CLAUDE.md`；规格见 `复刻-local`；架构底线见 `docs/04/06/07`。

## 当前快照（每次收尾覆盖）

- 更新时间：2026-06-30
- 当前批次/模块：**A120 BLD 页面统一协议落地已完成待用户确认提交**。本批目标是把销售模块打磨出的页面手感提升为全系统硬约束：动作按钮走注册表 + `ActionBar`，正式业务表格/表体走 `TableCore` / `EntryTable`，业务差异只放字段、列、动作定义和 handler。开工前已先提交基线 `71b13fb chore: record BLD UI protocol and stabilize regression baseline`。
- 进度：B0/B1/C/D/E/F/G/A2-A78 已完成验收。A8 已补多行明细和 A2-A7 多行/多状态回归。A9-A119 详见历史报告与 `docs/15`。A120 已新增动作注册表和统一动作条，`StandardDocument`、`DataListPage`、主数据、库存期初、编号规则、用户、权限、安全、通知、账套、生产任务、委外表面处理等页面动作条已接入 `ActionBar`；`SourceSelectorDialog`、`MasterSelectorDialog`、库存期初、编号规则、委外子件需求明细已接入 `TableCore`。
- 可跑命令：`./scripts/dev-up.sh`；`./scripts/dev-status.sh`；`./scripts/dev-down.sh`
- 本地服务地址：前端 `http://127.0.0.1:5173/`；后端 `http://127.0.0.1:8080/`；PostgreSQL `localhost:5432`；Redis `localhost:6379`
- 数据库状态：Docker context `desktop-linux`；`jdy-erp-postgres`、`jdy-erp-redis` 均 running；Flyway schema version `79`
- 最近通过的测试：`npm --prefix frontend run build` 通过；`node scripts/run-regression-tier.mjs smoke` 通过（10/10，结果 `verification/regression-tier-smoke-latest.json`）；`node scripts/run-regression-tier.mjs full --continue-on-failure` 通过（99/99，0 failed，0 skipped，结果 `verification/regression-tier-full-continue-latest.json`）；`cd backend && JAVA_HOME=/opt/homebrew/opt/openjdk@21 ./mvnw test` 通过（35/35）。
- 当前验收等级：A120 达到 A4，待用户确认是否提交。A120 不改业务规则；允许保留的 raw table 例外仅限 `DocumentDialogs.vue` 二级提示表、`DataListPage` 库存预警设置轻量表、`App.vue` 打印/追溯 HTML 字符串。未重跑 `build-jdy-feature-approval.mjs`，未手改 `featureScope.ts`。
- 已知阻塞：无
- 最近报告：全部归档到 `docs/验收报告/`；最近为 `docs/验收报告/验收报告-A120-BLD页面统一协议落地-20260630.md`、`docs/验收报告/全项目Review与回归报告-20260630.md`、`docs/验收报告/验收报告-A119-完整多账套架构迁移-20260630.md`、`docs/验收报告/验收报告-A118-委外加工闭环-20260629.md`。
- 列表 API 契约：`docs/13-列表API契约.md`
- 协作指南目录：`docs/guides/`，包含业务规则、动作按钮规则、`docs/guides/bld-page-unification-protocol.md`（BLD 页面统一协议 + 动作按钮注册表 + 表格/表体硬约束）、仓库文件地图、`docs/guides/bill-metadata-roadmap.md`、`docs/guides/new-project-migration-blueprint.md` 和 `docs/guides/account-set-architecture.md`。业务规则开头已记录云星空业务骨架优先级，并明确本项目第一版生产链走轻量路径。采购规则已补：采购退货源自采购入库，非作废退货占用可退数量，采购汇总只统计已审核采购订单/入库/退货。
- 批次坐标：`docs/12-当前批次验收清单.md` 固定区维护 A/C/D/E/F/G/A2-A101 ↔ B0-B6 ↔ 复刻-local 入口映射。
- 验证证据：历史 `verification/` 运行产物已归档到 `/Users/linzhenyue/Projects/JDY-资料库/verification-archive-20260627`；关键结论仍以 `docs/验收报告/` 为准。
- 验证产物目录：`verification/` 为本地临时产物，默认不进 Git，只保留 `.gitkeep` 目录骨架；审批 Excel 目录仍为 `outputs/`，不要改。A118 浏览器验收截图在 `verification/playwright/a118-outsourcing-source-picker-20260629131010.png`，关键结论已写入 `docs/验收报告/验收报告-A118-委外加工闭环-20260629.md`。
- Git 历史：2026-06-27 已重写本地历史剔除 `verification/**`，`.git/` 从约 1.7G 降至约 5.7M；重写前 bundle 备份在 `/Users/linzhenyue/Projects/JDY-资料库/git-backups/20260627-174203/`。
- 下一步：等待用户确认 A120 改造是否提交；后续优先拆瘦 `DataListPage.vue` 和 `EntryTable.vue` 的内部 composable，但保持外部协议不变。所有单据保存只落草稿，审核后才可被选源、下推、影响库存或参与后续链路；勿重跑 `build-jdy-feature-approval.mjs` 或手改 `featureScope.ts`。

## 开工前必读（廉价起步）

1. 本快照
2. 当前批次验收清单 `docs/12-当前批次验收清单.md`
3. 目标入口的 `复刻-local` 截图 + 对应 B 批次组件规格
4. 本任务相关代码文件

不要重读全部 docs；规格按需点开 `复刻-local/02_复刻规划`。

## 收尾必更（一行带过即可）

- 高风险逻辑（库存/资金/审核/权限）是否动过、是否测过
- 是否有临时实现/桩、未验证推理补完
- 是否完成截图视觉验收、达到的验收等级（A0-A4）
- 范围有无调整（调整走 `config/implementation-overrides.json` + 重跑生成脚本，勿手改 01/02/03）
