# 交接快照

每个会话第一份读的文件。**保持简短，覆盖更新，不追加历史。**
铁律见 `CLAUDE.md`；规格见 `复刻-local`；架构底线见 `docs/04/06/07`。

## 当前快照（每次收尾覆盖）

- 更新时间：2026-06-25
- 当前批次/模块：**A82 主数据表单迁出已完成**。236 商品、251 客户、252 供应商、255 仓库的新增/编辑对话框、字段定义、状态、save/启停/删除调用迁入 `frontend/src/modules/master-data/<entity>/` 与 `useMasterDataMaintenance.ts`；`DataListPage.vue` 只保留 B1 列表范式与工具栏触发，`App.vue` 继续收口为 shell/模块装配。
- 进度：B0/B1/C/D/E/F/G/A2-A78 已完成验收。A8 已补多行明细和 A2-A7 多行/多状态回归。A9-A20 已补订单剩余数量下推、分录效率、下推确认、订单执行列、行排序插入、批量调整、源单追踪、源行号追溯、Excel 多行粘贴、源行号定位、名称规格智能粘贴。A21-A37 已补双方向下推回归、下游执行反查、粘贴候选、仓库应用、键盘流、保存校验、零值原因、打印/引出备注、反审核/红冲风险提示、红字来源追踪、操作日志红冲检索。A38-A53 已补六类单据 PDF/HTML 输出、默认套打模板、打印模板维护、复制、角色默认、生产红冲来源打印和打印纸张/方向/边距/联次。A54-A62 已补权限目录、权限矩阵、前端权限驱动、后端权限拦截、本地 HTTP session 用户切换、用户维护页、正式登录与退出、员工自助改密、会话超时提示、登录失败锁定、安全审计和管理员解除锁定入口，前后端权限按同一登录用户生效。A64-A77 已补生产红字来源跳转、找回密码、跨标签/重复登录、安全策略、会话超时、密码策略、通知 outbox、重试、回执和供应商设置。A78-A82 已完成销售订单、销售出库、采购/生产单据、系统设置簇和主数据表单迁出，App.vue 持续收口。
- 可跑命令：`cd infra && docker compose up -d`；`cd backend && ./mvnw spring-boot:run`；`cd frontend && npm run dev -- --host 127.0.0.1 --port 5173`
- 本地服务地址：前端 `http://127.0.0.1:5173/`；后端 `http://127.0.0.1:8080/`；PostgreSQL `localhost:5432`；Redis `localhost:6379`
- 数据库状态：Docker context `desktop-linux`；`jdy-erp-postgres`、`jdy-erp-redis` 均 healthy；Flyway schema version `31`
- 最近通过的测试：`npm --prefix frontend run build` 通过；`cd backend && JAVA_HOME=/opt/homebrew/opt/openjdk@21 ./mvnw test` 通过；A2-A77 全量回归 `verification/a2-a82-regression-latest.json` 为 `ok=true`、`total=69`、`badCount=0`，未跳过。
- 当前验收等级：A82 主数据表单入口模块化完成；商品、客户、供应商、仓库均已迁到 `modules/master-data/` 对应目录，新增/编辑/启停/删除能力保持不变。`App.vue` 从 A82 基线 1391 行降到 1241 行，减少 150 行。
- 已知阻塞：无
- 最近报告：全部归档到 `docs/验收报告/`；最近为 `docs/验收报告/验收报告-A82-主数据表单迁出-20260625.md`、`docs/验收报告/验收报告-A81-系统设置簇迁出-20260625.md`、`docs/验收报告/验收报告-A80-单据表单批量迁移-20260625.md`。
- 列表 API 契约：`docs/13-列表API契约.md`
- 批次坐标：`docs/12-当前批次验收清单.md` 固定区维护 A/C/D/E/F/G/A2-A82 ↔ B0-B6 ↔ 复刻-local 入口映射，并新增 A82 主数据表单迁出坐标。
- 验证证据：A82 见 `docs/验收报告/验收报告-A82-主数据表单迁出-20260625.md`、`verification/a2-a82-regression-latest.json`。
- 验证产物目录：`verification/`；审批 Excel 目录仍为 `outputs/`，不要改。
- 下一步：继续按 B2/B3 入口模块化标准收尾剩余表单/列表残留；保持 App 只做页签装配和跨入口路由，继续执行行数下降、grep、全量回归硬门禁。

## 开工前必读（廉价起步）

1. 本快照
2. 当前批次验收清单 `docs/12-当前批次验收清单.md`
3. 目标入口的 `复刻-local` 截图 + 对应 B 批次组件规格
4. 本任务相关代码文件

不要重读全部 docs；规格按需点开 `复刻-local/02_复刻规划`。

## 收尾必更（一行带过即可）

- 高风险逻辑（库存/资金/审核/权限）是否动过、是否测过
- 是否有临时实现/桩、未验证推理补完
- 是否完成截图视觉验收、达到的验收等级（A0-A4）
- 范围有无调整（调整走 `config/implementation-overrides.json` + 重跑生成脚本，勿手改 01/02/03）
