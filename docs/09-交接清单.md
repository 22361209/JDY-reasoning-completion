---
roadmapItemIds: []
roadmapGateId: G0-regression-baseline-before-2
---

# 交接快照

每个会话第一份读的文件。保持简短、覆盖更新、不追加历史。铁律见 `CLAUDE.md`，当前任务见 `docs/12-当前批次验收清单.md`，规格仍以 `JDY-复刻-local` 为准。

## 当前快照

- 更新时间：2026-07-12
- 当前分支：`codex/review-cleanup-20260627`；本地相对远端 ahead 4，未获推送授权。
- 已收口本地提交：A130 `40c4f79`、A131 `2eed8b5`、长期路线合同 `0f1162b`、A132 阻塞证据 `df5c7ce`；均尚未推送。
- 当前唯一批次：**A133 G0 生产合同阻塞修复**；路线坐标仍为 `G0-regression-baseline-before-2`，未达到 A4 前不得进入问题 2。
- 前序真实基线：A132 smoke 10/10，full 83/109；63 个目标脚本 46/63。full 的 26 个失败为 `17 个目标 + 7 个原范围外 + A50/A51`。
- A133 获授权范围：执行列、销售出库来源追溯、销售订单草稿/审核数量合同、销售/采购红冲 `RED_REVERSE`、列表密度、稀疏填充、A50/A51 夹具隔离与 BLD-TEST preset 定向恢复；A112 密集样本改为至少 17 行，以兼容正式 `20–22.5px` 行高并保留真实滚动。
- 用户确认的数量规则：销售订单草稿数量允许 `0`、禁止负数，数量 `0` 不要求原因；审核时数量必须大于 `0`。单价 `0` 的既有提醒/原因合同不随本批删除。
- 数据恢复授权：允许在完整前快照后，仅恢复 A50/A51 影响的 `operation-log-list` preset；保留系统 preset 原 ID，不重建账套、不清整表、不动无关行。
- A133 硬退出：专项全绿、A49/A50/A51 重复运行稳定、后端测试和前端 build 通过、smoke 10/10、full 102/109 且仅剩 A32/A38/A57/A63/A101/A116/A118、skipped 0、工作区干净。
- 当前任务包：`docs/12-当前批次验收清单.md`。
- 当前范围待确认：`F021` 销售报价单、`F031` 采购申请、发货通知范围归属；本批不得触碰。
- 明确排除：权限 fail-open、库存直调、tenant 外键、主数据稀疏更新、未知列表 fallback、历史业务数据清理及路线 2–6/6A–6E 实现。
- 服务基线：后端 `8080`、前端 `5173` 此前 ready，PostgreSQL、Redis healthy；生产代码完成后必须停旧监听并重启到当前构建再验收。
- 最近报告：`docs/验收报告/验收报告-A132-回归夹具可信基线-20260712.md`；A133 报告尚未生成。
- CI 状态：本地提交均未推送，因此没有对应远端 workflow 记录。

## 开工协议

1. 先读本快照和 `docs/12-当前批次验收清单.md`。
2. 只打开当前任务列明的规格锚点和代码；实际改动不得超出 `allowedPaths`。
3. `pendingUserDecisions` 非空时，只允许完成不依赖该决策的内容；不得推进 F021/F031/发货通知范围。
4. 范围只认 `config/approved-feature-scope.json + config/implementation-overrides.json` 的推导结果。
5. 不运行 `build-jdy-feature-approval.mjs`，不手改生成物。
6. 任何数据库恢复先完整快照、再最小修改、后逐字段比对；失败不得扩大清理范围。

## 收尾协议

- 按 `docs/11` 跑后端快测、前端 build、专项、smoke 和 full，记录真实失败项。
- 输出 A133 验收报告，覆盖更新本快照；只有硬退出全部满足才可关闭 G0。
- 审计 `git status --short --untracked-files=all`、实际 diff 和允许路径，移除夹带改动。
- 运行产物留在 ignored `verification/`；关键结论和精选证据进入验收报告。
