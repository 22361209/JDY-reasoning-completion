# 交接快照

每个会话第一份读的文件。**保持简短，覆盖更新，不追加历史。**
铁律见 `CLAUDE.md`；规格见 `复刻-local`；架构底线见 `docs/04/06/07`。

## 当前快照（每次收尾覆盖）

- 更新时间：2026-06-23
- 当前批次/模块：**C/D 表单范式与真实主数据第一批**
- 进度：B0 页面外壳已完成并验收；B1 列表范式已收口；D 第一批已新增真实主数据迁移，商品/客户/供应商/仓库列表从数据库读取，库存查询从余额表读取，客户新增写库已验收。C 表单范式、主数据选择器、销售订单保存草稿仍待推进。
- 可跑命令：`cd infra && docker compose up -d`；`cd backend && ./mvnw spring-boot:run`；`cd frontend && npm run dev -- --host 127.0.0.1 --port 5173`
- 本地服务地址：前端 `http://127.0.0.1:5173/`；后端 `http://127.0.0.1:8080/`；PostgreSQL `localhost:5432`；Redis `localhost:6379`
- 数据库状态：Docker context `desktop-linux`；`jdy-erp-postgres`、`jdy-erp-redis` 均 healthy；Flyway schema version `2`
- 最近通过的测试：`backend ./mvnw test` 通过；`frontend npm run build` 通过；Playwright B1 核心流程通过；空态/列偏好/1000行渲染通过；API 契约验证 `sort/columnFilters/403/1000rows` 通过；客户新增真实写库验证通过
- 当前验收等级：B1 列表范式达到 A1+ 可复用；D 第一批真实主数据达到 A1 可操作；不是销售/库存业务闭环，不能当作 A2/A3 业务完成
- 已知阻塞：无
- 最近报告：`docs/验收报告-B1-高密度列表范式-20260623.md`、`docs/验收报告-D-真实主数据第一批-20260623.md`
- 列表 API 契约：`docs/13-列表API契约.md`
- 截图证据：`output/playwright/b1-list-vxe-1366.png`、`output/playwright/b1-list-states-prefs-1000-1366.png`、`output/playwright/d-master-customer-create-1366.png`
- 下一步：推进 C：销售订单/采购订单表单范式，并让表单选择器引用真实客户/供应商/商品

## 开工前必读（廉价起步）

1. 本快照
2. 当前批次验收清单 `docs/12-当前批次验收清单.md`
3. 目标入口的 `复刻-local` 截图 + 对应 B 批次组件规格
4. 本任务相关代码文件

不要重读全部 docs；规格按需点开 `复刻-local/02_复刻规划`。

## 收尾必更（一行带过即可）

- 高风险逻辑（库存/资金/审核/权限）是否动过、是否测过
- 是否有临时实现/桩、未验证推理补完
- 是否完成截图视觉验收、达到的验收等级（A0-A4）
- 范围有无调整（调整走 `config/implementation-overrides.json` + 重跑生成脚本，勿手改 01/02/03）
