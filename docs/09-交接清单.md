---
roadmapItemIds:
  - "4"
roadmapGateId: null
---

# 交接快照

每个会话第一份读的文件。保持简短、覆盖更新、不追加历史。铁律见 `CLAUDE.md`，当前任务见 `docs/12-当前批次验收清单.md`，规格仍以 `JDY-复刻-local` 为准。

## 当前快照

- 更新时间：2026-07-13。
- 当前分支：`codex/review-cleanup-20260627`；A137 开工基线 `bf931e1a`。A136 的 6 个独立提交尚未 push，远端仍停在 A135 `c53f2b78`。
- 当前唯一批次：**A137 Tenant Schema 约束与结构一致性**；roadmap item 4，已达到 A4，当前文档提交负责关闭。
- A136 已达到 A4并关闭 item 3；完整证据见 `docs/验收报告/验收报告-A136-操作日志真实主体与状态追溯-20260713.md`。
- 用户已确认按建议删除 7 条 A136 测试残留；完整 JSON 已先固化到 ignored `verification/a137-orphan-quarantine.json`，V98 只在每行 `(id, line_remark, product_code_snapshot, bill_id, product_id)` 完整 tuple 与父记录仍缺失同时满足时删除。迁移后 public 与两个 tenant 的盘点行缺父单、缺物料和 A136 marker 残留均为 0。
- V98 已由 Flyway 11.7.2 正式应用并在未发布阶段官方 repair 到最终源码；checksum `1143207774`、SHA-256 `c8fea70ac33b3f24c077b77c48f9c502bfa0f7d988a6002fb818b03313690a02`。V99 runtime guard checksum `-1144028933`、SHA-256 `aa1628b6776e00b1ce5fe6546d24c833ad27347a505d1d1db50720ddb9fd33a7`。
- 两个 tenant 各 72 张受管表，达到 PK 72、UK 64、有效 FK 153、CHECK 11；列漂移、参考约束漂移、未验证 FK、退役税字段和禁止的 4 个 `sys_account_set` FK 均为 0。
- 新建账套与既有 tenant 复用同一版本化同步计划；schema 唯一登记、拒绝收编既有 schema、备份完整性预检和按共同列/拓扑恢复均已固化。
- backend `167/167`、A119、A137 14 topology、area:system、smoke 通过；full `107/113`，仅冻结 A32/A38/A63/A101/A116/A118 失败，无新增失败。最终独立 review APPROVE、无剩余 P0/P1；中途发现的 2 个 P1 已由 `8733999b`、`f28626a3` 关闭。A137 合同 `a07582af`、实现 `49d6e109`、测试 `2f3e619b` 已提交。
- item 5、6、6A–6E 仅做提前只读审计，生产实现仍按 predecessor 顺序推进。

## A137 固定边界

1. 参考 DDL 只认 public 受管表；不从任意 tenant 反推或复制第二份手工约束清单。
2. 现有 tenant 和新建账套必须复用同一版本化同步计划。
3. tenant 业务 FK 指向同一 tenant；平台父表才明确指向 public。
4. 缺 schema、缺表、列类型漂移、未知额外数据列、孤儿或非安全 schema 名均 fail-closed。
5. 不删除未知孤儿、不置空引用、不创建伪父记录；已证明的测试残留也必须先取得用户确认。
6. 不修改 V97 或更早 migration；V98 在本地未发布期间修订只通过官方 Flyway repair 对齐，V99 提供运行时豁免护栏，并验证 empty fresh/upgrade/failure 拓扑。
7. item 4 达到 A4前不切换 item 5，也不借 A137 修改后续生产文件。

## 并行执行方式

- 主线程：A137 合同、用户决策、迁移集成、共享数据库回归、review 与独立提交。
- 审计线程：item 4 schema/孤儿清单，以及 item 5/6、6A–6E 的只读缺口盘点。
- migration、创建/删除临时账套、A119/A137、smoke/area/full 串行，避免共享 schema 与单活 session 互相污染。

## 下一步

1. 当前文档提交固化 A137 验收报告并关闭 item 4；保持不 push，除非用户明确要求。
2. 建立 A138/item 5“主数据稀疏更新/PATCH”唯一任务合同，再开始生产修改。
3. item 6、6A–6E 继续只读预审并按 predecessor 顺序推进，不与 A138 夹带提交。
