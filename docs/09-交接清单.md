# 交接快照

每个会话第一份读的文件。**保持简短，覆盖更新，不追加历史。**
铁律见 `CLAUDE.md`；规格见 `复刻-local`；架构底线见 `docs/04/06/07`。

## 当前快照（每次收尾覆盖）

- 更新时间：2026-06-26
- 当前批次/模块：**A93 销售出库建单/选单流已完成**。销售订单审核后下推销售出库不再弹确认框，直接打开销售出库草稿并按剩余可出数量带入商品、仓库、数量、单价、行备注、交期和客户；销售出库直接建单支持手填源订单号拉入该订单剩余明细，也支持先选客户后从该客户已下单销售订单明细中勾选同一订单的一行或多行带入。
- 进度：B0/B1/C/D/E/F/G/A2-A78 已完成验收。A8 已补多行明细和 A2-A7 多行/多状态回归。A9-A20 已补订单剩余数量下推、分录效率、下推确认、订单执行列、行排序插入、批量调整、源单追踪、源行号追溯、Excel 多行粘贴、源行号定位、名称规格智能粘贴。A21-A37 已补双方向下推回归、下游执行反查、粘贴候选、仓库应用、键盘流、保存校验、零值原因、打印/引出备注、反审核/红冲风险提示、红字来源追踪、操作日志红冲检索。A38-A53 已补六类单据 PDF/HTML 输出、默认套打模板、打印模板维护、复制、角色默认、生产红冲来源打印和打印纸张/方向/边距/联次。A54-A62 已补权限目录、权限矩阵、前端权限驱动、后端权限拦截、本地 HTTP session 用户切换、用户维护页、正式登录与退出、员工自助改密、会话超时提示、登录失败锁定、安全审计和管理员解除锁定入口，前后端权限按同一登录用户生效。A64-A77 已补生产红字来源跳转、找回密码、跨标签/重复登录、安全策略、会话超时、密码策略、通知 outbox、重试、回执和供应商设置。A78-A82 已完成前端模块化收口；A83 已建立后端单据骨架并迁销售出库；A84 已把其余 5 张核心单据迁到骨架；A85 已把销售出库/采购入库财务过账接到审核；A86 已证明库存新单广度生产线；A87 已补其他出库和直接调拨；A88 盘点链保留；A89 已修复销售核心流并全量回归；A90 已补单据编号；A91 已修共享前端 UX；A92 已补销售单据字段；A93 已重做销售出库下推直达和直接建单选源流。
- 可跑命令：`cd infra && docker compose up -d`；`cd backend && ./mvnw spring-boot:run`；`cd frontend && npm run dev -- --host 127.0.0.1 --port 5173`
- 本地服务地址：前端 `http://127.0.0.1:5173/`；后端 `http://127.0.0.1:8080/`；PostgreSQL `localhost:5432`；Redis `localhost:6379`
- 数据库状态：Docker context `desktop-linux`；`jdy-erp-postgres`、`jdy-erp-redis` 均 healthy；Flyway schema version `39`
- 最近通过的测试：`npm --prefix frontend run build` 通过；`cd backend && JAVA_HOME=/opt/homebrew/opt/openjdk@21 ./mvnw test` 通过；`node scripts/a93-sales-out-source-selection-regression.mjs` 通过；A2-A93 全量回归 `verification/a2-a93-regression-latest.json` 为 `ok=true`、`total=81`、`expectedTotal=81`、`badCount=0`、`skipped=0`，当前 mtime `2026-06-26 07:41:45 +0800`（提交后需以最终 HEAD 重跑生成的同名文件为准）。
- 当前验收等级：A93 达到 A3 业务链路回归。动到销售模块和共享外壳 `App.vue` 下推编排，已用 A93 专项、A2-A93 全量、Maven/前端 build 验证；保留后端剩余数量超量守卫，库存/财务/编号/税务口径未改。
- 已知阻塞：无
- 最近报告：全部归档到 `docs/验收报告/`；最近为 `docs/验收报告/验收报告-A93-建单选单流-20260626.md`、`docs/验收报告/验收报告-A92-单据字段-20260626.md`、`docs/验收报告/验收报告-A91-前端UX-20260626.md`。
- 列表 API 契约：`docs/13-列表API契约.md`
- 批次坐标：`docs/12-当前批次验收清单.md` 固定区维护 A/C/D/E/F/G/A2-A93 ↔ B0-B6 ↔ 复刻-local 入口映射。
- 验证证据：A93 见 `docs/验收报告/验收报告-A93-建单选单流-20260626.md`、`verification/a93-sales-out-source-selection-regression.json`、`verification/a2-a93-regression-latest.json`；A92/A91/A90/A89/A88 回归仍保留。
- 验证产物目录：`verification/`；审批 Excel 目录仍为 `outputs/`，不要改。
- 下一步：A93 已收口销售出库建单/选单流；库存广度后续可继续，但仍保持一次一张、三层参考先对齐、财务/编号/税务口径不顺手改。

## 开工前必读（廉价起步）

1. 本快照
2. 当前批次验收清单 `docs/12-当前批次验收清单.md`
3. 目标入口的 `复刻-local` 截图 + 对应 B 批次组件规格
4. 本任务相关代码文件

不要重读全部 docs；规格按需点开 `复刻-local/02_复刻规划`。

## 收尾必更（一行带过即可）

- 高风险逻辑（库存/资金/审核/权限）是否动过、是否测过
- 是否有临时实现/桩、未验证推理补完
- 是否完成截图视觉验收、达到的验收等级（A0-A4）
- 范围有无调整（调整走 `config/implementation-overrides.json` + 重跑生成脚本，勿手改 01/02/03）
