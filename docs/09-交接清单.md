---
roadmapItemIds:
  - "6"
roadmapGateId: null
---

# 交接快照

每个会话第一份读的文件。保持简短、覆盖更新、不追加历史。铁律见 `CLAUDE.md`，当前任务见 `docs/12-当前批次验收清单.md`，规格仍以 `JDY-复刻-local` 为准。

## 当前快照

- 更新时间：2026-07-13。
- 当前分支：`codex/review-cleanup-20260627`；远端仍停在 A135 `c53f2b78`，A136–A139 尚未 push。
- 当前收口批次：**A139 未知列表 Fail-Closed 与错误入口收口**；roadmap item 6 已达到 A4，开工基线 `cf1756e2`。
- A137 的数据删除授权只覆盖已列明的 7 条 A136 自动化孤儿行；A139 最新“按建议删除”只覆盖 3 个生产测试后门和 6 个未交付 catalog 入口。两次授权均已精确执行，不得泛化为其他数据或功能删除。
- A138 已落地字段白名单 PATCH：absent 保持、显式 null 只清可空列、version CAS、已审核拒绝、引用整笔回滚和真实错误反馈；四类旧 PUT 返回 405，三个范围外旧 PUT 类型不受影响。
- A138 后端 `179/179`、定向 `14/14`、`area:master-data 21/21`、smoke `10/10` 通过；full 为 `109/114`，失败仅 A32/A38/A63/A101/A118，比 A137 基线少 A116 且没有新增失败。
- A138 浏览器批次 `20260713060102` 的四页保存/重开与双端 CAS 冲突均通过并人工复核；独立 review 最终 APPROVE，无剩余 P0/P1。
- A139 已删除前端销售订单 fallback、Registry 通配/default、provider 销售行 fallback 和 selector 空集 fallback；未知普通/master/selector key 与三个退役后门在权限、adapter/provider、JDBC 前统一 404。
- 6 个未交付入口已隐藏，3 类盘点使用真实 header/SQL，采购汇总与任务跟踪使用真实权限且保留原 report key；feature level 未被自动晋级。
- A139 后端 `188/188`、A139 专项 `168` 断言/`65` 请求、`area:table 18/18`、`area:system 16/16`、smoke `10/10` 通过；inventory 仅冻结 A101。full 为 `110/115`，失败严格为 A32/A38/A63/A101/A118。
- 最新浏览器批次 `20260713110024` 的 5 张截图已人工复核，夹具清理残留为 0；独立 review 最终 APPROVE，无剩余 P0/P1。
- 6A 已完成只读预审，6B–6E 继续按 predecessor 顺序；不得与 A139 夹带提交。

## A139 完成事实

1. query/export 固定为精确 key 404 → 真实权限 403 → adapter/provider/JDBC；未知管理员和低权限用户都先 404。
2. Registry 只认可精确 key；provider default 不返回 salesRows，selector default 不返回 200 空集，前端不保留 sales-order fallbackDefinition。
3. `standard-list/error-list/permission-denied-list` 从 main 删除；测试只断言它们作为未知 key 返回 404。
4. 隐藏六个未交付入口，不实现报表；为三类盘点补真实 header，为采购汇总和任务跟踪补现有权限码。
5. 报表入口名称与“查询”都必须保留原 key，不拼 `*-report-list`。
6. 同步 delivery status 的 entry 归属，但不自动晋级 feature；补 item 2–6 的真实 historyRefs。
7. A115/A124/A128/A139、backend、build、area:table/inventory/system、smoke、full 和独立 review 均已执行；只保留登记的冻结失败。

## 下一步

1. 提交 A139 最终验收报告与 item 6 `historyRefs`，确认工作树干净；保持不 push，除非用户明确要求。
2. 建立 A140/6A“账户资料与员工主档”合同：租户内新增 `md_employee` 与 `md_financial_account`，员工主档与平台登录用户分离。
3. 冻结员工—账号按账套 0..1 显式关联、禁用不级联，以及首版财务账户仅 CASH/BANK/DEPOSIT、CNY、不含余额/多币种的边界后再实现。
