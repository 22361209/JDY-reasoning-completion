# 交接快照

每个会话第一份读的文件。**保持简短，覆盖更新，不追加历史。**
铁律见 `CLAUDE.md`；规格见 `复刻-local`；架构底线见 `docs/04/06/07`。

## 当前快照（每次收尾覆盖）

- 更新时间：2026-06-24
- 当前批次/模块：**A11 下推数量确认完成**
- 进度：B0/B1/C/D/E/F/G/A2/A3/A4/A5/A6/A7/A8/A9/A10 已完成验收；A8 已补多行明细和 A2-A7 多行/多状态回归。A9 已补销售/采购剩余数量下推和超量审核保护。A10 已补分录复制行、批量填仓库、键盘移动。A11 已补销售/采购下推前确认本次数量，确认后生成下游草稿。
- 可跑命令：`cd infra && docker compose up -d`；`cd backend && ./mvnw spring-boot:run`；`cd frontend && npm run dev -- --host 127.0.0.1 --port 5173`
- 本地服务地址：前端 `http://127.0.0.1:5173/`；后端 `http://127.0.0.1:8080/`；PostgreSQL `localhost:5432`；Redis `localhost:6379`
- 数据库状态：Docker context `desktop-linux`；`jdy-erp-postgres`、`jdy-erp-redis` 均 healthy；Flyway schema version `7`
- 最近通过的测试：`backend ./mvnw test` 通过；`frontend npm run build` 通过；Playwright A11 下推数量确认通过，销售/采购默认剩余数量均为 `6 / 5 / 4`，销售确认 `2 / 1 / 3`、采购确认 `1 / 2 / 3` 后均保存并审核成功，销售超剩余输入被前端阻止。
- 当前验收等级：A11 让销售/采购下推从“系统直接生成”变为“员工确认本次数量后生成”，更接近真实 ERP 日常操作。
- 已知阻塞：无
- 最近报告：全部归档到 `docs/验收报告/`；最近为 `docs/验收报告/验收报告-A11-下推数量确认-20260624.md`、`docs/验收报告/验收报告-A10-分录操作效率-20260624.md`、`docs/验收报告/验收报告-A9-订单剩余数量下推-20260624.md`、`docs/验收报告/验收报告-A2-A7-多行回归-20260624.md`、`docs/验收报告/验收报告-A8-表单明细多行真实渲染-20260624.md`、`docs/验收报告/验收报告-A7-多单据列表打开详情-20260624.md`。
- 列表 API 契约：`docs/13-列表API契约.md`
- 批次坐标：`docs/12-当前批次验收清单.md` 固定区维护 A/C/D/E/F/G/A2-A11 ↔ B0-B6 ↔ 复刻-local 入口映射。
- 截图证据：`verification/playwright/a11-sales-push-confirm-dialog-20260624065050.png`、`verification/playwright/a11-sales-push-confirm-20260624065050.png`、`verification/playwright/a11-purchase-push-confirm-dialog-20260624065050.png`、`verification/playwright/a11-purchase-push-confirm-20260624065050.png`、`verification/playwright/a10-entry-efficiency-20260624064015.png`、`verification/playwright/a9-sales-remaining-pushdown-20260624063328.png`
- 验证产物目录：`verification/`；审批 Excel 目录仍为 `outputs/`，不要改。
- 下一步：建议做 A12 单据详情执行数量/剩余数量只读列；或者做下推确认弹窗的“清零/全剩余/按比例”批量调整。

## 开工前必读（廉价起步）

1. 本快照
2. 当前批次验收清单 `docs/12-当前批次验收清单.md`
3. 目标入口的 `复刻-local` 截图 + 对应 B 批次组件规格
4. 本任务相关代码文件

不要重读全部 docs；规格按需点开 `复刻-local/02_复刻规划`。

## 收尾必更（一行带过即可）

- 高风险逻辑（库存/资金/审核/权限）是否动过、是否测过
- 是否有临时实现/桩、未验证推理补完
- 是否完成截图视觉验收、达到的验收等级（A0-A4）
- 范围有无调整（调整走 `config/implementation-overrides.json` + 重跑生成脚本，勿手改 01/02/03）
