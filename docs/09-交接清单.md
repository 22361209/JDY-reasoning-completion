---
roadmapItemIds:
  - "3"
roadmapGateId: null
---

# 交接快照

每个会话第一份读的文件。保持简短、覆盖更新、不追加历史。铁律见 `CLAUDE.md`，当前任务见 `docs/12-当前批次验收清单.md`，规格仍以 `JDY-复刻-local` 为准。

## 当前快照

- 更新时间：2026-07-13。
- 当前分支：`codex/review-cleanup-20260627`；A136 开工基线 `c53f2b78`，本地提交尚未 push，远端仍停在 A135。
- 当前唯一批次：**A136 操作日志真实主体与状态追溯**；已达到 A4，roadmap item 3 已关闭；在 A137 合同建立前仍保持 item 3 坐标，不提前切换任务文件。
- A136 已完成真实 USER UUID/username/display snapshot、明确 SYSTEM/ANONYMOUS/HISTORICAL_UNKNOWN、typed writer、状态白名单、独立失败审计、restore 保留、专用 SQL 查询和 current/platform/historical scope。
- V97 只遍历 `sys_account_set` 注册 schema；unsafe、missing、orphan 全 fail-closed。冻结的 130109 条历史日志 ID/核心字段不变，54203 条空 actor、114031 条空账套和 130109 条空状态未猜测回填。
- 草稿期 checksum 直接同步未被接受为最终证据；当前库已通过 full dump、隔离空库官方 Flyway 11.7.2 migrate 和排除 history 的数据恢复完成重建。298 张表、394935 行指纹一致，当前 95 条 history 全由官方 migration 生成，旧库/临时库已删除。
- 最终 backend `161/161`、frontend build、V97 拓扑、governance、`area:security 23/23`、smoke `10/10` 均通过。
- 最终 full `106/112`、skip 0；失败仍精确为 A32/A38/A63/A101/A116/A118，没有新增失败。
- A136 综合回归：双用户双账套 4 条生命周期日志通过；历史基线 114031 条全部可达，当前 historical 114032 条与 CSV 同序；临时用户/业务对象/通知/备份/schema 及 A136 fixture 日志计数为 0，7 个 session logout 均为 200；内置账号正常登录审计按合同保留。
- 三条独立 review 最终 APPROVE，无未关闭 P1/P2；页面 1366×768 current 与 1920×1080 historical/detail 截图目检通过。
- item 4–6 的提前只读审计没有改生产代码或路线坐标；正式实现仍按 predecessor 顺序推进。

## A136 固定边界

1. V97 只新增 actor provenance、对象快照和约束，不增加 tenant FK，不实施 item 4。
2. 历史 `operated_by IS NULL` 不按 module/action 推断 USER/SYSTEM；历史非空 UUID 只使用可证明的 immutable username，不伪造历史显示名快照。
3. 历史非空 `operated_by` 在 V97 前必须能关联 platform `sys_user`；current/published/tenant 任一拓扑发现 orphan 就 fail-closed 并输出清单，不置空 UUID 或伪造 username。
4. 新 USER 保存 UUID、immutable username 和操作时 display-name snapshot；用户以后改名不得改变旧日志显示。
5. SYSTEM、ANONYMOUS、HISTORICAL_UNKNOWN 是明确 actor type；HISTORICAL_UNKNOWN 只允许 V97 标记旧行，新生产 writer 禁止创建；登录失败的目标账号不是 actor，匿名找回不得泄露 username/contact。
6. 所有生产 producer 统一走 typed `OperationLogService`；新增原始 `INSERT sys_operation_log` 由后端合同测试阻断。
7. 失败审计独立于失败业务事务保存；状态快照只允许白名单字段，禁止密码、token、contact、webhook secret 和完整请求体。
8. `sys_operation_log` 可备份但不得随账套 restore 回滚或清空；恢复动作只追加新的 RESTORE 日志。
9. 操作日志列表和 CSV 共用同一查询、权限、筛选、排序和字段合同；匿名 401、无权 403、有权成功。

## 并行执行方式

- 主线程：合同、共享 API 冻结、集成、review、独立提交。
- 后端线程：V97、actor provider、typed writer、原始 INSERT 收口、状态/失败审计。
- 列表线程：专用日志查询、权限、当前账套/平台 scope、CSV 与详情展示。
- 验收线程：writer 合同测试、A136 E2E、manifest、迁移拓扑和独立 review。
- 共享数据库写回归、浏览器登录链、smoke/area/full 串行执行，避免单活 session、preset restore 和 tenant fixture 相互污染。

## 下一步

1. 保持 A136 独立提交不 push；用户授权后再推送当前分支。
2. 下一批建立 A137/item 4“tenant schema 没有外键”合同，再同步更新 docs/09 与 docs/12 的唯一路线坐标。
3. A137 先做 tenant 表与参考 schema 的只读约束/孤儿审计；发现孤儿即停止并提交处理清单，不擅自删除、回填或伪造引用。
