# 交接快照

每个会话第一份读的文件。**保持简短，覆盖更新，不追加历史。**
铁律见 `CLAUDE.md`；规格见 `复刻-local`；架构底线见 `docs/04/06/07`。

## 当前快照（每次收尾覆盖）

- 更新时间：2026-06-24
- 当前批次/模块：**A23 分录粘贴冲突候选选择完成**
- 进度：B0/B1/C/D/E/F/G/A2/A3/A4/A5/A6/A7/A8/A9/A10/A11/A12/A13/A14/A15/A16/A17/A18/A19/A20 已完成验收；A8 已补多行明细和 A2-A7 多行/多状态回归。A9 已补剩余数量下推和超量审核保护。A10-A20 已补分录效率、下推确认、订单执行列、行排序插入、下推数量批量调整、源单追踪入口、源行号追溯、分录 Excel 多行粘贴、下推确认选中行局部调整、源行号定位源单分录、分录名称规格智能粘贴。A21 已补销售/采购下推确认双方向同级回归。A22 已补源订单分录已执行数量反查下游执行单据。A23 已补分录粘贴同名多规格商品候选选择，并修正真实主数据列表不再被 1000 行演示扩展污染。
- 可跑命令：`cd infra && docker compose up -d`；`cd backend && ./mvnw spring-boot:run`；`cd frontend && npm run dev -- --host 127.0.0.1 --port 5173`
- 本地服务地址：前端 `http://127.0.0.1:5173/`；后端 `http://127.0.0.1:8080/`；PostgreSQL `localhost:5432`；Redis `localhost:6379`
- 数据库状态：Docker context `desktop-linux`；`jdy-erp-postgres`、`jdy-erp-redis` 均 healthy；Flyway schema version `8`
- 最近通过的测试：`backend ./mvnw test` 通过；`frontend npm run build` 通过；Playwright A23 分录粘贴冲突候选选择通过，候选商品 `A23候选商品20260624083218` 未带规格时弹候选，选择后保存销售订单 `XSDD-20260624-163219142-OQ3`，后端分录与界面一致；A17/A20 粘贴回归通过。
- 当前验收等级：A23 提高分录批量录入可靠性，避免同名多规格商品被静默取错。
- 已知阻塞：无
- 最近报告：全部归档到 `docs/验收报告/`；最近为 `docs/验收报告/验收报告-A23-分录粘贴冲突候选选择-20260624.md`、`docs/验收报告/验收报告-A22-源单行反查下游执行单据-20260624.md`、`docs/验收报告/验收报告-四项收口-20260624.md`、`docs/验收报告/验收报告-A2-A7-多行回归-20260624.md`、`docs/验收报告/验收报告-A21-下推确认双方向回归-20260624.md`、`docs/验收报告/验收报告-A20-分录名称规格智能粘贴-20260624.md`。
- 列表 API 契约：`docs/13-列表API契约.md`
- 批次坐标：`docs/12-当前批次验收清单.md` 固定区维护 A/C/D/E/F/G/A2-A23 ↔ B0-B6 ↔ 复刻-local 入口映射，含 `A2-A7 多行/多状态回归`。
- 截图证据：`verification/playwright/a23-entry-paste-conflict-dialog-20260624083218.png`、`verification/playwright/a23-entry-paste-conflict-form-20260624083218.png`、`verification/playwright/a17-entry-excel-paste-20260624083135.png`、`verification/playwright/a20-entry-name-spec-paste-20260624083157.png`
- 验证产物目录：`verification/`；审批 Excel 目录仍为 `outputs/`，不要改。
- 下一步：建议做下推确认弹窗“按选中行应用仓库”；或者补选择器/候选弹窗键盘上下选择与回车确认。

## 开工前必读（廉价起步）

1. 本快照
2. 当前批次验收清单 `docs/12-当前批次验收清单.md`
3. 目标入口的 `复刻-local` 截图 + 对应 B 批次组件规格
4. 本任务相关代码文件

不要重读全部 docs；规格按需点开 `复刻-local/02_复刻规划`。

## 收尾必更（一行带过即可）

- 高风险逻辑（库存/资金/审核/权限）是否动过、是否测过
- 是否有临时实现/桩、未验证推理补完
- 是否完成截图视觉验收、达到的验收等级（A0-A4）
- 范围有无调整（调整走 `config/implementation-overrides.json` + 重跑生成脚本，勿手改 01/02/03）
