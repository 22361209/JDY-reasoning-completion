# 验收报告：A169 列表表格密度与分录对齐

## 结论

A169 已把标准业务列表的数据行密度对齐到用户确认舒适的分录表基线。实际测量发现两者字号原本已经同为 `13px`，密集感主要来自列表数据行只有 `22px`；本批把销售、采购、库存、生产和基础资料标准列表的数据行统一为 `32px`，内容仍保持 `13px / 20px`，没有盲目放大文字。

本地 [http://127.0.0.1:5173/](http://127.0.0.1:5173/) 已重启到 A169 前端并连接 A167 后端 `18080`。原 `main` 工作区的 A166 未提交文件未被修改。

## 实现范围

- 只在标准 `DataListPage` 的 `.vxe-table--body-wrapper` 覆盖 `--table-core-row-height: 32px`，空白网格与真实数据单元格共同消费同一变量。
- 数据区明确保持 `13px` 字号；分录表原基线、列表表头、合计 footer、分页和页面工作区高度均未改动。
- 作用域不覆盖来源选择器、主数据选择器、导入预览、报表和结算单内部列表，避免把操作型弹窗无意放大。
- A99 密度回归把标准业务列表断言更新为 `30–33px`，弹窗选择表仍保留原 `20–22.5px` 紧凑合同。
- 新增 A169 只读浏览器回归，统一采样五类列表的行高、字号、行高变量、表头、固定列、勾选框、合计行、滚动责任和空白网格。

## 实际证据

- 生产计划分录基线：行/单元格 `32.5px`，输入框 `28px`，字号 `13px`，内容行高 `20px`，变量 `32px`。
- 销售订单、采购订单、其他入库、生产计划、物料资料五类列表：前五条可见数据行与全部首行单元格均为 `32px`，字号 `13px`，内容行高 `20px`。
- 五类列表表头均为 `28px`；固定勾选列保持 `sticky` 和 `32px`，勾选图标保持 `14×14px`，中心偏差约 `1.33px`。
- 销售、采购、库存、生产列表的合计单元格均为 `24.5px` 且互相对齐；物料资料无合计行。外层继续负责横向滚动，数据区继续负责纵向滚动。
- 可见截图：`verification/playwright/a169-sales-list-20260802022832.png`、`a169-production-list-20260802022832.png`、`a169-master-data-list-20260802022832.png`。

## 验证

```text
frontend npm run build                               PASS
A169 list density alignment regression               PASS (5/5 modules)
smoke                                                 PASS (10/10)
real browser 5173 -> A169 frontend -> backend 18080  PASS
regression manifest validation                        PASS
effective scope / delivery status contracts           PASS
documentation governance                              PASS
git diff --check                                      PASS
full                                                   NOT RUN (task budget 0)
```

首次 A169 定向回归在物料资料列表的空白背景网格断言停止。该页面因物料分类侧栏的既有视觉规则明确使用纯白背景，数据行、字号、固定列和滚动边界均已正确；回归随后把背景网格断言限制到实际使用网格的四类标准列表，不改产品代码，复跑通过。

## 保留边界

- A169 不改查询、筛选、分页、排序、权限或任何单据业务逻辑，不改变分录表和页面工作区高度。
- 本批未运行、未宣称修复 A112；行高变化可能改变其溢出表现，但 A112 仍需独立任务重新验收。
- 当前 `5173` 是 A169 工作树运行时；原 A166 工作区文件保持原状。未 push。
