# 验收报告：A161 P0-1 基础资料发布闭环

## 结论

P0-1 的六类基础资料入口达到 `verified / published / A3`：商品 F010、客户 F012、供应商 F013、仓库 F014、部门/车间 F015、计量单位 F018。

## 直接证据

- 新增 `unit`、`productionDepartment` 的稀疏 PATCH、`version` 冲突、权限、tenant 与生命周期共同边界；创建、启停、审核、反审核均记录真实操作日志。
- `scripts/a160-master-data-publication-regression.mjs` 覆盖六类资料的真实新增、保存重开、权限/冲突反馈、选择器消费与窄视口证据；在 `area:master-data` 与 full 中通过。
- A118 委外回归以应用上海业务日取数，并对主数据创建/审核日志使用 UUID、对象、前后状态和固定理由的精确归属校验。
- A161 恢复工具仅处理五个固定 fail-closed token，双读快照、锁表复核后精确删除；独立查询确认五批产品、BOM、委外链、库存、日志与用户残留均为零。

## 验证

```text
./scripts/backend-test.sh test -Dtest=MasterDataPatchServiceIntegrationTest,MasterDataReferenceIntegrationTest  PASS (15/15)
node scripts/a118-outsourcing-chain-regression.mjs                                  PASS, residue=0
node scripts/run-regression-tier.mjs smoke                                         PASS (10/10)
node scripts/run-regression-tier.mjs area:master-data                              PASS (25/25)
node scripts/run-regression-tier.mjs full                                          PASS (135/135)
node scripts/docs-governance-check.mjs                                             PASS
node scripts/effective-scope-contract-check.mjs                                    PASS
node scripts/feature-delivery-status-scan.mjs                                      PASS
node scripts/validate-regression-manifest.mjs                                      PASS
git diff --check                                                                    PASS
```

## 保留边界

不新增条码、多单位换算、附件、复杂分类、审批流或客户/供应商对账；采购、资金和成本业务不随本批扩大。
