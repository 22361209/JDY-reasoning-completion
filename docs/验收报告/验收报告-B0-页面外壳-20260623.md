# B0 页面外壳验收报告

## 基本信息

- 验收日期：2026-06-23
- 验收范围：B0 页面外壳，含工程骨架、最小系统 stub、5 区 ERP 外壳、内部页签、页签上限、未保存关闭确认、同单据列表锁定提示
- 测试账号：本地 stub 用户 `本地管理员 / 系统管理员`
- 测试数据：`博莱德机械测试账套`，会计期间 `2026-06`，业务期间 `2026-06`，示例单据 `XSDD-00001`
- 验收结论：通过 B0，可进入 B1 高密度列表范式
- 等级说明：B0 达到外壳和交互门禁；不是 A2/A3 业务闭环，不能当作真实销售/库存业务完成

## 通过项

- 工程骨架已建立：`backend/` Spring Boot 3.5.15 + Java 21，`frontend/` Vue 3 + TypeScript，`infra/docker-compose.yml` 提供 PostgreSQL + Redis。
- 后端模块包已按 `masterdata/sales/purchase/inventory/production/outsourcing/finance/system/reports` 建立，并补齐 `controller/application/domain/repository/infrastructure/api` 分层骨架。
- Flyway 已启用，数据库迁移策略写入 `docs/04-架构决策记录.md`。
- 复杂表格组件已选定为 VXE Table，写入 `docs/04-架构决策记录.md`。
- 前端已实现左侧主导航、鼠标悬停后浮出的模块功能面板、顶部账套/期间/搜索/消息/帮助/用户栏、内部页签、主内容区、右侧辅助入口。
- `stores` 已包含登录态、页签、用户偏好。
- 系统 stub API 已提供 `/api/system/session`、`/api/system/period`、`/api/system/navigation`。
- 页签上限提示文案符合要求：`已打开太多页签了，请关闭部分页签后再试！`
- 未保存关闭有确认弹窗；同一销售订单已在其它页签打开时，列表审核/删除/批量操作锁定并提示。
- `快捷应用` 仅保留壳层；`老板参谋/客户经营/协同助手/自定义中心` 不进入深层业务。
- 未重跑 `build-jdy-feature-approval.mjs`；未手改 `docs/01/02/03`、`config/approved-feature-scope.json`、`app/feature-data.js`、`frontend/src/app/featureScope.ts`。

## 验证命令

```bash
cd /Users/linzhenyue/Projects/JDY\ 推理补完/infra
docker compose up -d
docker compose ps

cd /Users/linzhenyue/Projects/JDY\ 推理补完/backend
export JAVA_HOME=/opt/homebrew/opt/openjdk@21/libexec/openjdk.jdk/Contents/Home
export PATH=/opt/homebrew/opt/openjdk@21/bin:$PATH
./mvnw test
./mvnw spring-boot:run

cd /Users/linzhenyue/Projects/JDY\ 推理补完/frontend
npm run build
npm run dev -- --host 127.0.0.1 --port 5173
```

## 自动化验收

- 后端：`./mvnw test` 通过，`Tests run: 1, Failures: 0, Errors: 0, Skipped: 0`。
- 前端：`npm run build` 通过，`vue-tsc -b && vite build` 成功。
- Playwright：通过 `session tenant and period visible`、`left nav opens master data panel tab`、`inventory query opens dense list workspace`、`sales same bill lock hint appears`、`dirty tab close confirmation works`、`tab overflow exact message appears`。

## 截图路径

- `/Users/linzhenyue/Projects/JDY 推理补完/verification/playwright/b0-shell-1366.png`
- `/Users/linzhenyue/Projects/JDY 推理补完/verification/playwright/b0-nav-hover-collapsed-default-1366.png`
- `/Users/linzhenyue/Projects/JDY 推理补完/verification/playwright/b0-nav-hover-open-1366.png`
- `/Users/linzhenyue/Projects/JDY 推理补完/verification/playwright/b0-nav-hover-collapsed-after-entry-1366.png`
- `/Users/linzhenyue/Projects/JDY 推理补完/verification/playwright/b0-overflow-1366.png`
- `/Users/linzhenyue/Projects/JDY 推理补完/verification/playwright/b0-shell-1920.png`

## 失败项

- 无 B0 阻断失败项。

## 阻塞项

- 无。

## 剩余风险

- 当前登录/会话是本地开发 stub，Spring Session Redis 已配置，但尚未实现真实登录、退出、权限校验和会话生命周期。
- 当前业务列表和单据是外壳样例数据，尚未接真实销售、库存、应收应付业务表。
- 当前表格为 B0 原生占位表格；B1 需要接入 VXE Table 并沉淀列表组件。
- B0 只验证外壳视觉和关键交互，库存/资金/审核一致性未进入本批。

## 下一步

进入 B1 高密度列表范式：接入 VXE Table，完成标准列表工具栏、筛选区、分页、选中态、空态/加载态/错误态，并先落 `商品资料/客户/供应商/销售订单/采购入库/库存查询` 这组样板。
