# 验收报告 A84 其余单据上骨架

日期：2026-06-25

## 范围

- 037 销售订单：`sales/application/SalesOrderAppService`
- 071 采购订单：`purchase/application/PurchaseOrderAppService`
- 072 采购入库：`purchase/application/PurchaseInAppService`
- 100/181 生产领料：`production/application/MaterialIssueAppService`
- 102/185 产品入库：`production/application/ProductInAppService`

本批只做结构迁移：瘦 Controller + AppService；沿用 A83 的 `BillLifecycleService`、`PostingPipeline`、`ConversionService`、`LookupService`、`OperationLogService`。未改财务口径，未改单据编号，未重跑 `build-jdy-feature-approval.mjs`，未手改 `frontend/src/app/featureScope.ts`。

## 提交

- `04f8200` A84 migrate sales order app service
- `8c81cd1` A84 migrate purchase order app service
- `eb086cf` A84 migrate purchase in app service
- `1562acf` A84 migrate material issue app service
- `430716e` A84 migrate product in app service

## Controller 瘦身

| 单据 | Controller | 迁移前 | 迁移后 | AppService |
| --- | --- | ---: | ---: | ---: |
| 销售订单 | `SalesOrderController` | 257 | 59 | 234 |
| 采购订单 | `PurchaseOrderController` | 221 | 42 | 206 |
| 采购入库 | `PurchaseInController` | 415 | 62 | 393 |
| 生产领料 | `ProductionController` | 530 | 103 | `MaterialIssueAppService` 222 |
| 产品入库 | `ProductionController` | 530 | 103 | `ProductInAppService` 223 |

生产 Controller 原先同时承载 BOM、任务、生产领料、产品入库。为保证 Controller 最终无业务 SQL，本批在产品入库提交中补出 `ProductionTaskAppService` 117 行承接 BOM/任务辅助逻辑。

## Grep 证据

命令：

```bash
grep -nE "INSERT INTO|UPDATE |postingService|status = '" backend/src/main/java/com/jdy/erp/sales/api/SalesOrderController.java
grep -nE "INSERT INTO|UPDATE |postingService|status = '" backend/src/main/java/com/jdy/erp/purchase/api/PurchaseOrderController.java
grep -nE "INSERT INTO|UPDATE |postingService|status = '" backend/src/main/java/com/jdy/erp/purchase/api/PurchaseInController.java
grep -nE "INSERT INTO|UPDATE |postingService|status = '" backend/src/main/java/com/jdy/erp/production/api/ProductionController.java
```

结果：四个 Controller 均无输出。

## 回归

- `npm --prefix frontend run build`：通过；Vite 构建成功，提示 chunk size warning，未失败。
- `JAVA_HOME=/opt/homebrew/opt/openjdk@21 ./mvnw test`：通过；`Tests run: 1, Failures: 0, Errors: 0, Skipped: 0`，`BUILD SUCCESS`。
- A2-A77 当前 69 脚本覆盖：`verification/a2-a84-regression-latest.json`
  - `ok=true`
  - `total=69`
  - `expectedTotal=69`
  - `badCount=0`
  - `skipped=0`

连续 runner 过程中安全设置页出现两次等待时序波动：一次 A70 保存响应断言失败，单跑 A70 通过；一次 A69 等待 `security-current-policy` 超时，直接探测确认接口和页面均更新后，单跑 A69 通过。随后补跑 A70-A77 尾段全绿，并合并为最终 `a2-a84-regression-latest.json`。单据链路、库存过账、生产红冲、打印和权限脚本均已在 runner 中覆盖。

## 结论

A84 完成。五张单已迁到后端单据骨架；Controller 无业务 SQL/过账/裸状态写入；库存过账与采购/销售执行量回写复用 shared 抽象；全量覆盖结果为 69/69 通过。
