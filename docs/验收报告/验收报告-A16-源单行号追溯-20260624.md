# 验收报告-A16-源单行号追溯

- 日期：2026-06-24
- 批次：A16 源单行号追溯
- 范围：销售出库、采购入库、源销售订单/采购订单执行回写
- 结论：通过

## 本批完成

1. `sales_out_line`、`purchase_in_line` 增加 `source_line_no`，旧数据按当前行号补齐。
2. 销售出库/采购入库详情分录表显示“源行号”列。
3. 下推、保存草稿、详情加载会保留源行号。
4. 审核、反审核、红冲按 `source_line_no` 回写源订单行，兼容旧单据时回退到下游 `line_no`。

## Playwright 与 API 断言

| 场景 | 下游单据 | 源单 | 断言 |
| --- | --- | --- | --- |
| 销售出库乱序追溯 | `XSCK-A16-20260624073003` | `XSDD-A16-20260624073003` | 下游显示源行号 `#3 / #1`；源销售订单已出库数量为 `4 / 0 / 2` |
| 采购入库乱序追溯 | `CGRK-A16-20260624073003` | `CGDD-A16-20260624073003` | 下游显示源行号 `#3 / #1`；源采购订单已入库数量为 `5 / 0 / 3` |

结果文件：`verification/a16-source-line-trace-regression.json`

## 截图

- `verification/playwright/a16-sales-out-source-line-20260624073003.png`
- `verification/playwright/a16-purchase-in-source-line-20260624073003.png`

## 验证命令

- `cd frontend && npm run build`：通过
- `cd backend && JAVA_HOME=/opt/homebrew/opt/openjdk@21 PATH=/opt/homebrew/opt/openjdk@21/bin:$PATH ./mvnw test`：通过，Flyway schema 升至 `8`
- `node scripts/a16-source-line-trace-regression.mjs`：通过

## 护栏

- 未重跑 `build-jdy-feature-approval.mjs`。
- 未手改生成物：`docs/01/02/03`、`config/approved-feature-scope.json`、`app/feature-data.js`、`frontend/src/app/featureScope.ts`。
- 本批改动了出入库分录追溯字段和源单执行数量回写坐标，未改变库存双写原则和审核才动账原则。
