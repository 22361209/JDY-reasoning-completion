# 验收报告 A26：主数据选择器选后跳格

验收日期：2026-06-24  
回归批次：`20260624085506`  
结论：PASS

## 验收范围

本批补齐主数据选择器的连续录入手感：选择器菜单打开时，`Enter` 或非 Shift 的 `Tab` 会选中当前高亮项并进入下一录入字段。

覆盖销售订单新增中的关键路径：客户选择后跳业务日期，商品选择后跳仓库，仓库选择后跳数量。鼠标点击选项也复用同一套跳格逻辑。

## 断言结果

| 项目 | 结果 |
| --- | --- |
| 客户 `KH-001` 选择后焦点为 `sales-bill-date` | PASS |
| 商品 `A26-P-20260624085506` 选择后焦点为 `sales-line-warehouse` | PASS |
| 仓库 `CK-002` 选择后焦点为 `sales-line-qty` | PASS |
| 分录金额 `133.00 = 7 × 19` | PASS |
| 保存后后端销售订单分录为 `A26-P-20260624085506 / CK-002 / 7 / 19` | PASS |
| A25 粘贴冲突候选键盘回归 | PASS |
| A23 粘贴冲突候选鼠标回归 | PASS |

## 自动化证据

- 主脚本：`scripts/a26-selector-next-field-regression.mjs`
- 结构化结果：`verification/a26-selector-next-field-regression.json`
- 兼容回归：
  - `scripts/a25-entry-paste-conflict-keyboard-regression.mjs`
  - `scripts/a23-entry-paste-conflict-candidates-regression.mjs`

## 截图

- 键盘选后跳格表单：`verification/playwright/a26-selector-next-field-form-20260624085506.png`
- A25 键盘候选回归弹窗：`verification/playwright/a25-entry-paste-conflict-keyboard-dialog-20260624085514.png`
- A23 鼠标候选回归弹窗：`verification/playwright/a23-entry-paste-conflict-dialog-20260624085514.png`

## 代码说明

- `handleSelectorKeydown` 增加非 Shift `Tab` 选中当前高亮项。
- 抽出 `chooseSelectorOption` 统一处理 `Enter` / `Tab`。
- 选择客户后聚焦业务日期；选择商品后聚焦同一行仓库；选择仓库后聚焦同一行数量。
- 鼠标点击选择器选项仍走原有 `select*Option` 函数，因此同步获得跳格行为。

## 验证命令

```bash
node scripts/a26-selector-next-field-regression.mjs
node scripts/a25-entry-paste-conflict-keyboard-regression.mjs
node scripts/a23-entry-paste-conflict-candidates-regression.mjs

cd "/Users/linzhenyue/Projects/JDY 推理补完/frontend"
npm run build

cd "/Users/linzhenyue/Projects/JDY 推理补完/backend"
export JAVA_HOME="/opt/homebrew/opt/openjdk@21/libexec/openjdk.jdk/Contents/Home"
export PATH="/opt/homebrew/opt/openjdk@21/bin:$PATH"
./mvnw test
```

## 护栏

- 未重跑 `build-jdy-feature-approval.mjs`。
- 未手改任何生成物：`docs/01/02/03`、`config/approved-feature-scope.json`、`app/feature-data.js`、`frontend/src/app/featureScope.ts`。
- 本批未调整范围例外，未改 `config/implementation-overrides.json`。
