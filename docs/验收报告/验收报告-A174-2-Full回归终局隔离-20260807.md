# 验收报告：A174-2 Full 回归终局隔离

## 结论

A174-2 已完成。终局对抗性 review 发现的 A43 传输歧义已限定为固定唯一键、可精确重查的测试仓库创建：仅在 `fetch failed` 后重查同一 run 所有权记录，未发现时才允许一次受限重试；普通业务写请求仍禁止重试。若 A43 只创建隔离身份而未产生业务夹具，cleanup 现在先验证零业务残留，再按精确 UUID 清除身份、角色、授权、scope、Redis 和日志。

A118 的修复仅覆盖幂等注销及随后的未认证 session 读取：`fetch` 建连与 `response.text()` 读取同属一次受限 TypeError 重试边界，任何业务写仍不重试。

独立终局 review 未发现 P0–P2。有效 full 完整执行 143/143，141 通过、2 项已登记 A175 历史夹具失败、0 跳过；preflight 与 postflight 均通过，工作区保持干净。

## 有效 full

```text
command       node scripts/run-regression-tier.mjs full --continue-on-failure
candidate     90bd3c66
started       2026-08-07T08:21:39.443Z
finished      2026-08-07T09:53:21.173Z
executed      143/143
passed        141
failed        2 (A25, A128；均归 A175)
skipped       0
preflight     PASS
postflight    PASS
artifact      verification/regression-tier-full-continue-latest.json
```

Postflight 复验 ADMIN/FINANCE/WAREHOUSE 权限矩阵、安全设置（`SINGLE_ACTIVE`、30 分钟会话、密码策略）和干净工作区均未漂移。

## 已登记后续事项

- A25：冲突对话框等待超时；夹具未保证两个同名候选同时进入其检索结果，属于 A175 分页/候选构造治理。
- A128：页面在 2026-08-07 正确返回 `2026-08-01 至 2026-08-31`，脚本仍硬编码 2026-07，属于 A175 动态日期期望治理。

两项不通过改产品行为规避。

## 定向证据

```text
node --check scripts/a43-operation-log-export-regression.mjs    PASS
A43 operation-log export regression                             PASS，cleanup 零残留
node --check scripts/a118-outsourcing-chain-regression.mjs      PASS
A118 outsourcing chain                                           PASS，cleanup 零残留
git diff --check                                                 PASS
final adversarial review                                         no P0–P2
```

未 push，未操作生产环境。
