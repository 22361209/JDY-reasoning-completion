# 验收报告 A28：新增行继承上一行仓库

验收日期：2026-06-24  
回归批次：`20260624090757`  
结论：PASS

## 验收范围

本批补齐销售订单分录新增行的仓库继承：点击“增加明细行”时，新行继承最后一行仓库；单价回车自动新增下一行时，新行继承当前行仓库。

## 断言结果

| 项目 | 结果 |
| --- | --- |
| 按钮新增第 2 行后仓库继承 `CK-002` | PASS |
| 单价 `Enter` 新增第 3 行后焦点为 `sales-line-product-3` | PASS |
| 单价 `Enter` 新增第 3 行后仓库继承 `CK-T413874` | PASS |
| 三行金额为 `60.00 / 24.00 / 8.00` | PASS |
| 合计金额 `92.00` | PASS |
| 保存后后端分录为 `CP-001 / CK-002 / 2 / 30`、`PJ-014 / CK-T413874 / 4 / 6`、`CP-T413874 / CK-T413874 / 1 / 8` | PASS |
| A27 分录数量/单价连续录入回归 | PASS |

## 自动化证据

- 主脚本：`scripts/a28-entry-new-line-inherit-warehouse-regression.mjs`
- 结构化结果：`verification/a28-entry-new-line-inherit-warehouse-regression.json`
- 兼容回归：`scripts/a27-entry-enter-next-line-regression.mjs`

## 截图

- 新增行继承仓库三行表单：`verification/playwright/a28-entry-new-line-inherit-warehouse-form-20260624090757.png`
- A27 连续录入回归：`verification/playwright/a27-entry-enter-next-line-form-20260624090733.png`

## 代码说明

- `defaultLine` 支持传入仓库编码，默认仍为 `CK-001`。
- `addLine` 使用最后一行仓库创建新行。
- `insertLineAfter` 使用当前行仓库创建新行，因此单价 `Enter` 自动新增行也会继承仓库。
- `copyLine` 不变，继续复制整行。

## 验证命令

```bash
node scripts/a28-entry-new-line-inherit-warehouse-regression.mjs
node scripts/a27-entry-enter-next-line-regression.mjs

cd "/Users/linzhenyue/Projects/JDY 推理补完/frontend"
npm run build

cd "/Users/linzhenyue/Projects/JDY 推理补完/backend"
export JAVA_HOME="/opt/homebrew/opt/openjdk@21/libexec/openjdk.jdk/Contents/Home"
export PATH="/opt/homebrew/opt/openjdk@21/bin:$PATH"
./mvnw test
```

## 护栏

- 未重跑 `build-jdy-feature-approval.mjs`。
- 未手改任何生成物：`docs/01/02/03`、`config/approved-feature-scope.json`、`app/feature-data.js`、`frontend/src/app/featureScope.ts`。
- 本批未调整范围例外，未改 `config/implementation-overrides.json`。
