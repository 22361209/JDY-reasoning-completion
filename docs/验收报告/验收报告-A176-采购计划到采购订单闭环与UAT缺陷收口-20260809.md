# 验收报告：A176 采购计划到采购订单闭环与 UAT 缺陷收口

## 结论

A176 已完成采购计划到采购订单的正式闭环，并收口现场验收暴露的逐行发料仓、页签满载成功提示及两项 UAT 缺陷。最终 review 未发现本批新增 P0–P2 问题。

- 已审核采购计划可按供应商和剩余数量生成采购订单草稿；订单保留计划头/行、采购申请、生产来源、价格和税率快照。
- 订单审核按计划行加锁并增加已订数量，不能超订；反审核释放计划占用并刷新申请占用，已有已审核采购入库时阻断破坏来源一致性的撤销。
- 生产任务下推领料逐行保留冻结用料仓库；下推成功但页签已满时明确显示“草稿已生成但未自动打开”、单据编号和列表恢复路径。
- UAT-DEF-001 不再显示 Spring 原始 JSON；用户创建、管理员重置和当前用户改密统一提取 `message/detail/reason/error` 中的用户可读信息。
- UAT-DEF-002 从物料表单新建名称时动作明确为“保存并审核”，审核成功才回到来源表单并选中新名称；选择器不再回退展示未审核名称。

F032、F033、F034 保持 `verified/published/A3`。A176 补充采购计划—订单—入库整链证据，但采购计划成功操作日志尚未完成 A4 专项验收；MRP、成本、预付款及跨币种结算仍不在范围内。

## 真实页面证据

### 采购生产闭环

- 第 1 遍：`SCJH000287 → CGSQ000265 → CGJH000183 → CGDD001887 → CGRK003460 → SCRW001223 → SOUT001306 → SCRK000838`。
- 第 2 遍：`SCJH000288 → CGSQ000266 → CGJH000184 → CGDD001888 → CGRK003461 → SCRW001224 → SOUT001307 → SCRK000839`。
- 两遍均先完成采购入库，再完成生产领料和产品完工入库；任务计划、领料、完工数量均为 2，最终为已完工/已审核。
- 第二遍复验多仓任务用料未被首行仓库覆盖；页签满载时生成草稿和自动打开详情被作为两个独立结果反馈。

### UAT 缺陷

- UAT-DEF-001：在 BLD-TEST 用户新增页输入缺少小写字母的 `ABC123!@`，页面显示 `密码需包含小写字母`，原始 JSON 出现次数为 0，用户未创建。
- UAT-DEF-002：在物料资料新增页输入 `UAT名称202608090755`，页面提示未找到并进入名称维护；顶部和底部均显示“保存并审核”。完成后自动回到新增物料资料，物料名称值精确等于该名称；测试物料随后取消，名称主数据保留为已审核验收数据。

## 自动验证

```text
UAT defects static regression                             PASS (UAT-DEF-001/002)
A176 purchase-plan/order closure                          PASS (42 assertions)
A167 multi-line migration                                 PASS (62 assertions; upgrade + fresh DB cleanup)
backend targeted integration tests                        PASS (13/13)
frontend npm run build                                    PASS
regression manifest validation                            PASS (166 discovered; smoke 11; full 145)
smoke                                                     PASS (11/11)
area:purchase                                             PASS (23/23)
area:master-data                                          STOP (19/26: 18 pass, A115 fail, 7 skipped)
unique full                                               STOP (101/145: 100 pass, A115 fail, 44 skipped)
real in-app browser acceptance                            PASS
git diff --check / JSON / docs governance                PASS
```

有效 `area:purchase` 开始于 `2026-08-09T00:27:08.638Z`、结束于 `2026-08-09T00:41:49.015Z`，A167、A170、A172、A176 均通过。唯一 full 开始于 `2026-08-09T00:42:17.620Z`、结束于 `2026-08-09T01:32:27.839Z`；未重跑。

## 范围外问题

`scripts/a115-metadata-definition-regression.mjs` 在 `area:master-data` 和唯一 full 中均失败于：

```text
整单/明细视图切换、列设置、更新库存必须集中在列表右侧工具区
```

A176 未修改对应列表工具区或 A115 脚本。最终 `area:master-data` 在该项前已有 18 项通过；唯一 full 在该项前累计 100 项通过。按 `record-and-stop` 保留为独立任务，不在 A176 中顺手修复、豁免或隐藏。

## 提交与预算

- 核心提交：`b7df5c4f`、`157bc778`、`4ffa261d`、`bdea9570`。
- UAT/门禁提交：`1e56c013`、`405c5ecc`、`251f8d68`；本报告和归档为第 8 个提交。
- 实际执行约 120 分钟；Goal token 使用低于 10%；提交 8/8，子代理 0，full 1/1。
- 未 push，未操作生产环境。
