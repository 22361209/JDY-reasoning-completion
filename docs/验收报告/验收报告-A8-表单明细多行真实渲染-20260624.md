# 验收报告 A8：表单明细多行真实渲染

日期：2026-06-24

## 结论

PASS。销售订单、销售出库、采购订单、采购入库四类核心表单已从首行写死推进到多行分录渲染和编辑；保存草稿后从列表打开详情，3 行明细和合计金额均可回显。

## 本批完成

- 表单分录改为 `v-for` 行级渲染，不再写死 `lines[0]`。
- 新增行、删除行、逐行商品/仓库选择器回填已接入。
- 草稿状态允许编辑；已审核、已反审核、已作废、已红冲状态下明细不可编辑。
- 合计金额由各行数量和单价实时计算。
- 保存时只提交业务字段，商品名称/规格只作为显示字段，不污染后端请求。

## 验收记录

- `frontend npm run build`：PASS
- `backend ./mvnw test`：PASS
- Playwright 浏览器验收：
  - 销售订单 `XSDD-A8ML-20260624052304`：3 行，合计 `524.00`。
  - 采购订单 `CGDD-A8ML-20260624052304`：3 行，合计 `440.00`。
  - 销售出库单 `XSCK-A8ML-20260624052338`：3 行，合计 `524.00`。
  - 采购入库单 `CGRK-A8ML-20260624052338`：3 行，合计 `440.00`。

## 截图证据

- `verification/playwright/a8-sales-order-multiline-1366.png`
- `verification/playwright/a8-purchase-order-multiline-1366.png`
- `verification/playwright/a8-sales-out-multiline-1366.png`
- `verification/playwright/a8-purchase-in-multiline-1366.png`

## 未完成与风险

- A2-A7 的多行/多状态回归尚未执行，下一步必须按 `docs/12` 的 4.1 门禁补《A2-A7 多行回归》报告。
- 生产领料、产品入库还需要在回归批次中准备 >=3 行、多状态样本。
- 部分出库/部分入库按剩余数量下推仍后置。
