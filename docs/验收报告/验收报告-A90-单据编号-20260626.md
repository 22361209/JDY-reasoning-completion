# 验收报告 A90 单据编号

- 日期：2026-06-26
- 范围：后端单据编号服务；前端新建单据清除 `billNo`、`sourceOrderNo`、`partyCode/customer` 等硬编码默认值；相关回归脚本适配。
- 金蝶对齐：参考 `k3cloud-system-mechanism-notes.md` §18、§45。采用固定前缀 + 6 位补零序号、无连字符，例如 `XSDD000001`、`XSCKD000001`；保持简单规则，不建设编号规则设计器。

## 前缀

| 单据 | 前缀 |
|---|---|
| 销售订单 | `XSDD` |
| 销售出库 | `XSCKD` |
| 采购订单 | `CGDD` |
| 采购入库 | `CGRK` |
| 调拨 | `ZJDB` |
| 其他出库 | `QTCK` |
| 其他入库 | `QTRK` |
| 产品入库 | `SCRK` |
| 生产领料 | `SOUT` |
| 生产任务 | `SCRW` |
| 盘点 | `PD` |
| 盘盈 | `PY` |
| 盘亏 | `PK` |

## 验收结论

- A90 专项：`node scripts/a90-numbering-regression.mjs` 通过。
- 后端连续新建两张销售订单空单号草稿返回 `XSDD000001`、`XSDD000002`；撞号兜底返回 `XSDD000003`。
- 前端销售出库直接新建返回 `XSCKD000001`，源订单号为空，客户为空。
- 前端新建单据改为调用 `/api/numbering/{documentType}/next` 获取可编辑预览号；保存后按后端实际返回单号回写表单。
- 未改财务口径；库存审核/反审核逻辑未调整。

## 门禁

- `npm --prefix frontend run build`：通过。
- `cd backend && JAVA_HOME=/opt/homebrew/opt/openjdk@21 ./mvnw test`：通过，含 ArchUnit 启动校验。
- A2-A90 全量回归：`verification/a2-a90-regression-latest.json`
  - `ok=true`
  - `total=78`
  - `expectedTotal=78`
  - `badCount=0`
  - `skipped=0`
  - 当前记录 mtime：以最终 HEAD 重跑后的 `verification/a2-a90-regression-latest.json` 文件时间为准

## 证据

- 专项结果：`verification/a90-numbering-regression.json`
- 全量结果：`verification/a2-a90-regression-latest.json`
- 浏览器截图：`verification/playwright/a90-sales-out-direct-new-20260625215244.png`
