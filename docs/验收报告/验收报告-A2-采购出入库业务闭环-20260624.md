# 验收报告：A2 采购/出入库业务闭环

- 日期：2026-06-24
- 范围：采购订单、采购入库单、销售出库单、库存过账、订单状态联动
- 结论：通过 A1+ 业务闭环验收；尚未进入反审核、部分出入库和正式打印/引出版式

## 本次完成

- 新增 Flyway 迁移 `V4__purchase_and_stock_documents.sql`。
- 新增真实单据表：
  - `purchase_order / purchase_order_line`
  - `purchase_in / purchase_in_line`
  - `sales_out / sales_out_line`
- 新增后端接口：
  - `POST /api/purchase-orders/draft`
  - `POST /api/purchase-orders/{billNo}/audit`
  - `POST /api/purchase-ins/draft`
  - `POST /api/purchase-ins/{billNo}/audit`
  - `POST /api/sales-outs/draft`
  - `POST /api/sales-outs/{billNo}/audit`
- 前端新增/接入：
  - 采购订单保存/审核
  - 采购入库单保存/审核
  - 销售出库单保存/审核
  - 采购订单、采购入库、销售出库真实列表
- 状态联动：
  - 采购入库审核后采购订单显示 `全部入库`
  - 销售出库审核后销售订单显示 `全部出库`

## API 验收证据

- 验收批次：`071736`
- 库存初始：`CP-001 / CK-001 onHand=1289 available=1129`
- 采购订单：`CGDD-GOAL-071736` 保存 `201`，审核 `200`
- 采购入库：`CGRK-GOAL-071736` 保存 `201`，审核 `200`
- 销售订单：`XSDD-GOAL-071736` 保存 `201`，审核 `200`
- 销售出库：`XSCK-GOAL-071736` 保存 `201`，审核 `200`
- 库存结果：`onHand=1294 available=1134`
- 流水：
  - `PURCHASE_IN | 8.0000 | PURCHASE_IN:CGRK-GOAL-071736`
  - `SALES_OUT | -3.0000 | SALES_OUT:XSCK-GOAL-071736`
- 列表：
  - 采购订单 `CGDD-GOAL-071736` 显示 `全部入库`
  - 销售订单 `XSDD-GOAL-071736` 显示 `全部出库`
  - 采购入库单、销售出库单均可按单据编号查询

## 页面验收证据

- Playwright 验收批次：`681247`
- 采购订单：`CGDD-UI-681247`
- 采购入库单：`CGRK-UI-681247`
- 销售订单：`XSDD-UI2-681247`
- 销售出库单：`XSCK-UI-681247`
- 截图：
  - `verification/playwright/goal-purchase-order-form-1366.png`
  - `verification/playwright/goal-purchase-in-form-1366.png`
  - `verification/playwright/goal-sales-out-form-1366.png`

## 构建与测试

- `frontend npm run build` 通过。
- `backend ./mvnw test` 通过。
- Flyway schema version：`4`。

## 已知差距

- 采购入库、销售出库目前按整单一次性更新为全部入库/全部出库，尚未实现部分出入库。
- 暂未实现反审核、红冲、作废和严格权限。
- 出入库单据没有正式打印模板或 Excel 引出。
