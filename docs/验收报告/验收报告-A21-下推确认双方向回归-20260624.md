# 验收报告 A21：下推确认双方向回归

- 日期：2026-06-24
- 批次：A21 下推确认弹窗销售/采购同级回归
- 范围：销售订单下推销售出库、采购订单下推采购入库
- 目标：确认 A18 的选中行局部调整能力不只在销售侧可用，采购侧也达到同级行为和门禁。

## 本次验证

- 未改业务代码，本批补齐双方向回归门禁。
- 销售订单下推销售出库：验证剩余数量、未选行批量调整、选中行局部调整、生成草稿数量。
- 采购订单下推采购入库：验证剩余数量、未选行批量调整、选中行局部调整、生成草稿数量。

## 验收数据

### 销售方向

- 源销售订单：`XSDD-A21-20260624080329`
- 默认剩余数量：`6 / 5 / 4`
- 未选行按 `50%`：`3 / 2.5 / 2`
- 仅选中第 2 行后按 `50%`：`6 / 2.5 / 4`
- 选中提示：`已选 1 行，本次工具只调整选中行`
- 生成销售出库单草稿数量：`6 / 2.5 / 4`

### 采购方向

- 源采购订单：`CGDD-A21-20260624080329`
- 默认剩余数量：`6 / 5 / 4`
- 未选行按 `50%`：`3 / 2.5 / 2`
- 仅选中第 2 行后按 `50%`：`6 / 2.5 / 4`
- 选中提示：`已选 1 行，本次工具只调整选中行`
- 生成采购入库单草稿数量：`6 / 2.5 / 4`

## Playwright 断言

- [x] 销售订单列表选中源订单并打开销售出库下推确认弹窗。
- [x] 断言销售方向默认剩余数量为 `6 / 5 / 4`。
- [x] 断言销售方向未选行按 `50%` 后全部行为 `3 / 2.5 / 2`。
- [x] 断言销售方向选中第 2 行按 `50%` 后为 `6 / 2.5 / 4`。
- [x] 断言生成销售出库单草稿数量为 `6 / 2.5 / 4`。
- [x] 采购订单列表选中源订单并打开采购入库下推确认弹窗。
- [x] 断言采购方向默认剩余数量为 `6 / 5 / 4`。
- [x] 断言采购方向未选行按 `50%` 后全部行为 `3 / 2.5 / 2`。
- [x] 断言采购方向选中第 2 行按 `50%` 后为 `6 / 2.5 / 4`。
- [x] 断言生成采购入库单草稿数量为 `6 / 2.5 / 4`。

## 截图

- 销售确认弹窗：`verification/playwright/a21-sales-push-confirm-selected-dialog-20260624080329.png`
- 销售生成草稿：`verification/playwright/a21-sales-push-confirm-selected-form-20260624080329.png`
- 采购确认弹窗：`verification/playwright/a21-purchase-push-confirm-selected-dialog-20260624080329.png`
- 采购生成草稿：`verification/playwright/a21-purchase-push-confirm-selected-form-20260624080329.png`

## 命令

- `node scripts/a21-push-confirm-bidirectional-regression.mjs`：通过
- `cd frontend && npm run build`：通过
- `cd backend && JAVA_HOME=/opt/homebrew/opt/openjdk@21 PATH=/opt/homebrew/opt/openjdk@21/bin:$PATH ./mvnw test`：通过

## 生成链护栏

- 未重跑 `build-jdy-feature-approval.mjs`。
- 未手改生成物：`docs/01/02/03`、`config/approved-feature-scope.json`、`app/feature-data.js`、`frontend/src/app/featureScope.ts`。
- 本批未调整范围例外，未改 `config/implementation-overrides.json`。
