# 验收报告 A112b 整单视图统一与修 bug

## 验收日期

2026-06-26

## 验收范围

- A112 残留：DataListPage 列表整单视图收进 A112 共享表格内核，与明细视图/分录同用 `TableCoreFrame` + `TableCoreHeaderCell` + 原生 table 列宽模型。
- 整单视图滚动条常驻：外层横向滚动条 `overflow-x: scroll`，body 纵向滚动条 `overflow-y: scroll`。
- 整单视图列宽独立：列宽拖动由共享 header resize handle 写入当前列，不再由 VXE fit 分摊压缩邻列。
- bug #4：分录“增加明细”新行默认空商品、空仓库、数量 0、单价 0。
- bug #5：销售订单页签切到销售订单列表页签再切回，保留已打开订单的表单数据。
- 护栏：未新增 A113 列/汇总；未重跑 `build-jdy-feature-approval.mjs`；未手改 `frontend/src/app/featureScope.ts`。

## 测试账号

本地管理员 / 系统管理员。

## 测试数据

- `scripts/a112-table-core-scroll-regression.mjs` 自动创建：
  - 销售订单：`XSDD-A112-20260626155317`
  - 发货通知单：`FHTZD-A112-20260626155317`
- `scripts/a28-entry-new-line-inherit-warehouse-regression.mjs` 自动创建：
  - 销售订单：`XSDD001187`

## 通过项

- 整单视图已改为原生共享表格结构，继续保留 `.vxe-*` 兼容类给既有回归定位；列表整单和明细视图的 header/filter/resize 均来自 `TableCoreHeaderCell`。
- 整单视图滚动条常驻：A112b 回归断言 `listScroll.overflowX === "scroll"`、`listBodyScroll.overflowY === "scroll"`。
- 整单视图列宽独立：拖宽 `billNo` 后 `customer`、`billDate` 宽度保持不变，断言值 `150 -> 194`、`220 -> 220`、`130 -> 130`。
- 新增分录行默认空：断言第二行商品 `""`、仓库 `""`、数量 `"0"`、单价 `"0"`。
- 页签状态保留：打开已审核销售订单后切到列表再切回，单据编号和首行商品仍为原单数据。
- 页签切换根因修复：表单切走快照改为 plain clone，避免审核后嵌套响应式对象被 `structuredClone` 克隆时报错导致内容区停留在旧表单。

## 截图证据

- 整单视图共享内核与滚动条常驻：`verification/playwright/a112-a112b-header-core-scrollbar-20260626155317.png`
- 整单视图列宽独立：`verification/playwright/a112-a112b-header-independent-column-resize-20260626155317.png`
- 页签切回保留订单内容：`verification/playwright/a112-a112b-tab-state-preserved-20260626155317.png`
- 新增分录行默认空：`verification/playwright/a112-a112b-new-line-blank-20260626155317.png`
- 列表明细视图与分录筛选图标同位：`verification/playwright/a112-list-filter-alignment-no-seq-20260626155317.png`
- 分录横滑库存列：`verification/playwright/a112-entry-stock-columns-horizontal-scroll-20260626155317.png`
- 分录共享 header 对齐：`verification/playwright/a112-entry-filter-alignment-seq-kept-20260626155317.png`

## 自动化验证

- `npm --prefix frontend run build`：通过。
- `cd backend && JAVA_HOME=/opt/homebrew/opt/openjdk@21 ./mvnw test`：通过，12 tests，含 ArchUnit。
- `node scripts/a112-table-core-scroll-regression.mjs`：通过，含 A112b 新增断言。
- `node scripts/a28-entry-new-line-inherit-warehouse-regression.mjs`：通过，旧继承仓库口径已改为新增行默认空。
- `node scripts/a107-table-structure-regression.mjs`：通过。
- `node scripts/a110-table-display-brand-regression.mjs`：通过。
- `node scripts/run-regression-tier.mjs full`：提交后执行，结果以 `verification/regression-tier-full-latest.json` 为准。

## 剩余风险

- A113 的列表新列和底部汇总未做，按护栏后置。
- DataListPage 已退掉 VXE body 渲染，但保留 `.vxe-*` 兼容类以降低既有脚本迁移成本；后续若完全清理 VXE 类名，需要同步更新历史回归选择器。
