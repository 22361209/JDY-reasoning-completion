# 验收报告：A180 业务单据仓库选择器初始筛选修复

## 结论

A180 已完成。用户的操作属于正常业务操作；异常在于整列表选择弹窗把当前行 `CK-001` 自动作为搜索关键字，用户继续输入目标仓库时形成 `CK-001UAT-20260809-009`，未主动搜索时也只显示当前值匹配项。

修复后，仓库整列表打开时搜索框为空并加载当前 tenant 全部“已审核且启用”仓库；当前行仓库值继续保留在单据分录，但不再过滤候选。销售报价、销售订单、采购订单、采购入库等共享单据以及销售出库均使用该规则。

## 先复现后修复

1. 工作树为 `/Users/linzhenyue/Projects/JDY 推理补完`、分支 `main`，5173 进程 cwd 为当前 `frontend`，8080 classpath 为当前 `backend/target/classes`；排除修复位于其他工作树。
2. Chrome 现场为 `http://127.0.0.1:5173/`、账套 `博莱德机械测试账套 / BLD-TEST`。首次读取到仓库弹窗关键字 `CK-001UAT-20260809-009`，结果 0 条。
3. 明确覆盖为 `UAT-20260809-009` 后，页面实际请求 `/api/lists/warehouse-master-selector?keyword=UAT-20260809-009&page=1&pageSize=100`，返回 200 和唯一仓库；证明 A179 后端候选契约已生效。
4. 新增失败优先回归，修复前稳定失败于共享单据仍把 `keywordValue` 写入弹窗关键字；随后将共享单据和销售出库的仓库弹窗初始关键字改为空，其他主数据选择器行为保持不变。

## BLD-TEST 真实页面回归

- 销售报价使用客户 `UAT-20260809-007`、物料 `UAT-20260809-006`。物料带出行内 `CK-001` 后打开仓库整列表，搜索框为空，未筛选显示 17 条且包含 `400041390 / UAT-20260809-009 / UAT-20260809-009 仓库`；编码和名称搜索各返回 1 条，点击后回填 `UAT-20260809-009`。
- 采购订单使用供应商 `UAT-20260809-003`、物料 `UAT-20260809-006`。从同样的行内 `CK-001` 打开弹窗，搜索框为空，未筛选显示 17 条且包含 UAT 仓库；编码和名称搜索各返回 1 条，点击后回填成功。
- 未保存、审核或删除上述 UAT 草稿。Chrome 已保留在采购订单回填现场。

## 自动验收

- `node scripts/a180-warehouse-selector-initial-filter-regression.mjs`：修复前失败、修复后通过。
- A179 原仓库候选回归继续通过；`npm run build` 通过，仅有既有 Vite 大 chunk 警告。
- manifest 校验通过：发现 168 个脚本，full 147、smoke 13；A180 登记到 smoke、sales、purchase、inventory 和 full。
- `area:sales`：24/24 通过，0 失败、0 跳过，postflight 通过。
- `area:purchase`：preflight 通过；首项 A59 登录回归因共享管理员 Redis 会话内容/过期边界变化失败，24 项 fail-fast 跳过，postflight 同样报告共享会话漂移。该门禁未执行到 A180 脚本，未出现仓库选择器失败；按任务包“一阶段一次门禁”规则未重跑。
- full 使用 0/1 次。

## Review、预算与交付

- 代码 review 未发现本批新增 P0–P2；改动只影响仓库整列表的初始筛选，不清空分录仓库值，不修改后端或数据。
- 实际执行约 45 分钟；累计子代理 0，并发 0/0；提交使用 3/3，full 0/1，Goal token 未触发 70% 停止阈值。
- 任务包提交 `e825f4ce`；实现提交 `d3c55966`。本地 5173 已从当前工作树重启，8080 健康为 `UP`；未 push。
