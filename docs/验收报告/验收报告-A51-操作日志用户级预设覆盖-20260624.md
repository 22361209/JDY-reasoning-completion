# 验收报告-A51-操作日志用户级预设覆盖

验收日期：2026-06-24  
结论：PASS

## 范围

本批在 A50 角色级操作日志预设基础上，补用户级预设覆盖能力：

- `sys_list_filter_preset` 增加 `user_name`，预设作用域形成通用 / 角色 / 用户三层。
- 当前用户可见通用、当前角色、本人预设。
- 本人默认预设优先于角色默认预设自动应用。
- 角色默认预设仍保留，不会被本人默认清掉。
- A51 回归脚本截图后清理临时本人预设，避免污染 A49/A50 角色默认回归。

## 验收结果

| 项目 | 结果 | 证据 |
| --- | --- | --- |
| session 返回用户名 | PASS | `/api/system/session` 返回 `username=本地管理员`、`roleCode=ADMIN` |
| 本人默认预设保存 | PASS | `userName=本地管理员`，`roleCode=ADMIN`，`isDefault=true` |
| 角色默认仍保留 | PASS | `系统默认-红冲审计` 仍为 `ADMIN` 角色默认、只读 |
| 默认优先级 | PASS | 本人默认在返回列表中排在角色默认之前 |
| 前端自动应用 | PASS | 打开操作日志后自动应用本人默认的 `PRODUCTION / RED_REVERSE_ISSUE / production_material_issue` |
| 列筛选恢复 | PASS | 业务单号列筛选恢复为 `包含 SCLL` |
| 测试数据清理 | PASS | 回归结束后 `A51本人默认-*` 预设数量为 0，角色默认仍存在 |
| A50 兼容回归 | PASS | 本人默认清理后，ADMIN 角色默认仍自动应用，WAREHOUSE 预设仍对 ADMIN 不可见 |

## 自动化证据

- 回归脚本：`scripts/a51-operation-log-user-preset-override-regression.mjs`
- 结构化结果：`verification/a51-operation-log-user-preset-override-regression.json`
- 前端截图：`verification/playwright/a51-operation-log-user-preset-override-20260624123516.png`
- A50 兼容结果：`verification/a50-operation-log-role-scoped-preset-regression.json`
- A50 兼容截图：`verification/playwright/a50-operation-log-role-scoped-preset-20260624123420.png`

## 验证命令

```bash
cd "/Users/linzhenyue/Projects/JDY 推理补完/frontend"
npm run build

cd "/Users/linzhenyue/Projects/JDY 推理补完/backend"
export JAVA_HOME="/opt/homebrew/opt/openjdk@21/libexec/openjdk.jdk/Contents/Home"
export PATH="/opt/homebrew/opt/openjdk@21/bin:$PATH"
./mvnw test

cd "/Users/linzhenyue/Projects/JDY 推理补完"
node --check scripts/a51-operation-log-user-preset-override-regression.mjs
node scripts/a51-operation-log-user-preset-override-regression.mjs
node --check scripts/a50-operation-log-role-scoped-preset-regression.mjs
node scripts/a50-operation-log-role-scoped-preset-regression.mjs
```

## 护栏

- 未重跑 `build-jdy-feature-approval.mjs`。
- 未手改任何生成物。
- 本批不涉及库存、资金、审核状态机。
- 数据库迁移已到 Flyway v16。
