# 验收报告 A25：粘贴冲突候选键盘选择

验收日期：2026-06-24  
回归批次：`20260624084852`  
结论：PASS

## 验收范围

本批补齐分录粘贴冲突候选弹窗的键盘操作：同名多规格商品触发候选后，弹窗自动获得焦点，员工可以用上下键移动候选，用 Enter 确认当前候选并写入分录，用 Escape 取消本次粘贴。

## 断言结果

| 项目 | 结果 |
| --- | --- |
| 弹窗打开后第一候选 `A25-A-20260624084852` 自动高亮 | PASS |
| `Escape` 关闭弹窗并提示“已取消本次粘贴。” | PASS |
| 重新粘贴后 `ArrowDown` 高亮第二候选 `A25-B-20260624084852` | PASS |
| `Enter` 写入第二候选并关闭弹窗 | PASS |
| 分录商品为 `A25-B-20260624084852`，规格为 `右旋 / 短款` | PASS |
| 分录仓库为 `CK-001`，数量 `4`，单价 `28`，金额 `112.00` | PASS |
| 保存后后端销售订单分录与界面一致 | PASS |
| A23 鼠标点击候选 + 点击确定路径回归 | PASS |

## 自动化证据

- 主脚本：`scripts/a25-entry-paste-conflict-keyboard-regression.mjs`
- 结构化结果：`verification/a25-entry-paste-conflict-keyboard-regression.json`
- 兼容回归：`scripts/a23-entry-paste-conflict-candidates-regression.mjs`

## 截图

- 键盘高亮候选弹窗：`verification/playwright/a25-entry-paste-conflict-keyboard-dialog-20260624084852.png`
- 键盘确认后表单：`verification/playwright/a25-entry-paste-conflict-keyboard-form-20260624084852.png`
- A23 鼠标候选回归弹窗：`verification/playwright/a23-entry-paste-conflict-dialog-20260624084904.png`
- A23 鼠标候选回归表单：`verification/playwright/a23-entry-paste-conflict-form-20260624084904.png`

## 代码说明

- 冲突候选弹窗新增 `tabindex` 和键盘事件处理，打开后自动聚焦。
- 每个冲突记录维护 `activeIndex`，用于显示键盘当前候选。
- `ArrowDown` / `ArrowUp` 移动当前候选，限制在候选范围内。
- `Enter` 选择当前候选；所有冲突解决后调用原有写入流程。
- `Escape` 调用原有取消流程。
- 鼠标点击候选路径仍复用 `selectEntryPasteCandidate`，兼容 A23。

## 验证命令

```bash
node scripts/a25-entry-paste-conflict-keyboard-regression.mjs
node scripts/a23-entry-paste-conflict-candidates-regression.mjs

cd "/Users/linzhenyue/Projects/JDY 推理补完/frontend"
npm run build

cd "/Users/linzhenyue/Projects/JDY 推理补完/backend"
export JAVA_HOME="/opt/homebrew/opt/openjdk@21/libexec/openjdk.jdk/Contents/Home"
export PATH="/opt/homebrew/opt/openjdk@21/bin:$PATH"
./mvnw test
```

## 护栏

- 未重跑 `build-jdy-feature-approval.mjs`。
- 未手改任何生成物：`docs/01/02/03`、`config/approved-feature-scope.json`、`app/feature-data.js`、`frontend/src/app/featureScope.ts`。
- 本批未调整范围例外，未改 `config/implementation-overrides.json`。
