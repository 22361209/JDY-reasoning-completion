# 验收报告 A110 表格显示与品牌

- 日期：2026-06-26
- 范围：共享 `DataListPage` / `EntryTable` 表格显示修正；登录页、登录后侧栏和浏览器标题/图标品牌改为 BLD。
- 未触碰：库存、在途、审核、资金、权限逻辑；未重跑 `build-jdy-feature-approval.mjs`；未手改 `frontend/src/app/featureScope.ts`。

## 修复结果

1. 列表与明细视图：按可见列总宽设置 VXE 表格最小宽度，容器底部可横向滚动；数值列右侧保留 12px 安全内边距。
2. 单据表体：单元格垂直居中，数字列和输入框保留右侧安全内边距，输入框高度不超过行高，避免裁切。
3. 分录列筛选按钮：右距调整为 6px，与单据列表列筛选按钮一致。
4. 品牌：登录页 `JDY` 改为 `BLD`，标题改为 `博莱德机械工作台`；登录后左侧导航顶部 `J` 改为 `BLD`；浏览器标题和 favicon 同步为 BLD。

## 前后对照截图

| 场景 | 修改前 | 修改后 |
| --- | --- | --- |
| 登录页品牌 | `verification/playwright/a110-before-login-brand-20260626125510.png` | `verification/playwright/a110-after-login-brand-20260626125825.png` |
| 登录后侧栏品牌 | `verification/playwright/a110-before-workbench-brand-20260626125510.png` | `verification/playwright/a110-after-workbench-brand-20260626125825.png` |
| 单据列表/明细视图横滑与数值列 | `verification/playwright/a110-before-detail-list-scrollbar-20260626125510.png` | `verification/playwright/a110-after-detail-list-scrollbar-20260626125825.png` |
| 单据表体横滑、数值列、筛选按钮 | `verification/playwright/a110-before-entry-table-scrollbar-20260626125510.png` | `verification/playwright/a110-after-entry-table-scrollbar-20260626125825.png` |

## A110 指标

- 修改前：`detailListHasHorizontalOverflow=false`，`detailListQtyPaddingRight=0`，登录页/侧栏仍为 `JDY` / `J`。
- 修改后：`detailListHasHorizontalOverflow=true`，`entryTableHasHorizontalOverflow=true`。
- 修改后：`detailListQtyPaddingRight=12`，`entryQtyPaddingRight=12`，`entryQtyInputPaddingRight=9`。
- 修改后：`entryFilterRightGap=6`，`listFilterRightGap=6`。
- 修改后：登录页品牌 `BLD`，登录标题 `博莱德机械工作台`，侧栏品牌 `BLD`。

## 验证

- `node scripts/a110-table-display-brand-regression.mjs --phase=before`：通过并生成前置截图。
- `node scripts/a110-table-display-brand-regression.mjs --phase=after`：通过并生成后置截图。
- `npm --prefix frontend run build`：通过。
- `JAVA_HOME=/opt/homebrew/opt/openjdk@21 ./mvnw test`：通过。

说明：尝试使用 Browser 插件打开本地页面时，`node_repl/js` 在当前工作目录返回 `sandboxCwd must be an absolute file URI`，连接链路未能执行；本报告采用 A110 Playwright 可见截图和断言指标作为截图验收证据。
