# 验收报告：A79 销售出库入口迁移

## 范围

A79 只迁移 `038 销售出库` 一个入口。已把销售出库详情/新增/表单/分录/工具栏动作迁入 `frontend/src/modules/sales/sales-out/`：

- `SalesOutForm.vue`：表单渲染，直接复用已有 `StandardDocument` / `EntryTable` / `DocumentDialogs`。
- `useSalesOutDocument.ts`：销售出库表单状态、computed、保存、审核、反审核、红冲、作废、引出、打印、源单追踪、分录编辑、选择器、粘贴、零值确认。

未重跑 `build-jdy-feature-approval.mjs`，未手改 `frontend/src/app/featureScope.ts` 等生成物，未改后端业务规则、数据库迁移、库存/资金/审核/红冲规则。

## 硬验收

| 项 | 结果 |
| --- | --- |
| 模块化边界 | 通过。销售出库状态、computed、composable 实例化和表单渲染在 `modules/sales/sales-out/`；`App.vue` 只保留页签打开、列表详情转发、下推草稿转发等装配职责。 |
| App.vue grep | 通过，`grep -nE "salesOut\|销售出库" frontend/src/app/App.vue` 无输出。 |
| App.vue 行数 | 通过。A78 基线 `4036` 行，本批收口 `3667` 行，减少 `369` 行。 |
| 范式复用 | 通过。复用既有 `StandardDocument` / `EntryTable`，未新建第二套表单范式或空壳包装。 |
| 原有能力 | 通过。新增、保存、审核扣库存、引出、打印、源单追踪、反审核/红冲、分录多行、键盘录入均由 A2-A78 全量回归覆盖；销售出库无独立删除后端能力，本批保持既有禁用状态。 |
| 95% 还原 | 通过。对照 `复刻-local/01_金蝶调研/截图/可写测试/销售订单/销售下游调研-销售出库入口点击后-003.png`、`销售出库单保存后-077.png`、`销售出库单审核后-078.png`，当前列表/单据仍保持高密度页签、单头字段、分录表、工具栏和状态章布局；无 RA/RB/RC 偏离登记。 |

## 命令与输出

```bash
npm --prefix frontend run build
```

结果：通过，`vue-tsc -b && vite build` 成功。

```bash
cd backend && JAVA_HOME=/opt/homebrew/opt/openjdk@21 ./mvnw test
```

结果：通过，`Tests run: 1, Failures: 0, Errors: 0, Skipped: 0`。

```bash
wc -l frontend/src/app/App.vue
grep -nE "salesOut|销售出库" frontend/src/app/App.vue || true
```

输出：

```text
3667 frontend/src/app/App.vue
```

grep 无输出。

```bash
node -e 'const r=require("./verification/a2-a78-regression-latest.json"); console.log(JSON.stringify({ok:r.ok,failed:r.failed,total:r.total,expectedTotal:r.expectedTotal,badCount:r.badCount,appVueLines:r.a78Gate.appVueLines,grep:r.a78Gate.grepSalesOutInApp},null,2))'
```

输出：

```json
{
  "ok": true,
  "failed": null,
  "total": 69,
  "expectedTotal": 69,
  "badCount": 0,
  "appVueLines": 3667,
  "grep": ""
}
```

## 回归证据

- 全量汇总：`verification/a2-a78-regression-latest.json`
- 销售出库多状态/多行：`verification/playwright/a2-a7-regression-sales-out-20260625012844.png`
- 销售出库源单追踪：`verification/playwright/a15-sales-out-source-trace-20260625012903.png`
- 销售出库源行号：`verification/playwright/a16-sales-out-source-line-20260625012905.png`
- 销售出库 PDF 打印：`verification/playwright/a39-sales-out-pdf-print-20260625012947.png`

## 过程修正

- A9/A16/A39 暴露销售出库模块打开详情存在旧行/空行瞬态，已改为列表打开前预取详情并同步灌入模块，同时保留源订单号输入框的既有 test-id。
- A47 暴露打印模板设置异步加载会覆盖用户已输入值，已补本地编辑标记和保存前 DOM 同步；A46→A47 连续回归通过。

## 结论

A79 硬验收通过。销售出库入口已迁入独立模块，App.vue 清洁边界和行数门禁达标，A2-A78 全量回归 69 项全绿。
