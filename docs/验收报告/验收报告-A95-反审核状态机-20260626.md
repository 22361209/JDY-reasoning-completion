# 验收报告 A95 反审核状态机 + 工作台取号

- 日期：2026-06-26
- 范围：反审核统一 `AUDITED -> DRAFT`；过账单反审核保留库存/财务冲销；红冲 `RED_REVERSED` 不变；首页工作台建单接 A90 取号。
- 非范围：未重跑 `build-jdy-feature-approval.mjs`，未手改 `frontend/src/app/featureScope.ts`，未扩展 A96/A97/A98。

## 变更

- 后端单据反审核目标态改为 `DRAFT`，销售订单反审核后可编辑并再次审核。
- 销售出库、采购入库、其他出/入库、调拨、盘点、盘盈/盘亏、生产领料、产品入库反审核仍执行原库存/财务冲销，再回草稿。
- 生产领料/产品入库补齐单据生命周期需要的 `version`、`updated_at` 契约列。
- 编号服务改为：已存在草稿单号按编辑处理，非草稿撞号才自动取新号。
- 首页工作台销售订单、销售出库等表单入口触发 `startNew()` 自动取号；取号异步返回只回填单号，不覆盖用户已输入的表单内容。

## 回归断言

- `反审核后可再编辑再审核`：`scripts/a95-lifecycle-workbench-regression.mjs` 覆盖销售订单、销售出库反审核后回 `DRAFT`，保存编辑内容，再次审核回 `AUDITED`。
- `工作台建单自动取号`：同一 A95 脚本覆盖首页工作台销售订单 `XSDD******`、销售出库 `XSCKD******` 自动取号。
- A34、A2-A7、A86/A87/A88、A89 等旧 `REVERSED` 断言已改为反审核后 `DRAFT/草稿`。

## 验证

- `npm --prefix frontend run build`：通过。
- `cd backend && JAVA_HOME=/opt/homebrew/opt/openjdk@21 ./mvnw test`：通过，含 ArchUnit。
- `node scripts/a90-numbering-regression.mjs`：通过。
- `node scripts/a95-lifecycle-workbench-regression.mjs`：通过。
- `node scripts/run-a2-a95-regression.mjs`：通过。

## 全量结果

- 文件：`verification/a2-a95-regression-latest.json`
- mtime：`2026-06-26 10:37:17 +0800`
- ok：`true`
- scripts：`83`
- expectedTotal：`83`
- badCount：`0`
- skipped：`0`

提交后需以最终 HEAD 后重跑的 `verification/a2-a95-regression-latest.json` mtime 为准。
