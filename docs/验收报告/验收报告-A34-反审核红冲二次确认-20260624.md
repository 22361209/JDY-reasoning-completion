# 验收报告 A34：反审核/红冲二次确认

验收日期：2026-06-24
回归批次：`20260624095653`
结论：PASS

## 验收范围

本批把销售出库、采购入库的高风险库存动作接入二次确认：点击反审核/红冲不会立即执行，确认后才调用后端。红冲成功后前端加载后端生成的红字单，避免界面显示原单号却标记已红冲。

## 断言结果

| 项目 | 结果 |
| --- | --- |
| 销售出库点击 `反审核` 弹出 `反审核确认` | PASS |
| 取消反审核后单据仍为 `已审核` | PASS |
| 确认反审核后界面为 `已反审核` | PASS |
| 后端销售出库状态为 `REVERSED` | PASS |
| 采购入库点击 `红冲` 弹出 `红冲确认` | PASS |
| 红冲确认弹窗显示 `HC-...` 红冲单号 | PASS |
| 取消红冲后单据仍为 `已审核` | PASS |
| 确认红冲后界面加载红字单 | PASS |
| 后端红字采购入库状态为 `RED_REVERSED` | PASS |

## 自动化证据

- 主脚本：`scripts/a34-risky-action-confirm-regression.mjs`
- 结构化结果：`verification/a34-risky-action-confirm-regression.json`
- 销售出库：`XSCK-A34-R-20260624095653`
- 采购入库原单：`CGRK-A34-HC-20260624095653`
- 采购入库红字单：`HC-CGRK-A34-HC-20260624095653`
- 截图：`verification/playwright/a34-reverse-confirm-dialog-20260624095653.png`
- 截图：`verification/playwright/a34-red-reverse-confirm-dialog-20260624095653.png`

## 代码说明

- 反审核、红冲按钮改为先打开 `risky-action-dialog`。
- 取消确认只关闭弹窗并提示取消，不调用后端接口。
- 确认反审核后调用既有反审核接口。
- 确认红冲后调用既有红冲接口，并读取 `HC-...` 红字单详情回填当前表单。

## 验证命令

```bash
node scripts/a34-risky-action-confirm-regression.mjs

cd "/Users/linzhenyue/Projects/JDY 推理补完/backend"
export JAVA_HOME="/opt/homebrew/opt/openjdk@21/libexec/openjdk.jdk/Contents/Home"
export PATH="/opt/homebrew/opt/openjdk@21/bin:$PATH"
./mvnw test

cd "/Users/linzhenyue/Projects/JDY 推理补完/frontend"
npm run build
```

## 护栏

- 未重跑 `build-jdy-feature-approval.mjs`。
- 未手改任何生成物：`docs/01/02/03`、`config/approved-feature-scope.json`、`app/feature-data.js`、`frontend/src/app/featureScope.ts`。
- 本批未调整范围例外，未改 `config/implementation-overrides.json`。
