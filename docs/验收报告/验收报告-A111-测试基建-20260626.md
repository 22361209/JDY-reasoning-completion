# 验收报告 A111 测试基建

## 验收日期

2026-06-26

## 验收范围

- 新增分层回归 runner：`scripts/run-regression-tier.mjs`
- 三档回归：`smoke`、`area:<module>`、`full`
- 后端快测：生命周期、过账 pipeline、税额计算、预留库存三量不变式
- 文档门禁：`CLAUDE.md`、`docs/11-验收标准与测试门禁.md`、`docs/15-核心流稳定化追踪.md`

## 测试账号

- Playwright 回归使用既有本地管理员登录流程：`admin`
- 后端快测使用事务回滚和 mock 当前会话，不新增业务账号

## 测试数据

- Playwright 使用各脚本原有 `batch` 唯一单号
- 后端快测使用唯一 A111 单号并在测试事务内回滚

## 通过项

- `node scripts/run-regression-tier.mjs smoke` 通过：10/10，`verification/regression-tier-smoke-latest.json`
- `node scripts/run-regression-tier.mjs area:finance` 通过：10/10，`verification/regression-tier-area-finance-latest.json`
- `node scripts/run-regression-tier.mjs full` 通过：95/95，`verification/regression-tier-full-latest.json`
- `JAVA_HOME=/opt/homebrew/opt/openjdk@21 ./mvnw test` 通过：12 tests，含新增后端快测与 ArchUnit
- `npm --prefix frontend run build` 通过
- `CLAUDE.md` 与 `docs/11` 已写明表现/行为/共享核心逻辑判定规则

## 失败项

- 无最终失败项。
- 首次 full 串跑在旧脚本 `a15-source-trace-regression.mjs` 出现一次 Playwright 导航等待超时；单独重跑 A15 通过，随后 full 重跑 95/95 通过。

## 阻塞项

无。

## 截图路径

- 本批没有新增 UI 行为；截图由既有 Playwright 脚本继续产出到 `verification/playwright/`
- 分层 runner 汇总 JSON：
  - `verification/regression-tier-smoke-latest.json`
  - `verification/regression-tier-area-finance-latest.json`
  - `verification/regression-tier-full-latest.json`

## 剩余风险

- 完整 Spring Modulith `ApplicationModules.verify()` 当前会暴露既有模块循环与非暴露类型依赖；A111 未做架构重构，新增 ArchUnit 先覆盖“应用层不依赖 API 层”的低风险规则。
- `smoke` 当前为 10 条核心快乐路径，后续新增核心业务时需要同步维护白名单。

## 下一步

- 后续批次按改动分类选择档位：表现改动跑 `smoke` + 人工眼验；行为改动跑 `smoke` + `area:<module>`；共享核心逻辑跑 `full`。

## 护栏确认

- 未改业务行为。
- 未改 UI。
- 未重跑 `build-jdy-feature-approval.mjs`。
- 未手改生成物 `frontend/src/app/featureScope.ts`。
