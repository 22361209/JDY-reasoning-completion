# 验收报告 A7：多单据列表打开详情

日期：2026-06-24

## 结论

PASS。销售出库、采购订单、采购入库列表已接入真实详情打开路径；点击列表 `单据编号` 后，前端按单据类型读取后端详情接口并回填对应表单。查询列表页签不再误标未保存状态。

## 本批完成

- 后端新增：
  - `GET /api/sales-outs/{billNo}`
  - `GET /api/purchase-orders/{billNo}`
  - `GET /api/purchase-ins/{billNo}`
- 前端新增统一单据详情读取：`fetchDocumentDetail(type, billNo)`。
- 列表组件从销售订单专用事件改为 `openDocument` 通用事件。
- App 按 `salesOrder / salesOut / purchaseOrder / purchaseIn` 打开对应表单并回填单头、状态和明细。
- 查询入口打开的列表页签不再继承表单 `dirty` 星号。

## 验收记录

- `frontend npm run build`：PASS
- `backend ./mvnw test`：PASS
- API 验证：
  - `sales-out-form-list` 第一张 `XSCK-20260624-100200351-EQI`，详情返回 `AUDITED` 和 1 行明细。
  - `purchase-order-form-list` 第一张 `CGDD-00001`，详情返回 `AUDITED` 和 1 行明细。
  - `purchase-in-form-list` 第一张 `HC-CGRK-HCA-230205`，详情返回 `AUDITED` 和 1 行明细。
- Playwright 浏览器验收：
  - 销售出库列表点击单据编号，表单单据号回填一致。
  - 采购订单列表点击单据编号，表单单据号回填一致。
  - 采购入库列表点击单据编号，表单单据号回填一致。

## 截图证据

- `verification/playwright/a7-sales-out-open-detail-1366.png`
- `verification/playwright/a7-purchase-order-open-detail-1366.png`
- `verification/playwright/a7-purchase-in-open-detail-1366.png`

## 未完成与风险

- 当前表单明细已经按数组回填，但界面范式仍偏向单行编辑；A8 需要把四类核心表单推进到多行真实渲染和逐行编辑。
- 销售订单部分出库、采购订单部分入库的剩余数量控制仍后置。
- 本批未调整审批范围，未触碰任何生成物。
