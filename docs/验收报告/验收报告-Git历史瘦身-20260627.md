# 验收报告 - Git 历史瘦身 - 2026-06-27

## 结论

本批只做 Git 历史瘦身，不改业务功能、不改生成链、不重跑 `build-jdy-feature-approval.mjs`。

- 已备份重写前完整历史到 `/Users/linzhenyue/Projects/JDY-资料库/git-backups/20260627-174203/JDY-推理补完-before-history-rewrite.bundle`。
- 已用历史重写从本地分支中剔除 `verification/**`。
- 已删除 `refs/original/*` 和 `refs/codex/turn-diffs/*` 等旧快照引用。
- 已执行 reflog expire 和 aggressive gc。

## 结果

| 项目 | 重写前 | 重写后 |
| --- | ---: | ---: |
| `.git/` 大小 | 1.7G | 5.7M |
| 当前有效历史中的 `verification/` 引用 | 697 | 0 |
| 所有 refs 中的 `verification/` 引用 | 697 | 0 |

## 验证命令

```bash
git rev-list --objects --all | rg '^.+ verification/' | wc -l
du -sh .git
git count-objects -vH
git show-ref
```

## 备注

本次是本地仓库历史重写，提交 hash 已变化。当前没有远端，因此不涉及强推；若未来接入远端，应以本次重写后的仓库作为基线。
