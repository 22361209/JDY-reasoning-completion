# 验收报告 A23：分录粘贴冲突候选选择

验收日期：2026-06-24  
回归批次：`20260624083218`  
结论：PASS

## 验收范围

本批补齐分录粘贴的冲突处理：当商品名称对应多个商品资料且粘贴内容不能唯一定位时，不再静默取第一个商品，而是弹出候选让员工选择后再写入分录。

同时修正列表 API 的演示扩行边界：已接入数据库的主数据/业务列表不再因 `pageSize=1000` 被扩展为伪造样本行，避免污染商品候选和选择器。

## 断言结果

| 项目 | 结果 |
| --- | --- |
| 同名商品未带规格时弹出候选 | PASS |
| 候选展示编码、名称、规格、单位 | PASS |
| 未选择候选前“确定”不可用 | PASS |
| 选择 `A23-A-20260624083218` 后第 1 行写入粗牙商品 | PASS |
| 第 2 行带规格 `细牙 / 右` 自动命中 `A23-B-20260624083218` | PASS |
| 页面合计 `173.00 = 62.00 + 111.00` | PASS |
| 保存后后端销售订单分录与界面一致 | PASS |
| A17 编码粘贴回归 | PASS |
| A20 名称/规格粘贴回归 | PASS |

## 自动化证据

- 主脚本：`scripts/a23-entry-paste-conflict-candidates-regression.mjs`
- 结构化结果：`verification/a23-entry-paste-conflict-candidates-regression.json`
- 兼容回归：
  - `scripts/a17-entry-excel-paste-regression.mjs`
  - `scripts/a20-entry-name-spec-paste-regression.mjs`

## 截图

- 候选弹窗：`verification/playwright/a23-entry-paste-conflict-dialog-20260624083218.png`
- 确认后表单：`verification/playwright/a23-entry-paste-conflict-form-20260624083218.png`
- A17 编码粘贴回归：`verification/playwright/a17-entry-excel-paste-20260624083135.png`
- A20 名称规格粘贴回归：`verification/playwright/a20-entry-name-spec-paste-20260624083157.png`

## 代码说明

- 粘贴解析从“直接返回分录”改为返回 `lines + conflicts`。
- 能用商品编码或名称+规格唯一定位时直接写入。
- 同名多规格且粘贴内容不能唯一定位时，暂停本次粘贴并显示候选。
- 选择候选后一次性写入分录，保留仓库、数量、单价。
- 仓库匹配增加“原材料/原料”文本归一，兼容员工口头习惯。
- 列表 API 仅允许纯 stub 列表做 1000 行演示扩展，真实主数据和业务列表返回数据库结果。

## 验证命令

```bash
node scripts/a23-entry-paste-conflict-candidates-regression.mjs
node scripts/a17-entry-excel-paste-regression.mjs
node scripts/a20-entry-name-spec-paste-regression.mjs

cd "/Users/linzhenyue/Projects/JDY 推理补完/frontend"
npm run build

cd "/Users/linzhenyue/Projects/JDY 推理补完/backend"
export JAVA_HOME="/opt/homebrew/opt/openjdk@21/libexec/openjdk.jdk/Contents/Home"
export PATH="/opt/homebrew/opt/openjdk@21/bin:$PATH"
./mvnw test
```

## 护栏

- 未重跑 `build-jdy-feature-approval.mjs`。
- 未手改任何生成物：`docs/01/02/03`、`config/approved-feature-scope.json`、`app/feature-data.js`、`frontend/src/app/featureScope.ts`。
- 本批未调整范围例外，未改 `config/implementation-overrides.json`。
