# 验收报告 A113a 销售链前端范式

## 验收日期

2026-06-27

## 验收范围

- 销售订单 / 发货通知 / 销售出库共享单据前端范式。
- 基本信息区从均匀四列改为云星空式固定业务列宽。
- 分录表按云星辰截图定标为清爽录入密度。
- 分录序号列 hover 显示 `+ / -` 快捷增删行。
- 列头筛选按钮 hover 可见；列头批量填充按钮常驻。
- 仓库/交期迁到列头批量填充；交期支持文本输入、格式化和日历面板。

## 参考截图

- `/Users/linzhenyue/Projects/HW-WIN2019/image/JDY/jdy-sales-order-list-2026-06-27T04-14-46.png`
- `/Users/linzhenyue/Projects/HW-WIN2019/image/JDY/jdy-sales-order-detail-2026-06-27T04-15-35.png`
- `/Users/linzhenyue/Projects/HW-WIN2019/image/JDY/jdy-sales-order-delayed-2-2026-06-27T04-18-39.png`
- `/Users/linzhenyue/Projects/HW-WIN2019/image/JDY/jdy-sales-order-delayed-3-2026-06-27T04-20-06.png`
- `/Users/linzhenyue/Projects/HW-WIN2019/image/JDY/jdy-current-delayed-4-2026-06-27T04-21-12.png`
- `/Users/linzhenyue/Projects/HW-WIN2019/image/K3Cloud/kingdee-sales-quotation-new-delayed-2026-06-27T04-10-22.png`
- 用户补充单价批量填充截图：`/var/folders/97/7tqts6sx2hl16hrmyjjzmy6m0000gn/T/codex-clipboard-b8faf97b-2b43-49f4-8d8c-7466f399138e.png`

## 测试账号

本地管理员 / 系统管理员。

## 通过项

- 基本信息区已改为固定业务列宽，A113a DOM 实测 `grid-template-columns = 270px 270px 270px 866px`。
- 分录行高已按截图口径改为清爽录入密度：行高 `32px`，表头 `28.5px`，输入框 `26px`。
- 分录序号列 hover 后显示蓝色 `+` 和红色 `-`；`+` 触发 `insertLineAfter`，`-` 触发 `removeLine`。
- 分录顶部旧的“批量仓库 / 批量交期 / 应用交期”外置控件已移除，仓库和交期改为列头批量填充入口。
- 交期列支持输入 `2026/05/01` / `2026-05-01` 并格式化为 `YYYY-MM-DD`。
- 交期列头批量日期弹窗和日历面板可打开。
- 列头筛选按钮默认隐藏，hover/激活时显示；筛选弹窗可打开。

## 截图证据

- 基本信息区 + 32px 分录基础态：`verification/playwright/a113a-sales-entry-base-20260627063203.png`
- 序号列 hover `+ / -`：`verification/playwright/a113a-sales-entry-row-hover-20260627063203.png`
- 列头筛选弹窗：`verification/playwright/a113a-sales-entry-filter-date-20260627063203.png`
- 列头批量日期 + 日历弹窗：`verification/playwright/a113a-sales-entry-bulk-date-picker-20260627063203.png`

## 自动化验证

- `npm --prefix frontend run build`：通过。
- A113a 自定义 Playwright 验收：`verification/a113a-sales-front-pattern-regression.json`，`ok=true`。
- `node scripts/run-regression-tier.mjs smoke`：通过，10/10，0 skipped。
- `node scripts/a26-selector-next-field-regression.mjs`：通过。
- `node scripts/a27-entry-enter-next-line-regression.mjs`：通过。
- `node scripts/a28-entry-new-line-inherit-warehouse-regression.mjs`：通过。

## 剩余风险

- 单价批量填充只完成规格确认，尚未实现价格来源统计接口。下一批 A113b/A114 需要补默认价、最近成交价、历史最高价、历史最低价、平均价、成本价等来源，禁止前端写假数据。
- 本批是表现/交互改动，未改后端业务规则；按 `docs/11` 跑 smoke + 分录专项 + 截图眼验。若后续将列头批量能力扩展到所有模块，可视影响范围补 full。
