# 验收报告：A175 历史夹具分页、日期与清理治理

## 结论

A175 已完成。未改变产品物料选择器或日期快捷过滤行为；只使回归夹具与真实行为重新对齐。

- A23/A25 的 run-owned 同名物料编码使用稳定前页前缀，真实覆盖冲突候选与键盘选择；销售单和物料均在 finally 中按精确编号删除并复验零残留。
- A128 以 `Asia/Shanghai` 动态计算当前月、上月和上季度，消除写死 2026-07；其两张测试销售单也在 finally 中精确删除。
- 最终 review 无 P0–P2。

## 验收

```text
A23 conflict candidates                                     PASS，物料/单据零残留
A25 keyboard conflict                                       PASS，物料/单据零残留
A128 list query                                             PASS，25 assertions，单据零残留
node --check / git diff --check                             PASS
full --continue-on-failure                                  143/143 PASS，0 failed，0 skipped
postflight                                                  PASS
```

有效 full：开始 `2026-08-07T10:26:29.094Z`，结束 `2026-08-07T11:56:47.604Z`，报告为 `verification/regression-tier-full-continue-latest.json`。未 push，未操作生产环境。
