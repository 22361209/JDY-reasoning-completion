# 验收报告：A78 前端范式重构

日期：2026-06-25

## 结论

A78 本次只迁移 `037 销售订单` 一个入口，已把销售订单详情/新增/表单/分录渲染迁入 `frontend/src/modules/sales/sales-order/`，并落到有实际职责的 `StandardDocument` / `EntryTable`。未重跑 `build-jdy-feature-approval.mjs`，未手改生成物，未改后端业务规则。

## 硬门禁结果

| 门禁 | 实测结果 |
| --- | --- |
| `App.vue` 行数下降 >= 600 行 | 重构前基线 4651 行；当前 `frontend/src/app/App.vue` 4036 行；下降 615 行，达标 |
| `StandardDocument` / `EntryTable` 不是空壳 | `StandardDocument.vue` 63 行，负责单据壳、头部、状态、动作区、消息区；`EntryTable.vue` 403 行，负责分录表格、输入、选择、键盘、批量仓库、拖拽/行操作事件 |
| 销售订单详情/新增由模块渲染 | `SalesOrderForm.vue` 183 行，`useSalesOrderDocument.ts` 57 行；销售订单保存/审核/删除 API 调用仅在模块 hook 中 |
| `App.vue` 不再含销售订单表单/分录逻辑 | grep 未发现 `salesOrderForm`、`saveSalesOrderDraft`、`auditSalesOrder`、`deleteSalesOrder`、`sales-line-product`、`sales-line-qty`、`sales-line-price`、`sales-line-warehouse`；`App.vue` 仅保留通用分录滚动定位 `.entry-table tr[data-line-no=...]` 和非销售单据兼容渲染 |
| 销售订单原有能力保留 | A2-A77 全量回归覆盖新增、保存、审核、删除、引出、打印、下推、反审核/红冲、分录多行、键盘录入；最新结果 69 项全绿 |
| A9/A13 登录态修复并全量回归 | 已修 A9/A13 等早期脚本登录态/API session；`verification/a2-a77-regression-latest.json`：`ok=true`，`failed=null`，`total=69`，`badCount=0` |

## 关键命令与输出

```bash
wc -l frontend/src/app/App.vue frontend/src/components/StandardDocument.vue frontend/src/components/EntryTable.vue frontend/src/modules/sales/sales-order/SalesOrderForm.vue frontend/src/modules/sales/sales-order/useSalesOrderDocument.ts
```

输出要点：

```text
4036 frontend/src/app/App.vue
  63 frontend/src/components/StandardDocument.vue
 403 frontend/src/components/EntryTable.vue
 183 frontend/src/modules/sales/sales-order/SalesOrderForm.vue
  57 frontend/src/modules/sales/sales-order/useSalesOrderDocument.ts
```

```bash
rg -n "salesOrderForm|saveSalesOrderDraft|auditSalesOrder|deleteSalesOrder|<table|entry-table|sales-line-product|sales-line-qty|sales-line-price|sales-line-warehouse" frontend/src/app/App.vue frontend/src/modules/sales/sales-order frontend/src/components/StandardDocument.vue frontend/src/components/EntryTable.vue -S
```

输出要点：

```text
frontend/src/components/EntryTable.vue:16:    <table>
frontend/src/app/App.vue:797:          :entry-table-colspan="entryTableColspan"
frontend/src/app/App.vue:865:          :entry-table-colspan="entryTableColspan"
frontend/src/app/App.vue:1460:  const target = document.querySelector<HTMLElement>(`.entry-table tr[data-line-no="${highlightedSourceLineNo.value}"]`);
frontend/src/modules/sales/sales-order/SalesOrderForm.vue:77:        :entry-table-colspan="entryTableColspan"
frontend/src/modules/sales/sales-order/useSalesOrderDocument.ts:3:import { auditSalesOrder, deleteSalesOrder, saveSalesOrderDraft } from "../../../services/salesOrderApi";
```

```bash
node -e 'const r=require("./verification/a2-a77-regression-latest.json"); const bad=r.results.filter(x=>x.status!==0); console.log(JSON.stringify({ok:r.ok,failed:r.failed,total:r.results.length,badCount:bad.length,bad:bad.map(x=>x.script)},null,2));'
```

输出：

```json
{
  "ok": true,
  "failed": null,
  "total": 69,
  "badCount": 0,
  "bad": []
}
```

```bash
npm --prefix frontend run build
```

结果：通过。Vite 仅提示 chunk size warning。

```bash
cd backend && JAVA_HOME=/opt/homebrew/opt/openjdk@21 ./mvnw test
```

结果：`BUILD SUCCESS`；`Tests run: 1, Failures: 0, Errors: 0, Skipped: 0`。

## 额外修复

A70 回归暴露安全设置页两个 number 输入在保存时可能提交旧值。已把会话超时和密码最小长度输入改为 `v-model.number`，并将 A70 脚本改成真实键盘输入路径。该修复属于前端提交一致性修复，不改后端安全规则。

## 证据文件

- `verification/a2-a77-regression-latest.json`
- `verification/a70-session-timeout-settings-regression.json`
- `verification/playwright/a2-a7-regression-sales-20260624191210.png`
- `verification/playwright/a13-entry-row-reorder-20260624191226.png`
- `verification/playwright/a70-session-timeout-7-minutes-20260624192133.png`
- `verification/playwright/a70-session-timeout-reset-30-minutes-20260624192133.png`
