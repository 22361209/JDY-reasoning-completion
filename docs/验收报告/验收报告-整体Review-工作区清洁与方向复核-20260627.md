# 验收报告 - 整体 Review / 工作区清洁与方向复核 - 2026-06-27

## 结论

本批不新增业务功能，完成项目级方向固化和外围工程收口：

- 代码与资料边界已写入 README / 工程规范：本仓库保留实现代码、必要脚本、架构文档和验收报告；原始采集资料、大批量截图和临时验证产物不混入实现提交。
- 表格方向已统一为原生共享表格内核：不再使用 VXE Table 作为运行时依赖。
- A109 口径已明确为采购在途；完整可用库存后续再组合采购在途 + 生产在途。
- 本地服务脚本补齐为 `dev-up` / `dev-status` / `dev-down`。

## 用户已确认的方向

| 事项 | 决策 |
| --- | --- |
| 代码与资料 | 分开治理；实现仓库不承载大批量采集资料和临时截图 |
| 表格内核 | 使用原生共享表格内核，不需要 VXE |
| 验证产物 | `verification/` 默认本地临时，不进 Git |
| A109 | 只做采购在途；生产在途后续单独做 |
| 成本价 | 暂时占位，不在当前批次完善成本核算 |
| 单价批填成功提示 | 不需要额外反馈，分录表直接显示结果 |

## 变更记录

| 类型 | 文件 | 内容 |
| --- | --- | --- |
| 仓库边界 | `README.md`、`docs/08-工程规范.md` | 增加实现仓库/调研资料/验证产物边界 |
| 验证产物 | `.gitignore` | `verification/` 默认忽略，避免继续产生大量未跟踪截图 |
| 表格 ADR | `docs/04-架构决策记录.md` | ADR-008 标记为历史；新增 ADR-011 原生共享表格内核 |
| 前端规范 | `docs/10-前端体验与视觉规范.md` | 表格规范改为 ADR-011 |
| VXE 去运行时 | `frontend/src/main.ts`、`frontend/package.json`、`frontend/package-lock.json` | 移除 `vxe-table` / `xe-utils` 依赖和全局注册 |
| 库存口径 | `docs/06-数据一致性与库存原则.md` | 明确可用库存公式和 A109/A109b/A109c 边界 |
| 服务脚本 | `scripts/dev-up.sh`、`scripts/dev-status.sh`、`scripts/dev-down.sh` | 补齐启动、状态、停止三件套，日志进 `verification/logs/` |
| 交接 | `docs/09-交接清单.md`、`docs/12-当前批次验收清单.md`、`docs/15-核心流稳定化追踪.md` | 更新当前状态、报告路径和下一步边界 |

## 工作区分类说明

当前工作区仍有大量历史未提交内容，不能一次性混提交：

- 业务代码：A113b 价格来源接口、A109 采购在途相关代码、销售链前端改动等，应按批次拆分。
- 文档和规则：本批新增/修改的 README、ADR、库存口径、工程规范、交接文档、验收报告可作为“整体 Review / 工程收口”提交。
- 生成物：`config/approved-feature-scope.json` 仅时间戳变化，后续应单独决定还原或保留，不混入本批提交。
- 验证产物：`verification/` 仍有历史大量文件，本批只新增忽略规则，不删除历史文件。

## 验证

```bash
./scripts/dev-status.sh
bash -n scripts/dev-up.sh scripts/dev-status.sh scripts/dev-down.sh
npm --prefix frontend run build
```

结果：

- `dev-status` 正常输出端口、健康检查和 Docker compose 状态。
- 三个 shell 脚本语法检查通过。
- 前端 build 通过。

当前 `dev-status` 观测：

- frontend `5173` 正在监听。
- backend `8080` 未监听。
- PostgreSQL / Redis compose 服务处于 healthy。

## 后续建议

1. 先决定是否拆提交：A113b、A109、整体 Review、验证产物规则应分开。
2. 单独处理 `config/approved-feature-scope.json` 的 generatedAt 变化。
3. 后续可开技术债批次，把旧 `.vxe-*` class/test-id 命名逐步改为中性命名，并同步升级历史回归脚本。
4. 若继续销售链，优先补 A109 采购在途的批次报告和生产在途后续计划。
