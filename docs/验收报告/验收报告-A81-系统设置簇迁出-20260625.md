# 验收报告 A81 系统设置簇迁出 App.vue

日期：2026-06-25

## 范围

A81 只搬迁 5 个系统设置区域，不加深安全、权限、用户或通知能力，不触碰单据、主数据、列表和后端业务规则；未重跑 `build-jdy-feature-approval.mjs`，未手改生成物（含 `frontend/src/app/featureScope.ts`）。

## 迁移结果

| 区域 | 目标目录 | 文件行数 | 提交 |
| --- | --- | ---: | --- |
| 登录/找回密码/改密 | `frontend/src/modules/system/auth/` | `LoginPage.vue` 81；`PasswordChangeDialog.vue` 50；`useAuthForms.ts` 167 | `e5e9f0f`、`20926bd` |
| 安全设置 | `frontend/src/modules/system/security/` | `SecuritySettingsPage.vue` 138；`useSecuritySettingsPage.ts` 164 | `703ef37` |
| 权限/角色矩阵 | `frontend/src/modules/system/permission/` | `PermissionMatrixPage.vue` 64；`usePermissionMatrixPage.ts` 99 | `9354d49` |
| 用户管理 | `frontend/src/modules/system/user/` | `UserManagementPage.vue` 144；`useUserManagementPage.ts` 224 | `e497cfb` |
| 通知设置/outbox | `frontend/src/modules/system/notification/` | `NotificationOutboxPanel.vue` 51；`NotificationProviderSettingsPage.vue` 118；`useNotificationOutbox.ts` 89；`useNotificationProviderSettingsPage.ts` 88 | `3c049f9` |

App.vue 行数对比：A81 基线 2444 行，完成后 1391 行，减少 1053 行，满足“再降 >=700 行”。

## 边界说明

App.vue 保留当前会话用户、登录态切换、权限码读取、左侧导航/按钮显隐、tab 装配和跨页签 session 失效处理，这些属于 shell 职责。五个设置区域的页面、对话框、表单状态、保存逻辑和 composable 实例化已迁入对应 `modules/system/<area>/`。

A60 回归曾捕获迁移后的登录页消息丢失：密码修改后 shell 清空 session 时 LoginPage 尚未挂载，提示未能显示。已在 `20926bd` 中将登录页消息作为 shell 状态传入 `LoginPage`，保持原行为，不新增功能；随后 A60 单独通过并完成全量回归。

## 回归与门禁

每个区域迁完均执行 `npm --prefix frontend run build`，均通过。

最终全量 A2-A80 回归：

```json
{
  "ok": true,
  "failed": null,
  "total": 69,
  "expectedTotal": 69,
  "badCount": 0,
  "generatedAt": "2026-06-25T02:50:44.197Z"
}
```

重点链路覆盖：A54 权限矩阵、A55-A56 权限前后端守卫、A57-A60 登录/会话/改密、A61-A62 登录锁定与解锁、A65 找回密码、A67-A72 跨页签/重复登录/安全设置、A73-A77 通知 outbox 与供应商设置均在全量回归中通过。

最终工程门禁：

```text
npm --prefix frontend run build
PASS
```

```text
cd backend && JAVA_HOME=/opt/homebrew/opt/openjdk@21 ./mvnw test
Tests run: 1, Failures: 0, Errors: 0, Skipped: 0
BUILD SUCCESS
```

说明：仓库根目录没有 `./mvnw`，wrapper 位于 `backend/mvnw`，因此后端门禁在 `backend` 目录执行。
