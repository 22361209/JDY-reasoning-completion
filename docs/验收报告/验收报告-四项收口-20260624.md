# 验收报告：四项收口

验收日期：2026-06-24  
结论：PASS

## 收口结果

| 项目 | 结果 | 证据 |
| --- | --- | --- |
| A2-A7 多行/多状态回验 | PASS | `scripts/a2-a7-multiline-regression.mjs` 已重跑；结果见 `verification/a2-a7-multiline-regression.json` 和 `docs/验收报告/验收报告-A2-A7-多行回归-20260624.md` |
| 批次编号回贴坐标系 | PASS | `docs/12-当前批次验收清单.md` 固定区维护 A/C/D/E/F/G/A2-A21 与 B0-B6、复刻-local 入口映射，本次补入 `A2-A7 多行/多状态回归` |
| 验收报告归档 | PASS | `docs/` 根目录无 `验收报告-*.md`；报告统一位于 `docs/验收报告/` |
| 验证目录与审批目录歧义 | PASS | 验证产物目录为 `verification/`；审批 Excel 目录保留 `outputs/`；仓库内无旧验证目录的产物路径引用 |

## A2-A7 回验摘要

| 单据 | 单号 | 状态章 | 行数 | 合计 |
| --- | --- | --- | --- | --- |
| 销售订单 | `XSDD-A2A7-A-20260624081505` | 已审核 | 3 | 502.00 |
| 销售出库 | `XSCK-A2A7-HC-20260624081505` | 已红冲 | 3 | -502.00 |
| 采购订单 | `CGDD-A2A7-A-20260624081505` | 已审核 | 3 | 902.00 |
| 采购入库 | `CGRK-A2A7-R-20260624081505` | 已反审核 | 3 | 902.00 |
| 生产领料 | `SCLL-A2A7-HC-20260624081505` | 已红冲 | 3 | -12.00 |
| 产品入库 | `CPRK-A2A7-R-20260624081505` | 已反审核 | 3 | 800.00 |

状态覆盖：草稿、已审核、已反审核、已红冲均覆盖；销售订单和采购订单当前第一版只实现草稿/已审核，反审核和红冲由出入库/生产库存单据覆盖。

## 验证命令

```bash
node scripts/a2-a7-multiline-regression.mjs
node --check export-approved-scope.mjs
node --check generate-frontend-scope.mjs
node --check build-app-data.mjs
node --check build-jdy-feature-approval.mjs
```

同时校验：

- `docs/09-交接清单.md` 中最近报告路径全部存在。
- `find docs -maxdepth 1 -type f -name '验收报告-*.md'` 无输出。
- `find . -maxdepth 2 -type d -name 'output' -o -name 'outputs' -o -name 'verification'` 仅保留 `outputs/` 与 `verification/`。
- 已扫描业务源码、脚本和文档，确认无旧验证目录的产物路径引用。

## 护栏

- 未重跑 `build-jdy-feature-approval.mjs`。
- 未手改任何生成物：`docs/01/02/03`、`config/approved-feature-scope.json`、`app/feature-data.js`、`frontend/src/app/featureScope.ts`。
- `outputs/` 仍保留给用户审批 Excel 和生成链输入，不改名、不移动。
