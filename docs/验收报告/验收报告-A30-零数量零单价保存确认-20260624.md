# 验收报告 A30：零数量/零单价保存确认

验收日期：2026-06-24  
回归批次：`20260624092358`  
结论：PASS

## 验收范围

本批补齐分录保存前的零值确认：有效分录里数量或单价为 0 时，先弹出确认明细；取消不保存，确认后继续保存，并保留零值分录。该能力用于区分赠品、样品、补录等真实业务与误填。

## 断言结果

| 项目 | 结果 |
| --- | --- |
| 零值确认弹窗列出 2 行异常分录 | PASS |
| 弹窗包含 `单价为 0` 和 `数量为 0` 原因 | PASS |
| 取消后提示 `已取消保存，请检查零数量/零单价分录。` | PASS |
| 确认保存后提示 `草稿已保存，已确认 2 行零值分录` | PASS |
| 保存后界面分录行数为 `3` | PASS |
| 保存后合计金额为 `60.00` | PASS |
| 后端保存零单价行 `CP-001 / CK-001 / 1 / 0`、零数量行 `PJ-014 / CK-002 / 0 / 5`、正常行 `CP-T413874 / CK-001 / 2 / 30` | PASS |
| A29 保存前空白行/重复行提示回归 | PASS |

## 自动化证据

- 主脚本：`scripts/a30-entry-zero-value-confirm-regression.mjs`
- 结构化结果：`verification/a30-entry-zero-value-confirm-regression.json`
- 兼容回归：`scripts/a29-entry-save-validation-regression.mjs`
- 兼容回归结果：`verification/a29-entry-save-validation-regression.json`

## 截图

- 零值确认弹窗：`verification/playwright/a30-entry-zero-value-confirm-dialog-20260624092358.png`
- 确认保存后表单：`verification/playwright/a30-entry-zero-value-confirm-saved-20260624092358.png`
- A29 兼容回归：`verification/playwright/a29-entry-save-validation-clean-20260624092405.png`

## 代码说明

- 新增 `pendingZeroEntrySave` 状态和零值确认弹窗。
- `zeroEntryWarnings` 在 A29 清理空白行、校验重复行之后运行，只针对有效分录提示。
- 取消确认不保存，确认保存后继续调用原销售订单/通用单据保存路径。
- `saveSuccessMessage` 保持 A29 原文案兼容，并在确认零值时追加确认行数。

## 验证命令

```bash
node scripts/a30-entry-zero-value-confirm-regression.mjs
node scripts/a29-entry-save-validation-regression.mjs

cd "/Users/linzhenyue/Projects/JDY 推理补完/frontend"
npm run build

cd "/Users/linzhenyue/Projects/JDY 推理补完/backend"
export JAVA_HOME="/opt/homebrew/opt/openjdk@21/libexec/openjdk.jdk/Contents/Home"
export PATH="/opt/homebrew/opt/openjdk@21/bin:$PATH"
./mvnw test
```

## 护栏

- 未重跑 `build-jdy-feature-approval.mjs`。
- 未手改任何生成物：`docs/01/02/03`、`config/approved-feature-scope.json`、`app/feature-data.js`、`frontend/src/app/featureScope.ts`。
- 本批未调整范围例外，未改 `config/implementation-overrides.json`。
