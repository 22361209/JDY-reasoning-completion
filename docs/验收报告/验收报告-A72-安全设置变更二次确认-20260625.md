# 验收报告：A72 安全设置变更二次确认

- 日期：2026-06-25
- 批次：A72 安全设置变更二次确认
- 范围：系统设置/安全设置、登录会话、系统设置/操作日志
- 结论：通过

## 覆盖内容

- 安全设置保存新增当前管理员密码校验。
- 错误密码返回 400 `当前密码不正确`，不触发全局 401 登出。
- 错误密码不写入重复登录策略。
- 正确密码才允许写入安全设置，并保留 `UPDATE_SECURITY_SETTING` 审计日志。
- A69/A70/A71 回归脚本已补填当前管理员密码，旧安全设置保存链保持可跑。

## 验收结果

| 项目 | 结果 | 证据 |
| --- | --- | --- |
| 错误密码返回 400 | 通过 | `verification/a72-security-settings-password-confirm-regression.json` |
| 错误密码不写设置 | 通过，策略保持 `SINGLE_ACTIVE` | `verification/a72-security-settings-password-confirm-regression.json` |
| 错误密码不误登出 | 通过，页面保留安全设置提示态 | `verification/playwright/a72-security-settings-wrong-password-20260624165129.png` |
| 正确密码保存 | 通过，策略写为 `ALLOW_CONCURRENT` | `verification/playwright/a72-security-settings-correct-password-20260624165129.png` |
| 回归后恢复默认策略 | 通过，恢复 `SINGLE_ACTIVE` | `verification/a72-security-settings-password-confirm-regression.json` |
| 操作日志 | 通过，检出 `UPDATE_SECURITY_SETTING` 成功记录 40 条 | `verification/a72-security-settings-password-confirm-regression.json` |

## 命令记录

```bash
node --check scripts/a72-security-settings-password-confirm-regression.mjs
node --check scripts/a69-security-settings-policy-regression.mjs
node --check scripts/a70-session-timeout-settings-regression.mjs
node --check scripts/a71-password-policy-settings-regression.mjs
npm --prefix frontend run build
JAVA_HOME=/opt/homebrew/opt/openjdk@21 ./mvnw test
node scripts/a69-security-settings-policy-regression.mjs && node scripts/a70-session-timeout-settings-regression.mjs && node scripts/a71-password-policy-settings-regression.mjs && node scripts/a72-security-settings-password-confirm-regression.mjs
```

## 结构化结果

```json
{
  "wrongPasswordStatus": 400,
  "policyAfterWrongPassword": "SINGLE_ACTIVE",
  "correctPasswordStatus": 200,
  "policyAfterCorrectPassword": "ALLOW_CONCURRENT",
  "restoredPolicy": "SINGLE_ACTIVE",
  "settingAuditRows": 40
}
```

## 不扩范围确认

- 未重跑 `build-jdy-feature-approval.mjs`。
- 未手改生成物：`docs/01/02/03`、`config/approved-feature-scope.json`、`app/feature-data.js`、`frontend/src/app/featureScope.ts`。
- 未调整 `config/implementation-overrides.json`。
- 未改库存、资金、单据审核、红冲业务规则。
