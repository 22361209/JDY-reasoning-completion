# 验收报告：A179 业务单据仓库选择器实时账套候选修复

## 结论

A179 已完成。业务单据仓库候选不再复用包含未审核/禁用资料的仓库维护列表，也不再合并静态 CK 仓库集合；销售报价、销售订单、采购订单、采购入库及同类共享分录现在统一查询当前 tenant 的 `warehouse-master-selector`，只返回“已审核且启用”的仓库。

BLD-TEST 的 `400041390 / UAT-20260809-009 / UAT-20260809-009 仓库` 在回归时已存在，状态为“启用 / 已审核”。销售报价与采购订单均能按编码、名称查询到唯一结果并回填分录仓库。最终 review 未发现本批新增 P0–P2。

## 先复现后修复

1. 修复前业务分录直接查询 `warehouse-master-list`，该维护列表会返回草稿、禁用和已审核资料；分录粘贴识别还会把 `knownWarehouseOptions` 静态 CK 集合合并回候选。
2. 先新增 `WarehouseSelectorIntegrationTest`，首次运行稳定失败：`/api/lists/warehouse-master-selector` 返回 `404 Unknown list key`。
3. 新增独立 `warehouse-master-selector` 契约；仓库维护列表继续保留所有生命周期状态，业务候选 SQL 追加 `enabled = TRUE AND audit_status = 'AUDITED'`。
4. 前端行内联想、整列表弹窗、粘贴匹配、物料默认仓库、销售出库和委外分录统一改用候选接口，并删除静态 `knownWarehouseOptions` 死代码。CK-001–CK-008 数据库默认主数据未删除、未改名、未替换。
5. 候选接口未写死 schema 或账套；API 请求由 `TenantContextInterceptor` 建立当前账套上下文，`TenantRoutingDataSource` 按当前 tenant 的 database/schema 获取连接。

## 自动验收

- 后端针对性门禁：`WarehouseSelectorIntegrationTest`、`ListFailClosedIntegrationTest`、`ListQueryContractRegistryTest` 合计 `13/13` 通过。
- 生命周期集成测试证明：审核前不可见；审核后下一次查询立即可见；禁用后立即不可见；按编码、名称均返回唯一候选；仓库维护列表仍能看到已审核、草稿和禁用三类资料。
- 前端：`npm run build` 通过；仅保留既有 Vite 大 chunk 警告，无编译错误。
- A179 静态回归与 manifest 校验通过；A179 已登记到 smoke、sales、purchase、inventory、master-data 和 full 清单。
- `area:sales`：`23/23` 通过，`0` 失败、`0` 跳过，postflight 通过。
- `area:purchase`：`24/24` 通过，`0` 失败、`0` 跳过，postflight 通过。
- full 使用 `0/1` 次预算；销售和采购正式 area 已覆盖本批共享路径与交叉链，本任务未重复运行 full。

## BLD-TEST 可见页面回归

1. 仓库资料页面确认 `UAT-20260809-009` 的系统编号为 `400041390`，仓库类型“普通仓”，状态“启用”，审核状态“已审核”。
2. 销售报价仓库整列表选择器按编码 `UAT-20260809-009` 查询显示“共 1 条”；按名称 `UAT-20260809-009 仓库` 查询同样返回该仓库，点击后分录仓库回填 `UAT-20260809-009`。
3. 采购订单仓库整列表选择器按同一编码查询显示“共 1 条”；按同一名称查询同样返回该仓库，点击后分录仓库回填 `UAT-20260809-009`。
4. 销售报价、销售订单、采购订单、采购入库四个表单均通过各自薄封装调用同一 `useDocumentModule`；因此销售订单与采购入库共享本次已验收的候选查询、弹窗和回填实现，而非另有 CK-001 白名单。

## Review、预算与交付

- 实际墙钟耗时约 45 分钟；累计子代理 `0`，并发子代理 `0/0`，full `0/1`。
- A179 使用 3 个提交额度：任务包、实现/回归、验收归档，低于 `10` 个提交上限；Goal token 未触发 70% 停止阈值。
- 变更范围仅包含仓库候选契约、共享前端查询入口、自动回归、manifest 与本任务治理文档；未修改库存过账、成本、来源下推、单据生命周期、迁移或生产环境。
- 实现提交：`41723c09 fix(a179): use live audited warehouse selectors`。本地服务验收后仍保持 frontend/backend ready，PostgreSQL/Redis healthy。
- 未 push。
