# 验收报告：A131 可复现测试与 Flyway 基线

## 基本信息

- 验收日期：2026-07-12
- 基线提交：`40c4f797132f3b71000a524f5b3a7b1d60b1bcf7`（`40c4f79 建立 A130 项目防偏移治理基线`）
- 当前分支：`codex/review-cleanup-20260627`
- 前序变更集：A130 独立治理提交 `40c4f797132f3b71000a524f5b3a7b1d60b1bcf7`
- 改动分类：共享工程核心 / 测试夹具与 Flyway 历史收敛
- 目标等级：A4（本批可复现基线）
- 业务范围：无 scope ID 变更；未修改正式业务代码、页面、业务迁移或业务规则
- 测试环境：Java 21、Spring Boot 3.5.15、Flyway 11.7.2、PostgreSQL 16.14、Redis 7，本地前端 `5173`、后端 `8080`

## 结论

A131 达到本批 A4 验收标准。V90/V91 从未进入 Git，不能作为低版本迁移恢复到已经发布 V94/V95 的共享 source，临时恢复文件已从 worktree 撤回。共享 source 保持 93 个 migration、最高版本 V95，V94/V95 是生产车间清理的唯一正式实现。

当前开发库中多出的 V90/V91 属于本地历史漂移。处理前已完成可读取备份，并先在备份恢复副本验证 Flyway 11.7.2 `repair`；随后使用相同命令处理当前库。原 V90/V91 SQL history 保留，installed_rank 96/97 新增对应 `DELETE` 记录，没有手工编辑 `flyway_schema_history`。

修复后的当前库、由已发布 `4037847` 创建且只有 93 条正式 history 的 V95 数据库、全新空库三种拓扑均完成 `clean test` 48/48。两个临时测试库和 repair trial 均已删除，数据库名复核为 0 条。

本批没有放松自动编号、主数据引用或生产任务生命周期规则。旧测试夹具改为提交空单号、读取系统实际生成单号，并将该单号传给审核、下推、查询、打印和导出断言；旧“生产部”引用改用已审核、启用的 `CY / 冲压车间`。生产链夹具补齐既有“生产任务审核后才能领料”的前置动作，这是服从现行业务规则，不是规则变更。

这不表示整个 ERP 已通过发布门禁。smoke 仍为 4 通过、6 失败，失败集合与 A130 完全一致，没有新增失败。

## 数据保护与迁移对账

### 1. 变更前备份

- 文件：`verification/backups/a131-before-flyway-reconcile-20260712-064540.dump`
- 格式：PostgreSQL custom archive，已用 `pg_restore -l` 验证可读取
- TOC：1514 项
- 大小：17,984,763 bytes
- SHA-256：`c55a6f3c7c7f647b16ceff16c5c3723cfd0cc3926b306fcb59c6ab8a4c780317`

备份位于 ignored 运行产物目录；路径、格式、TOC、大小和完整校验和固化在本报告中。

### 2. V90/V91 最终决策

`git log --all` 证明 V90/V91 从未进入版本控制；已发布的 `4037847` 直接包含 V94/V95。把 V90/V91 加回共享 source，会让已经到 V95、但没有 90/91 history 的数据库出现低版本已解析未应用迁移，因此该方案被撤回。

对账结果保留如下，用于证明本地 V90/V91 是正式 V94/V95 的重复历史，而不是需要重新发布的独立业务变更：

| 本地旧历史 / 正式迁移 | SQL SHA-256 | Flyway checksum | 对照结果 |
| --- | --- | ---: | --- |
| V90 / V94 | `efd5bb30405e387d52309abdbf57e93f5f567584b80cdc321aa2ffcefe5b28b5` | `1941773716` | 逐字节一致 |
| V91 / V95 | `65b8e0ec40038da1886b118e4381fc31f66da585b3fc0fa7cf248d01bbc3a494` | `-469635984` | 逐字节一致 |

最终共享 source 状态：

- migration 文件总数：93；
- 最高版本：V95；
- V90/V91：不存在；
- V94/V95：内容未修改，是唯一正式实现。

### 3. Flyway repair 审计

repair 先在 `jdy_erp_a131_repair_trial` 验证。该库由变更前备份完整恢复；使用 `4037847` 的 93-migration source 执行 Flyway 11.7.2 `repair` 后，Spring context 通过。验证成功后，才对当前 `jdy_erp` 执行相同 repair。

当前库最终保留以下审计轨迹：

| installed_rank | version | type | script | checksum | success |
| ---: | ---: | --- | --- | ---: | --- |
| 90 | 90 | SQL | `V90__retire_generic_production_department.sql` | `1941773716` | true |
| 91 | 91 | SQL | `V91__enforce_factory_workshops_across_tenants.sql` | `-469635984` | true |
| 94 | 94 | SQL | `V94__retire_generic_production_department.sql` | `1941773716` | true |
| 95 | 95 | SQL | `V95__enforce_factory_workshops_across_tenants.sql` | `-469635984` | true |
| 96 | 90 | DELETE | `V90__retire_generic_production_department.sql` | `1941773716` | true |
| 97 | 91 | DELETE | `V91__enforce_factory_workshops_across_tenants.sql` | `-469635984` | true |

原 SQL history 没有被改写或删除；installed_rank 96/97 是 Flyway 官方 repair 追加的删除标记。没有直接执行针对 `flyway_schema_history` 的 `INSERT`、`UPDATE` 或 `DELETE`。

### 4. 三拓扑可复现验证

| 拓扑 | 初始状态 | 当前 source 验证 | 后端测试 | 清理 |
| --- | --- | --- | --- | --- |
| 修复后当前库 `jdy_erp` | 原 V90/V91 SQL history + repair DELETE 标记；正式版本到 V95 | 93 个 source 文件；Flyway 计入 repair 审计后报告 95 migrations validated | 48/48，0 failure、0 error、0 skipped | 保留 |
| 已发布 V95 库 `jdy_erp_a131_upgrade` | 用 `4037847` 创建，93 条 success history，无 90/91 | 93 条 success history，无 90/91 | 48/48，0 failure、0 error、0 skipped | 已删除 |
| 全新空库 `jdy_erp_a131_clean2` | 无 history | 从 0 迁移出 93 条 success history，无 90/91 | 48/48，0 failure、0 error、0 skipped | 已删除 |

repair trial `jdy_erp_a131_repair_trial` 也已删除。三个验收数据库名在 `pg_database` 中复核为 0 条。

### 5. 精确复现命令

以下命令从项目根目录执行。`jdy_dev` 是本项目提交在本地开发配置中的默认数据库密码；命令未包含其他凭据。

先定义本次固定变量并验证备份：

```bash
BASE=40378478e48f5012777363002f11093b0a3bc87b
WORKTREE=/tmp/jdy-a131-upgrade-base-20260712
UPGRADE_DB=jdy_erp_a131_upgrade
CLEAN_DB=jdy_erp_a131_clean2
REPAIR_DB=jdy_erp_a131_repair_trial
BACKUP=verification/backups/a131-before-flyway-reconcile-20260712-064540.dump

stat -f '%z' "$BACKUP"
shasum -a 256 "$BACKUP"
docker exec -i jdy-erp-postgres pg_restore -l < "$BACKUP"
```

建立 `4037847` worktree，并由已发布 source 的 `JdyErpApplicationTests` 创建 93 条正式 V95 history：

```bash
git worktree add --detach "$WORKTREE" "$BASE"
docker exec jdy-erp-postgres createdb -U jdy "$UPGRADE_DB"

(
  cd "$WORKTREE"
  JDY_DB_URL="jdbc:postgresql://localhost:5432/$UPGRADE_DB" \
    ./scripts/backend-test.sh clean test -Dtest=JdyErpApplicationTests
)
```

先在备份恢复副本验证 Flyway 11.7.2 repair：

```bash
docker exec jdy-erp-postgres createdb -U jdy "$REPAIR_DB"
docker exec -i jdy-erp-postgres pg_restore \
  -U jdy -d "$REPAIR_DB" --no-owner --no-privileges < "$BACKUP"

(
  cd "$WORKTREE"
  ./scripts/backend-test.sh \
    org.flywaydb:flyway-maven-plugin:11.7.2:repair \
    -Dflyway.url="jdbc:postgresql://localhost:5432/$REPAIR_DB" \
    -Dflyway.user=jdy \
    -Dflyway.password=jdy_dev \
    -Dflyway.locations="filesystem:$WORKTREE/backend/src/main/resources/db/migration"

  JDY_DB_URL="jdbc:postgresql://localhost:5432/$REPAIR_DB" \
    ./scripts/backend-test.sh test -Dtest=JdyErpApplicationTests
)
```

trial 通过后，对当前开发库执行同一官方 repair，并完成三拓扑 full clean test：

```bash
./scripts/backend-test.sh \
  org.flywaydb:flyway-maven-plugin:11.7.2:repair \
  -Dflyway.url=jdbc:postgresql://localhost:5432/jdy_erp \
  -Dflyway.user=jdy \
  -Dflyway.password=jdy_dev \
  -Dflyway.locations="filesystem:$PWD/backend/src/main/resources/db/migration"

./scripts/backend-test.sh clean test

JDY_DB_URL="jdbc:postgresql://localhost:5432/$UPGRADE_DB" \
  ./scripts/backend-test.sh clean test

docker exec jdy-erp-postgres createdb -U jdy "$CLEAN_DB"
JDY_DB_URL="jdbc:postgresql://localhost:5432/$CLEAN_DB" \
  ./scripts/backend-test.sh clean test
```

核对两种临时测试拓扑均为 93 条成功 history、没有 V90/V91，再删除全部临时资源：

```bash
for db in "$UPGRADE_DB" "$CLEAN_DB"; do
  docker exec jdy-erp-postgres psql -U jdy -d "$db" -Atc \
    "SELECT current_database(), count(*), min(version::int), max(version::int),
            bool_and(success),
            count(*) FILTER (WHERE version = '90'),
            count(*) FILTER (WHERE version = '91')
     FROM flyway_schema_history;"
done

docker exec jdy-erp-postgres dropdb -U jdy "$UPGRADE_DB"
docker exec jdy-erp-postgres dropdb -U jdy "$CLEAN_DB"
docker exec jdy-erp-postgres dropdb -U jdy "$REPAIR_DB"

docker exec jdy-erp-postgres psql -U jdy -d postgres -Atc \
  "SELECT count(*) FROM pg_database
   WHERE datname IN ('$UPGRADE_DB', '$CLEAN_DB', '$REPAIR_DB');"

git worktree remove "$WORKTREE"
```

最后一条数据库复核结果为 `0`。

## 测试夹具修复

### 自动编号

修改以下五个测试文件：

- `CoreBusinessFastIntegrationTest`
- `TenantSalesChainIsolationTest`
- `TenantPurchaseChainIsolationTest`
- `TenantProductionChainIsolationTest`
- `TenantReportingIsolationTest`

统一处理方式：创建请求传 `null` 单号，立即断言服务返回的 `billNo` 非空，再沿完整链路复用实际单号。没有删除碰撞、手工单号禁用、租户隔离、库存、来源单、打印或导出断言。

生产链额外补齐 `auditTask(...)`，使领料夹具满足正式“任务已审核”前置条件；正式 AppService 和状态校验未修改。

定向编号/隔离测试结果：19/19 通过，0 failure、0 error。

### 规范车间

`MasterDataReferenceIntegrationTest` 的默认车间从已退役的“生产部”改为编码 `CY`，并断言解析名称“冲压车间”。Controller 的“引用必须存在、已审核且启用”校验保持不变。

定向主数据测试结果：2/2 通过，0 failure、0 error。

## 自动化结果

| 门禁 | 结果 | 证据/说明 |
| --- | --- | --- |
| `clean test`（修复后当前库） | 通过 | 93 个 source 文件；Flyway 计入 repair 审计后报告 95 migrations validated；48/48 |
| `clean test`（已发布 V95 升级库） | 通过 | 93 success history、无 90/91；48/48 |
| `clean test`（全新空库） | 通过 | 从 0 生成 93 success history、无 90/91；48/48 |
| Flyway repair trial | 通过 | 备份恢复副本先验证；Spring context 通过；试验库已删除 |
| A119 full isolation | 通过 | 8/8 suites，`verification/a119-full-isolation-regression.json` |
| effective scope contract | 通过 | 104 项，override=`F063` |
| delivery status scan | 通过 | 覆盖 93 个 non-exclude 功能 |
| regression manifest scan | 通过 | 130=109 full+21 exemption，smoke=10 |
| A124 lifecycle scan | 通过 | 16 个登记单据类型，0 failure/warning |
| A128 list scan | 通过 | `A128 list query contract scan passed` |
| smoke | 基线一致 | 10 项跑满，4 通过、6 失败、0 skipped；无新增失败 |
| `git diff --check` | 通过 | 无空白错误 |

## smoke 基线对照

| 脚本 | A130 | A131 | 当前失败原因 |
| --- | --- | --- | --- |
| A59 formal login | 通过 | 通过 | — |
| A90 numbering | 通过 | 通过 | — |
| A85 finance posting | 失败 | 失败 | 红冲未产生旧脚本期待的负应收 |
| A86 other stock in | 失败 | 失败 | 前端等待 30 秒超时 |
| A87 other stock out | 失败 | 失败 | 前端等待 30 秒超时 |
| A93 sales-out source selection | 失败 | 失败 | 仓库不存在、未审核或已禁用 |
| A94 tax amount | 通过 | 通过 | — |
| A105 bill lifecycle | 通过 | 通过 | — |
| A108 delivery notice reservation | 失败 | 失败 | 回归脚本仍手工指定单号，返回 409 |
| A110 table display brand | 失败 | 失败 | 回归脚本仍手工指定单号，返回 409 |

A131 只修 Java 后端测试夹具，不顺手修改 JavaScript 回归脚本，因此上述旧失败被如实保留。运行结果：`verification/regression-tier-smoke-latest.json`。

## 文件边界审计

- 正式业务代码：零 diff。
- 前端代码：零 A131 diff。
- 正式 migration：零 diff；V94/V95 未修改。
- V90/V91：从未进入 Git，临时恢复文件已从 worktree 撤回，不纳入 A131。
- Flyway history：只通过 Flyway 11.7.2 官方 `repair` 追加 DELETE 审计记录，零人工编辑。
- A131 代码改动只包含 6 个 Java 测试文件；另包含当前任务清单和本验收报告。
- A130 的治理、CI、配置和脚本已由 `40c4f79` 独立提交，不计入 A131；A130 任务包归档随进入 A131 的状态迁移纳入本提交。

## 剩余风险与下一步

1. 整个 ERP 仍未达到发布门禁：smoke 6 项失败，A130 full 基线仍有 71 项失败。
2. 下一批应单独处理回归脚本夹具：先收敛手工单号和仓库状态，再分别诊断 UI 超时与财务红冲断言。
3. F021、F031、发货通知范围归属仍待用户确认，本批未触碰。
4. 权限 fail-open、库存直调入口、tenant 外键、主数据稀疏更新等高风险问题继续保留，不能与回归夹具批次混做。
5. `repair` 只收敛当前本地数据库独有的历史漂移；共享 source 和其他数据库不需要、也不应补 V90/V91。

## 验收等级与提交状态

- A131 可复现测试与 Flyway 基线：A4。
- 整个 ERP 发布状态：未通过。
- 本报告生成时改动尚未提交；最终由本 A131 提交独立收口。A130/A131 均尚未推送，本报告不把本地验收伪装成远端 CI 结果。
