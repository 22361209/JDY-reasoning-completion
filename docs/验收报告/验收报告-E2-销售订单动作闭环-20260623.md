# 验收报告：E2 销售订单动作闭环

- 日期：2026-06-23
- 范围：销售订单保存、审核、删除、引出、打印
- 结论：通过 A1 单据动作验收；打印/引出当前返回结构化数据，尚未做正式模板文件

## 本次完成

- 后端新增销售订单动作接口：
  - `POST /api/sales-orders/{billNo}/audit`
  - `DELETE /api/sales-orders/{billNo}`
  - `GET /api/sales-orders/{billNo}/export`
  - `GET /api/sales-orders/{billNo}/print`
- 前端销售订单表单按钮接入真实接口：
  - 保存
  - 审核
  - 删除
  - 引出
  - 打印

## 验收证据

- `frontend npm run build` 通过。
- `backend ./mvnw test` 通过。
- API 验证 `XSDD-ACT-234624`：
  - 保存草稿：HTTP `201`
  - 审核：HTTP `200`，状态 `AUDITED`
  - 引出：HTTP `200`，返回单头和分录
  - 打印：HTTP `200`，返回单头和分录
  - 删除：HTTP `200`
- Playwright 验证页面按钮链路 `XSDD-UI-595938`：
  - 保存后出现 `草稿已保存`
  - 审核后出现 `审核成功`
  - 引出后出现 `引出数据已生成`
  - 打印后出现 `打印数据已生成`
  - 删除后出现 `删除成功`
- 截图：`verification/playwright/e-sales-order-actions-1366.png`

## 已知差距

- 引出还不是 Excel/CSV 文件下载。
- 打印还不是正式打印模板。
- 删除当前直接删除销售订单草稿/测试单据，后续要按审核状态和权限收紧。
