# 验收报告 A33：下游执行单据影响链提示

验收日期：2026-06-24
回归批次：`20260624095024`
结论：PASS

## 验收范围

本批在源订单分录的下游执行单据弹窗中补充反审核/红冲影响链，避免员工只看到“已有下游单据”，却不知道操作后会回退源单执行数量、冲销库存流水并重算状态。

## 断言结果

| 项目 | 结果 |
| --- | --- |
| 销售订单第 3 行返回下游销售出库单影响字段 | PASS |
| 销售链包含 `反审核将冲销销售出库库存流水` | PASS |
| 销售链包含 `红冲将生成负数销售出库单` | PASS |
| 采购订单第 3 行返回下游采购入库单影响字段 | PASS |
| 采购链包含 `反审核将冲销采购入库库存流水` | PASS |
| 采购链包含 `红冲将生成负数采购入库单` | PASS |
| 前端弹窗显示统一 `影响提示` | PASS |
| 点击下游单据编号可打开销售出库/采购入库详情 | PASS |

## 自动化证据

- 主脚本：`scripts/a33-downstream-impact-chain-regression.mjs`
- 结构化结果：`verification/a33-downstream-impact-chain-regression.json`
- 销售订单：`XSDD-A33-20260624095024`
- 销售出库：`XSCK-A33-20260624095024`
- 采购订单：`CGDD-A33-20260624095024`
- 采购入库：`CGRK-A33-20260624095024`
- 截图：`verification/playwright/a33-sales-downstream-impact-20260624095024.png`
- 截图：`verification/playwright/a33-purchase-downstream-impact-20260624095024.png`

## 代码说明

- `SalesOrderController` 的下游销售出库引用增加反审核/红冲影响文案。
- `PurchaseOrderController` 的下游采购入库引用增加反审核/红冲影响文案。
- 下游执行弹窗增加影响提示、反审核影响列和红冲影响列。
- 前端对历史数据保留兜底文案，不要求旧接口一次性补齐所有字段。

## 验证命令

```bash
node scripts/a33-downstream-impact-chain-regression.mjs

cd "/Users/linzhenyue/Projects/JDY 推理补完/backend"
export JAVA_HOME="/opt/homebrew/opt/openjdk@21/libexec/openjdk.jdk/Contents/Home"
export PATH="/opt/homebrew/opt/openjdk@21/bin:$PATH"
./mvnw test

cd "/Users/linzhenyue/Projects/JDY 推理补完/frontend"
npm run build
```

## 护栏

- 未重跑 `build-jdy-feature-approval.mjs`。
- 未手改任何生成物：`docs/01/02/03`、`config/approved-feature-scope.json`、`app/feature-data.js`、`frontend/src/app/featureScope.ts`。
- 本批未调整范围例外，未改 `config/implementation-overrides.json`。
