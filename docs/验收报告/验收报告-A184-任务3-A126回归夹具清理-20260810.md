# 验收报告：A184 任务 3 A126 回归夹具清理

## 结论

A126 新运行的夹具清理债务已经关闭；历史 77 批的物理清理没有被包装成已完成。

- A126 现在使用 run-unique ADMIN/WAREHOUSE 身份、随机 run ID 和唯一 marker，所有写入均纳入 intent、所有权、来源闭包、锁、CAS、逐表删除计数和提交前零残留校验。
- 主流程成功或失败都会先关闭并排空两条 request fence；只有两条 fence 都为 `CLOSED/0` 才允许清理业务事实。业务清理完成后再删除隔离身份，主错误、清理错误和证据持久化错误分别聚合。
- 修后 A126 定向、A136 定向、会话不变量合同和唯一 `area:sales` 均通过。A126 成功批次清理了 1 报价、12 订单、16 发货通知、8 销售出库、6 应收、25 库存流水和 46 操作日志，并完整恢复库存余额与非本批流水摘要。
- 实现过程中一次失败运行 `6dcb3350-338d-4e08-8313-ece928ea1356` 留下的精确残留，已通过不可变 dry receipt、输入 SHA、snapshot digest、sorted allowlist 和 recovery digest 五重绑定恢复；最终业务残留、外部引用、两名隔离用户和运行时访问均为 0。
- 历史 dry-run 识别 77 批旧 A126 数据：76 批满足现有所有权/来源闭包，1 批 `20260714063721` 缺少可审计的 reserve/out posting facts。即使只处理 76 批，物理删除也会令保留的不可变 `qty_on_hand_after` 与余额分离；因此工具对 `--apply` 永久 fail closed，本批没有删除历史行或改写历史余额。
- 当前任务 3 diff 的最终只读 review 为 P0=0、P1=0、P2=0、P3=0。历史数据下一步必须另立产品决策：新增 append-only 三量恢复日志，或显式授权并归档“历史重述迁移”；不能在 A184 中顺手绕过。

## 前向夹具修复

### 身份、所有权和在途请求

- `createIsolatedRoleSessionFixture()` 泛化了既有隔离管理员夹具，A126 分别创建 ADMIN 与 WAREHOUSE；旧管理员调用保持兼容。
- A126 只允许由 tier runner 启用受管理 request fence 后执行。每个请求都归属于本次 run，cleanup 首先关闭、排空两条 fence；任一 fence 异常、非 `CLOSED` 或 active count 非 0 时，业务删除会明确跳过并 fail closed。
- 所有单据、分录、seed 流水、日志和测试身份使用 UUID run ID 与精确 marker。请求发出前即持久化 intent，响应丢失时可按 marker、创建时间、actor 与来源拓扑找回；冻结后只按 exact UUID allowlist 删除。

### 库存与业务事实恢复

- 首个写入前捕获 `CP-001 / CK-001 / BLD-TEST` 的完整 `inv_stock_balance` 行、完整 route 流水 ID/行摘要和版本，而不是只记录三项数量。
- snapshot 从 run-owned source/header/line/bill number 全局发现库存事实，再显式拒绝跑出受控 product/warehouse/account-set 的流水；错路事实不能因先过滤受控路线而隐藏。
- 清理事务在首个快照前锁住所有 owned、external、库存和日志关系，再取 advisory lock 与余额行锁；锁内重算 canonical JSON digest，只有与冻结 snapshot 完全一致才继续。
- 外部闭包覆盖报价下推订单行、订单/通知/出库头行来源、红字出库、销售退货、三类应收消费者、outbox、notification 和编辑锁。每类 DELETE 都校验精确 row count，所有头、行、应收、流水、日志和锁在 COMMIT 前复验为 0。
- 余额按完整 baseline 行恢复；随后要求完整非本批流水摘要、余额和零残留全部等于基线，否则整笔回滚。单号序列保持单调，不做回退。

### 证据持久化与故障路径

- evidence 在 baseline、pending intent、响应/找回、cleanup snapshot 和结果阶段原子写入；最终写失败不会遮蔽主错误或清理错误。
- 已用受控注入覆盖 first seed、报价服务端提交后响应丢失、通知审核、出库审核、红字审核和浏览器前失败点，证明清理转换和零残留路径可执行。
- 最终 managed-fence 版本通过成功定向和完整销售 area；独立只读 review 复核 fence gating、全局来源发现、digest/CAS、删除闭包和证据错误聚合，无未关闭 P0–P3。

## 失败运行的精确恢复

失败批次 `6dcb3350-338d-4e08-8313-ece928ea1356` 不是按前缀或估算数量清理。

- dry receipt：`verification/a184-a126-current-run-recovery-20260810020311315-6dcb3350-dry-run-42f1ba18.json`
- dry receipt SHA-256：`bbc89bac96cf8a57e056d1c1878b806a80e9d545f5277a2d789017305ad87f84`
- snapshot digest：`05b53c239340782eb02a64d6ddeb0c94a3cabaebe531188efd760473c582b4dc`
- recovery digest：`127778585cf4bb704405ba9ebb29c9ffefc68f90e40bcfa0ed93a562729c25eb`
- apply receipt：`verification/a184-a126-current-run-recovery-20260810020331045-6dcb3350-apply-f27551e9.json`
- apply receipt SHA-256：`c355fd8495a5676d912cdce408f377b216c4fbc0fb11767a2c6b9810ee94ba37`
- disposition：`APPLIED_AND_VERIFIED`

恢复结果：

| 项 | 精确结果 |
| --- | --- |
| 删除 run-owned 事实 | 报价 1、订单 12、通知 16、出库 8、应收 6、库存流水 25、操作日志 0、编辑锁 0 |
| 隔离身份 | ADMIN、WAREHOUSE 均关闭运行时访问并删除，最终状态均为 `ABSENT` |
| 库存余额 | on-hand `13,115,997`、reserved `38,019`、available `13,077,978`、version `41,843` |
| 非本批库存流水 | count `45,610`；ID digest `76089fe97d61c219c33935321146c20d9ce23022c9304c60a49d20fad554bf56`；row digest `667690644e250bb9c3a7eea020abe8c340992906ced15bd2d63f6081a9bd1745` |
| 最终闭包 | run facts 0、12 类 external 0、两身份 0、zeroRunResidue=true |

## 历史 dry-run 与明确阻断

- artifact：`verification/a184-a126-historical-recovery-20260810010923.json`
- SHA-256：`892f82fc3f69fd445795eca367b7d9da0253175250884496bac31d2463a3bbc4`
- manifest digest：`490b39038d2f29f5e1d408b3ae94c6575d3ff8f3461cd73ef5b591e5ab85740b`
- 现场识别 77 批；76 批满足 EXACT 或可由旧迁移合同证明的 HEADER_ONLY 来源闭包，1 批 `20260714063721` 因 11 张通知与 6 张出库缺少 canonical posting facts 被阻断并原样保留。
- 76 批候选包含报价 74、订单 839、通知 1,120、出库 576、应收 432、库存流水 1,768；可证明当前三量影响为 on-hand `+72,112`、reserved `+20,950`、available `+51,162`。
- 不能据此物理删除：最早候选之后存在大量正式保留流水，且其 `qty_on_hand_after` 是不可改写的过账事实。删除候选而只改余额会破坏流水尾值；批量改写保留事实又违反 append-only 合同和 A184 范围。
- 工具只支持 dry-run/manifest verify；dry-run manifest 将状态标记为 `BLOCKED_APPEND_ONLY_CONTRACT`，带 manifest 的 `--apply` 会硬失败并抛出阻断错误。本轮历史删除数为 0，历史余额改写数为 0。

## 门禁

- A126 定向：1/1，通过；run ID `187a27c06c2547418a17994913210046`，artifact SHA-256 `de6f382e79b68ff66d6d2bcdd7826e253d996e42c4d318c14474f80d0d51a621`。
- 成功批次：`9bdad3e8-9c53-4a66-8588-c225d69e0320`，135 条业务断言、3 张截图；清理精确删除 1/12/16/8/6/25/46，库存与流水摘要恢复，两个隔离身份删除。
- A136 定向：1/1，通过；run ID `886b46fbb4c8427c9345501afedbb50b`，artifact SHA-256 `dcce20c58a7f11c4e6041dea3128660d754ac3197be15c3850c70cbdce8d48de`。
- 会话不变量合同：通过；主会话 logical expiry、重复 primary key 排除和完整 snapshot expiry retry 均成立。
- `area:sales`：24/24，通过，0 失败、0 跳过；run ID `d9d47c2d32844ec7ab4784bb39a3ca37`，时间 2026-08-10 10:14:26–10:31:32 CST，artifact SHA-256 `6d0ad254b006b5700ccf56628a5290bded732d432b3cfb59266e852f74063925`。
- 本任务没有运行 full；A184 的唯一 full 配额保留到四项集成后。

## Review、范围与后续

- 最终两条独立只读审查均确认当前运行清理、精确恢复工具、identity 幂等续跑、receipt 链和数据库事务边界无 P0–P3。
- 未修改销售、库存、应收等产品业务代码和正式 migration；未操作生产环境，未 push。
- 失败运行恢复属于本任务精确 run-owned 数据的收尾；历史 77 批只读取证，没有执行 apply。
- 后续独立任务必须在“append-only 三量恢复日志”与“显式历史重述迁移”之间做产品决策；在该决策前，历史物理清理保持阻断是正确状态。
