# 验收报告 A3：七方向业务闭环补完

- 日期：2026-06-24
- 范围：部分出入库、反审核/作废/冲销、打印引出、应收应付、生产、权限日志、高级交互
- 结论：通过。本批达到本机可验收的 A2+ 闭环；下游反审核校验、资金账户流水和生产分批执行后置。

## 实现结果

1. 部分出入库：订单行新增 `received_qty / shipped_qty`，入库/出库审核累计数量，列表显示 `部分入库 / 部分出库`。
2. 反审核、红冲与作废：采购入库、销售出库支持反审核、显式红字单据红冲和草稿作废；反审核/红冲写反向库存流水并回算订单状态。
3. 打印引出：新增通用 CSV 引出和 HTML 打印页接口，覆盖销售订单、采购订单、采购入库、销售出库。
4. 应收应付：销售订单生成应收、收款核销；采购订单生成应付、付款核销；列表读取真实状态。
5. 生产：BOM、生产任务、领料、完工入库最小闭环可跑通，库存随动作变化。
6. 权限日志：初始化角色、权限码和 admin 绑定；关键动作写操作日志，并提供角色/日志列表。
7. 高级交互：选择器支持上下键、回车选中、ESC 收起；出入库表单暴露反审核/作废按钮；新列表补齐列定义。

## 验收证据

- 后端：`cd backend && ./mvnw test` 通过，Flyway 校验 6 个迁移，schema version `6`。
- 前端：`cd frontend && npm run build` 通过。
- API：A3 验证脚本通过，批次号 `792848`：
  - `CGDD-A3-792848`：采购部分入库、全部入库、反审核后回到部分入库。
  - `CGRK-A3V-792848`：采购入库草稿作废后列表显示 `已作废`。
  - `XSDD-A3-792848`：销售部分出库、全部出库、反审核后回到部分出库。
  - `XSCK-A3V-792848`：销售出库草稿作废后列表显示 `已作废`。
  - `YS-XSDD-A3-792848`：应收单部分核销后完成核销。
  - `YF-CGDD-A3-792848`：应付单付款后完成核销。
  - `SCTASK-A3-792848`：生产领料、完工后任务显示 `已完工`。
  - 操作日志覆盖 `AUDIT / REVERSE / VOID / CREATE_AR / PAY / COMPLETE`。
- API 红冲专项通过，批次号 `230205`：
  - `HC-CGRK-HCA-230205`：采购红字单生成，金额 `-288.00`，源采购订单回到 `部分入库`。
  - `HC-XSCK-HCA-230205`：销售红字单生成，金额 `-258.00`，源销售订单回到 `部分出库`。
  - 操作日志覆盖 `RED_REVERSE`。
- Playwright：
  - `verification/playwright/a3-selector-keyboard-1366.png`
  - `verification/playwright/a3-receivable-list-clean-1366.png`
  - `verification/playwright/a3-production-task-list-1366.png`
  - `verification/playwright/a3-red-reverse-button-1366.png`

## 剩余风险

- 反审核还未完整检查所有可能下游单据。
- 财务未接资金账户流水，当前只做应收/应付核销状态。
- 生产未做分批领料、分批完工和在制品成本。
- 新 Controller 里仍有较多 SQL 和业务规则，后续应下沉到 application/domain。
