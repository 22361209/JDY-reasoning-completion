# 验收报告：A133 G0 生产合同阻塞修复

## 基本信息

- 验收日期：2026-07-12
- 基线提交：`df5c7cea41b3540eb0930ebb49cb9f72caec28c9`（A132 阻塞证据）
- 当前分支：`codex/review-cleanup-20260627`
- 路线坐标：`G0-regression-baseline-before-2 / A133`
- 目标等级：A4
- 变更分类：受控生产合同、回归夹具、测试账套 preset 定向恢复与验收证据
- 范围变更：无 scope ID、migration、生成范围配置或业务范围变更

## 结论

**A133 达到 A4，G0 回归基线门可以关闭。**

- A132 暴露的 17 个目标失败全部转绿；新增纳入的 A50、A51 夹具隔离也稳定通过，因此 A133 的 19 个专项为 `19/19`。
- smoke 为 `10/10`，`skipped=0`。
- full 为 `102/109`，`skipped=0`；7 个失败精确等于本批预先排除的 A32、A38、A57、A63、A101、A116、A118，没有新增失败。
- 后端全测试 `64/64`，前端 production build、范围合同、交付状态、回归 manifest 与 `git diff --check` 全部通过。
- A49/A50/A51 在独立、串行、目标批次和两轮 full 中重复通过；每次恢复调用前完整 preset 快照，最终全表指纹与恢复后基线一致。
- 最终收口中发现的 A93，以及首轮 full 发现的 A102、A129，都是本轮已确认业务合同使旧断言失效；经收紧断言、独立 review 和独立提交后转绿，没有把它们加入允许失败集合。

本结论只关闭 G0，不代表整个 ERP 已完成或 full 已全绿。下一路线项是问题 2“权限 fail-open、生命周期和库存直调接口可绕过”；在另立任务前，本批不实施该项。

## 用户确认的业务合同

- 销售订单草稿每行数量允许为 `0`，但不得小于 `0`。
- 数量为 `0` 的草稿不要求填写原因，也不得写入零数量原因备注。
- 销售订单审核时每行数量必须大于 `0`；零数量审核返回正式业务原因并保持 `DRAFT`。
- 单价为 `0` 的既有提醒、原因选择和备注合同保持不变。
- 允许在完整快照后定向恢复 BLD-TEST 中由 A50/A51 历史失败污染的 operation-log preset；不得重建账套、清空整表或修改无关 preset。

## 完成的生产修复

### 1. 执行列与来源追溯

- 销售订单恢复“已出库数量/未出库数量”，采购订单恢复“已入库数量/未入库数量”，并显式透传执行列开关。
- 发货通知下推销售出库时分别保存原销售订单 `XSDD` 与直接发货通知 `FHTZD` 两层来源。
- 销售出库可见来源与点击追溯指向精确原销售订单；遗留只有 FHTZD 的行仍按发货通知类型打开。
- A93、A102 的旧 FHTZD 可见来源断言同步到 XSDD；A98 继续严格证明持久化的 `XSDD + FHTZD` 双层来源和精确行定位。

### 2. 销售订单数量合同

- 后端草稿保存使用非负校验，审核保留严格正数校验；负数量仍以 `400` 拒绝。
- 前端零值确认只收集单价为 `0` 的原因，数量为 `0` 的行不进入原因弹窗、不写备注。
- A30、A31 统一使用浏览器同一认证会话，避免 `SINGLE_ACTIVE` 下 Node API 重登录把浏览器会话踢掉造成假失败。
- A129 显式验证零数量销售订单草稿返回 `201`、保存后为 `DRAFT`、数量为 `0`、备注为空；审核返回 `400`“销售订单数量必须大于 0”，之后仍为 `DRAFT`。采购订单零数量仍按原合同返回 `400`。

### 3. 红冲领域日志与事务

- 销售出库和采购入库红字审核在库存、来源与财务反向事实全部成功后，于同一事务写入 `RED_REVERSE`。
- 创建红字草稿只写 `CREATE_RED_DRAFT`；红字审核同时保留共享 `AUDIT`；普通、重复或失败审核不产生错误/重复的 `RED_REVERSE`。
- 真实 PostgreSQL 集成测试覆盖销售/采购成功、重复审核和真实财务落库后的晚期异常；异常时单据、库存、来源、财务、流水与 `AUDIT/RED_REVERSE` 一并回滚，仅保留先前独立提交的 `CREATE_RED_DRAFT`。

### 4. 列表密度与滚动

- 正式列表、主数据选择器和源单选择器行高恢复为 `22px`；单据分录保持 `32.5–33px`。
- `.data-list-main` 与直接 TableCore 子元素恢复 flex 填充；稀疏一行表体实测 `348px`，表格、分页和主区底边误差不超过 `2px`。
- 密集场景使用 17 条真实记录，实测 `scrollHeight=375 > clientHeight=348`，保留真实纵向滚动，没有 padding、伪造高度或缩短视口。

### 5. 操作日志筛选回归

- A41/A42/A43 从已删除的旧状态/日期控件迁移到统一日期范围和状态列筛选。
- 三项均显式选择“等于 成功”，并截获列表请求验证 `columnFilters.status={operator:"等于",value:"成功"}`；A43 额外验证导出请求携带同一精确条件。
- API 正反例、操作人/对象类型组合条件和 CSV 内容断言全部保留，未把“等于”放宽为默认“包含”。

## BLD-TEST preset 恢复与隔离

### 恢复动作

- 恢复前完整快照：281 行。
- 在单个事务和精确前置条件下删除 141 条 `A50仓库不可见-*` 残留、2 条已确认的 A51 本人默认残留，并把原系统 preset `b2f41261-b7df-496e-a8e1-3c03569fcaf0` 恢复为 ADMIN、只读、共享、默认。
- 恢复后 138 行；A50/A51 残留均为 0。系统 preset 当前为：`系统默认-红冲审计 | ADMIN | userName 空 | isDefault=true | readOnly=true | shared=true`。
- 未受影响的 137 行逐字段摘要在恢复前后均为 `c0f0e7f40fa689c7a71324fd8e5ab65f`。

### 快照与最终状态

| 证据 | SHA-256 |
| --- | --- |
| `verification/a133-preset-recovery-before.json` | `7a7a9a16ce519a985a9e3056ce931e3e44410d6b772e2fe8a23eb76ff7232b10` |
| `verification/a133-preset-recovery-before.sql` | `6f11d1c89b1b3d202045abdd16682bdb73d08ce0a9ba8be150a9c2ad3642631a` |
| `verification/a133-preset-recovery-after.json` | `45898de5265444ecab8b8c3e41b7852bd6fef4f30f8ebd69f6492d9138dd3baf` |
| `verification/a133-preset-recovery-after.sql` | `61c64841517ee7576e9aac9ea90c1d064820fb58e67d11de3c62e312aeea8e33` |

- 最终 full 后，138 行完整 `to_jsonb` 有序摘要为 `138|275758f62a20a7354e150e1d14454297`，与恢复后及各轮 A49/A50/A51 调用前后相同。
- A50/A51 改为完整 DB 快照、精确作用域删除/重插并保留 ID、时间戳和默认状态；`finally` 清理失败不会覆盖主错误，同时会让脚本最终失败。
- 历史 A51 失败曾在 A133 开始前改写 136 条 A44/A45 preset 的 `updated_at`。因没有可信旧时间戳，本批没有伪造或猜测恢复；恢复后以现存完整快照为基线，后续运行均逐字段无漂移。

## 自动化结果

| 门禁 | 结果 | 结论 |
| --- | ---: | --- |
| A133 目标专项 | 19/19 | 通过 |
| smoke | 10/10，skipped 0 | 通过 |
| full continue | 102/109，skipped 0 | 达到合同门槛 |
| 后端 Maven 测试 | 64/64，失败 0、错误 0、跳过 0 | 通过 |
| 前端 production build | `vue-tsc -b && vite build` | 通过；仅保留既有大 chunk 提示 |
| effective scope contract | 104 条，ok | 通过 |
| feature delivery status scan | 93 个有效功能，ok | 通过 |
| regression manifest | full 109、smoke 10、豁免 21，ok | 通过 |
| `git diff --check` | 通过 | 无空白错误 |

full 最终失败集合：

- A32、A38、A57、A63：仍使用手工销售订单号，命中正式自动编号 `409`。
- A101：`CP-118 / 成品仓` 当前不满足库存预警列表夹具。
- A116：默认生产车间不存在、未审核或已禁用。
- A118：BOM 审核命中正式新版本确认 `409`。

以上 7 项在 A133 合同中明确排除，本批未修改其生产代码或脚本，也未用放宽断言维持基线。

## 关键浏览器证据

- A30：`verification/playwright/a30-entry-zero-value-confirm-saved-20260712070004.png`
- A31：`verification/playwright/a31-zero-quantity-audit-rejected-20260712070006.png`
- A98：`verification/playwright/a98-multi-source-line-trace-20260712070241.png`
- A99：`verification/playwright/a99-density-list-20260712070244.png`
- A112：`verification/playwright/a112-a112b-header-sparse-fill-20260712070320.png`
- A112：`verification/playwright/a112-a112b-header-seventeen-row-scroll-20260712070320.png`

对应结构化结果保存在 `verification/*-regression.json`、`verification/regression-tier-smoke-latest.json` 与 `verification/regression-tier-full-continue-latest.json`。

## 独立 review 与提交边界

独立 review 覆盖：

- 前端执行列、双层来源、零数量交互、22px/33px 密度和稀疏/密集滚动。
- A30/A31 单会话与请求级 400 断言。
- A41/A42/A43 精确“等于”筛选及导出请求。
- A93/A102 可见 XSDD 与 A98 双层来源边界。
- A129 `201 -> DRAFT -> audit 400 -> DRAFT` 完整链。
- 后端真实事务提交/回滚、事件唯一性与测试数据清理。

生产与夹具已拆为独立提交：

- `5280920` 建立 A133 G0 生产阻塞修复合同
- `0d6a9f5` 隔离操作日志预设回归夹具
- `dc8142f` 修复草稿数量与红冲审计事务合同
- `b4b9b44` 更新操作日志回归到统一筛选合同
- `ffb3f92` 修复执行追溯与列表交互合同
- `b8db94a` 对齐销售出库来源选择回归合同
- `d7e6b53` 对齐来源追溯与零数量全量回归

本报告、`docs/09` 与 `docs/12` 由最终治理提交独立收口。未获得推送授权，因此所有提交只保留在本地分支。

## 数据副作用与边界

- 回归在 BLD-TEST 创建了真实草稿、审核单据、库存调整、操作日志及其他验收业务事实；本批不清理历史业务数据。
- preset 恢复只处理精确识别的 A50/A51 污染和系统默认语义；没有清表、重建账套、修改编号规则、业务库存或财务事实。
- migration、有效范围、交付状态 JSON 与回归 manifest 均零 diff。
- 后端、前端、PostgreSQL、Redis 在最终提交代码上均处于 ready/healthy。

## 下一步

1. 将路线坐标切到问题 2“权限 fail-open、生命周期和库存直调接口可绕过”，另立受控任务与 allowedPaths。
2. 开工前明确员工侧写接口、生命周期权限映射和库存调整测试入口的授权边界。
3. 保留 7 个 full 基线失败为后续独立任务，不与问题 2 顺带混改。
4. 在用户明确授权前不 push 当前 12 个本地 ahead 提交。
