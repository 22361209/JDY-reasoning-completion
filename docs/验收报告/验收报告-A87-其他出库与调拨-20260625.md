# 验收报告 A87：其他出库与调拨

日期：2026-06-25

## 范围

- 其他出库：`OtherStockOutAppService` + `modules/inventory/other-stock-out/`，审核扣库存、反审核补回，库存不足审核失败。
- 调拨单：`StockTransferAppService` + `modules/inventory/stock-transfer/`，`STK_TransferDirect` 两腿过账，审核源仓减/目标仓增，反审核回滚两腿。
- 两单均复用 `BillLifecycleService`、`PostingPipeline`、`LookupService`、`OperationLogService`、`BillStatus`；不产生应收/应付，不改编号策略。

## 提交

- 其他出库：`6142214 A87 add other stock out document`
- 调拨单：本次后续提交包含 V34、调拨 AppService/Controller、前端模块、回归和文档。

## 后端瘦身 grep

命令：

```bash
rg -n "JdbcTemplate|SELECT |UPDATE |INSERT |DELETE |status\\s*=|BillStatus|PostingPipeline|BillLifecycleService" \
  backend/src/main/java/com/jdy/erp/inventory/api/OtherStockOutController.java \
  backend/src/main/java/com/jdy/erp/inventory/api/StockTransferController.java \
  frontend/src/app/App.vue \
  frontend/src/modules/inventory/other-stock-out \
  frontend/src/modules/inventory/stock-transfer
```

结果：无命中。两个 Controller 只委托 AppService；前端模块无业务 SQL、过账或裸状态转换。

## 库存断言

其他出库回归：`verification/a87-other-stock-out-regression.json`

- 单号：`QTCK-A87-20260625150958`
- 商品/仓库：`CP-001` / `CK-001`
- 数量：`3`
- 审核：库存 `1226606 -> 1226603`，`OTHER_STOCK_OUT` 流水 `1`
- 反审核：库存 `1226603 -> 1226606`，`OTHER_STOCK_OUT_REVERSE` 流水 `1`
- 库存不足：`QTCK-A87-SHORT-20260625150958` 审核返回 `409`，库存不变，失败单无出库流水。

调拨回归：`verification/a87-stock-transfer-regression.json`

- 单号：`DBD-A87-20260625152012`
- 商品/源仓/目标仓：`CP-001` / `CK-001` / `CK-002`
- 数量：`4`
- 审核：源仓 `1226606 -> 1226602`，目标仓 `1224802 -> 1224806`
- 审核流水：`STOCK_TRANSFER_OUT=1`，`STOCK_TRANSFER_IN=1`
- 反审核：源仓回 `1226606`，目标仓回 `1224802`
- 反审核流水：`STOCK_TRANSFER_IN_REVERSE=1`，`STOCK_TRANSFER_OUT_REVERSE=1`
- 源仓不足：`DBD-A87-SHORT-20260625152012` 审核返回 `409`，源仓/目标仓均不变，出/入两腿流水均为 `0`。
- 财务：脚本断言调拨成功单与失败单均未生成 `ar_receivable` / `ap_payable`。

## 前端 95% 截图

- 其他出库表单：`verification/playwright/a87-other-stock-out-form-audited-20260625150958.png`
- 其他出库列表：`verification/playwright/a87-other-stock-out-list-20260625150958.png`
- 调拨表单：`verification/playwright/a87-stock-transfer-form-audited-20260625152012.png`
- 调拨列表：`verification/playwright/a87-stock-transfer-list-20260625152012.png`

两单均使用库存管理模块入口、独立前端模块、统一 `DocumentForm` / `EntryTable` 范式；调拨额外启用目标仓列。

## 回归输出

新增脚本：

```bash
node scripts/a87-other-stock-out-regression.mjs
node scripts/a87-stock-transfer-regression.mjs
```

现存脚本复跑：

```bash
node scripts/a86-other-stock-in-regression.mjs
```

构建与后端测试：

```bash
npm --prefix frontend run build
cd backend && JAVA_HOME=/opt/homebrew/opt/openjdk@21 ./mvnw test
```

结果均通过。A2-A86 全量基线仍记录为 `verification/a2-a86-regression-latest.json`：`ok=true`、`total=70`、`expectedTotal=70`、`badCount=0`、`skipped=0`。
