# 验收报告：A156 文档导航与架构一致性

## 验收范围

- 建立现行文档导航，并将 README、接手指南、文件地图与运行手册指向同一入口体系。
- 修正当前 ERP 的定位、架构、tenant 与表格路线描述，使其与 ADR-011、ADR-012、ADR-015、A119 和 A137 的已交付事实一致。
- 修复现行 ADR 的失效引用，消除顶层 `12` 文件命名冲突。
- 保留新项目孵化和 A115 元数据路线正文，但将其标为历史计划，避免作为当前默认路线。

## 通过项

- `docs/README.md` 区分开工入口、现行规则、历史证据与生成物；`docs/00` 不再把 JDY 像素级复刻写为本项目唯一目标。
- `docs/05` 与 `docs/guides/account-set-architecture.md` 准确说明当前为 `public` 平台域加 `tenant_*` schema-first 路由，未把每账套独立数据库写成已完成。
- `docs/11` 将 JDY 降为参考驱动的体验验收，不以缺少截图或未达到像素级相似度阻断已满足业务、安全、数据与可用性门槛的功能。
- ADR-010 改为指向现行模块边界和生命周期协议；本地开发说明移动到 `docs/guides/local-development.md`。
- 历史方案保留正文、增加 `status: historical` 与 superseded 标识；历史验收报告不被批量改写。

## 验证

```text
active Markdown link scan PASS
node scripts/effective-scope-contract-check.mjs PASS
node scripts/feature-delivery-status-scan.mjs PASS
node scripts/validate-regression-manifest.mjs PASS
git diff --check PASS
```

## 范围外

- 未改动业务代码、数据库、生成物、交付状态、路线图或回归 manifest。
- 历史报告中的旧链接不作为现行链接门禁输入；其可追溯性由历史目录和 Git 保留。
- 未执行 full、后端测试、前端构建或浏览器回归：本任务只改变文档。

最终结论：通过
