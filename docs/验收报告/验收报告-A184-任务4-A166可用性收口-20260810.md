# 验收报告：A184 任务 4 A166 可用性收口

## 结论

A166 已进入当前主线但未独立归档的可用性变更已按现行代码重新 review；本轮确认并修复 3 个当前缺口，并用真实 Chromium 页面验收其余主路径。

- BOM 母件输入现在会在新关键字进入时立即使旧请求失效，而不是等 180 ms 防抖后才切换 generation；旧响应不能清除新查询的加载态或覆盖候选。“正在查询”与“没有匹配”互斥。
- 物料页的物料名称初始加载由当前 request sequence 持有；旧请求的 `finally` 不再把较新请求的 loading 提前关闭。
- 共享来源/主数据选择弹窗在加载期间的数量、明细汇总和分页都显示加载文案，分页控件禁用，不再把未完成请求误表达成“0 条”业务空数据。
- 标准列表的明细切换、列设置和更新库存仍位于同一动作栏右侧；物料名称新建后保存并审核、返回源物料页；BOM 整列表选择和库存期初未保存行焦点均通过。
- 库存期初的“保存后同步余额”由隔离 fixture 的精确后端集成方法补齐：绝对期初数保存后 on-hand/reserved/available 为 `120/30/90`，期初流水日期与上海业务日一致；低于已预留的期初数被阻断且库存/期初/流水快照不变。
- 最终代码/脚本独立只读 review 为 P0=0、P1=0、P2=0、P3=0。

## 实现与范围

- `BomForm.vue`：输入事件立即递增 lookup generation、清除旧候选并进入 loading；防抖回调只能使用已分配的 generation，响应只能更新自己仍持有的状态。
- `MasterDataRecordPage.vue`：为显式 loading 请求记录 owner sequence；取消、完成和重入时只能清理当前 owner。
- `SourceSelectorDialog.vue`：加载期间三处计数界面统一为加载语义，页大小、上一页、下一页同时禁用。
- 新增 `a184-a166-usability-regression.mjs`：为三类响应顺序建立可控 gate，对非白名单业务写请求一律 abort；登录先于该防护完成，不屏蔽正常会话建立。
- `regression-manifest.json`：新脚本进入 `inventory` / `production` / `master-data` / `table` 与 `full`，不进入 `smoke`。
- 本轮没有修改 `DataListPage.vue`、`base.css`、库存期初保存或后端过账逻辑；这些已有实现只做现行页面回归。

## 真实页面验收

有效 run ID：`5b37dda9-aebb-4fb3-919c-fbaa2ed1fa9a`；时间 2026-08-10 10:56:43–10:56:46 CST；结果 `ok=true`。

| 路径 | 实测结果 |
| --- | --- |
| 列表工具栏 | 动作组与工具组垂直中心均为 `213px`；工具组距右边 `7px`，三个工具全部在同一 utility group，旧的第二工具行不存在。 |
| 物料名称加载竞态 | 两个初始请求倒序返回；第一个旧响应到达后，第二个请求的输入禁用和“正在加载”占位保持不变。 |
| 物料名称新建回流 | 未命中名称 `A184 A166 名称 5b37dda9aebb` 确认新建，顶部/底部均显示“保存并审核”；受控 create→audit 响应后返回源物料页并保持 dirty。该两个响应由浏览器路由提供，数据库没有写入。 |
| BOM 输入联想 | `A184-SLOW` 旧响应在 `A184-FAST` 新输入后到达，不会清 loading、不显示假空态、不覆盖新候选；最终只显示 `A184-FAST-OPTION`。 |
| BOM 整列表选择 | 首个真实 `CP-` 请求挂起时，页面显示“数量/明细/总数/分页加载中”且三个分页控件禁用。较新精确查询先返回后，再释放带 stale sentinel 的旧响应，界面仍保持 `共 1 条`且旧行不可见。选中真实后端候选 `CP-001 / 控制臂总成 / 左前 / 黑色 / 只 / CK-001`，编码、名称、规格型号、单位和默认仓库五个字段均精确回填。 |
| 库存期初焦点 | 基线 `3` 行加载完后添加本地第 `4/5` 行；逐字输入物料/仓库时 active element 不丢失，`Tab` 落在同行仓库字段，新增第二行不改写第一行。两行最后移除，未点击保存。 |
| 期初保存—余额合同 | `InventoryFormalPostingTraceIntegrationTest#openingPreservesReservationsUsesShanghaiFactDateAndRejectsBelowReserved` 在隔离 fixture 中真实调用 `OpeningStockService.saveRows`；保存 `120` 后三量为 `120/30/90`，同时写入 `OPENING_STOCK` 流水；低于 reserved 的 `24` 返回 conflict 并保持整体快照。测试 `@AfterEach` 删除本次 fixture。 |

业务写边界：脚本仅白名单物料名称 create/audit，且两者都在浏览器中返回受控响应；`blockedUnexpectedWrites=[]`。真实 Chromium 脚本未保存共享测试账套的 BOM 或期初库存，没有新增业务单据或改变共享库存余额；隔离后端 fixture 的期初保存已由上述精确方法执行并在 `@AfterEach` 清理。

## 证据与门禁

- 主 artifact：`verification/a184-a166-usability-regression.json`，SHA-256 `8284879d17b21c6c865bc89957343f3cef7199952de549525e29c932d522f267`。
- 加载态截图：`verification/playwright/a184-a166-selector-loading-5b37dda9aebb.png`，SHA-256 `8c17f88d828638db03ea251aadccc87300b5667390e2885d4911a0edd18eba56`。
- 期初页零残留截图：`verification/playwright/a184-a166-opening-focus-5b37dda9aebb.png`，SHA-256 `9c785b3960dbb0f60c474d9438ce0068c2c70ae4a0622db154f061a840c877f7`。
- `frontend npm run build`：通过，244 modules transformed；仅保留已知 chunk-size warning。
- `node --check scripts/a184-a166-usability-regression.mjs`：通过。
- `node scripts/validate-regression-manifest.mjs`：通过；discovered `169`、full `148`、smoke `13`，area 为 inventory `19` / production `18` / master-data `17` / table `20`。
- 库存期初精确后端方法：1/1 通过，0 failure、0 error、0 skipped，Surefire 时间 `4.243s`。首次在受限沙箱中启动时 Mockito/ByteBuddy 无法自附加，测试体未执行；在允许 JVM 自附加的环境中重跑同一方法后通过。
- 真实 Chromium 针对性：有效运行 1/1 通过。在此前有一次测试断言失败（run `8c3f3dbd-a53c-49ca-b696-2871d4579a1b`）：页面已证明同行且右对齐，但脚本误把 Chrome 已解析的 `margin-left=794.688px` 期待为原始字符串 `auto`。改为右边几何和正间距断言后通过；该失败未触发业务写，也未因此修改产品布局。
- 本任务未运行 area 或 full；A184 的唯一 full 配额保留到四项集成 review 之后。

## Review 与已知边界

- UI 代码预审、脚本预审和修后终审均为只读审查；已关闭意外写泄漏、选择器旧响应覆盖、固定 sleep 和防抖墙钟假设等测试缺口。
- 物料名称的本次 create/audit 是浏览器受控响应，用于验证页面路由、“保存并审核”和回流状态；不宣称为新增了一条真实数据库记录。
- 真实 Chromium 仅验证 A166 的未保存行稳定键/焦点缺陷，并恢复到原 `3` 行；不对共享测试账套重复写期初库存。真实保存、三量同步、流水和低于预留的回滚合同由上述隔离后端方法验证，两段证据共同覆盖任务包的“不丢焦点且保存同步余额”。
- 未操作生产环境，未 push。
