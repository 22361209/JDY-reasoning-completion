# 验收报告 A91 前端 UX 批量修复

日期：2026-06-26

## 范围

- `DataListPage`：单据列表新增直达新表单页签，主数据新增仍走弹窗；同单据页签锁收窄到对应单据行。
- `EntryTable`：数字列固定宽度和右对齐；行操作改为右键/`⋮` 菜单；合计按数量/金额列对齐；增加分录列设置（显隐、顺序、列宽）。
- 单据错误提示：前端读取后端错误体 `reason/message`，后端 `ResponseStatusException` 响应体补 `reason`。

## 行为变更

| 项 | 结果 |
| --- | --- |
| 列表“新增” | 单据列表点击新增直接打开对应新单据页签；不再落入 `pendingAction='新增'` 通用确认框 |
| 分录行操作 | 插入/删除/复制从常驻按钮改为行菜单；底部保留“增加明细” |
| 回归脚本 | A10 复制行、A13 插入行已改为先打开行菜单；其他脚本保持兼容 |

## 验证

| 命令 | 结果 |
| --- | --- |
| `npm --prefix frontend run build` | PASS |
| `cd backend && JAVA_HOME=/opt/homebrew/opt/openjdk@21/libexec/openjdk.jdk/Contents/Home ./mvnw test` | PASS，含 ArchUnit |
| `node scripts/a91-frontend-ux-regression.mjs` | PASS |
| `node scripts/run-a2-a91-regression.mjs` | PASS，`total=79`、`ok=true`、`badCount=0`、`skipped=0` |

当前全量聚合文件：`verification/a2-a91-regression-latest.json`

- ok：`true`
- total / expectedTotal：`79 / 79`
- badCount：`0`
- skipped：`0`
- mtime：`2026-06-25T22:39:03.813Z`（预提交全量；最终提交后已按硬门禁重跑并更新）

## 证据

- A91 专项：`verification/a91-frontend-ux-regression.json`
- A91 截图：`verification/playwright/a91-frontend-ux-20260625223331.png`
- A2-A91 全量：`verification/a2-a91-regression-latest.json`

## 说明

- 未改库存、财务、编号、税务业务口径。
- 未重跑 `build-jdy-feature-approval.mjs`，未手改生成物 `frontend/src/app/featureScope.ts`。
- Browser 插件做 in-app Browser 可视检查时返回 `sandboxCwd must be an absolute file URI`，无法连接；本批可视验收以 Playwright 截图和回归脚本为准。
