# 验收报告：A186 A185 全量 UAT 缺陷复现、修复与主链重验

## 结论

A186 已完成。A185 的 8 个已确认问题、3 个高风险观察，以及 A186 修后 UAT 新发现的 3 个用户可见缺陷，共 `14` 个 defect ID，均已形成根因、实现、最低充分自动合同、真实页面结论和独立 review。

- A185 的 `UAT-A185-006/007/008` 已先用相同步骤独立复现，再进入修复，没有把观察直接当成已确认缺陷。
- 修后真实 Chrome 已执行原 25 项主链：`22` 项没有新增问题，`3` 项产生 `UAT-A186-012/013/014`；这 3 项随后完成实现和 5 个直接页面场景复验。
- 销售链显式经过销售订单—发货通知—销售出库—销售退货—应收/收款；发货通知没有被订单直出库路径替代。
- 最终三路只读 review 无未关闭 P0–P2；代码/迁移/测试没有通过放宽权限、tenant、生命周期、库存、正式编号或来源累计数量来换取页面可走通。
- 用户授权的第二次且最终 full 完整执行 `151/151`，`PASS=151`、`scriptBadCount=0`、`badCount=0`、`skipped=0`、`ok=true`。因此 A186 的最终验收结论为 **通过**。

## 环境与边界

- 真实 UAT：Chrome 可见界面 `http://127.0.0.1:5173`，账套 `UAT20260810 / 全量UAT账套20260810`，数据前缀 `UAT-20260816-*`。
- 自动回归：只由 canonical runner 和 run-owned/明确登记的回归前置写 `BLD-TEST/public`；没有手工重置 `BLD-TEST`。
- `tenant_uat20260810` 在 A137 中只进入已登记 tenant 的只读 registration/metrics/schema+data fingerprint；没有被 sync、重置或删除。持久 sync 精确限两套 A119 fixture tenant。
- A126 历史 77 批继续保持既有 append-only/缺 posting facts 结论；本任务没有删除或重述历史库存事实。
- 本任务没有操作生产环境，没有 merge、rebase、squash 或 force push。

## 14 个缺陷的最终结论

| 编号 | 根因与最小修复 | 最终验收 |
| --- | --- | --- |
| `UAT-A185-001` | 系统只有预置角色编辑能力，没有最小角色创建 API/UI；新增 `system.role_permission.manage` 守卫的创建合同，并封住刷新把 ADMIN 权限复制到创建草稿的竞态。 | ADMIN 可创建最小权限角色/用户；当前账套授权、菜单、详情和写权限严格匹配。 |
| `UAT-A185-002` | 20 页签上限时先写 master record 状态、忽略 `openTab(false)`，形成 ghost 并回首页。 | 页签打开成功后才提交记录；达到 20 页时无隐藏表单，释放一页后同一新增可重试。 |
| `UAT-A185-003` | tenant BOM 列表 SQL 错把 `sys_user` 当 tenant 表，计划页又吞掉加载错误。 | 改用 `public.sys_user`；BOM 加载失败、真空和成功三态可区分，当前 BOM 可被计划读取。 |
| `UAT-A185-004` | 非物料列单格粘贴被共享分录按“物料/仓库/数量/价格”整行解析，并存在 index/异步代际风险。 | 仅物料列接管结构化粘贴；仓库选择/粘贴只改当前稳定行仓库，五类目标表单及异步结构变化均覆盖。 |
| `UAT-A185-005` | 报价价格列单格粘贴被误作物料编码整行替换。 | 普通录价与单格粘贴只改价格；物料 UUID、编码、名称、规格、单位和仓库保持。 |
| `UAT-A185-006` | 保存后预览清空任务单号，且没有稳定草稿身份和在途闸门，响应丢失/双击会再次取号。 | tenant-scoped `draftId` advisory lock + 最终 UUID/状态复验；重复保存、双击和丢包重试保持同一 ID/单号，序号只推进一次。 |
| `UAT-A185-007` | UI 把 `CK-001` 当显式默认仓提交，后端未读取母件默认仓。 | 手工任务仓为空时锁定并复验母件已审核启用默认仓；任务、完工行、AUDIT trace 与余额进入约定仓，`CK-001` 增量为 0。 |
| `UAT-A185-008` | 委外退货与报废分开累计，审核时没有共同来源上限或统一锁序。 | 收货头—来源行—处置头—库存固定锁序和条件 claim/release；串行/并发下 `returned+scrapped<=receipt.qty`，旧 `1+1>1` 形状可用正式反审核恢复。 |
| `UAT-A185-009` | document output 未登记 `stock-count-loss`，前端又把所有非 2xx 伪装成“未保存”。 | 正式 DRAFT/AUDITED 盘亏可 CSV/HTML/PDF；未保存、VOID、不存在和无权限继续 fail closed，原始错误可见，输出不改状态/余额/流水。 |
| `UAT-A185-010` | 物料反审核没有按当前 tenant 的真实 FK 关系检查下游，生命周期与首次引用写也未完整串行。 | 父行 `FOR UPDATE` + pg_catalog/OID 引用扫描；首次引用写使用事务内 `FOR KEY SHARE`，纯读保持无锁。 |
| `UAT-A185-011` | 供应商状态调用失败信息被列表刷新覆盖，候选与保存端也未统一要求已审核启用。 | 有业务引用时允许禁用但仍禁止反审核；禁用供应商退出专用候选，保存端复验，批量失败原因保持。 |
| `UAT-A186-012` | 领料详情归一化丢弃顶层 `productInfo`，历史任务空快照缺受限来源回退。 | SOUT 详情展示任务冻结母件 8 个可见字段；仅 `PLAN_ROOT + product_id + bom_id` 同源时回退计划冻结值，BOM 子任务不串根母件。 |
| `UAT-A186-013` | 产品入库详情存在同一 `productInfo` 丢失和历史空快照问题。 | SCRK 与任务/领料预览/详情使用一致冻结口径；主档单位从 PCS 漂移到 BOX 后仍显示计划 PCS。 |
| `UAT-A186-014` | 三类页面已发送 `remark`，但 `production_completion`、`stock_transfer`、`stock_count` 的表结构、DTO、保存和详情未承接。 | 新增且仅新增 V121 nullable `remark TEXT`；SCRK/DBD/PD 创建、同号更新、自动回读、关闭重开和审核/反审核均保留，旧行继续为 SQL NULL。 |

## 真实 Chrome 与主链证据

- `006` 复现：同一任务表单连续保存得到 `SCRW000003/000004`。
- `007` 复现：`SCRW000004 → SOUT000002 → SCRK000002`，完工错误进入 `CK-001`。
- `008` 复现：`WWJG000002 / WWFL000002 / WWRK000002 / WWTH000002 / WWBF000002`，收货 1 后退货 1 与报废 1 同时成立。
- 修后 25 项主链覆盖销售、采购、生产、委外、库存、资金与打印；销售链显式包含发货通知。最终 full 中 `a108-delivery-notice-reservation-regression.mjs` 也 PASS，覆盖审核增加预留、反审核释放、超量/重复占用阻断、作为出库来源和下游后反审核阻断。
- `UAT-A186-012/013`：`SOUT000003/SCRK000003` 显示冻结母件编码、名称、规格、单位、任务数量、剩余数量、BOM 与版本。
- `UAT-A186-014`：`SCRK000002`、`ZJDB000002`、`PD000004` 的唯一备注在保存、关闭和重开后保持；两张新库存草稿未审核，没有改变库存余额。

## 分阶段自动门禁

- 权限/主数据组：后端最终目标矩阵 `38/38`；受控 Chromium `baa03b5a-aab7-4625-9ddc-a6264e3ff21e` 为 `ok=true`，unexpected writes 为 0。
- 选择器/BOM 组：tenant 隔离 `2/2`；受控 Chromium `28b4cb89-d024-4332-8e50-ef3489f2e26b` 为 `ok=true`。
- 生命周期/数量/打印组：后端首轮 14 项中的 11 项通过，3 项仅修测试夹具/期望后精确 `3/3`；受控 Chromium `1d01f6ec-754a-4ba5-84b9-4fb0642c1e79` 为 `ok=true`。
- 新增产品信息/备注组：V121 migration runner `235` 条断言通过；受控 Chromium `c4d5f4ed-7baa-408e-a03b-dc06fff957b6` 为 `ok=true`，5 场景、3 个同号草稿更新与 8 组锁 acquire/release 闭合。
- `area:production` 为 `32/33`，唯一 A116 旧共享夹具失败后精准 A116 `1/1`；第二次 full 中 A116 再次 PASS，证明固定 `RM-A116-COMPONENT` 可只读复用。
- `area:inventory` 为 `27/28`，唯一 A101 使用禁用 `CP-118` 被正确拒绝后精准 A101 `1/1`；第二次 full 中 A101 再次 PASS，CP-001 完整行未被改写。

## 首次 full、恢复与精准闭环

首次 full 当时发布到可变指针 `verification/regression-tier-full-continue-latest.json`；其历史失败结论由 run `1a95439fea844e3dbc3beec31b42d38c`、当时 SHA-256 `42f7ab451d53123de9561a4987d94dd7b8003c4dda469a109ed73487c23d7206` 和下列统计冻结。该共享 latest 随后按用户授权推进到第二次 full，不能把当前文件字节冒充首轮实体：

- expected 151、executed 120、PASS 115、脚本失败 5、skipped 31、badCount 6、ok=false。
- 失败脚本为 A115、A126、A136、A137、A163；第六个 bad 是 postflight。
- 首轮历史失败结论没有被改写；用户看到完整失败、未启动项和残留事实后，才授权一次精确恢复与第二次 full 例外。

恢复只作用于完整 UUID/actor/endpoint/关系指纹匹配的本批测试残留：4 条 A186 `WRITE_DENIED` orphan，以及 A163 固定 role/user/permission/link/grant 和 7 条 owned logs。恢复后五个日志 schema 的 orphan/A186/A163 均为 0，角色矩阵恢复为 4；`ZJZZ000121` 保持 DRAFT/version 2，四条 AUDIT/REVERSE append-only facts 原样保留、净额 0.00。

随后 A115/A126/A136/A137/A163 五条 canonical targeted 均 `1/1`，bad/skipped 0/0；RolePermission 后端用例 `3/3`。A126 新 evidence 为 `ce8da5b2-6025-41ca-bc55-095233399de9`，135/135 断言通过，两 fence CLOSED/0、两 identity closed/deleted，业务/库存/日志/Redis 回到共同 baseline 或 0。

## 第二次且最终 full

- 命令：`node scripts/run-regression-tier.mjs full --continue-on-failure`。
- runId：`f53af589c12841deb4b4d6138a5e9862`。
- 时间：CST `2026-08-17 00:08:00–01:27:16`，约 79 分 16 秒。
- 直接执行基线：干净 HEAD `102efca990fb778fa18f7c73d9dcffd1da91347a`，execution rootDigest `776b8262ad350e1eb082a72c8f8faecae339c9afa8500a69a83582658a32e6e8`；通过后的证据文档只 amend 同一逻辑提交，所以最终 Git SHA 会变化，不能声称最终 amend SHA 被该 full 直接执行。
- 权威文件：`verification/regression-tier-full-continue-latest.json`。
- SHA-256：`af1206f90f005a05dd100e174c152089978928e8cf57b77c059f5da59c89d4d3`。
- 结果：`expectedTotal=151 / total=151 / PASS=151 / scriptBadCount=0 / badCount=0 / skipped=0 / ok=true`。
- preflight/postflight 均 `ok=true`；角色矩阵与安全设置前后相等。
- auxiliary fixtures `closed=true`；suite lock `releasePrepared/releaseCommitted=true`。
- process ownership、Docker lease、execution integrity 均 complete，execution `strictClean=true`。
- secret scan `ok=true / residueFree=true`；151 个结果的 `ok` 全为 true。
- suite identity cleanup 为 `ok/runtimeAccessCleaned/closed=true`；`cleaned/principalDeleted=false` 与 `retainedDisabled/reusableTombstone=true` 表示按设计保留不可登录的禁用墓碑，并非删除 principal，也不能笼统写成“全部身份字段为 true”。
- 普通 `verification/regression-tier-full-latest.json` 仍是旧基线，不是本次第二轮证据。
- full 额度最终使用 `2/2`；没有运行第三次 full。

## 最终数据与安全边界

- A126 本轮与首轮失败批的 run-owned 业务事实、external facts、身份、角色关联、账套授权、scope、actor lock、五 schema 日志与 Redis primary 均为 0；受控库存余额与 non-owned transaction 双 digest 回共同 baseline。
- A163 run-owned role/user/permission/link/grant/scope/log 均为 0，global actor orphan 为 `0→0`。
- RolePermission 集成测试产生的拒绝日志由事务后 UUID+username 双锚点、REQUIRES_NEW 精确回收；现场测试用户和日志为 0。
- `UAT20260810` 保留为用户创建的持久 UAT 账套；A137 只读观察其前后 topology/fingerprint，不把它加入可写 sync allowlist。
- `RM-A116-COMPONENT` 及其配套物料名称是一个不按 run 增长的 canonical 回归前置；不能把它表述为逐次业务夹具已清理。
- A126 历史 77 批未处理；未删除或改写 append-only 库存事实。

## Review、预算与已知边界

- 修复组、首轮 full 恢复补丁和最终 10 文件收口均经过三路独立只读 review；进入第二次 full 前最新结论为 P0/P1/P2/P3 均无 actionable finding。
- Goal token 上限为 15,000,000；运行时没有可审计的实际 token 计量值，不用墙钟反推 token 使用。
- A186 最终逻辑提交使用 `5/10`，低于第 7 个提交的 70% 停止线；第二次 full 通过后的报告、归档和交接只 amend 同一第 5 个逻辑提交。
- full 预算最终 `2/2`，第二次来自用户在看到首轮完整失败证据后的明确例外授权。
- 非阻断后续增强：可为生产任务净重/毛重快照增加非空判别夹具，并为少数并发测试增加更精确的锁点闸门；这些不影响当前已证明的生产锁序和本次结论。

## 发布边界

本报告所在候选只允许在文档治理、干净工作树、只读 `git fetch origin main`、behind=0、远端仍为祖先、完整 `origin/main..main` outbound 范围与凭据/生成物审计全部通过后普通 push。禁止 pull、merge、rebase、squash、force push；最终提交 SHA 与远端同步状态以 Git/GitHub 事实为准。
