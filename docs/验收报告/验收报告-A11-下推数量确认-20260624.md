# A11 下推数量确认验收报告

验收日期：2026-06-24  
回归批次：`20260624065050`  
结论：PASS

## 验收范围

本次补齐销售订单下推销售出库、采购订单下推采购入库前的数量确认弹窗。员工可以看到源单数量、已执行数量、剩余数量，并修改本次下推数量；确认后才生成下游草稿。

| 项目 | 断言 | 结果 |
| --- | --- | --- |
| 销售默认数量 | 部分出库后，销售下推弹窗默认本次数量为剩余 `6 / 5 / 4` | PASS |
| 销售前端保护 | 将首行改为 `7` 超过剩余 `6` 时，弹窗提示错误且不生成草稿 | PASS |
| 销售确认数量 | 改为 `2 / 1 / 3` 后生成销售出库草稿，表单明细数量即为 `2 / 1 / 3` | PASS |
| 采购默认数量 | 部分入库后，采购下推弹窗默认本次数量为剩余 `6 / 5 / 4` | PASS |
| 采购确认数量 | 改为 `1 / 2 / 3` 后生成采购入库草稿，表单明细数量即为 `1 / 2 / 3` | PASS |
| 后端承接 | 销售出库草稿、采购入库草稿均保存并审核成功 | PASS |

## 视觉证据

- `verification/playwright/a11-sales-push-confirm-dialog-20260624065050.png`
- `verification/playwright/a11-sales-push-confirm-20260624065050.png`
- `verification/playwright/a11-purchase-push-confirm-dialog-20260624065050.png`
- `verification/playwright/a11-purchase-push-confirm-20260624065050.png`

## 自动化证据

- 回归脚本：`scripts/a11-push-confirm-regression.mjs`
- 结构化结果：`verification/a11-push-confirm-regression.json`

## 验证命令

```bash
cd "/Users/linzhenyue/Projects/JDY 推理补完/frontend"
npm run build

cd "/Users/linzhenyue/Projects/JDY 推理补完/backend"
export JAVA_HOME="/opt/homebrew/opt/openjdk@21/libexec/openjdk.jdk/Contents/Home"
export PATH="$JAVA_HOME/bin:$PATH"
./mvnw test

cd "/Users/linzhenyue/Projects/JDY 推理补完"
npx --yes --package playwright node scripts/a11-push-confirm-regression.mjs
```
