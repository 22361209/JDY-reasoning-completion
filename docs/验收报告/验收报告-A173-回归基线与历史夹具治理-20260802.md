# 验收报告：A173 回归基线与历史夹具治理

## 结论

A173 已完成回归执行基线和范围内历史夹具治理。原来由错误端口、权限缺口、共享 ADMIN 变更、SINGLE_ACTIVE 会话互踢、脏工作区、写死迁移 head、过期日期及违反现行业务规则的生产夹具造成的级联假失败，已经被前置阻断或修正。

唯一 full 在干净候选提交 `9fe66bae` 上完整执行 143 项，结果为 **141 通过、2 失败、0 跳过**。两项失败均已用首跑证据完成只读归因：A25 是超过 1000 条物料后失效的候选夹具，A128 是把“本月”写死为 2026-07 的日期夹具。它们不在 A173 允许路径内，也不是本批产品代码回归；按 `record-and-stop` 另立后续任务。故本报告不宣称 full 全绿，也不宣称达到 143/143。

## 实现范围

- `regression-preflight`：full 在任何登录前先拒绝脏工作区；确认 5173 的实际监听进程来自当前 worktree，解析该进程实际 Vite 配置并要求 `/api` 精确代理到 `http://127.0.0.1:8080`，同时比对直连与前端健康接口的当前构建 fingerprint。
- 身份与共享状态：登录必须精确命中 `BLD-TEST/public`、`admin/ADMIN`；启用权限 catalog、ADMIN 授权和 session 有效权限必须三方全集相等。本批基线为 28/28。执行器记录完整角色启用/授权矩阵与安全设置，并在共享状态变更脚本后及整批结束时复验。
- 会话与权限夹具：A55/A56 使用 WAREHOUSE 的拒绝路径，不再修改共享 ADMIN；A162/A163 每个浏览器上下文建立独立新会话；A44 清理浏览器、localStorage、session/Redis 和本轮预设残留。
- 历史夹具：生产链使用来源任务母件 `CP-001` 覆盖三行/三仓，保留 A167 混入其他母件必须 409 的负向合同；A112 固定视口和 25 行数据，确定性形成纵向溢出。
- 迁移与静态合同：A141/A142/A143/A146/A151 动态读取当前 migration head V113，仍显式验证 V112/V113 成功及 V105–V111 的历史 checksum、单次应用和拓扑合同；A146/A151 只冻结各自拥有的静态合同。
- 后端测试：销售链日期基于数据库当前日期生成；报表 tenant 隔离测试使用 test-only 权限替身，不依赖共享运行时角色状态。

## 定向验收

```text
modified .mjs node --check                                  PASS
git diff --check                                            PASS
effective scope / feature status / manifest / docs          PASS
TenantReportingIsolationTest                                1/1 PASS
TenantSalesChainIsolationTest                               2/2 PASS
A44 + A162 + A163                                           PASS
A55 + A56                                                   PASS
A112 deterministic overflow                                 PASS (row 32px; 25 rows; overflow 801 > 348)
A2–A7 / A40 / A46 / A53 / A64 / A167 production            PASS
A146 numbering closure                                      PASS (110 assertions)
A151 material scrap backend                                 PASS (113 assertions)
A141 / A142 / A143 / A146 / A151 migration                 PASS (current head V113)
smoke                                                        10/10 PASS
```

另以故意制造脏工作区的负向检查证明 full preflight 会在登录前拒绝执行，既有管理员 session 不受影响；恢复干净后才生成候选提交并执行唯一 full。

## 唯一 full

```text
command       node scripts/run-regression-tier.mjs full --continue-on-failure
candidate     9fe66bae
started       2026-08-02T09:05:20.952Z
finished      2026-08-02T09:21:48.350Z
executed      143/143
passed        141
failed        2
skipped       0
preflight     PASS
postflight    PASS
artifact      verification/regression-tier-full-continue-latest.json
```

Preflight 确认 5173→8080 精确代理，直连/代理 fingerprint 均为 `43ccbd7ac15e2b2a5499c67300da217b8729ea34330fdaf5fb6d00c1de641119`；权限 catalog、ADMIN 授权和 session 均为 28 项。Postflight 确认 ADMIN/FINANCE/WAREHOUSE 角色矩阵未变，安全设置仍为 SINGLE_ACTIVE、30 分钟 session timeout 和原密码策略，工作区保持干净。两个失败项的 `sharedStateInvariantError` 均为 `null`。

## 两项范围外失败

### A25 分录粘贴冲突键盘选择

失败点是等待 `entry-paste-conflict-dialog` 30 秒超时。首跑批次创建的两个物料均已审核且启用；数据库只读核验显示当前 `public.md_product` 共 2207 条。粘贴实现调用 `product-master-list?page=1&pageSize=1000`，列表默认按编码排序；本轮 `A25-A-20260802090625` 排名第 879，`A25-B-20260802090625` 排名第 1090，因此只加载到一个同名候选，解析器按唯一物料直接填入，不会打开多候选对话框。紧邻的 A23 两个候选排名第 447、667，仍在第一页，所以同一功能刚刚通过。

建议在独立 A174 中让 A23/A25 通过本轮唯一关键字查询或完整分页取得候选，并补齐本轮销售单、物料及证据的精确 cleanup；不得靠扩大固定 pageSize 或清空共享资料库恢复偶然通过。

### A128 列表查询统一

脚本把“本月”结果写死为 `2026-07-01 至 2026-07-31`，实际运行日为 2026-08-02，页面正确返回 `2026-08-01 至 2026-08-31`。建议 A174 用运行时当前月计算期望和同月/上月夹具，或在测试上下文显式冻结时钟；不得修改页面把“本月”退回七月。

## 执行预算与后续

- 实际执行约 50 分钟，低于 300 分钟上限。
- 累计使用 4/4 个子代理；唯一 full 1/1；最终使用 2/3 个提交。
- Goal token 预算为 15,000,000；本次未启用运行时 Goal 计量器，实际 token 读数不可用。
- 未修改产品业务代码、非 `BLD-TEST/public` 数据或权限模型；未重跑 full；未 push。
- 下一批建议为 A174《A23/A25/A128 历史夹具分页、日期与清理治理》，先跑两项定向门禁，再在新的 full 配额下验证全绿。
