# 验收报告：A184 四项独立债务统一收口

## 结论

A184 的四项显性债务均已完成实现、针对性验收、独立 review 和提交；唯一 full 的历史结论不是“148/148 全绿”。

- 资金转账详情 GET 现在声明并执行 `finance.cash_transfer.audit`，未授权角色在存在/不存在单据上均先返回 `403`，授权角色保留 `200/404` 合同。
- 资金账户候选改用绑定 tenant、完整查询形状和完整候选集合的服务端快照 token；缺 token、伪造/跨查询复用、页间等量删增均 fail closed，账户资料维护权限没有扩大。
- A126 新运行具备 run-owned 身份、两条 request fence、完整业务/库存 snapshot、来源闭包、锁内 digest/CAS、逐表 row count 和异常也执行的 cleanup；本轮新运行与先前失败运行均已证明业务、身份和运行时零残留。
- A166 的 BOM/物料名称旧响应竞态和选择器加载语义已修复；真实 Chromium 复验工具栏、名称新建回流、真实 BOM 五字段回填和期初新增行焦点。

唯一 full run `cbfbfce50925404683f5035859ebc41d` 在第 107 项 A126 的父级 auxiliary fixture closure 处 fail closed：前 106 个 per-script 结果被 runner 接受为 PASS，A126 子脚本已执行但未提交结果，第 108–148 项共 41 项未启动，suite summary/latest 未发布。该事实保持原样；本批没有运行第二次 full，也没有把补充定向证据改写成 148/148。

full 配额消耗后，原任务 4 的 `smoke + area:master-data + area:production + area:inventory` 以及“full 完整执行”已经无法在不违反门禁预算的前提下继续。Codex 随后向用户明确提出替代收口矩阵：只修复并 review 当前 P1，运行 auth-origin、fixture-ledger、后端定向合同，再运行 A126、A136、A163、A184-A166 四条 final-HEAD targeted，不重跑 full 或 area。用户回复“确认，你继续。不过需要列出显性计划和codex预计时间。”，明确授权按该矩阵完成 A184。该授权不把未运行项视为通过；本报告仍保留 38 项未补跑的明确边界。

## 交付提交

| 提交 | 内容 |
| --- | --- |
| `096fefbd` | 建立 A184 四任务包、预算、范围和门禁边界 |
| `7ec68b96` | 关闭资金转账详情读取权限缺口 |
| `49766c8b` | 为资金账户候选建立稳定快照分页 |
| `aba072b9` | 建立 A126 前向完整清理、精确失败恢复与历史 fail-closed 工具 |
| `bcd4708f` | 关闭 A166 三项可用性缺口并补真实浏览器合同 |
| `1b2dcb5f` | 修复 auxiliary fixture 数据库代际前移后的请求门恢复 |

最终归档为第 7 个提交；未 push、未操作生产环境。

## 唯一 full 原始证据

- HEAD：`bcd4708f4e8fac3c1cad6b51a3298876669214d2`；manifest SHA-256 `61c5923e15fa9c53ad4b28cf19b585687ad978b5627eef4315ad5f165a06776e`，full 共 148 项。
- retained owner 可审计窗口：2026-08-10 11:12:10.231–12:14:40.811 CST，约 `1:02:30.580`。
- 分段结果：1–106 为 runner accepted per-script PASS；107 A126 executed-uncommitted；108–148 共 41 项未启动。
- `badCount=2` 是 infrastructure/setup error 1 项加 unpublished fail-closed sentinel 1 项，不是两个脚本失败；`skipped=42` 是 A126 未提交 1 项加后续未启动 41 项。
- owner SHA-256：`c72f619ba4672c79359b005504bde7a3cf77b39147bbf08df061a93b6da569a6`。
- child ledger SHA-256：`e41646e88e8c81563c4de1c220f3fa6158fc3962e5903af2013d1f15bb965b9d`。
- A126 failed evidence：run `3d45a553-812c-4a55-97ce-a17af4f0ef95`，SHA-256 `bd73f4db057c0942dd90724014a52ee9a4a40a59f68d3edf332ed1e7231506ea`。
- `regression-tier-full-continue-latest.json` 仍是恢复的 A183 pre-run baseline `e9db65a6db564e858cfba7ad9ff25a72`，SHA-256 `4ef0d25340a3df5fb35e34c3428576f2ea38bba9262b5d84cf6cecd51df71a2b`，不是本轮结果。

## Full 后 P1 根因与修复

A126 已把 ADMIN/WAREHOUSE generation 0 请求门关闭为 `CLOSED/0`；随后 ADMIN identity cleanup 的本地 PostgreSQL/Redis 辅助调用失败，而 hard-disable 已令数据库 `session_generation` 前移。父 runner 仍以 OWNER capability 按 ledger generation 0 重关，Controller 在进入 tracker 前以精确 409“请求门代际尚未生效”拒绝；父级虽再次 hard-disable，却保留原 close error 为 fatal，因而中断 suite。

修复没有放宽 Controller：

- `manageRegressionRequestFence()` 为非 2xx 增加已清洗的 `code/status/reason/action`，不附加 capability、headers、request body 或原始响应。
- 只有 ledger 为 `PREPARED/HARD_DISABLED`、metadata/ownership 精确匹配、身份 disabled、数据库代际大于 ledger，且第一次恰为结构化 `409 + CLOSE_AND_DRAIN + 请求门代际尚未生效` 时，才以同一身份、同一 capability 和当前数据库代际重试一次。
- 其他 403/409、enabled 身份、代际未前移、ownership drift、transport、非法 JSON、未排空响应均不跨代；第二次失败保留两次脱敏诊断，并继续既有 hard-disable + fatal。
- capability 在字符清洗和 240 字符截断前脱敏；合同覆盖“209 个普通字符 + 32 位 capability”边界，聚合 message、JSON 和 nested errors 均不保留 31/32 位前缀。

定向合同：`regression-auth-origin-contract`、`regression-fixture-ledger-contract` 均通过；后端 `RegressionFixtureControllerTest,RegressionActiveRequestTrackerTest` 共 13/13 通过。两路独立只读 review 最终均为 P0=0、P1=0、P2=0、P3=0。

## Final-HEAD 补充验收

| 项 | Runner / 子运行 | 结果与证据 |
| --- | --- | --- |
| A126 | runner `b3636febaa9a4e46988585f8e62c96a6`；child `0cca8010-f954-4a39-a2fc-4fe2a513568c` | 1/1，bad 0，skipped 0；summary SHA `8232eb5f20503d63158803c6ab80083b136855946cc5376fd46e273068eb4fc5`；child evidence SHA `67dedaea941672500bee51f54a840594662c40572419194eee61904196a9e242`；73.695s |
| A136 | runner `414f61c278e3432aa6171e9d1e61631c` | 1/1，bad 0，skipped 0；summary SHA `61106c5bcbcd6329683bfbd35a9c8b7ce432a58cdaa1188da16dc5ff63cd6bc2`；48.621s；cleanup 全零 |
| A163 | runner `0902225cb050408a82bce7ff5eedd199`；batch `20260810063308-833be2ea` | 1/1，bad 0，skipped 0；summary SHA `ff959160352d918bd1bcfcc459b9507e2404704da114c886b008042755d6886b`；67.401s；等量替换 409、权限先于 token 403、transfer-only 资料权限 403 |
| A184-A166 | runner `9004c6e7ea75449f8c976d9a97a637aa`；Chromium `be6b772a-5bfe-4f7c-bfe9-c2926964c593` | 1/1，bad 0，skipped 0；summary SHA `f85554dd493ee08551f4731bb663e70ce774b97ce4d11fbe198b0a57c619d977`；5.951s；`blockedUnexpectedWrites=[]` |

四个 runner 均 preflight/postflight 通过，使用当前 build fingerprint `db418193b59e59f29347527b57231383169ce3591b8469dd7d68cff543538a52`，最终 canonical suite lock 均已释放。

## A126 incident 专属零残留复核

- 旧 run `3d45a553…` 与新 run `0cca8010…` 的 12 组 owned arrays 和 12 类 external 全为 0。
- 新运行两条 fence 均 `CLOSED/0`；business cleanup `restored=true`，精确清理报价/订单/通知/出库/应收/库存流水/日志 `1/12/16/8/6/25/46`；ADMIN/WAREHOUSE 均 `runtimeAccessCleaned=true`、`closed=true`、`principalDeleted=true`，`phaseErrors=[]`。
- 库存保持 on-hand `13,178,325`、reserved `38,135`、available `13,140,190`、version `42,030`、amount `110080.00`、unit cost `86.000000`。
- 非本批库存流水保持 count `45,797`，ID digest `aa093c6813b9052e4f713cf89d9e2ff8423d130b616c01453c618f9025478b44`，row digest `b540f08234ae528a3ebf9390cd672d8bd72a11e41fca3c5bd2d847d2949f3323`。
- 旧 ADMIN、全部 `r_a126_admin_* / r_a126_warehouse_*` principal、角色、授权、scope、四 schema actor logs、Redis session 均为 0；旧 `107.fixtures.jsonl` 与 retained suite lock 均不存在。

## Review、预算与保留边界

- 四任务集成代码 review 与 full 后 P1 修复 review 均无未关闭 P0–P3。
- 唯一 full 配额 1/1；提交 7/12；并发只读子代理最多 3。运行时 Goal token 计量器未启用，无法给出实际 token 数；`15,000,000` 是治理上限，不能由墙钟时间反推。
- 可核验 A184 执行窗口从首提交 2026-08-10 07:45 CST 到归档约 15:00 CST，约 7 小时 15 分；其中唯一 full 的 retained owner 窗口约 1 小时 2 分 31 秒。
- full 未 accepted 的 42 项中，A126、A136、A163、A184-A166 已在 final HEAD 上补充通过；其余 38 项本轮未补跑，不能声称为本轮通过。它们不是当前四项直接归属，本批按用户授权只做上述 final-HEAD targeted。
- 历史 A126 共 77 批仍保持原结论：76 批虽可证明来源闭包，但物理删除会破坏保留流水 append-only `qty_on_hand_after`；1 批缺 canonical posting facts。下一任务必须在 append-only 三量恢复日志与显式历史重述迁移之间做产品决策，当前工具继续硬阻断 apply。
- 未 push，未操作生产环境；未扩展跨币种转账、汇率、预收预付、退款、自动对账或银行直连。
