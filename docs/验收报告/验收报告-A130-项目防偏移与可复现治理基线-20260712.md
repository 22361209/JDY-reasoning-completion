# 验收报告：A130 项目防偏移与可复现治理基线

## 基本信息

- 验收日期：2026-07-12
- 基线提交：`40378478e48f5012777363002f11093b0a3bc87b`
- 当前分支：`codex/review-cleanup-20260627`
- 改动分类：共享工程核心 / 治理基线
- 目标等级：A4（治理工具本身）
- 业务范围：无 scope ID 变更；未修改业务代码、业务数据、页面或数据库结构
- 测试环境：本地前端 `5173`、后端 `8080`、PostgreSQL 16、Redis 7
- 测试账号：自动化使用现有 `admin`、`warehouse` 等回归账号；A130 未新增或修改账号

## 结论

A130 的治理工具达到 A4：有效范围、交付状态和回归清单已经成为可机器校验、受版本控制、可在干净 checkout 解析的三类真相源；F ID 不再随 Excel 行重排，全部 catalog 入口和六类能力状态也已纳入反向校验；当前快照和当前任务包恢复唯一职责。

这不表示整个 ERP 已通过发布门禁。本批首次按 tracked manifest 跑出的真实结果是：后端 48 项中 9 个 error，smoke 10 项中 6 项失败，full 109 项中 71 项失败。失败被完整保留并进入后续整改基线，没有为追求全绿而修改业务或旧测试。

## 实施范围

### 1. 有效范围

- 新增 `scripts/helpers/effective-feature-scope.mjs`，统一合并原始审批和实施 override。
- 新增生成物 `config/effective-feature-scope.json`。
- `export-approved-scope.mjs` 与 `generate-frontend-scope.mjs` 复用同一推导逻辑。
- 新增 `scripts/effective-scope-contract-check.mjs`，逐字节校验生成物新鲜度。
- 原始审批快照不应用 override，不因重复生成改变时间或内容。
- 导出按既有 `module + feature` 保存 F ID；真实 Excel dry-run 为 104 项、零内容变化，行重排保持 ID，重复/新增/删除/重命名身份均 fail closed。

当前统计：

- 原始/有效功能：104 项。
- 非 exclude 功能：93 项。
- 当前 override：仅 `F063`。
- `F021` 仍为 later，`F031` 仍为 exclude，发货通知未擅自新增 scope ID。

### 2. 交付状态

- 新增 `config/feature-delivery-status.json`。
- 新增 `scripts/feature-delivery-status-scan.mjs`。
- 台账只引用 effective scope ID，不复制或改变范围。
- 缺少当前证据的实现使用 `unverified + knownGaps`，没有猜测为完成。
- 93 个 feature 均包含入口、后端、权限、日志、自动化、浏览器六类 `capabilityChecks`；日志与自动化当前全部保守为 unverified，没有借历史报告猜测 A4。
- 56 个 catalog 入口全部反向入账：48 个由唯一 feature 认领，8 个因缺少正式 scope 或属于平台/工作台派生入口进入带原因的显式 exception；无重复 owner、无未登记入口。

当前分布：

| 维度 | 数量 |
| --- | ---: |
| verified | 35 |
| unverified | 38 |
| deferred | 20 |
| published | 25 |
| hold | 23 |
| hidden | 26 |
| deferred exposure | 19 |

台账已明确登记主数据稀疏更新、销售退货空壳、错误报表 fallback、账户/资金页面、Excel 导入等已知缺口。

### 3. 回归 manifest

- 新增 `config/regression-manifest.json`。
- 新增 `scripts/validate-regression-manifest.mjs`。
- `scripts/run-regression-tier.mjs` 的 smoke / area / full 全部从 tracked manifest 选集。
- `full` 不再读取 ignored `verification/a2-a110-regression-latest.json`。
- 新增 `--list`，无需启动服务即可验证 tier 清单。

当前统计：

| 项目 | 数量 |
| --- | ---: |
| 发现 `*-regression.mjs` | 130 |
| full 登记 | 109 |
| smoke | 10 |
| 历史聚合 runner 豁免 | 21 |

旧 full 的 99 项顺序被保留，并新增 A114、A115、A116、A116b、A118、A119 两项、A126、A128、A129 共 10 项。

### 4. 文档与 CI

- `docs/09` 改为唯一短快照。
- `docs/12` 改为唯一 A130 当前任务包，带 base commit、允许路径、明确不做事项、待决项和门禁。
- 原 `docs/12` 历史归档到 `docs/archive/2026-07-12-历史批次验收清单.md`。
- 原 `docs/15` 队列归档到 `docs/archive/2026-06-核心流稳定化追踪.md`，根文件只保留历史指针。
- 更新 README、接手指南、仓库地图、验收门禁和 ADR-014。
- 新增 `.github/workflows/project-gates.yml`，复用本地 scope/status/manifest、前端 build 和后端测试命令。
- CI 在校验前重生成 effective scope 与正式前端 `featureScope.ts` 并要求 tracked 文件零差异，stale frontend 生成物不能通过。

CI 文件已完成本地语法和命令验证；本报告验收时尚未提交、未推送，因此没有 GitHub Actions 远端运行记录。A130 快照中的 backend job 按真实基线预计为红，需由后续独立批次修复后端测试。

## 自动化结果

| 门禁 | 结果 | 证据/说明 |
| --- | --- | --- |
| effective scope contract | 通过 | 104 项，override=`F063` |
| 连续生成确定性 | 通过 | effective hash `d675b74e…a78878` 两次一致 |
| 原始审批保护 | 通过 | approved hash `997e2ca5…c00c11b` 不变 |
| delivery status scan | 通过 | 93 项唯一覆盖；56 catalog=`48 owner + 8 exception`；六类 capability checks 完整 |
| regression manifest scan | 通过 | 130=109 full+21 exemption；A113b/A116/A116b/A126/A128/A129 已进入对应 area |
| full `--list` | 通过 | 109 项，干净清单不依赖 verification |
| 前端 build | 通过 | 193 modules；仅保留既有大 chunk warning |
| A115 metadata | 通过 | `A115 metadata definition regression passed` |
| A124 lifecycle scan | 通过 | 16 个登记单据类型，0 failure/warning |
| A128 list scan | 通过 | `A128 list query contract scan passed` |
| 后端测试 | 失败 | 48 total / 9 errors / 0 failures / 0 skipped |
| smoke | 失败 | 10 total / 4 passed / 6 failed / 0 skipped |
| full | 失败 | 109 total / 38 passed / 71 failed / 0 skipped，耗时 665 秒 |
| `git diff --check` | 通过 | 无空白错误 |

运行产物：

- `verification/regression-tier-smoke-latest.json`
- `verification/regression-tier-full-continue-latest.json`
- `backend/target/surefire-reports/`

这些是 ignored 本地产物，不作为清单源；关键结论以本报告为准。

## 已知失败分类

### 后端 9 个 error

- 8 个测试夹具仍手工指定单号，与正式 `NumberingService` 自动编号规则冲突。
- 1 个主数据引用测试使用已退役/不可用的默认生产车间。

### full 71 个失败

| 主要类别 | 数量 | 说明 |
| --- | ---: | --- |
| 仓库夹具未满足已审核/启用规则 | 24 | 库存调整及其下游脚本被前置校验阻断 |
| 手工单号旧夹具 | 22 | 包含 21 个明确 409 和 A57 同类 409 |
| 浏览器等待超时 | 17 | 多数发生在旧分录/库存 UI 脚本 |
| 零/负数量旧夹具 | 2 | 当前后端正数量守卫生效 |
| 默认生产车间夹具 | 1 | A116 使用不存在/未审核/禁用引用 |
| 财务红冲断言 | 1 | 未生成旧脚本期待的负应收 |
| 打印参数断言 | 1 | 页面方向保存结果不符 |
| 库存预警夹具 | 1 | 预期仓库行未出现 |
| BOM 版本确认 | 1 | 新版本审核要求显式确认 |
| A119 后端聚合门禁 | 1 | 聚合执行命中上述 9 个后端 error |

这些分类是整改队列，不代表可批量修改生产规则。下一批应优先修测试夹具和可复现数据库基线，再分别诊断剩余业务断言。

## 范围待确认

- `F021 销售报价单`：原始审批为 later，但当前已有实现。
- `F031 采购申请`：原始审批为 exclude，但当前已有实现。
- 发货通知：已有实现和历史用户确认，但原始审批表没有 scope ID。

A130 未改变三项结论。对应业务继续扩展前，必须取得明确决策并写入 implementation override/addition 机制。

## 验收等级说明

- A130 治理工具：A4。具备自动化、真实全量运行、失败证据、文档、CI 配置和交接说明。
- 当前 ERP 发布状态：未通过。后端、smoke 和 full 均为红色基线。
- 本报告生成时的 worktree：未提交；最终提交状态和 commit ID 以 Git 历史为准，本报告不把验收时状态伪装成 verified commit。

## 下一步

进入独立“可复现基线”批次：

1. 只修 8 个手工单号测试夹具和 1 个默认车间测试夹具，不放松生产规则。
2. 在备份后对账 V90/V91 与 V94/V95，并额外验证“已发布 V95、无 90/91”升级拓扑；若低版本从未进入 Git，不得直接补回共享 source，应以正式 V94/V95 为准并用 Flyway 官方机制收敛本地历史。
3. 后端 48/48 后再处理回归脚本的仓库夹具、手工单号和 UI 超时簇。
4. 基线转绿后，进入权限 fail-closed 和库存直调入口整改。
