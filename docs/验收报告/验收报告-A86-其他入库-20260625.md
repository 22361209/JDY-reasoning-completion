# A86 其他入库验收报告

日期：2026-06-25

## 结论

A86 已完成。其他入库作为库存广度第一张新单，已按 Flyway 表 + 瘦 Controller + AppService + 前端模块 + 三层参考 + 新回归落地。

范围控制：本次只做其他入库；未改其他库存单、未改财务口径、未改单据编号规则；未重跑 `build-jdy-feature-approval.mjs`，未手改 `frontend/src/app/featureScope.ts`。

## 三层参考对齐

1. 范围参考

- `docs/02-第一版模块边界.md`：`库存管理 / 其他入库: 要 (P1, 第一版)`。
- `config/approved-feature-scope.json`：其他入库为 P1、第一版。

2. UI 参考

- 像素规范：`/Users/linzhenyue/Projects/JDY-复刻-local/02_复刻规划/像素级布局复刻规格-1221证据版.md` 的标准单据页和列表页约束。
- 真实截图：`/Users/linzhenyue/Projects/JDY-复刻-local/01_金蝶调研/截图/库存管理/库存H3/1692_其他入库单_新增初始态.*`、`1693_其他入库单_历史列表真实行态.*`。
- 对齐点：`QTRK` 单号、业务类型“其他入库”、单据日期、部门、供应商/往来字段、商品编码/名称、仓库、单位、数量、单位成本/金额、审核状态、列表筛选和高密度列表。
- 当前截图：
  - `verification/playwright/a86-other-stock-in-form-audited-20260625145310.png`
  - `verification/playwright/a86-other-stock-in-list-20260625145310.png`

3. 数据/机制参考

- `/Users/linzhenyue/Projects/HW-WIN2019/k3cloud-system-mechanism-notes.md` 第 10 节、第 31 节：库存按维度维护余额，同时写移动日志；其他入库属于简单库存增加移动。
- 本实现审核写正向库存移动，反审核写负向冲回；只影响 `inv_stock_balance` 与 `inv_stock_txn`，不产生应付和财务过账。

## 实现范围

- Flyway：`V32__other_stock_in_document.sql`
  - `other_stock_in`
  - `other_stock_in_line`
  - 权限 `inventory.other_stock_in.audit`
- 后端：
  - `OtherStockInController`
  - `OtherStockInAppService`
  - `BillLifecycleService` 表白名单补 `other_stock_in`
  - 列表 API `other-in-form-list`
- 前端：
  - `modules/inventory/other-stock-in/OtherStockInForm.vue`
  - `modules/inventory/other-stock-in/useOtherStockInDocument.ts`
  - 复用 `DocumentForm` / `StandardDocument` / `EntryTable` / 主数据选择器
  - `App.vue` 只做模块装配，没有业务逻辑
- 回归：
  - `scripts/a86-other-stock-in-regression.mjs`

## 后端瘦身检查

Controller grep：

```bash
rg -n "INSERT INTO|UPDATE |JdbcTemplate|PostingPipeline|BillStatus|status = '|AUDITED|DRAFT|REVERSED|VOID|inv_stock" \
  backend/src/main/java/com/jdy/erp/inventory/api/OtherStockInController.java
```

结果：无输出。Controller 只保留路由、权限注解和 AppService 转发。

AppService 复用骨架 grep 命中：

```text
BillLifecycleService
PostingPipeline
InventoryPostingHook
LookupService
BillStatus
OTHER_STOCK_IN
OTHER_STOCK_IN_REVERSE
```

## 库存断言

最新 A86 专项回归：`verification/a86-other-stock-in-regression.json`

```json
{
  "billNo": "QTRK-A86-20260625145310",
  "productCode": "CP-001",
  "warehouseCode": "CK-001",
  "qty": 7,
  "beforeQty": 1226606,
  "afterAuditQty": 1226613,
  "afterReverseQty": 1226606,
  "auditTxnCount": 1,
  "reverseTxnCount": 1,
  "statuses": {
    "afterAudit": "AUDITED",
    "afterReverse": "REVERSED"
  }
}
```

结论：审核后库存余额增加 7，写入 `OTHER_STOCK_IN` 流水；反审核后余额回到审核前，写入 `OTHER_STOCK_IN_REVERSE` 流水。

## 回归与构建

- `JAVA_HOME=/opt/homebrew/opt/openjdk@21 ./mvnw test`：通过。
- `npm --prefix frontend run build`：通过。
- `verification/a2-a86-regression-latest.json`：`ok=true`、`total=70`、`expectedTotal=70`、`badCount=0`、`skipped=0`。
- A70 在一次全量串跑中出现过瞬时失败，单脚本复跑通过后纳入最新 A2-A86 汇总；A86 专项随后多次通过。

## 视觉验收

- 表单截图：`verification/playwright/a86-other-stock-in-form-audited-20260625145310.png`
- 列表截图：`verification/playwright/a86-other-stock-in-list-20260625145310.png`
- 视觉结构与复刻-local 其他入库真实截图对齐，采用同一单据头、工具栏、分录表格、状态章和库存列表范式。

备注：本轮尝试使用 in-app Browser 插件做可视检查时，`node_repl` 返回 `sandboxCwd must be an absolute file URI`，因此改用 Playwright 截图和本地 `view_image` 完成视觉 QA。
