# 验收报告 A85 财务过账接审核

日期：2026-06-25

## 范围

- 销售出库审核接入 `FinancePosting`：自动写 `ar_receivable`，来源为销售出库单号。
- 采购入库审核接入 `FinancePosting`：自动写 `ap_payable`，来源为采购入库单号。
- 销售出库/采购入库反审核、红冲写负额冲销行，并与原库存过账处于同一个 `@Transactional` 方法内。
- 退役旧手动端点：`/api/finance/receivables/from-sales-order/{billNo}`、`/api/finance/payables/from-purchase-order/{billNo}`。
- 前端应收/应付列表文案改为出库/入库审核自动生成；未新增“从订单生成应收/应付”入口。

本批未改库存扣减逻辑、审核状态机、单据编号；未重跑 `build-jdy-feature-approval.mjs`，未手改 `frontend/src/app/featureScope.ts`。

## 自动生成断言

A85 专项脚本：`node scripts/a85-finance-posting-regression.mjs`

- 输出：`verification/a85-finance-posting-regression.json`
- batch：`20260625132315`
- 前端可见截图：
  - `verification/playwright/a85-receivable-list-20260625132315.png`
  - `verification/playwright/a85-payable-list-20260625132315.png`

脚本断言：

- `XSCK-A85-A-20260625132315` 审核后生成应收，来源为该销售出库单，金额 `208.00`；收款接口可继续核销自动应收，状态变为“部分核销”。
- `CGRK-A85-A-20260625132315` 审核后生成应付，来源为该采购入库单，金额 `328.00`；付款接口可继续核销自动应付，状态变为“部分核销”。
- `XSCK-A85-R-20260625132315` 反审核生成负额应收冲销 `-208.00`。
- `CGRK-A85-R-20260625132315` 反审核生成负额应付冲销 `-328.00`。
- `XSCK-A85-HC-20260625132315` 红冲生成负额应收冲销 `-208.00`。
- `CGRK-A85-HC-20260625132315` 红冲生成负额应付冲销 `-328.00`。

DB 原始断言：

```text
ar_auto_open | YS-XSCK-A85-RS-20260625132315 | XSCK-A85-RS-20260625132315 |  208.00 | received_amount 0.00 | OPEN
ar_reverse   | YS-CX-XSCK-A85-R-20260625132315 | XSCK-A85-R-20260625132315 | -208.00 | received_amount 0.00 | OPEN
ar_red       | YS-HC-1HGKYLN | XSCK-A85-HC-20260625132315 | -208.00 | received_amount 0.00 | OPEN

ap_auto_open | YF-CGRK-A85-RS-20260625132315 | CGRK-A85-RS-20260625132315 |  328.00 | paid_amount 0.00 | OPEN
ap_reverse   | YF-CX-CGRK-A85-R-20260625132315 | CGRK-A85-R-20260625132315 | -328.00 | paid_amount 0.00 | OPEN
ap_red       | YF-HC-C49DPH | CGRK-A85-HC-20260625132315 | -328.00 | paid_amount 0.00 | OPEN
```

## 退役端点

A85 专项脚本断言旧端点返回 404：

```text
POST /api/finance/receivables/from-sales-order/{billNo} -> 404
POST /api/finance/payables/from-purchase-order/{billNo} -> 404
```

保留端点：

```text
POST /api/finance/receivables/{billNo}/receipt
POST /api/finance/payables/{billNo}/payment
```

## 回归脚本清单

本批新增财务相关回归脚本：

- `scripts/a85-finance-posting-regression.mjs`

本批未修改既有 A2-A77 回归脚本。全量 runner 使用 `verification/a2-a84-regression-latest.json` 的 69 条既有脚本顺序执行，最终输出：

- `verification/a2-a85-regression-latest.json`
- `ok=true`
- `total=69`
- `expectedTotal=69`
- `badCount=0`
- `failed=null`

## 构建与后端测试

```text
npm --prefix frontend run build
结果：通过；Vite build 成功，仍有既有 chunk size warning，未失败。
```

```text
JAVA_HOME=/opt/homebrew/opt/openjdk@21 ./mvnw test
结果：BUILD SUCCESS；Tests run: 1, Failures: 0, Errors: 0, Skipped: 0。
日志包含 ArchUnit PluginLoader: Detected Java version 21.0.11。
```

## 结论

A85 完成。应收/应付产生时点已从订单手动生成切换到销售出库/采购入库审核自动生成；反审核/红冲生成负额冲销；旧手动端点已退役；收款/付款继续消费自动生成的应收/应付；既有非财务回归 69/69 全绿。
