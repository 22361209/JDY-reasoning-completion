# 验收报告 - 仓库资料分离 - 2026-06-27

## 结论

本批只做仓库清洁，不改业务功能、不改生成链、不重跑 `build-jdy-feature-approval.mjs`。

- `verification/` 历史运行产物已从实现仓库工作区迁出，完整归档到 `/Users/linzhenyue/Projects/JDY-资料库/verification-archive-20260627`。
- 实现仓库内 `verification/` 仅保留 `.gitkeep` 和本地服务当前日志目录，后续仍按本地临时产物处理，不进 Git。
- `outputs/` 暂不迁移，因为 `outputs/jdy-feature-approval/JDY复刻功能审批表.xlsx` 是审批范围生成链输入，移动会影响 `export-approved-scope.mjs`。
- `.claude/` 已加入忽略规则，避免个人工具目录进入实现提交。

## 迁移明细

| 项目 | 处理 |
| --- | --- |
| `verification/*.json` | 归档到资料库，并从 Git 跟踪中移除 |
| `verification/*.pdf` | 归档到资料库，并从 Git 跟踪中移除 |
| `verification/playwright/` | 归档到资料库，并从 Git 跟踪中移除 |
| `verification/logs/` | 保留当前本地运行日志；目录内容仍被忽略 |
| `verification/.gitkeep` | 新增，用于保留运行产物目录骨架 |
| `outputs/` | 保留在实现仓库，属于审批生成链的必要输入/少量证据 |

## 验证

```bash
du -sh verification
git ls-files verification
find /Users/linzhenyue/Projects/JDY-资料库 -maxdepth 2 -type d
./scripts/dev-status.sh
```

结果：

- 迁移前 `verification/` 约 2.3G；迁移后实现仓库内 `verification/` 约 20K。
- 资料库归档目录大小约 2.3G。
- `git ls-files verification` 只保留 `verification/.gitkeep`。
- 本地前后端服务仍可正常检查状态。

## 备注

本批不重写 Git 历史。由于历史提交曾包含大量 `verification/` 产物，`.git/` 目录仍然较大；若未来要彻底瘦身，需要单独做“重写历史/重新建仓”批次，并确认所有协作者重新拉取。
