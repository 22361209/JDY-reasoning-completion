# 验收报告 A98 源单模型

- 日期：2026-06-26
- 范围：Wave 2 #8/#9；销售出库/采购入库源单号行级化，销售出库多源追加、行级追溯、重复下推状态守卫。未改税务、编号；未重跑 `build-jdy-feature-approval.mjs`；未手改生成物或 `frontend/src/app/featureScope.ts`。

## 实现摘要

1. Flyway `V43__line_level_source_order_no.sql` 为 `sales_out_line`、`purchase_in_line` 增加 `source_order_no` 并回填历史表头源单，建立行级源单索引。
2. 销售出库/采购入库保存、详情、列表、打印/导出改为分录行展示源单号；表头不再承载单一源单。
3. 销售出库从源订单下推/追加明细时，每行携带 `sourceOrderNo + sourceLineNo`；审核/反审核/红冲回写订单执行数量按行级源单匹配。
4. “拉入明细”改为“从源订单追加明细”；“源单单号”改为行级源单追溯，在新页签只读查看源订单，不切走当前出库草稿。
5. 订单剩余数量超量守卫保留，重复凭空下推超过剩余时后端拒绝。

## 回归断言

- 新增 `scripts/a98-line-level-source-regression.mjs`
- 新增断言：`多源单出库行级源单`
- 新增断言：`下推后源单追溯不丢明细不重复下推`
- 更新 A5/A15/A16/A19/A22/A33/A89/A92/A93/A9 等源单追踪、源行追溯、选单/下推回归为行级源单语义。

## 验证结果

- `node scripts/a98-line-level-source-regression.mjs`：通过，`verification/a98-line-level-source-regression.json` 记录 `duplicateAuditStatus=409`，多源销售出库 `lineSources` 来自两张销售订单。
- `node scripts/run-a2-a98-regression.mjs`：通过，脚本数 `86`，`ok=86`，`expectedTotal=86`，`badCount=0`，`skipped=0`。
- `verification/a2-a98-regression-latest.json`：`ok=true`，mtime `2026-06-26 12:13:35 +0800`（提交后最终 HEAD 已重跑，以最终答复 mtime 为准）。
- `npm --prefix frontend run build`：通过。
- `cd backend && JAVA_HOME=/opt/homebrew/opt/openjdk@21 ./mvnw test`：通过，含 ArchUnit 上下文测试。

## 风险与边界

- 动到销售/采购源单模型、销售出库下推流程、订单执行数量回写和输出列；库存/财务过账、税务金额模型、编号规则未扩展改动。
- `sales_out.source_order_id`、`purchase_in.source_order_id` 暂保留兼容旧数据/旧接口，但业务展示和回写以分录行源单为准。
- 无临时桩；验证截图见 `verification/playwright/a98-multi-source-line-trace-20260626041333.png`。
