# 验收报告 A27：分录数量单价连续录入

验收日期：2026-06-24  
回归批次：`20260624090138`  
结论：PASS

## 验收范围

本批补齐销售订单分录录入的最后一段键盘路径：数量格回车进入单价格，单价格回车新增下一行并聚焦新行商品编码。

## 断言结果

| 项目 | 结果 |
| --- | --- |
| 数量 `Enter` 后焦点为 `sales-line-price` | PASS |
| 单价 `Enter` 后焦点为 `sales-line-product-2` | PASS |
| 单价 `Enter` 后分录行数为 2 | PASS |
| 第 1 行金额 `63.00 = 3 × 21` | PASS |
| 第 2 行金额 `10.00 = 2 × 5` | PASS |
| 合计金额 `73.00` | PASS |
| 保存后后端分录为 `CP-001 / CK-001 / 3 / 21` 与 `PJ-014 / CK-002 / 2 / 5` | PASS |
| A26 主数据选择器选后跳格回归 | PASS |

## 自动化证据

- 主脚本：`scripts/a27-entry-enter-next-line-regression.mjs`
- 结构化结果：`verification/a27-entry-enter-next-line-regression.json`
- 兼容回归：`scripts/a26-selector-next-field-regression.mjs`

## 截图

- 连续录入两行表单：`verification/playwright/a27-entry-enter-next-line-form-20260624090138.png`
- A26 选择器跳格回归：`verification/playwright/a26-selector-next-field-form-20260624090109.png`

## 代码说明

- `handleLineCellKeydown` 将普通 `Enter` 与上下箭头移动拆开。
- 数量格 `Enter` 聚焦同一行单价格。
- 单价格 `Enter` 复用 `insertLineAfter`，新增下一行并聚焦商品编码。
- `Ctrl/⌘ + Enter` 插入下一行的旧快捷路径保留。

## 验证命令

```bash
node scripts/a27-entry-enter-next-line-regression.mjs
node scripts/a26-selector-next-field-regression.mjs

cd "/Users/linzhenyue/Projects/JDY 推理补完/frontend"
npm run build

cd "/Users/linzhenyue/Projects/JDY 推理补完/backend"
export JAVA_HOME="/opt/homebrew/opt/openjdk@21/libexec/openjdk.jdk/Contents/Home"
export PATH="/opt/homebrew/opt/openjdk@21/bin:$PATH"
./mvnw test
```

## 护栏

- 未重跑 `build-jdy-feature-approval.mjs`。
- 未手改任何生成物：`docs/01/02/03`、`config/approved-feature-scope.json`、`app/feature-data.js`、`frontend/src/app/featureScope.ts`。
- 本批未调整范围例外，未改 `config/implementation-overrides.json`。
