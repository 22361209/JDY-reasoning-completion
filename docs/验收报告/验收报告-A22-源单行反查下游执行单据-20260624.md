# 验收报告 A22：源单行反查下游执行单据

验收日期：2026-06-24  
回归批次：`20260624082117`  
结论：PASS

## 验收范围

本批补齐源订单详情的分录级执行追踪：销售订单、采购订单的已执行数量在存在下游执行单据时可点击，弹窗列出下游单据，并可直接打开下游详情。

## 断言结果

| 方向 | 源单 | 源行 | 下游单据 | 下游行 | 数量 | 金额 | 结果 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 销售 | `XSDD-A22-20260624082117` | #3 | `XSCK-A22-20260624082117` | #1 | 2 | 24.00 | PASS |
| 采购 | `CGDD-A22-20260624082117` | #3 | `CGRK-A22-20260624082117` | #1 | 3 | 24.00 | PASS |

## 自动化证据

- 回归脚本：`scripts/a22-source-line-downstream-regression.mjs`
- 结构化结果：`verification/a22-source-line-downstream-regression.json`
- Playwright 断言：
  - 后端详情 API 返回第 3 行 `downstreamDocs.length = 1`。
  - 下游单据类型、单号、源行、下游行、数量、金额正确。
  - 销售订单第 3 行点击已执行数量后弹出销售出库执行单据。
  - 采购订单第 3 行点击已执行数量后弹出采购入库执行单据。
  - 点击弹窗下游单号后打开下游详情，源订单号保持正确。

## 截图

- 销售下游弹窗：`verification/playwright/a22-sales-downstream-dialog-20260624082117.png`
- 销售下游详情：`verification/playwright/a22-sales-downstream-open-20260624082117.png`
- 采购下游弹窗：`verification/playwright/a22-purchase-downstream-dialog-20260624082117.png`
- 采购下游详情：`verification/playwright/a22-purchase-downstream-open-20260624082117.png`

## 代码说明

- 销售订单详情 API 在分录上返回 `downstreamDocs`，来源为已审核销售出库单。
- 采购订单详情 API 在分录上返回 `downstreamDocs`，来源为已审核采购入库单。
- 弹窗中明确区分源行和下游行，避免用下游单据行号冒充源单行号。

## 验证命令

```bash
node scripts/a22-source-line-downstream-regression.mjs

cd "/Users/linzhenyue/Projects/JDY 推理补完/frontend"
npm run build

cd "/Users/linzhenyue/Projects/JDY 推理补完/backend"
export JAVA_HOME="/opt/homebrew/opt/openjdk@21/libexec/openjdk.jdk/Contents/Home"
export PATH="/opt/homebrew/opt/openjdk@21/bin:$PATH"
./mvnw test
```

## 护栏

- 未重跑 `build-jdy-feature-approval.mjs`。
- 未手改任何生成物：`docs/01/02/03`、`config/approved-feature-scope.json`、`app/feature-data.js`、`frontend/src/app/featureScope.ts`。
- 本批未调整范围例外，未改 `config/implementation-overrides.json`。
