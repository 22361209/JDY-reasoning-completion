# 验收报告 A94 税务

- 日期：2026-06-26
- 范围：销售订单、销售出库、采购订单、采购入库的单价含税/不含税、每行税率、税额、价税合计、A85 财务过账含税金额口径。
- 机制对齐：K3Cloud 行税务字段口径 `FTAXPRICE/FPRICE/FTAXRATE/FTAXAMOUNT/FALLAMOUNT`，本仓库落为 `unit_price/amount/tax_rate/tax_amount/price_tax_total`，单头 `is_tax_inclusive` 控制单价口径。

## 变更摘要

- Flyway `V40__document_tax_amounts.sql` 给四类销售/采购单头加 `is_tax_inclusive`，分录加 `tax_rate/tax_amount/price_tax_total`，并把历史 `total_amount` 回填为价税合计。
- 后端保存草稿按含税/不含税口径推导净额、税额、价税合计；下推、选单、红冲保留税率和含税标识。
- 前端单头增加价格口径切换，分录增加税率、税额、价税合计列，合计行显示价税合计。
- A85 财务过账继续走审核/反审核/红冲管线，但金额来源变为单据 `total_amount` 的价税合计。
- 库存数量逻辑未改，库存流水仍只按数量过账。

## 专项断言

- `scripts/a94-tax-amount-regression.mjs` 覆盖：
  - 销售出库不含税单价：13% 与 9% 两行税率分别推导税额与价税合计。
  - 采购入库含税单价：13% 与 9% 两行税率反推不含税金额、税额与价税合计。
  - 应收/应付金额等于价税合计。
  - UI 显示税率、税额、价税合计，合计行落在价税合计列。
- `scripts/a85-finance-posting-regression.mjs` 已更新为含税口径：销售出库应收 `235.04`，采购入库应付 `370.64`，反审核/红冲生成等额负数冲销。

## 验证结果

- `npm --prefix frontend run build`：通过。
- `JAVA_HOME=/opt/homebrew/opt/openjdk@21 ./mvnw test`：通过，含 ArchUnit。
- `node scripts/a94-tax-amount-regression.mjs`：通过。
- `node scripts/a85-finance-posting-regression.mjs`：通过。
- `node scripts/run-a2-a94-regression.mjs`：通过。

全量结果：

```json
{
  "ok": true,
  "total": 82,
  "expectedTotal": 82,
  "badCount": 0,
  "skipped": 0,
  "resultPath": "verification/a2-a94-regression-latest.json",
  "mtime": "2026-06-26T00:19:47.985Z"
}
```

## 验收结论

A94 达到 A3 业务链路回归。金额/应收/应付口径已从净额切换为价税合计；销售/采购四类核心单据支持单头含税/不含税切换和每行税率调整。库存数量逻辑未变。
