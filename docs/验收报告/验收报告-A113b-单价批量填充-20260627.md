# 验收报告：A113b 单价批量填充与价格来源

日期：2026-06-27
范围：销售订单 / 发货通知 / 销售出库分录表“单价”列头批量填充

## 本批完成

1. 后端新增 `GET /api/sales-prices/unit-price-sources`，按客户 + 商品集合返回真实价格来源。
2. 价格来源包括：默认价格、最近成交价、历史最高价、历史最低价、历史平均价、库存加权成本价。
3. 前端 `EntryTable` 新增单价列头批量填充弹窗：价格来源下拉、`+/-/*//` 运算符、系数输入、确定写回。
4. 销售订单、发货通知、销售出库共用同一套批填能力；非销售链未开启。
5. 没有前端假价格；成本价来自 `inv_stock_balance.unit_cost` 加权均价，暂无成本时接口仍返回该来源但标记不可用。

## 验收结果

- `npm --prefix frontend run build`：通过。
- `JAVA_HOME=/opt/homebrew/Cellar/openjdk@21/21.0.11/libexec/openjdk.jdk/Contents/Home ./mvnw test`：通过，13 tests，0 failures。
- `node scripts/a113b-unit-price-bulk-fill-regression.mjs`：通过。
- `node scripts/run-regression-tier.mjs smoke`：通过，10/10，0 skipped。

## A113b 专项断言

脚本：`scripts/a113b-unit-price-bulk-fill-regression.mjs`

- 造数：2 个商品、1 个客户、2 张已审核销售订单历史价。
- 接口断言：
  - 商品 A 默认价 = `101`，历史最高价 = `150`。
  - 商品 B 历史最高价 = `260`。
  - 成本价来源字段存在，不用前端假数据。
- 前端断言：
  - 打开销售订单新增页。
  - 两行分录分别填商品 A / 商品 B。
  - 单价列头选择“历史最高价”，运算符 `+`，系数 `5`。
  - 批填后两行单价分别为 `155`、`265`。

## 截图证据

- `verification/playwright/a113b-unit-price-bulk-popup-20260627070837.png`
- `verification/playwright/a113b-unit-price-bulk-applied-20260627070837.png`

## 边界说明

- 未重跑 `build-jdy-feature-approval.mjs`。
- 未手改生成物（含 `frontend/src/app/featureScope.ts`）。
- 未改单据保存、审核、库存、财务口径；本批只新增价格查询接口和销售链前端批填交互。
- 8080 后端曾因 shell `JAVA_HOME` 不可见导致 `./mvnw test` 首次失败，已反查当前 JDK 路径后通过。
