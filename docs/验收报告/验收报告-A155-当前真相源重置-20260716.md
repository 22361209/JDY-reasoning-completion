# 验收报告：A155 当前真相源重置

## 验收范围

- 将已关闭的 A144–A153 6E 任务合同从当前入口归档为历史合同。
- 重置 `docs/09-交接清单.md` 为当前 `main` 分支、0–6E 已关闭、无活动任务的快照。
- 将 `docs/12-当前批次验收清单.md` 改为 idle 模板，并要求后续任务声明产品目标、预算、范围和停止条件。
- 让交付状态扫描接受显式 `taskKind: governance` 且无 remediation roadmap 坐标的治理/idle 状态；不放松 roadmap 任务的坐标校验。

## 通过项

- A144–A153 历史合同保留在 `docs/archive/2026-07-16-A144-A153-6E-关闭任务合同.md`，正文与原合同一致，仅增加 archive frontmatter 标识。
- 当前快照不再出现旧分支、未提交 A153、待 staged review 或“当前 6E”描述。
- `docs/09` 和 `docs/12` 使用同一 governance 空坐标；roadmap 任务仍要求恰好一个 item 或 gate。
- 当前任务模板不包含已关闭合同的 168 个允许路径、执行日志或过期“不 push”指令。
- A153 永久关单脚本继续把当前 `docs/09` / `docs/12` 视为已提交的 archived replay 受控文件，不把历史任务正文当作当前状态。

## 验证

```text
node scripts/effective-scope-contract-check.mjs PASS
node scripts/feature-delivery-status-scan.mjs PASS
node scripts/validate-regression-manifest.mjs PASS
git diff --check PASS
```

## 范围外

- 文档索引、历史路线状态、现行多账套说明和 Markdown 链接修复由 A156 处理。
- 证据保存策略和文档治理脚本由 A157 处理。
- 不修改业务代码、生成范围、交付状态、回归 manifest 或历史验收报告正文。

最终结论：通过
