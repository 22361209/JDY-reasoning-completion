# 验收报告 A31：零值原因写入分录备注

验收日期：2026-06-24  
回归批次：`20260624093401`  
结论：PASS

## 验收范围

本批把 A30 的零数量/零单价确认从“一次性提示”升级为可追溯数据：确认弹窗选择业务说明后，前端写入分录备注并随草稿保存；后端四类核心单据行新增 `line_remark` 并在详情接口返回。

## 断言结果

| 项目 | 结果 |
| --- | --- |
| 零值确认弹窗可选择第 1 行 `样品`、第 2 行 `补录` | PASS |
| 保存后界面第 1 行备注为 `零值原因：样品（单价为 0）` | PASS |
| 保存后界面第 2 行备注为 `零值原因：补录（数量为 0）` | PASS |
| 后端销售订单详情读回三行备注：样品、补录、空备注 | PASS |
| A30 零数量/零单价保存确认回归 | PASS |

## 自动化证据

- 主脚本：`scripts/a31-zero-value-reason-remark-regression.mjs`
- 结构化结果：`verification/a31-zero-value-reason-remark-regression.json`
- 兼容回归：`scripts/a30-entry-zero-value-confirm-regression.mjs`
- 兼容回归结果：`verification/a30-entry-zero-value-confirm-regression.json`

## 截图

- 零值原因选择弹窗：`verification/playwright/a31-zero-value-reason-dialog-20260624093401.png`
- 保存后分录备注：`verification/playwright/a31-zero-value-reason-saved-20260624093401.png`
- A30 兼容回归：`verification/playwright/a30-entry-zero-value-confirm-saved-20260624093329.png`

## 代码说明

- 新增迁移 `backend/src/main/resources/db/migration/V9__document_line_remarks.sql`，给销售订单、采购订单、销售出库、采购入库分录增加 `line_remark`。
- 前端分录表增加备注列，保存 payload 携带 `lineRemark`。
- 零值确认弹窗增加业务说明下拉，确认时写入 `零值原因：...` 行备注。
- 后端保存和详情接口贯通 `lineRemark`，红冲生成单复制源行备注。

## 验证命令

```bash
node scripts/a31-zero-value-reason-remark-regression.mjs
node scripts/a30-entry-zero-value-confirm-regression.mjs

cd "/Users/linzhenyue/Projects/JDY 推理补完/frontend"
npm run build

cd "/Users/linzhenyue/Projects/JDY 推理补完/backend"
export JAVA_HOME="/opt/homebrew/opt/openjdk@21/libexec/openjdk.jdk/Contents/Home"
export PATH="/opt/homebrew/opt/openjdk@21/bin:$PATH"
./mvnw test
```

## 护栏

- 未重跑 `build-jdy-feature-approval.mjs`。
- 未手改任何生成物：`docs/01/02/03`、`config/approved-feature-scope.json`、`app/feature-data.js`、`frontend/src/app/featureScope.ts`。
- 本批未调整范围例外，未改 `config/implementation-overrides.json`。
