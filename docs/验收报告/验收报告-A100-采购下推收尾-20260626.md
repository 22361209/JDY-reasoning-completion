# 验收报告 A100：采购下推收尾

- 日期：2026-06-26
- 范围：采购订单下推采购入库直连、采购入库行级多源、删除旧下推确认弹窗/按比例机制。未改税务、编号；未重跑 `build-jdy-feature-approval.mjs`；未手改生成物或 `frontend/src/app/featureScope.ts`。

## 实现摘要

1. 采购订单列表/详情下推采购入库改为直接打开采购入库草稿，不再弹下推确认对话框。
2. 采购下推草稿按剩余可入数量带入商品、仓库、数量、单价、行备注和行级源单；数量在采购入库单内修改。
3. 采购入库保存/详情/审核按分录行 `sourceOrderNo + sourceLineNo` 处理，一张采购入库可来自多张采购订单。
4. 删除共享下推确认弹窗模板、App 级 pending 状态、比例/清零/全剩余/选中行/应用仓库/确认生成草稿逻辑，以及相关空绑定和 CSS。
5. 后端审核阶段剩余数量超量守卫保留，A100 专项断言重复超量审核返回 409。

## 脚本变更

新增：

- `scripts/a100-purchase-direct-push-regression.mjs`
- `scripts/run-a2-a100-regression.mjs`

改写：

- `scripts/a9-partial-pushdown-regression.mjs`：采购下推改为等待采购入库草稿，断言确认弹窗不存在。
- `scripts/a11-push-confirm-regression.mjs`：改写为销售/采购直连后的退役说明。
- `scripts/a14-push-confirm-bulk-tools-regression.mjs`：改写为旧批量工具退役说明。
- `scripts/a18-push-confirm-selected-lines-regression.mjs`：改写为旧选中行调整退役说明。
- `scripts/a21-push-confirm-bidirectional-regression.mjs`：改写为双方向确认弹窗退役说明。
- `scripts/a24-push-confirm-selected-warehouse-regression.mjs`：改写为旧确认框应用仓库退役说明。

删除脚本：无。

## 验证结果

- `npm --prefix frontend run build`：通过。
- `cd backend && JAVA_HOME=/opt/homebrew/opt/openjdk@21 ./mvnw test`：通过，含 ArchUnit 上下文测试。根目录无 `./mvnw`，实际 wrapper 位于 `backend/`。
- `node scripts/a100-purchase-direct-push-regression.mjs`：通过，`verification/a100-purchase-direct-push-regression.json` 记录采购直连、多源行级源单、超量 409。
- `node scripts/run-a2-a100-regression.mjs`：预提交通过，脚本数 `88`，`ok=true`，`expectedTotal=88`，`badCount=0`，`skipped=0`。
- `verification/a2-a100-regression-latest.json`：预提交 mtime `2026-06-26 13:04:20 +0800`；最终提交后已重跑，以最终答复 mtime 为准。

## 风险与边界

- 动到共享前端弹窗组件、App 下推路径、销售/采购/库存/生产表单的旧空绑定、A9/A11/A14/A18/A21/A24 回归脚本和 A100 聚合脚本。
- 未扩展库存/财务/税务/编号规则；采购订单执行数量仍由后端审核/反审核/红冲阶段按行级源单回写。
- 采购单据当前无“交期”字段/schema，本批未为采购新增 schema；销售侧交期携带保持 A92/A93 行为。
- 无临时桩；截图证据见 `verification/playwright/a100-purchase-direct-push-20260626050058.png`。
