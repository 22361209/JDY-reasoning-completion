# 验收报告：A165 BOM 页面工作区高度收口

## 结论

A164 已将 BOM 子件明细改为表内滚动；A165 进一步将 BOM 页面主体改成受限 flex 工作区，消除了表体之外的额外页面纵向滚动。

## 实现与真实页面证据

- `master-record-body` 改为纵向 flex 且受限高度；基本信息、产品信息保持固定高度。
- “子件明细”区使用 `flex: 1` 取得剩余高度，表格框架同样填满该区域；表体继续使用既有 `overflow-y: auto`。
- 已登录浏览器实测：`.content-area` `clientHeight=723px`、`scrollHeight=723px`；BOM 页面本身不再溢出。子件区高度为 308px，表体有独立 200px 的可用滚动高度。

## 验证

```text
npm run build                                PASS
node scripts/run-regression-tier.mjs smoke  PASS (10/10, badCount=0)
node scripts/docs-governance-check.mjs       PASS
git diff --check                             PASS
```

## 保留边界

不改共享 `TableCore`、BOM 业务逻辑、后端接口或材料报废等其他表格入口；不运行 full，不 push。
