# A9 订单剩余数量下推验收报告

验收日期：2026-06-24  
回归批次：`20260624063328`  
结论：PASS

## 验收范围

本次补齐销售订单、采购订单在已有部分执行后，再次下推时必须按剩余数量生成下游草稿，避免重复全量出库/入库。

| 方向 | 源单 | 已执行数量 | 下推草稿数量断言 | 截图 |
| --- | --- | --- | --- | --- |
| 销售订单 -> 销售出库 | `XSDD-A9-20260624063328` | 4 / 3 / 2 | PASS，剩余数量为 6 / 5 / 4 | `verification/playwright/a9-sales-remaining-pushdown-20260624063328.png` |
| 采购订单 -> 采购入库 | `CGDD-A9-20260624063328` | 5 / 4 / 3 | PASS，剩余数量为 6 / 5 / 4 | `verification/playwright/a9-purchase-remaining-pushdown-20260624063328.png` |

## 后端约束

API 绕过前端直接超量审核时，后端必须拒绝：

| 单据 | 断言 |
| --- | --- |
| `XSCK-A9-OVER-20260624063328` | PASS，审核返回 409 |
| `CGRK-A9-OVER-20260624063328` | PASS，审核返回 409 |

## 自动化证据

- 回归脚本：`scripts/a9-partial-pushdown-regression.mjs`
- 结构化结果：`verification/a9-partial-pushdown-regression.json`
- Playwright 断言：
  - 从销售订单列表勾选已审核且部分出库的订单，点击“销售出库”。
  - 销售出库草稿源单号正确，三行数量为剩余可出数量 `6 / 5 / 4`。
  - 从采购订单列表勾选已审核且部分入库的订单，点击“采购入库”。
  - 采购入库草稿源单号正确，三行数量为剩余可入数量 `6 / 5 / 4`。

## 验证命令

```bash
cd "/Users/linzhenyue/Projects/JDY 推理补完/frontend"
npm run build

cd "/Users/linzhenyue/Projects/JDY 推理补完/backend"
export JAVA_HOME="/opt/homebrew/opt/openjdk@21/libexec/openjdk.jdk/Contents/Home"
export PATH="/opt/homebrew/opt/openjdk@21/bin:$PATH"
./mvnw test

cd "/Users/linzhenyue/Projects/JDY 推理补完"
npx --yes --package playwright node scripts/a9-partial-pushdown-regression.mjs
```
