# A13 分录行排序插入验收报告

验收日期：2026-06-24  
回归批次：`20260624070538`  
结论：PASS

## 验收范围

本次补齐草稿单据分录的高频编辑动作：行后插入、键盘插入、拖拽调整行顺序。目标是让员工在录入多行明细时可以快速整理顺序，不需要删除重录。

| 项目 | 断言 | 结果 |
| --- | --- | --- |
| 按钮插入 | 点击首行“插入”后，首行后新增一行 | PASS |
| 按钮插入顺序 | 插入并填入后，商品顺序为 `CP-001 / CP-T413874` | PASS |
| 键盘插入 | 在第二行数量输入框按 `Ctrl/Cmd+Enter` 后，在当前行后新增一行 | PASS |
| 键盘插入顺序 | 填入第三行后，商品顺序为 `CP-001 / CP-T413874 / PJ-014` | PASS |
| 拖拽排序 | 将第三行拖到第一行位置后，商品顺序为 `PJ-014 / CP-001 / CP-T413874` | PASS |

## 视觉证据

- `verification/playwright/a13-entry-row-reorder-20260624070538.png`

## 自动化证据

- 回归脚本：`scripts/a13-entry-row-reorder-regression.mjs`
- 结构化结果：`verification/a13-entry-row-reorder-regression.json`

## 验证命令

```bash
cd "/Users/linzhenyue/Projects/JDY 推理补完/frontend"
npm run build

cd "/Users/linzhenyue/Projects/JDY 推理补完/backend"
export JAVA_HOME="/opt/homebrew/opt/openjdk@21/libexec/openjdk.jdk/Contents/Home"
export PATH="$JAVA_HOME/bin:$PATH"
./mvnw test

cd "/Users/linzhenyue/Projects/JDY 推理补完"
npx --yes --package playwright node scripts/a13-entry-row-reorder-regression.mjs
```
