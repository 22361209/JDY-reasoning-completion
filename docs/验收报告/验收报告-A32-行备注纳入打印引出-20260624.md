# 验收报告 A32：行备注纳入打印/引出

验收日期：2026-06-24
回归批次：`20260624093912`
结论：PASS

## 验收范围

本批把 A31 的分录备注继续贯通到单据输出：CSV 引出和打印 HTML 均增加备注列，保证零值原因随单据归档和流转。

## 断言结果

| 项目 | 结果 |
| --- | --- |
| CSV 表头包含 `备注` 列 | PASS |
| CSV 正文包含 `零值原因：样品（单价为 0）` | PASS |
| CSV 正文包含 `零值原因：补录（数量为 0）` | PASS |
| 打印 HTML 表头包含 `<th>备注</th>` | PASS |
| 打印 HTML 正文包含两条零值原因备注 | PASS |
| 打印 HTML 备注列带 `remark` 样式 | PASS |

## 自动化证据

- 主脚本：`scripts/a32-line-remark-output-regression.mjs`
- 结构化结果：`verification/a32-line-remark-output-regression.json`
- 验收单据：`XSDD-A32-20260624093912`

## 代码说明

- `DocumentOutputController` 的 CSV 表头和行数据新增备注列。
- 打印 HTML 表格新增备注列，并增加基础换行样式。
- 销售订单、采购订单、销售出库、采购入库输出查询均读取 `line_remark`。

## 验证命令

```bash
node scripts/a32-line-remark-output-regression.mjs

cd "/Users/linzhenyue/Projects/JDY 推理补完/backend"
export JAVA_HOME="/opt/homebrew/opt/openjdk@21/libexec/openjdk.jdk/Contents/Home"
export PATH="/opt/homebrew/opt/openjdk@21/bin:$PATH"
./mvnw test

cd "/Users/linzhenyue/Projects/JDY 推理补完/frontend"
npm run build
```

## 护栏

- 未重跑 `build-jdy-feature-approval.mjs`。
- 未手改任何生成物：`docs/01/02/03`、`config/approved-feature-scope.json`、`app/feature-data.js`、`frontend/src/app/featureScope.ts`。
- 本批未调整范围例外，未改 `config/implementation-overrides.json`。
