# 验收报告 A105 状态机

## 范围

- 单据关闭/反关闭：表头 `close_status`，行级 `line_close_status`，可逆，记录原因。
- 冻结/解冻：表头 `frozen_status`，行级 `line_frozen_status`，可逆，记录原因。
- 作废硬化：统一危险操作入口，账号密码二次验证，已有下游影响禁止作废。
- 下推/执行守卫：销售/采购源单整单或行级关闭/冻结后，不再参与可选行、下推和审核执行。

## 规则对齐

- 按 `docs/14-单据平台骨架.md` 的“关闭 vs 作废”和“冻结/解冻”绑定规则实现。
- 不做终止。
- 保留反审核、红冲、库存和财务过账逻辑。
- 未改 A102 UI/价格/锁逻辑；未重跑 `build-jdy-feature-approval.mjs`；未手改 `featureScope.ts`。

## 验证

| 门禁 | 结果 |
| --- | --- |
| `npm --prefix frontend run build` | 通过 |
| `JAVA_HOME=/opt/homebrew/opt/openjdk@21 ./mvnw test` | 通过 |
| `node scripts/a105-bill-lifecycle-regression.mjs` | 通过，6 项断言 |
| `node scripts/run-a2-a105-regression.mjs` | 通过，91/91，0 failed，0 skipped |

## 回归产物

- 专项结果：`verification/a105-bill-lifecycle-regression.json`
- 全量结果：`verification/a2-a105-regression-latest.json`
- 全量结果 mtime：提交后按硬门禁重跑，以最终文件 mtime 为准。
- UI 截图：
  - `verification/playwright/a105-lifecycle-danger-dialog-20260626080052.png`
  - `verification/playwright/a105-lifecycle-line-actions-20260626080052.png`

## 结论

A105 后端骨架级状态机已接入共享 `BillLifecycleService`，前端普通按钮和作废危险区可见；作废硬化、关闭/冻结两级状态、下推/执行跳过均有回归断言覆盖。
