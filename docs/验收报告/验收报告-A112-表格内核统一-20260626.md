# 验收报告 A112 表格内核统一

## 验收日期

2026-06-26

## 验收范围

- 列表 `DataListPage` 与分录 `EntryTable` 统一到共享表格原语：`TableCoreFrame` / `TableCoreHeaderCell`。
- 共享表头定义筛选图标位置、拖拽态、列宽拖动命中区、表格密度、竖向网格线和横向滚动容器。
- 单据列表删除无意义的“序号”列；单据内分录保留“序号”列。
- 单据列表明细视图和单据内表体列总宽超过容器时，底部横向滚动条真实可滑动。

## 测试账号

本地管理员：`admin`

## 测试数据

- 销售订单：`XSDD-A112-20260626135416`
- 发货通知单：`FHTZD-A112-20260626135416`
- A112 专项批次：`20260626135416`

## 通过项

- 共享核心：列表和分录表头均使用同一个 `TableCoreHeaderCell`，筛选按钮右侧间距均为 `6px`，专项断言 `filterRightGapDelta = 0`。
- 横滑生效：列表明细视图 `clientWidth=1220 / scrollWidth=1922`；单据内表体 `clientWidth=1220 / scrollWidth=2586 / scrollLeft=1104`。
- 库存列可达：分录横滑后 `即时库存 / 可用库存 / 锁定库存` 均在表体可视区域内。
- 列边界一致：列表与分录均保留竖向网格线。
- 列序号边界：单据列表无“序号”列；单据内分录保留“序号”列。

## 失败项

无。

## 阻塞项

无。

## 截图路径

- 单据内表体横滑到库存列可见：`verification/playwright/a112-entry-stock-columns-horizontal-scroll-20260626135416.png`
- 列表筛选图标同位且列表无序号列：`verification/playwright/a112-list-filter-alignment-no-seq-20260626135416.png`
- 分录筛选图标同位且分录保留序号列：`verification/playwright/a112-entry-filter-alignment-seq-kept-20260626135416.png`

## 回归与门禁

- 本批归类：共享核心表格内核变更，按 `docs/11` 选择 `full`。
- `node scripts/a112-table-core-scroll-regression.mjs`：通过，结果 `verification/a112-table-core-scroll-regression.json`。
- `node scripts/a107-table-structure-regression.mjs`：通过，已按 A112 更新列表无序号断言。
- `node scripts/a110-table-display-brand-regression.mjs`：通过，列表/分录横向 overflow 和筛选按钮右间距均通过。
- `npm --prefix frontend run build`：通过。
- `cd backend && JAVA_HOME=/opt/homebrew/opt/openjdk@21 ./mvnw test`：通过，12 tests，含 ArchUnit。
- `node scripts/run-regression-tier.mjs full`：通过，`95/95`，`badCount=0`，`skipped=0`，结果 `verification/regression-tier-full-latest.json`。

## 剩余风险

- A112 不新增列表明细库存列、不新增汇总列；这些留给 A113。
- 当前共享内核保留 vxe-table 的列表数据能力与原生分录编辑能力，统一的是表头、滚动框、密度和视觉交互原语。

## 下一步

- A113 在共享内核上补列表明细库存列、销售订单执行列和底部汇总。
