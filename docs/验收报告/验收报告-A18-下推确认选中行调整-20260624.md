# 验收报告 A18：下推确认选中行局部调整

- 日期：2026-06-24
- 批次：A18 下推确认弹窗按选中行局部调整
- 范围：销售订单下推销售出库确认弹窗
- 目标：在下推确认弹窗中允许勾选分录后只调整选中行；未勾选任何行时保持 A14 的全行批量调整行为。

## 本次实现

- 下推确认弹窗增加分录勾选列。
- 增加表头全选、反选和已选行提示。
- `清零`、`全剩余`、`按比例` 三个工具在未选行时作用于全部分录。
- `清零`、`全剩余`、`按比例` 三个工具在存在选中行时只作用于选中分录。
- 生成销售出库单草稿时按弹窗最终本次数量带入分录。

## 验收数据

- 源销售订单：`XSDD-A18-20260624074330`
- 源单剩余数量：`6 / 5 / 4`
- 未选中任何行时按 `50%`：`3 / 2.5 / 2`
- 点 `全剩余` 恢复后，仅选中第 2 行再按 `50%`：`6 / 2.5 / 4`
- 选中提示：`已选 1 行，本次工具只调整选中行`
- 生成销售出库单草稿分录数量：`6 / 2.5 / 4`
- 生成销售出库单草稿合计：`799.00`

## Playwright 断言

- [x] 打开销售订单列表并选中测试订单。
- [x] 打开销售出库下推确认弹窗。
- [x] 断言默认本次数量为 `6 / 5 / 4`。
- [x] 断言未选中任何行时按 `50%` 会调整全部行，结果为 `3 / 2.5 / 2`。
- [x] 断言只选中第 2 行时按 `50%` 只调整第 2 行，结果为 `6 / 2.5 / 4`。
- [x] 断言选中提示为 `已选 1 行，本次工具只调整选中行`。
- [x] 断言生成的销售出库单草稿分录数量为 `6 / 2.5 / 4`。

## 截图

- 弹窗局部调整：`verification/playwright/a18-push-confirm-selected-dialog-20260624074330.png`
- 生成销售出库单：`verification/playwright/a18-push-confirm-selected-form-20260624074330.png`

## 命令

- `node scripts/a18-push-confirm-selected-lines-regression.mjs`：通过
- `cd frontend && npm run build`：通过
- `cd backend && JAVA_HOME=/opt/homebrew/opt/openjdk@21 PATH=/opt/homebrew/opt/openjdk@21/bin:$PATH ./mvnw test`：通过

## 生成链护栏

- 未重跑 `build-jdy-feature-approval.mjs`。
- 未手改生成物：`docs/01/02/03`、`config/approved-feature-scope.json`、`app/feature-data.js`、`frontend/src/app/featureScope.ts`。
- 本批未调整范围例外，未改 `config/implementation-overrides.json`。
