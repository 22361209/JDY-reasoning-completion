# 验收报告：A178 采购库存销售退货与收付款核销验收能力

## 结论

A178 已在 `BLD-TEST` 使用同一组 UAT 主数据完成采购入库、库存余额、销售出库、销售/采购退货和收付款审核核销闭环。库存由 `0 → 10 → 6 → 7 → 6`，销售应收与采购应付最终净余额均为 `0`。本轮未确认业务代码缺口，因此未修改业务代码、迁移或有效 scope 配置。

A176 已修复的物料名称回填和弱密码提示各只做一次回归，均通过，不重复开发。总账、凭证等不存在于有效功能目录的能力正式标为本轮不适用；期间结账和自动核销保持第二版 deferred，均未伪报通过。

## 环境与验收数据

- 环境：本地 ERP `http://127.0.0.1:5173/`，账套 `BLD-TEST / 博莱德机械测试账套`。
- 客户：`200003729 / UAT-20260809-002`。
- 供应商：`300002879 / UAT-20260809-003`。
- 物料：`100009865 / UAT-20260809-004`。
- 仓库：`400041371 / UAT-20260809-005`。
- 币种：销售、采购与收付款来源均为 `CNY`。

## 采购、库存与销售正向链

1. 初始库存查询按物料 `UAT-20260809-004`、仓库 `UAT-20260809-005` 无余额行，作为零基线。
2. 采购订单 `CGDD001930` 使用指定供应商、物料、仓库，数量 `10`、未税单价 `100`、税率 `13%`，保存并审核。
3. 采购入库 `CGRK003531` 从 `CGDD001930 #1` 选源，保存并审核后库存现存量、可用量均为 `10`。
4. 销售报价 `XSBJ000085` 使用指定客户、物料、仓库，数量 `4`、未税单价 `150`、税率 `13%`，保存并审核。
5. 销售订单 `XSDD013909` 从 `XSBJ000085 #1` 选源；发货通知 `FHTZD006429` 从该订单下推；销售出库 `XSCKD006639` 从发货通知下推。三张单据均保存并审核。
6. 销售出库审核后库存现存量、可用量由 `10` 降为 `6`。

采购申请到采购订单的生命周期和订单到入库发布路径另由本轮 `area:purchase` 的 `a129-purchase-requisition-order-lifecycle`、`a162-purchase-publication`、`a176-purchase-plan-order-closure` 继续覆盖；本轮真实 UAT 页面使用采购订单作为采购入库的合法直接来源，没有虚构采购申请可直接入库的路径。

## 可用库存守卫

- 以同一客户、物料和仓库新建并审核报价 `XSBJ000087`、订单 `XSDD014032`，数量 `7`；订单页面同时显示即时库存 `6`、可用库存 `6`。
- 从订单下推发货通知草稿 `FHTZD006506`，审核时页面明确提示“可用库存不足，不能预留或释放”。
- 审核未成功，未生成销售出库，也未扣减库存；该失败草稿随后通过页面删除并写入操作日志。报价和订单保留为可审计验收来源。

## 退货反向库存与来源状态

1. 销售退货 `XSTH001423` 从 `XSCKD006639 #1` 选源，退货数量 `1`，保存并审核后库存由 `6` 增为 `7`。
2. 再次打开销售退货来源选择器，页面显示源出库数量 `4`、已退 `1`、剩余可退 `3`。
3. 采购退货 `CGTH000123` 从 `CGRK003531 #1` 选源。先输入数量 `11`，审核被明确拦截为“采购退货数量不能超过源采购入库剩余可退数量”；改为 `1` 后保存并审核，库存由 `7` 降为 `6`。
4. 再次打开采购退货来源选择器，页面显示源入库数量 `10`、已退 `1`、剩余可退 `9`。

## 应收、应付与收付款核销

### 销售应收

- 销售出库生成来源应收 `YS-XSCKD006639`，含税金额 `678.00`。
- 销售退货形成退货冲销 `169.50`，来源选择器显示未核销 `508.50`。
- 收款单 `SKD001853` 选择同一客户、同币种来源，资金账户 `A163-CNY-T-20260802062521`，结算与收款金额均为 `508.50`；审核后页面提示核销正式生效。
- 应收汇总显示本期发生 `678.00`、已收 `508.50`、退货冲销 `169.50`、期末净余额 `0`。

### 采购应付

- 采购入库生成来源应付 `YF-CGRK003531`，含税金额 `1130.00`。
- 采购退货按现行模型形成独立负应付 `113.00`，不改写正应付来源；净应付为 `1017.00`。
- 付款单 `FKD000733` 选择同一供应商、同币种来源，资金账户 `A163-CNY-S-20260802062521`，结算与付款金额均为 `1017.00`；审核后页面提示核销正式生效。
- 应付汇总显示本期发生 `1017.00`、已付 `1017.00`、期末净余额 `0`。正应付来源剩余 `113.00` 与退货负应付相抵是当前正式模型，不冒充单一来源已全额结清。

## A176 两项修复回归

- 物料名称：在新增物料资料中输入新名称 `A178回归名称20260809`，选择新建并执行“保存并审核”后自动返回来源物料表单，名称字段准确回填；未保存测试物料。结果通过。
- 密码校验：当前密码正确时输入不含小写字母的 `ADMIN123!`，页面显示“新密码需满足全部强度要求。”并保留五项强度规则，不出现原始 JSON；保存被拦截，密码未修改。结果通过。

## 财务范围正式裁定

- 第一版范围内并已有正式交付状态：F023 销售出库、F024 销售退货、F026 销售收款、F034 采购入库、F035 采购退货、F036 采购付款、F041 库存查询、F080 收款单、F082 付款单。本轮真实闭环与自动门禁继续验证这些能力。
- F055 业务关账/期末结账和 F089 自动核销在有效范围中均为 `later / 第二版`，交付状态为 `deferred`；本轮从验收目录剔除，不标通过。
- F088 旧“按单立即结算”能力保持 `hidden / unverified / A1`，因为其协议不符合“保存草稿、审核才核销”的统一生命周期；本轮不把 F080/F082 的正式分录核销错误回填成 F088 已通过。
- 总账、凭证录入/查询、业务单据自动生成凭证、总账报表/辅助核算、费用、固定资产和折旧在 `config/effective-feature-scope.json` 中没有功能条目，因此本轮正式判定为不适用，不属于当前 ERP 有效验收范围。

## 自动门禁

- `area:sales`：`22/22` 通过，postflight 通过。
- `area:purchase`：`23/23` 通过，postflight 通过。
- `area:inventory`：`23/23` 通过，postflight 通过。
- `area:finance`：执行至 `15/19`，其中 `14` 通过、`1` 失败、`4` 因 fail-fast 跳过；postflight 完成。
- 资金 area 唯一失败为既有 `scripts/a141-settlement-migration-regression.mjs`：旧断言仍要求 V111/V105 拓扑形状，当前隔离库返回 `legacyRows=0, publicChecks=118, tenantChecks=118, versionCopies=3`。A178 没有修改迁移、业务代码或该夹具，故按 `record-and-stop` 记录为独立历史夹具问题，不在本任务顺手修复。
- 四个 area 均自带 smoke；不再单独重复 smoke。A178 无业务代码变更，未运行 full，使用 `0/1` 次 full 预算。

## Review 与交付

- 业务代码、迁移、catalog、权限和配置：无变更。
- 版本受控变更只包含 A178 任务治理、验收报告、归档、交接快照和报告索引。
- Review 结论：真实页面链路未发现新增 P0–P2；资金 area 的旧迁移夹具失败单独记录，不改变本轮业务验收结论。
- 测试数据保留在 BLD-TEST 供审计；唯一失败草稿 `FHTZD006506` 已通过可见页面删除。
