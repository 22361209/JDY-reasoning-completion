---
archiveStatus: historical
archivedAt: 2026-08-09
taskKind: governance
roadmapItemIds: []
roadmapGateId: null
taskState: completed
taskId: A178
title: 采购库存销售退货与收付款核销验收能力
timeBudgetMinutes: unbounded
goalTokenBudget: 15000000
maxCommits: 10
maxConcurrentSubagents: 0
maxTotalSubagents: 0
maxFullGateRuns: 1
outOfScopePolicy: record-and-stop
---

# A178 任务包（已归档）

## 目标与范围

A178 与 A177 缺陷确认分离，只验证 BLD-TEST 同一 UAT 主数据下的采购入库、库存、销售出库、退货、应收应付和同币种收付款审核核销闭环；只有确认业务缺口后才修改代码。A176 已修复的物料名称回填和弱密码提示各只做一次回归。

总账、凭证、固定资产等能力必须依据有效 scope 裁决，不得因“财务全链路”字样擅自扩展范围或伪报通过。

## 结论

- 采购订单 `CGDD001930`、采购入库 `CGRK003531`、销售报价 `XSBJ000085`、销售订单 `XSDD013909`、发货通知 `FHTZD006429`、销售出库 `XSCKD006639`、销售退货 `XSTH001423`、采购退货 `CGTH000123`、收款 `SKD001853`、付款 `FKD000733` 均在真实页面完成保存/审核或合法来源下推。
- 指定物料/仓库库存按审核动作从 `0 → 10 → 6 → 7 → 6`，来源剩余可退数量分别为销售 `3`、采购 `9`。
- 超库存订单 `XSDD014032` 数量 `7`、可用库存 `6`；发货通知 `FHTZD006506` 审核被“可用库存不足”阻断，失败草稿已删除。
- 应收发生 `678.00`、收款 `508.50`、退货冲销 `169.50`，期末 `0`；净应付 `1017.00`、付款 `1017.00`，期末 `0`。
- 物料名称回填和无小写字母密码提示各回归一次并通过，未重复开发。
- 总账、凭证、自动凭证、总账报表/辅助核算、费用、固定资产和折旧不在有效功能目录，标为本轮不适用；F055 期间结账、F089 自动核销保持 deferred；F088 旧立即核销保持 hidden/unverified。
- `area:sales 22/22`、`area:purchase 23/23`、`area:inventory 23/23` 通过；`area:finance` 为 14 通过、1 个既有 A141 迁移夹具形状断言失败、4 跳过。A178 不修改范围外既有夹具。
- 本任务共使用 2/10 个提交、0 个子代理、0/1 次 full；Goal token 使用低于 1%，实际执行约 90 分钟。未 push，未操作生产环境。

完整证据见 `docs/验收报告/验收报告-A178-采购库存销售退货与收付款核销验收能力-20260809.md`。
