---
archiveStatus: historical
archivedAt: 2026-08-10
taskKind: governance
roadmapItemIds: []
roadmapGateId: null
taskState: completed
taskId: A184
title: 四项独立债务统一收口
timeBudgetMinutes: unbounded
goalTokenBudget: 15000000
maxCommits: 12
maxConcurrentSubagents: 3
maxTotalSubagents: unbounded
maxFullGateRuns: 1
outOfScopePolicy: record-and-stop
---

# A184 任务包（已归档）

> 以下目标、验收矩阵、允许路径和不做事项保留自最后活动任务包；full 中断后的用户授权与最终完成口径见文末，不能用最终摘要反向改写原合同。

## 目标

在不扩大四项原始业务边界的前提下，把资金转账详情权限、账户候选分页一致性、A126 回归夹具清理和 A166 可用性遗留分别修复、验收、review 并归档。JDY 不作为本批实现参考；本批只认当前 ERP 的安全、tenant、生命周期和现行 UI 合同。

## 时间、预算与执行策略

- 墙钟时间不设上限；按当前代码面和一次完整 full 估算，Codex 有效执行时间约 `5–9` 小时。网络或门禁排队不从质量目标中扣减。唯一 full 异常后的剩余收口，Codex 预计有效执行时间为 `70–110` 分钟。
- Goal token 上限 `15,000,000`；经用户于 2026-08-10 确认，最大提交由 `8` 扩至 `12`；同时只读子代理最多 `3`，累计子代理不设上限。
- 最多运行一次 full。每个编号任务先完成静态 review 和一次针对性门禁；只有四项均收口后才运行唯一 full。
- 停止条件：达到 Goal token/提交/full 预算的 70%、累计出现 3 个范围外失败或 3 次上下文压缩时，按项目执行策略停止并报告。唯一 full 已消耗 `1/1` 并触发停止线后，用户明确授权在不增加、不重跑 full 的前提下继续当前 P1、四项 final-HEAD targeted 和归档；提交停止线改按扩展后的 `12` 次计算。

## 显性任务目录

### 1. 资金转账详情读取权限

目标：`GET /api/cash-transfers/{billNo}` 必须声明并执行 `finance.cash_transfer.audit`，不能继续依赖“已登录 GET 默认放行”。

验收：

- 仅资金转账权限与管理员可读取同 tenant 转账详情。
- 不含该权限的仓库/普通角色返回 `403`，不存在的单据仍只在授权后按原合同返回。
- tenant 隔离、草稿/审核/反审核权限和账户候选最小读取权限不被放宽。
- 权限集成测试与 A163 真实 HTTP 权限矩阵覆盖详情 GET 的 `200/403`。

### 2. 账户候选多页一致性

目标：`financial-account-settlement-selector` 的多页读取必须使用服务端签发、绑定 tenant/查询条件/候选集合的稳定快照或等价游标；不能只用 `offset + total` 判断一致性。

验收：

- 首屏返回不透明快照标识；后续页必须携带同一标识，参数、tenant 或候选集合变化时 fail closed 并提示重新加载。
- 覆盖页间“删 1、增 1且 total 不变”、更新候选资格、重复/遗漏、伪造或跨查询复用标识。
- 正常单页和多页候选、错误/空态/重试、同币种过滤继续可用；保存和审核的后端复验保持不变。
- 协议只用于最小资金账户候选，不扩展账户资料读取/维护权限，不引入跨币种转账。

### 3. A126 回归夹具清理债务

目标：为 `scripts/a126-sales-daily-usability-regression.mjs` 建立 run-owned、fail-closed、异常也执行的完整清理；对可证明属于历史 A126 的残留使用显式清单和事务守卫恢复。

验收：

- 运行前捕获所有会被测试改变的库存、余额、单据、来源关系和日志边界；运行后按依赖顺序精确恢复，不用前缀模糊删除或简单库存相减。
- 主流程任一点失败时仍执行 cleanup，并聚合主错误与清理错误；所有权、CAS、锁和零残留校验在提交前完成。
- 历史恢复只处理能够由单号、批次、来源、actor/备注及库存流水语义共同证明的 A126 事实；证据不完整即停止，不删除。
- 修后 A126 定向、A136/相关清理门禁和 `area:sales` 通过；不改写 A144–A153 历史报告。

状态（2026-08-10）：前向夹具修复已完成。A126 定向 1/1、A136 定向 1/1、会话不变量合同和 `area:sales` 24/24 均通过；成功批次的业务事实、库存余额、操作日志、隔离身份和运行时访问均精确清零。实现过程中失败批次 `6dcb3350-338d-4e08-8313-ece928ea1356` 已用不可变 receipt、输入 SHA、snapshot digest、allowlist 和 recovery digest 精确恢复，最终 run residue 与两身份均为 0。

历史治理结论：dry-run 识别 77 批，76 批满足现有所有权/来源闭包，`20260714063721` 因缺失 canonical posting facts 被阻断。对 76 批做物理删除也会破坏保留流水的不可变 `qty_on_hand_after`；因此历史工具只允许 dry-run/verify，dry-run manifest 标记 `BLOCKED_APPEND_ONLY_CONTRACT`，带 manifest 的 `--apply` 会硬失败并抛出阻断错误。本批没有删除历史行或改写历史余额；后续须另立 append-only 三量恢复日志或显式历史重述迁移任务。证据见 `docs/验收报告/验收报告-A184-任务3-A126回归夹具清理-20260810.md`。

### 4. A166 列表布局与物料—BOM 录入可用性

目标：对已进入当前主线但从未正式归档的 A166 变更重新做现行代码 review、补缺和真实页面验收，而不是假定旧分支已完成。

验收：

- 标准列表的列设置、明细切换和更新库存位于动作栏右侧，原工具行不再额外占高；既有 testid 与行为不变。
- BOM 母件只允许已审核、启用、可自制物料；支持输入联想、键盘选择和 `...` 整列表选择，准确回填名称/规格/单位/默认仓库，后端继续复验。
- 物料名称可从前台维护；未命中时经确认新建，保存并审核后返回来源物料页、刷新并选中新名称。
- 物料列表/选择器在加载期间显示加载态，不误显示业务空数据；库存期初新增行连续输入编码/仓库不丢焦点且保存同步余额。
- 前端构建、`smoke`、`area:master-data`、`area:production`、`area:inventory` 及真实 Chrome 关键路径通过。

状态（2026-08-10）：已关闭 BOM 输入 generation、物料名称 loading owner 和选择器加载语义 3 个当前缺口。真实 Chromium run `5b37dda9-aebb-4fb3-919c-fbaa2ed1fa9a` 通过工具栏、两类旧响应竞态、物料名称新建回流、真实 `CP-001` BOM 五字段回填和库存期初本地行焦点；页面本地行未保存，共享账套无新增。隔离的 `InventoryFormalPostingTraceIntegrationTest#openingPreservesReservationsUsesShanghaiFactDateAndRejectsBelowReserved` 1/1 通过，精确证明期初保存后三量 `120/30/90`、流水落账和失败回滚，fixture 后清理。前端构建、manifest 校验和独立 review 均通过；各 area/smoke 由四项集成后的唯一 full 统一执行。

## 唯一 full 首轮取证与补充收口授权

唯一 full run `cbfbfce50925404683f5035859ebc41d` 在 HEAD `bcd4708f4e8fac3c1cad6b51a3298876669214d2`、干净工作区和 manifest SHA-256 `61c5923e15fa9c53ad4b28cf19b585687ad978b5627eef4315ad5f165a06776e`（full `148` 项）上执行。retained owner 可审计窗口为 2026-08-10 11:12:10.231–12:14:40.811 CST，约 `1:02:30.580`，不能冒充已发布的 suite summary 时间。执行到第 107 项 A126 后 fail closed：前 106 个脚本的 per-script 结果已由 runner 接受为 PASS，但 suite-level summary/latest 从未提交或发布；A126 子进程已执行但结果未提交；第 108–148 项共 41 项未启动。终端的 `badCount=2` 由 A126 auxiliary fixture closure 的 `setupError` 1 项和 suite 未达到 `release-ready` 的未发布保护哨兵 1 项组成，不代表两个脚本失败；`skipped=42` 包含 A126 这 1 个已执行但未提交的结果和后续 41 个未启动脚本。

- retained owner：`/var/folders/97/7tqts6sx2hl16hrmyjjzmy6m0000gn/T/jdy-erp-regression-suite.lock/owner.json`；SHA-256 `c72f619ba4672c79359b005504bde7a3cf77b39147bbf08df061a93b6da569a6`。owner state 为 `cleanup`、dead PID `26180`、`latestPublication=null`；suite cleanup、postflight、process/docker/execution integrity 和 verification artifact secret scan 均通过，唯一未闭合项为 `auxiliaryFixturesClosed=false`。artifact secret scan 不证明数据库、Redis 或 fixture identity 已零残留。
- child ledger：`/var/folders/97/7tqts6sx2hl16hrmyjjzmy6m0000gn/T/jdy-erp-regression-suite.lock/private/107.fixtures.jsonl`；SHA-256 `e41646e88e8c81563c4de1c220f3fa6158fc3962e5903af2013d1f15bb965b9d`。ADMIN 为 generation 0 `HARD_DISABLED`，WAREHOUSE 为 generation 0 `CLOSED`。
- A126 evidence：`verification/a126-sales-daily-usability-20260810040414-3d45a553.json`；run ID `3d45a553-812c-4a55-97ce-a17af4f0ef95`，窗口 2026-08-10 12:04:14.813–12:12:08.144 CST；SHA-256 `bd73f4db057c0942dd90724014a52ee9a4a40a59f68d3edf332ed1e7231506ea`。业务主流程没有 failure；admin/warehouse fence 均为 `CLOSED/0`；串行化业务 cleanup 精确恢复 1 报价、12 订单、16 通知、8 出库、6 应收、25 库存流水、46 日志和完整库存 baseline，并验证 run-owned 业务事实零残留；warehouse identity 已删除。admin identity cleanup 因 ownership lookup、operation-log schema discovery 和 Redis `HGET` 辅助调用同时失败而未完成，因此 A126 整体仍为失败。
- 失败后 fresh 只读复核：A126 run-owned 报价/订单/通知/出库/应收/库存流水/日志/编辑锁均为 0，12 类 external 均为 0；库存余额精确回到 on-hand `13,178,325`、reserved `38,135`、available `13,140,190`、version `42,030`，非本批流水 count `45,797`、ID digest `aa093c6813b9052e4f713cf89d9e2ff8423d130b616c01453c618f9025478b44`、row digest `b540f08234ae528a3ebf9390cd672d8bd72a11e41fca3c5bd2d847d2949f3323`。
- 唯一剩余身份是精确 ADMIN owned identity `4c6be1b2-c8c3-4dcb-8048-2e70d2d80a3a / r_a126_admin_d1eaab366c5d63f3`：已被精确 hard-disable，签名 ledger 为 `HARD_DISABLED`，认证入口已封锁；fresh 只读现场为无角色/账套授权/session scope/数据库活跃会话/Redis 会话，public 另有 10 条精确 actor 日志。但失败 evidence 的 `runtimeAccessCleaned=false`、`closed=false`、`principalDeleted=false`、`reusableTombstone=false`，所以这不是已完整清理或可复用的安全墓碑，仍待 canonical stale recovery 证明完整身份闭包。禁止手工删除。
- `verification/regression-tier-full-continue-latest.json` 仍是 A183 run `e9db65a6db564e858cfba7ad9ff25a72`（2026-08-10 04:56:36.151–06:28:36.603 CST、147 项），SHA-256 `4ef0d25340a3df5fb35e34c3428576f2ea38bba9262b5d84cf6cecd51df71a2b`；这是未达到原子发布条件时恢复的 pre-run baseline，不是本轮 full 证据，也不能因 mtime 更新误认成新结果。

下一次 canonical runner 会先恢复 baseline、清除本轮新建的 ignored artifact，并在 stale recovery 成功后移走 retained lock/ledger；因此上述最小事实、路径和 SHA 必须先随本修复提交固化。

根因定为当前任务引入的 P1：请求门已成功 CLOSED/0，但完整 identity cleanup 成功前 ledger 仍未终态；hard-disable 又把数据库 session generation 前移，父 runner 持 OWNER capability 按旧 ledger generation 重关时在 Controller 代际守卫处收到 409。不得放宽 OWNER 关闭旧代际的后端安全合同；修复只允许在精确 owned、disabled、数据库代际前移且第一次为结构化 `409/请求门代际尚未生效` 时，以当前数据库代际做第二次 close，第二次失败继续 hard-disable 并 fail closed。

修复后定向合同已通过：`regression-auth-origin-contract` 与 `regression-fixture-ledger-contract` 均 `ok=true`；前者覆盖精确 `[ledger,current]` 单次追代、历史代际首次成功，以及 enabled/current 不前移/ownership drift/其他 409/transport/invalid JSON/incomplete drain 全部不跨代，且截断边界不泄露 capability。后端 `RegressionFixtureControllerTest,RegressionActiveRequestTrackerTest` 共 `13/13` 通过，保留 OWNER 不能关闭旧代际与 RECOVERY 仅能关闭授权历史代际的安全合同，并证明旧代际已排空时 OWNER 可按数据库当前 disabled 代际关闭。首次受限沙箱执行在测试构造前被 Mockito/Byte Buddy attach 拒绝；使用项目 `backend-test.sh` 在允许 JVM attach 的环境中执行同一测试集后 13/13 通过，未修改产品代码规避环境限制。两路独立只读 review 最终均为 P0=0、P1=0、P2=0、P3=0。

用户确认的补充收口只允许：固化上述证据；修复并 review 该 P1；运行 auth/fixture-ledger/backend 定向合同；随后依次运行 A126、A136、A163、A184-A166 四项 final-HEAD targeted。它们只作为补充闭合证据，不得把唯一 full 改写成 148/148，也不得运行第二次 full。

## 验收

- 四个编号任务各自满足其验收矩阵、独立 review 无未关闭 P0–P2，并完成对应针对性门禁。
- 任务 4 完成真实 Chrome 关键路径；四项集成后唯一 full 完整执行，preflight/postflight 和清理证据可审计。
- 验收报告分别给出实际改动、单号/批次、测试结果、范围外问题、预算和唯一 full 结论；失败不改写为通过。

## 允许路径

- 任务 1：`backend/src/main/java/com/jdy/erp/finance/api/CashTransferController.java`、直接权限测试与 `scripts/a163-cash-transfer-regression.mjs`。
- 任务 2：资金账户候选直接相关的 list controller/contract/provider/DTO、`frontend/src/services/listApi.ts`、`frontend/src/services/financeApi.ts`、`CashTransferForm.vue`、A163/A184 针对性测试与 `docs/13-列表API契约.md`。
- 任务 3：`scripts/a126-sales-daily-usability-regression.mjs`、`scripts/helpers/a126-fixture-cleanup.mjs`、`scripts/helpers/regression-auth.mjs`、`scripts/helpers/regression-preflight.mjs`、`scripts/regression-admin-credential-contract.mjs`、`tools/a184-a126-current-run-recovery.mjs`、`tools/a184-a126-historical-recovery.mjs`、必要的定向测试与 A184 显式恢复证据；不改 A126 产品业务语义。
- 任务 4：A166 原任务包列出的 `DataListPage.vue`、`base.css`、物料名称/物料/BOM/库存期初直接前后端代码；本轮实际补缺仅涉及 `SourceSelectorDialog.vue`、`MasterDataRecordPage.vue`、`BomForm.vue`、A184-A166 针对性脚本与 regression manifest。
- A184 任务包、验收报告、归档、报告索引、`docs/09-交接清单.md`、`docs/12-当前批次验收清单.md` 和必要的 regression manifest 登记。

## 明确不做

- 不操作生产环境，不 push；不修改生成范围文件或已关闭 A144–A153 历史报告。
- 不扩展余额初始化、汇率、跨币种转账、预收预付、退款、自动对账或银行直连。
- 不把 A166 扩大成全站布局重构，不改变 BOM/库存/财务过账生命周期。
- 不用模糊 SQL 清理历史数据；无法证明所有权的记录一律保留并登记。
- 当前 diff 之外的门禁失败按 `record-and-stop` 记录，不顺手修复。

## 提交与 review 边界

1. 任务包提交。
2. 任务 1 独立实现、review、针对性验收和提交。
3. 任务 2 独立实现、review、针对性验收和提交。
4. 任务 3 独立实现、review、针对性验收和提交。
5. 任务 4 独立 review、必要补缺、真实 UI 验收和提交。
6. 四项集成 review 后运行唯一 full；最后归档并提交。


## Full 中断后的显式替代验收授权

唯一 full 已消耗 `1/1` 且在第 107 项 A126 父级 auxiliary fixture closure 处 fail closed 后，原计划中的“任务 4 依次通过 smoke、area:master-data、area:production、area:inventory”以及“唯一 full 完整执行”已经不能在不违反门禁预算的前提下继续完成。Codex 随后向用户给出的显性收口计划限定为：修复并 review 当前 P1；运行 auth-origin、fixture-ledger 和后端定向合同；再依次运行 A126、A136、A163、A184-A166 四条 final-HEAD targeted；不运行第二次 full，也不补跑 area。用户于 2026-08-10 回复“确认，你继续。不过需要列出显性计划和codex预计时间。”，明确授权按该替代矩阵继续。

该授权只替代本任务包的剩余动态验收矩阵，不把任何未运行脚本改写为通过：full 原始结果仍是 106 个 per-script 结果 accepted PASS、A126 executed-uncommitted、后 41 项未启动；四条 targeted 补充闭合 42 个未 accepted 项中的 4 项，其余 38 项本轮未补跑。

## 最终完成结论

- 资金转账详情 GET 权限、账户候选快照 token、A126 前向完整清理和 A166 三项 loading/generation 缺口均已实现、独立 review 并提交。
- full 后 P1 在不放宽 Controller OWNER/RECOVERY 合同的前提下修复；auth-origin、fixture-ledger 与后端 13/13 定向合同通过。
- A126、A136、A163、A184-A166 四条用户授权的 final-HEAD targeted 均为 1/1、bad 0、skipped 0；A126 incident 专属业务、库存、身份、日志、Redis、ledger 和 suite lock 已复核为基线或零残留。
- 集成终审纠正了“37→38”的未补跑计数，并保留本任务包的完整原始验收合同；最终无未关闭 P0–P3。
- full 配额 1/1，提交 7/12；Goal token 实际计量不可用，15,000,000 仅为治理上限。未 push、未操作生产环境。
- 历史 A126 77 批的物理 apply 继续因 append-only 合同与缺失 posting facts 而硬阻断；后续必须另立产品决策任务。

完整证据见 `docs/验收报告/验收报告-A184-四项独立债务统一收口-20260810.md`、任务 3 与任务 4 两份分项报告。
