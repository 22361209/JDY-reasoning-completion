---
archiveStatus: historical
archivedAt: 2026-08-02
taskKind: governance
roadmapItemIds: []
roadmapGateId: null
taskState: completed
taskId: A174
title: A173 回归会话与历史门禁复核修复
timeBudgetMinutes: 180
goalTokenBudget: 8000000
maxCommits: 3
maxConcurrentSubagents: 3
maxTotalSubagents: 3
maxFullGateRuns: 1
outOfScopePolicy: record-and-stop
---

# A174 任务包（已归档）

## 目标

修复 A173 二次 review 确认的回归治理缺口：回归执行器不得污染共享管理员会话，显式低权限 Cookie 的 401 不得自动升级为 ADMIN，角色/权限直接 SQL 变更必须触发共享状态复验，Redis 清理证据不得接受空快照；同时使已声明的 Redis HTTP session 配置在运行态真实生效，并补齐隔离身份审计日志清理、cleanup 后成功证据、tenant 动态日期及已发布迁移 checksum 合同。

本批只修改回归基础设施、测试夹具和运行依赖，不修改产品业务行为、权限规则、tenant 路由或正式 migration SQL。JDY 不作为本治理任务的需求、交互或视觉参考。

## 允许路径

- 会话、认证与 preflight helper，以及 A44、A56、A162、A163 的隔离身份、Redis session、成功证据与 cleanup。
- `backend/pom.xml` 的 Redis starter 和仅限 Surefire 的 Session 自动配置排除；生产运行态仍使用 Redis `SESSION`，既有 MockMvc 继续使用 `MockHttpSession`。
- `TenantSalesChainIsolationTest`、`TenantReportingIsolationTest` 的隔离身份、动态会计期间和整条业务日期链。
- `scripts/helpers/current-migration-head.mjs`、A141/A142/A143/A146/A151 migration regression，以及唯一 full 暴露的 A146 closure 旧静态断言。
- 当前任务、交接、归档和验收报告。

## 明确不做

- 不修改产品业务逻辑、正式 migration SQL、权限 catalog、正式角色授权、tenant 路由或安全策略。
- 不处理 A25 的分页/多候选夹具和 A128 的写死月份；两项顺延 A175。
- 不清理 A126 或其他历史业务数据，不 push，不重复运行 full。

## 验收与结论

- 运行态只接受 Redis `SESSION`，且必须精确定位非空 Redis 主 session hash；正常、故障注入和 cleanup 路径均验证无隔离用户、scope、Redis session 或 public/tenant 审计孤儿。
- 显式低权限 Cookie 的 401 不会触发 ADMIN 重试；A140/A141/A142 的直接 SQL 权限变更会在脚本后立即复验共享状态。
- 隔离身份在删除前跨所有获授权 schema 清零自身操作日志；通用认证 helper 清理失败时保留身份并使门禁失败，Java tenant 夹具还会先禁用再保留；成功 artifact 只在 cleanup 完成后写入。
- tenant 销售链和报表测试使用数据库当前日期推导完整的上一月期间；既有 MockMvc 会话测试保持通过。
- 已发布 V105–V113 由中央 helper 固定版本、脚本名和 Flyway checksum，upgrade/fresh 历史均验证 exactly-once、success、script 和 checksum。
- 唯一 full 在候选提交 `7b250720` 上完整执行 143 项：140 通过、3 失败、0 跳过；preflight/postflight 均通过且共享状态无漂移。A25、A128 是已知历史夹具失败；A146 是本批中央迁移合同重构后 closure 仍断言旧实现，已在 full 后修复并以 117 条针对性断言通过，按一次 full 上限未重跑 full。
- 独立 review 在候选提交前和 A146 收口后均无 P0–P2；实现提交为 `7b250720`、`fe2fe79f`。
- 精确移除此前测试遗留的 31 条 public 与 8 条 tenant 孤儿操作日志；最终隔离用户、scope、Redis session 和两类审计孤儿均为 0。
- 实际执行约 86 分钟，低于 180 分钟上限；使用 3/3 个子代理、1/1 次 full、3/3 个提交。Goal token 预算为 8,000,000，本次未启用运行时 Goal 计量器，实际读数不可用。未 push。
