---
archiveStatus: historical
archivedAt: 2026-08-02
taskId: A171
title: 多层生产与采购计划生命周期修复
taskKind: governance
taskState: completed
roadmapItemIds: []
roadmapGateId: null
timeBudgetMinutes: 120
goalTokenBudget: 4000000
maxCommits: 1
maxConcurrentSubagents: 0
maxTotalSubagents: 0
maxFullGateRuns: 0
outOfScopePolicy: record-and-stop
---

## 目标

修复 A170 二次 review 的四项问题：采购计划占用时机与反审核释放、显式子 BOM 当前可用性、BOM 分录仓库继承，以及相关回归覆盖。

JDY 仅作为既有单据术语和流程参考；本批以当前 ERP 的生命周期、来源占用和 BOM 可用性规则为准，不扩展功能范围。

## 允许路径

- `PurchaseRequisitionAppService`、`PurchasePlanAppService` 的计划来源占用/释放和 V112 既有数据重算。
- `ProductionTaskAppService` 的递归 BOM 校验及采购需求仓库快照。
- A170/A116b/A129 定向脚本、采购业务规则、当前快照、验收报告与索引。

## 明确不做

- 不改采购订单、采购入库、库存过账、生产领料、产品入库、权限模型或采购计划 UI。
- 不改表结构或既有历史迁移，不扩展为库存净需求、提前期、批量策略或完整 MRP。
- 不修复 A167 产品入库夹具或 A129 暴露的销售角色权限基线。

## 验收与结论

- V112 已在隔离 `18080` 后端真实执行；`planned_qty` 与已审核采购计划分录汇总的数据库核对为 `mismatches=0`。
- A170 定向回归通过：草稿计划可直接选源、审核占用、反审核释放、草稿计划不阻断采购申请反审核、失效显式子 BOM 阻断、多层采购需求仓库继承均得到覆盖。
- A116 与更新后的 A116b 兼容回归通过；A116b 完整生产任务—领料—入库链路同时验证了草稿不占用、审核占用、反审核释放。
- A129 在采购申请—采购订单断言通过后，停于当前 `BLD-TEST` 管理员缺少 `sales.order.audit` 的既有销售权限配置；按 `record-and-stop` 记录，未扩大本批。
- 后端编译、脚本语法、回归 manifest、diff 检查和文档治理检查均通过；未运行 full，也未 push。
- 实际执行约 50 分钟；0/0 子代理、0/0 full、1/1 提交上限内完成。
