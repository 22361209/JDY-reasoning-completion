---
archiveStatus: historical
archivedAt: 2026-08-17
taskKind: governance
roadmapItemIds: []
roadmapGateId: null
taskState: completed
taskId: A186
title: A185 全量 UAT 缺陷复现、修复与主链重验
timeBudgetMinutes: unbounded
goalTokenBudget: 15000000
maxCommits: 10
maxConcurrentSubagents: 3
maxTotalSubagents: unbounded
maxFullGateRuns: 2
fullGateAuthorization: user_authorized_a186_second_full_20260816
firstFullGateRunId: 1a95439fea844e3dbc3beec31b42d38c
firstFullGateExecutedScripts: 120
firstFullGateSummarySha256: 42f7ab451d53123de9561a4987d94dd7b8003c4dda469a109ed73487c23d7206
secondFullGateRepairScope: a115_a126_a136_a137_a163_role_permission_postflight
secondFullGateNonRepeat: no_third_full_without_new_authorization
outOfScopePolicy: record-and-stop
---

# A186 任务包（已归档）：A185 全量 UAT 缺陷复现、修复与主链重验

> 以下目标、预算、停止条件、允许路径、显性任务和执行进度保留自最后活动任务包；最终完成结论只在文末追加，不反向改写原合同。

## 目标

以 A185 在独立账套 `UAT20260810 / 全量UAT账套20260810` 得到的原 11 个问题线索，以及任务 7 修后 25 项 UAT 新确认的 `UAT-A186-012/013/014` 三个用户可见缺陷为完整输入：原 11 项已完成复现、修复、review 和定向验收；当前只收口新增三项，再复验直接场景并完成最终门禁与发布。新增项按 `3` 个 defect ID、`2` 条共享根因线、`5` 个页面场景（SOUT/SCRK 产品信息，SCRK/DBD/PD 备注）记账，不能因共享实现而压成两个对外缺陷。

完成标准不是“代码已改”或“自动测试通过”，而是问题的业务不变量、真实页面操作、相关单号/余额/来源和权限边界同时成立。发货通知必须作为销售主链的显式节点验收，不得被销售订单直出库路径替代或省略。

## 时间、预算与执行策略

- 墙钟时间不设上限。原 Codex 活跃执行预计 `8–15 小时`；2026-08-16 首次 full 失败后的获授权收口阶段另预计 `3.5–5 小时`，包括精确恢复、五条失败链的定向收口、独立 review、例外第二次 full、证据 amend 与 outbound 审计，不以人类工作日估算。
- Goal token 上限 `15,000,000`；最大提交 `10`，但达到 70% 即第 7 个提交时停止并汇报。用户于 2026-08-16 在看到首次 full 完整失败证据后，显式授权将 full 上限由 `1` 扩为 `2`；任何第三次 full 都必须重新停止并请求授权。
- 同时只读子代理最多 `3`，加主执行者共 4 个运行槽位；累计子代理不设上限。共享 docs/config、权限目录、迁移和公共组件只由主执行者写入。
- A186 当前已有 `4` 个历史提交和一个以 `c4947ec9` 为首轮 full 基线的未推送候选，计 `5/10`。获授权收口继续 amend 这一个逻辑提交，最终 SHA 以 amend 后的 Git 事实为准，不新增第二个 A186 提交；第二次 full 通过后仍只允许将证据写入交接/报告并 amend 同一候选，代码、迁移、config 和测试树不得再变。用户已明确授权最终普通 push；禁止 rebase、squash、force push 或夹带未经审计的未提交改动。
- 每个新增定向门禁只运行一次；恢复、代码、review finding 和环境均稳定后才运行获授权的第二次 full。第二次 full 失败即停止，不得第三次重跑或 push。

## 强制停止条件

- Goal token 达到 `10,500,000`、提交达到 `7/10`、full 已使用 `2/2` 后又需要重跑、授权收口后再出现 3 个范围外失败，或本任务累计 3 次上下文压缩时停止并向用户报告。
- 发现需要改变已批准功能范围、跨 tenant 清理、粗暴重置账套、改写历史 append-only 库存事实、操作生产环境或扩大为会计总账/成本核算时停止。
- 真实页面无法确认当前账套、Chrome 持续不可用或测试数据所有权不清时停止 UI 写入；单个业务问题阻断时记录并继续其他独立任务。
- 当前 diff 未引入的既有门禁失败只登记；唯一例外是用户在首次 full 失败后显式授权的 A126 Docker-intent 放大合同、A163 精确夹具清理韧性与 A137 tenant 测试边界，不得借此修改产品功能或其他历史测试。

## 问题输入与验收合同

| 编号 | 当前证据 | A186 明确任务 | 修复验收点 |
| --- | --- | --- | --- |
| `UAT-A185-001` | 已确认 | 修复用户角色新增页无字段 | ADMIN 可创建最小权限角色/用户；低权限登录后菜单、按钮、详情和写操作严格匹配授权 |
| `UAT-A185-002` | 已确认 | 修复资金账户“新增”跳回首页 | 新增表单可见；可创建、审核第二个同币种账户；不得放宽账户资料权限 |
| `UAT-A185-003` | 已确认 | 修复 BOM 列表加载失败和当前 BOM 不可用 | BOM 可保存、审核、作为当前版本被生产计划读取；自引用、失效物料和下游反审核仍 fail closed |
| `UAT-A185-004` | 已确认 | 修复仓库选择覆盖物料/相邻字段 | 销售报价、销售订单、发货通知、采购订单、库存调拨逐表验证；选择仓库只更新当前行仓库字段 |
| `UAT-A185-005` | 已确认 | 修复销售报价录价覆盖物料 | 录入单价只改变价格/金额相关字段，物料 UUID、编码、名称、规格和仓库保持不变 |
| `UAT-A185-006` | 已同步骤复现确认 | 在同一已保存生产任务草稿、不点“新增”时再次保存 | 重复保存必须更新同一单据且单号不变，并覆盖响应丢失/重复点击边界 |
| `UAT-A185-007` | 已同步骤复现确认 | 从生产任务生成第二条完工链并核对入库仓 | 完工默认使用成品/BOM 约定的入库仓，且来源仓快照和手工允许项一致 |
| `UAT-A185-008` | 已同步骤复现确认 | 新建隔离委外收货并分别尝试退货/报废 | 同一来源累计退货、报废及其他处置不得超过已审核收货可处置数量，并发也须阻断 |
| `UAT-A185-009` | 已确认 | 修复正式盘亏单草稿/已审核状态打印失败 | 已持久化且有正式单号的允许状态可输出；未保存草稿仍阻断；打印不得改变单据状态或库存 |
| `UAT-A185-010` | 已确认 | 修复有下游事实物料仍可反审核 | BOM、生产、委外、库存或其他有效下游引用存在时反审核 fail closed；无下游时保持正常生命周期 |
| `UAT-A185-011` | 已确认 | 修复供应商禁用无效 | 已审核供应商可按合同禁用并刷新保持；禁用后从业务候选移除，重新启用后恢复；tenant 隔离不变 |
| `UAT-A186-012` | 修后 UAT 新确认；`SOUT000003` 同步骤重开复现 | 修复生产领料详情顶层产品信息被前端归一化丢弃 | 保存后、离页重开及审核状态重开均显示来源任务母件 8 个可见字段；后端另保留 `warehouseCode` 合同，且不得误取首行领料子件 |
| `UAT-A186-013` | 修后 UAT 新确认；`SCRK000003` 同步骤重开复现 | 修复产品入库后端详情缺少产品信息及前端归一化丢弃 | 保存后、离页重开及审核状态重开均显示来源任务母件 8 个可见字段；后端 `warehouseCode` 与完工分录仓均须和任务快照一致 |
| `UAT-A186-014` | 修后 UAT 新确认；`SCRK000003`、`DBD000001` 重复保存/重开复现，`PD000001` 同构观察 | 修复产品入库、库存调拨、库存盘点三类头部备注未持久化 | 三个备注页面场景分别给出结论；创建、同号更新、重开、审核/反审核后保持，旧 NULL 只显示空且不伪造历史值 |

## 显性执行任务

1. **任务包与基线**：冻结 A185 报告、账套、单号、8+3 证据口径；核对范围、交付状态、现有回归归属和允许路径。
2. **三项真实复现**：仅用 Chrome、视觉、鼠标和键盘，在 `UAT20260810` 使用 `UAT-20260816-*` 新数据分别复现 `006/007/008`；每项记录前置、操作、第二套单号、可见结果和最终状态。
3. **权限与主数据状态组**：定位并修复 `001/002/010/011`，覆盖用户角色、资金账户入口、物料反审核引用守卫、供应商启停和候选过滤。
4. **选择器、BOM 与报价组**：定位并修复 `003/004/005`，优先检查共享分录稳定键、列字段映射、选择器回填和异步响应代际，不为单一表单复制补丁。
5. **单据生命周期与数量组**：修复 `009`，并仅在同步骤确认后修复 `006/007/008`；保持正式编号、库存双写、来源数量、审核/反审核和并发锁不变量。
6. **定向测试与 review**：每组补最低充分的后端/前端/浏览器合同；对权限、tenant、响应丢失、并发、来源闭包、清理边界和共享组件回归做独立只读 review。
7. **真实页面重验**：在当前 UAT 账套重跑 A185 的 12 个 FAIL、10 个 BLOCKED 及所有直接修复点；形成新的销售、发货通知、出库、退货、应收/收款，采购、生产、委外、调拨、资金和打印单号。
8. **原十一项最终门禁与交付**：原三组实现分别由 `b9ece0f3`、`0a0e63d1`、`0b227a46` 提交；真实主链重验形成任务 9–14 的三个新增问题，不再按本条另建提交。
9. **`UAT-A186-012` 生产领料产品信息**：修复生产领料单保存并重开后“产品信息”头部为空；以 `SOUT000003` 同步骤复现为基线，验收来源任务、成品、规格、单位与仓库快照完整且不改变领料分录、状态和库存事实。
10. **`UAT-A186-013` 产品入库产品信息**：修复产品入库单保存并重开后“产品信息”头部为空；以 `SCRK000003` 同步骤复现为基线，验收来源任务、成品、规格、单位与完工仓快照完整且不改变完工入库分录、状态和库存事实。
11. **`UAT-A186-014` 三类标准单据头部备注**：只修复本次报告命中的产品入库、库存调拨、库存盘点三类头部备注保存后丢失；以 `SCRK000003`、`DBD000001`、`PD000001` 可见结果为基线，保存、重开、审核/反审核后备注均保持，tenant 与权限边界不变。其他同构单据若发现相同缺口只登记后续，不以本任务泛化为全部标准单据。
12. **新增缺陷定向验收**：三个用户可见缺陷保持三个独立结论，即使实现归并为共享根因；补后端持久化/详情合同、前端回填合同与真实 Chrome 同步骤回归，每套共享测试须显式映射覆盖的缺陷编号。
13. **未提交与 outbound 审计**：检查全部未提交文件、暂存区、生成物和任务允许路径；另审计 `origin/main..main` 的完整 outbound 提交范围，确认没有凭据、临时证据、范围外改动或破坏性迁移。
14. **原候选提交与首轮发布门禁**：定向门禁、Chrome 和独立 review 无未关闭 P0–P2 后，已在本地 `main` 形成未推送候选 `c4947ec9` 并以干净工作树运行原计划唯一 full；该 full 失败，因此没有 push。后续只有用户看到完整失败证据后明确授权的任务 15–20 可以例外继续，仍禁止自行 merge/rebase/force push。
15. **首次 full 失败事实冻结**：以 `runId=1a95439fea844e3dbc3beec31b42d38c`、当时 summary SHA、失败脚本、31 个未启动项、postflight 与现场残留冻结历史结论；`full-continue-latest` 是后续会推进的共享指针，不得把其当前字节、旧 canonical latest 或旧单脚本 artifact 替代首轮证据。
16. **精确恢复**：先按 4 个固定日志 UUID、actor UUID、用户名和 endpoint 指纹，在单一事务中删除本批 RolePermission 测试产生的孤儿日志；再按 A163 固定 role/user/permission/link/grant/log UUID 集合执行 guarded recovery。每类行数、ownership 或最终 role matrix 任一不符即整体回滚；不清 A126 业务或历史 77 批。
17. **失败合同收口**：修正 A115 已过期的引用锁静态断言；为 RolePermission 集成测试增加事务后 `REQUIRES_NEW` 双锚点精确日志回收；让 A163 在任何 identity/业务夹具前要求全局 orphan 基线为零、事务内只验证自身残留并先提交、事务后再把全局 orphan 漂移作为独立失败证据，证据结构将自身 `transferOnlyFixtureResidue` 与独立的 `globalActorOrphans:{baseline,final}` 明确分开；A126 保留 `1_024` Docker intent 安全上限，只移除对每个全局 Redis session key 额外启动 Docker `TYPE` 的线性放大，后续 `HGET` 仍 fail closed、原子 owner Lua 删除仍保留。不改变生产日志独立提交语义。
18. **A137 tenant 边界**：先对所有已登记 tenant 做只读 registration/metrics/schema+data fingerprint 基线与终态闭包；只有以 account-set code、schema、database、enabled、initialized 精确认领的两个专用 A119 fixture tenant 可执行持久 sync 与回滚式 enforcement。现有 `tenant_a137_*` 新鲜/漂移拓扑矩阵仅作为 run-owned、单事务 `ROLLBACK` 的临时例外。`UAT20260810` 是用户建立的持久 UAT 账套，只读进入前后指纹，不删除、不重置、不 sync、不写入。
19. **定向闭环**：精确运行 RolePermission 后端测试、A115、A126、A136、A137、A163 与 postflight 所需合同；每项保存 final-HEAD 证据并完成独立只读 review，不重跑已经关闭的 area。
20. **例外第二次 full 与发布**：将全部收口改动 amend 到 `c4947ec9` 形成新的干净候选后，运行用户授权的第二次且最终一次 full。只有 `151/151`、postflight、cleanup、secret/process/Docker/fixture closure 全绿时，才允许证据 amend、完整 outbound 审计和普通 push；否则停止且不 push。

## 当前执行进度

- 任务 1 已提交为 `ef0792b7`；任务包、8+3 输入、UAT/BLD 边界与发货通知验收合同已冻结。
- 任务 2 已完成 Chrome 同步骤复现：
  - `006`：同一生产任务表单未点击“新增”连续保存，先后生成 `SCRW000003`、`SCRW000004`，确认同一草稿被重复创建为两张正式任务。
  - `007`：任务 `SCRW000004` 的成品默认仓应来自物料约定仓 `UAT-20260810-001`，但领料 `SOUT000002` 后完工入库 `SCRK000002` 实际进入 `CK-001`，确认默认完工仓错误。
  - `008`：委外工单 `WWJG000002`、发料 `WWFL000002`、收货 `WWRK000002`、退货 `WWTH000002`、报废 `WWBF000002` 均审核；收货数量 `1` 后退货 `1` 与报废 `1` 同时成立，累计处置 `2 > 1`，确认来源累计数量守卫缺失。
- 任务 4 的 `003/004/005` 已实现并完成双路只读 review，当前无未关闭 P0–P3：
  - `003` 根因为 tenant BOM 列表 SQL 错把 `sys_user` 当 tenant 表；现改用 `public.sys_user`，并区分 BOM 加载失败、真空结果与成功状态。
  - `004/005` 的现场普通键入与选择器回填不覆盖物料；确定根因为共享分录把仓库/单价列的单格粘贴也按“物料、仓库、数量、价格”整行解析。现仅物料列接管结构化粘贴，其他列保持原生单格粘贴，并以稳定行对象和请求代际封住选择器、自动价、批量价及新旧粘贴竞态。
  - 后端租户隔离测试 `2/2`、前端构建、脚本语法与 diff 检查通过；最新 Chromium `runId=28b4cb89-d024-4332-8e50-ef3489f2e26b` 为 `ok=true`，仅 mock 两项保存、unexpected writes 为 `0`，截图为 `verification/playwright/a186-selector-bom-28b4cb89d024.png`。
- 任务 3 的 `001/002/010/011` 已完成实现和定向动态验收：
  - `001` 不只是字段位置问题：系统此前只能编辑三个预置角色，没有创建最小角色的 API/UI；现新增受 `system.role_permission.manage` 守卫的角色创建合同，刷新请求不会把 ADMIN 权限复制到新增草稿，用户新增字段聚拢在核心区并默认仅授权当前账套。
  - `002` 根因为 `openMasterRecord` 在页签打开成功前写入记录状态且忽略 20 页签上限的失败返回，形成 ghost；现只有页签成功打开后才提交记录状态，已有 ghost 也会清理。Chromium 已证明达到精确 20 页签时不产生隐藏表单，释放一页后同一“新增”动作可成功重试。
  - `010` 根因为物料反审核未执行动态下游引用扫描；现反审核先锁父行，再按 tenant 当前 schema 的真实 FK 元数据检查引用。所有首次写入物料/供应商 FK 的业务事务用 `FOR KEY SHARE` 与反审核串行，纯读查询保持无锁，避免只读事务回归和 broad table lock 死锁。
  - `011` 现允许已审核供应商在有业务引用时禁用，但仍禁止有引用时反审核；禁用后专用供应商选择器只返回已审核启用行和 `id/code/name/status/auditStatus`，前端仅展示编码/名称，手工录入保存也由后端复验，批量失败原因不再被通用提示覆盖。
  - 最终源码后端目标矩阵 `38/38` 通过，包含角色/列表权限、资金结算、物料/供应商引用与双向并发锁；前端构建、脚本语法、manifest 与 diff 检查通过。最新 Chromium `runId=baa03b5a-aab7-4625-9ddc-a6264e3ff21e`、证据 SHA-256 `cb43c0c3d0f3a1f2d271397b2382cca99272183a5d663f5741d869d78b78f6f1` 为 `ok=true`，mock 角色/用户写入各一次、真实业务写入 `0`、unexpected writes 为 `0`，密码仅保留长度而未落证据；三张截图覆盖页签溢出、重试表单和供应商最小列。
  - 角色/用户与浏览器、供应商边界、主数据引用锁三路最终只读 review 均为 P0/P1/P2=0；引用锁 review 仅登记两个非阻断 P3 增强项（未来若开放复用时联合校验采购申请生成行的 BOM 物料锚点，以及补 product DELETE 双向并发用例）。
- 任务 5 的 `006/007/008/009` 已完成实现、定向动态门禁和受控 Chromium 验证：
  - `006` 根因为生产任务保存后预览把单号清空，且 UI 没有在途闸门或稳定草稿身份；现用 tenant-scoped `draftId` advisory lock 在取号前串行化，最终 upsert 复验同一 UUID、草稿状态与非子任务来源，响应丢失、双击和改量重存都保持同一 ID/正式单号，编号流水只推进一次。生产计划预览同时补回 ID、单号、计划行和来源类型，重复保存不会退化为手工任务。
  - `007` 根因为表单把 `CK-001` 作为显式默认值提交，后端也未读取母件默认仓；现手工任务仓库留空时锁定并复验母件已审核启用默认仓，显式仓仍可覆盖。隔离测试证明任务、完工单、正式库存 AUDIT trace 和成品余额都进入 `A186-PROD-FINISH`，`CK-001` 增量为 `0`。
  - `008` 根因为退货与报废分别累计、审核时未共享来源行上限；现统一以收货头、来源行 UUID、处置头、库存的固定锁序执行条件 claim/release，`returned + scrapped + demand <= receipt.qty` 在数据库写侧强制成立。确定性并发覆盖 return/scrap 双顺序、同类 push 唯一、push 与父反审核双顺序；隔离构造的旧 `1+1>1` 形状可用正式反审核恢复并生成库存 reversal。
  - `009` 根因为 document output 未登记 `stock-count-loss`，且前端把所有非 2xx 伪装成“未保存”；现 DRAFT/AUDITED 正式盘亏单可 CSV/HTML/PDF 输出，未保存表单禁用输出，VOID/不存在和权限继续 fail closed，输出前后表头、分录、余额和库存流水快照不变，后端原始错误文案可见。
  - 后端最终定向门禁首轮执行 `14` 项：`11` 通过，`3` 项仅为新增测试夹具/期望基线错误；只修夹具后精确重跑失败项 `3/3` 通过，没有重跑其他已通过项。前端生产构建、后端 test-compile、脚本语法、manifest 与 diff 检查通过。
  - 受控 Chromium `runId=1d01f6ec-754a-4ba5-84b9-4fb0642c1e79` 为 `ok=true`：双击仅 `1` 个在途请求，失败重试复用 `draftId=5c3f95c1-8795-4c46-9600-59e3bf9139a8` 与 `SCRW900001`，预览仓为 `A186-FINISH`；盘亏未保存禁打印，DRAFT/AUDITED 打印成功，受控 409 原文显示，真实业务写入与 unexpected writes 均为 `0`。证据 SHA-256 为 `b0ccd2d43acff909621766452358a41ff1d1df86bd77f748fdbd88a377af6f34`。
  - 生产、委外与打印三路最终只读 review 均无未关闭 P0–P2；仅保留 1 个非阻断 P3：mixed 新旧生产任务客户端的并发测试只在入口同时放行，未为测试引入生产锁点 hook，静态锁序已经统一为 source 锁后 final task upsert。
- 任务 7 的真实 Chrome UAT 已在 `UAT20260810` 完整执行 `25/25`：`22` 项无新增问题，`3` 项形成新的用户可见缺陷 `UAT-A186-012/013/014`。销售订单—发货通知—销售出库—应收/收款及采购、生产、委外、库存、资金主链均已执行；当前只收口上述三个新缺陷，不重跑已经通过且未受影响的独立路径。
- 任务 9–13 已完成实现、定向验收、真实 Chrome 复验和最终只读 review：
  - `UAT-A186-012/013` 的共享前端根因是详情归一化丢弃顶层 `productInfo`；后端同时补齐任务冻结母件在任务详情、领料预览/详情和产品入库详情中的一致展示，并将历史空字符串/NULL 快照按受限 `PLAN_ROOT + product_id + bom_id` 来源回退到计划冻结值，禁止 BOM 子任务误取根母件。生产隔离合同覆盖计划单位 `PCS`、主档漂移为 `BOX` 后仍显示冻结值，以及 code/name/unit 空字符串的历史兼容；最终精确用例 `1/1` 通过。
  - `UAT-A186-014` 的根因是三个页面已把 `remark` 发到共享草稿请求，但 `production_completion`、`stock_transfer`、`stock_count` 的表结构、DTO、保存和详情均未承接。新增且仅新增 `V121` nullable `remark TEXT`，严格校验 public、登记 live tenant 与已有 backup 的列形状；旧行继续为 SQL NULL，不伪造历史备注。三个服务均覆盖创建、同号更新、重开和审核/反审核保留。
  - 前端构建、后端目标集成合同、脚本语法与 diff 检查通过；迁移 runner `a167` 最终 `235` 条断言通过，包含 fresh/upgrade/replay、public/live/inactive/backup 列形状和旧 NULL 保留。受控 Chromium `runId=c4d5f4ed-7baa-408e-a03b-dc06fff957b6` 为 `ok=true`，5 个页面场景、精确详情 GET、三个同号草稿更新和 8 组锁 acquire/release 均闭合，unexpected writes 为 `0`。
  - 真实 Chrome 在可见账套 `UAT20260810 / 全量UAT账套20260810` 完成 5 个直接页面场景：`SOUT000003`、`SCRK000003` 均可见母件编码 `UAT-20260816-FG-001`、名称 `UAT-20260816-成品A`、规格 `FG-A`、单位 `PCS`、任务数量 `3`、剩余 `0`、BOM `UAT-20260816-BOM-001`、版本 `1`；`SCRK000002` 备注 `UAT-20260816-A186-014-SCRK-001`、新调拨草稿 `ZJDB000002` 备注 `UAT-20260816-A186-014-DBD-001`、新盘点草稿 `PD000004` 备注 `UAT-20260816-A186-014-PD-001` 均在保存、关闭并重开后保持。两张新库存草稿未审核，没有改变库存余额。
  - 三路最终只读 review 均为 P0/P1/P2=0；仅保留非阻断 P3：未来可为任务净重/毛重快照补非空判别夹具。`area:production` 已于 2026-08-16 执行唯一一次（`runId=5be92406348a4d84a547433dbbd8e0ba`）：`33` 项中 `32` 项通过，唯一失败是旧 A116 夹具尝试反审核已有引用的共享 `PJ-014` 而被本批引用守卫正确以 `409` 拦截；postflight 正常且其余脚本全部完成。修正后 A116 精确定向 `runId=e8c8269bf05a41f28e53f59c7f633c90` 为 `1/1`，权威 summary `verification/regression-tier-area-production-only-a116-production-plan-pushdown-regression-latest.json` 的 SHA-256 为 `e5bde2778b54d4b20e08aa56cd21f1f9808eae8a5cec3d128eb97e6ba63c84c7`；`componentCreated=true`，固定组件、BOM 与请购快照均锚定 `RM-A116-COMPONENT`，生成 `SCJH000342 / SCRW001359 / CGSQ000300`。第二次且最终 full 中 A116 再次 PASS，证明固定前置的后续只读复用成立；没有重跑整套 production area。
  - `area:inventory` 也已于 2026-08-16 执行唯一一次（`runId=225dcbe0f0d940158b7efa0f5b2578e2`）：`28` 项中 `27` 项通过，唯一失败是 A101 仍使用正式禁用种子 `CP-118` 写安全库存设置，被本批“商品必须已审核启用”的引用锁守卫正确以 `400` 拒绝；A101 自身 guarded cleanup 为 `passed=true`、七类残留均为 `0`，suite postflight 正常。修正只将 A101 改为只读使用正式启用的 canonical `CP-001`，并以完整行 SHA-256 前后相等证明未改共享商品。A101 精确定向 `runId=dd2a7852ed6a449da798e6bdd8ba7cf6` 为 `1/1`，权威 summary `verification/regression-tier-area-inventory-only-a101-stock-alert-regression-latest.json` 的 SHA-256 为 `1c2fdbde8f90e6a60fb010bde37e003a03e79eef1da21989122877f4e10829cf`；Chromium 截图为 `verification/playwright/a101-stock-alert-list-20260816130131-MSVTD6XZ-DF4D55D7-F52.png`、`verification/playwright/a101-stock-alert-settings-20260816130131-MSVTD6XZ-DF4D55D7-F52.png`，setting/warehouse/balance 清理与七类 residue 均为 `0`，`productPassed=true`，CP-001 前后完整行 digest 均为 `1de2507bd19b4a38c1466945191f444fc3efec765220dc667c58be2547a3e3a3`。没有重跑整套 inventory area；第二次且最终 full 中 A101 再次 PASS。
  - 首次 full 已于 2026-08-16 执行，其历史结论以 `runId=1a95439fea844e3dbc3beec31b42d38c`、当时 `verification/regression-tier-full-continue-latest.json` 的 SHA-256 `42f7ab451d53123de9561a4987d94dd7b8003c4dda469a109ed73487c23d7206` 和统计冻结；该共享 latest 后续按授权推进，当前字节不再是首轮实体。`expected=151 / executed=120 / PASS=115 / script failures=5 / skipped=31 / badCount=6 / ok=false`。失败为 A115、A126、A136、A137、A163，第六个 bad 是 postflight；A163 的 shared-state drift 使 #121–151 未启动。suite lock、process、Docker lease、execution integrity、secret scan 与 suite identity runtime closure 已关闭，但当时数据库仍有 4 条 A186 测试孤儿日志和 A163 run-owned role matrix 残留，因此未 push。
  - 用户已在收到上述完整解释后授权任务 15–20；结构化收口范围为 `repairScope=a115_a126_a136_a137_a163_role_permission_postflight`：允许按固定 UUID/完整指纹精确恢复本批测试残留，扩展到 A115/A126/A137/A163/RolePermission 的窄测试合同，并额外运行第二次 full。A126 子 cleanup 已恢复业务、库存与 warehouse identity，父级 fixture recovery 收掉剩余 admin identity；A126 incident 对应 Redis 与日志均为零，不执行第二次业务清理。
  - 任务 16 已于 2026-08-16 23:03 CST 按两阶段 `SERIALIZABLE` 事务完成。第一阶段在五个 operation-log schema、identity/role/grant/scope/lock 全表锁和完整 provenance 守卫下，恰好删除上述 4 条 A186 orphan；第二阶段在全局 orphan=0、A163 两用户名 Redis session=0、固定 UUID/名称/角色/权限/关联/7 日志、非 A163 角色矩阵 digest 全部吻合后，精确删除 A163 role/user/permission/userRole/accountSetGrant 与 7 条 owned logs。验后五个 schema 的 orphan/A186/A163 均为 `0/0/0`，identity/permission/link/grant/scope/lock 全零，Redis 两用户名均 0；角色矩阵回到 `4` 个角色、SHA-256 `36c77bd76669610e208ae85663fb7d6a9cd2a107e3c98d920fb606febaf14c1d`。`ZJZZ000121` 仍为 DRAFT/version 2，四条 AUDIT/REVERSE append-only facts 原样保留且净额 `0.00`。
  - 任务 17 的静态/后端合同已通过：RolePermission 精确用例 `3/3`、failures/errors/skipped=`0/0/0`，Surefire XML SHA-256 为 `6e079e4823eee318a70a7639d4e8dc9566b8529e4c1d4108ad0e354007af46bd`；事务后双锚点拒绝日志清理完成，现场 global actor orphan、测试用户和测试日志均为 0。A126 子进程、Redis owner 与 session invariant 三项合同全部通过，Docker intent 上限仍为 `1_024`，HGET ownership 与原子 Lua 删除未放宽。
  - 五条 canonical targeted 均为 `1/1`、bad/skipped=`0/0`，preflight/postflight 与 suite-lock release 全绿。A115 `runId=be412792de3e4d8686ca54beb02e4f7e`，summary SHA-256 `9764cd3c949d1fda4c030fafbb7b6a4b5975a16639b9ac726e2bdaa97ec5be08`；A126 `runId=0cb6da58d2e944c5809fccfa1890ac0b`，summary SHA-256 `b5b5a81192fcfeffd87f2915aab47a101d404060b14b3f0c9865c7ee639f16df`；A136 `runId=d187854364574195960ea8f0b0bfc3fc`，summary SHA-256 `1bcdfde94e0246eb3ce97b2a73e525a1f9ccf9e53f97cadf0d8968a379f44a4f`；A137 `runId=4fb752747d8f4307b608573ec2c25071`，summary SHA-256 `8831bec215630dc1faaeddd1022a2128c9b4457a985077e63b23782f1c4c07b3`；A163 `runId=75ac2497069940dbb28fc03bf3a66c33`，summary SHA-256 `40f1c448f3e89dff95a803e1f05d8733fde6713b3fbefd82520440dc0b9d7007`。A115 首次尝试仅在执行任何脚本前由 preflight 发现 Docker Desktop 重启后复起的旧后端指纹，按标准 `dev-down/dev-up` 重建为当前 `ccd32df20940…` 后才形成上述有效 targeted；未把环境预检停止记为脚本失败或重复门禁。
  - A126 新 evidence `runId=ce8da5b2-6025-41ca-bc55-095233399de9`、SHA-256 `7961ec736d9682f112979d0bc46dd1888cb39060aed29fc1c5a9299adc7ccb09`，`135/135` 断言为 true；业务/库存清理 `1/12/16/8/6/25/46/0`、两 fence `CLOSED/0`、两 identity closed/deleted，首轮失败与本次 run 的业务、external、身份、五 schema 日志和 Redis 均为 0，余额完整字段及 non-owned transaction count/双 digest 精确回共同 baseline。A137 证据只认新的 targeted summary：3 个 registered tenant 中仅 `tenant_uat20260810` 为只读，持久 sync 精确限两套 A119，14 项 topology 与 5 类 cleanup 全绿；被 runner baseline restore 的旧固定 A137 JSON 不冒充新证据。A163 自身 role/user/permission/link/grant/scope/log 全 0，`globalActorOrphans` 为 `0→0`；现场角色数仍为 4，`ZJZZ000121` 保持 DRAFT/version 2，4 条 append-only facts 原样保留、净额 `0.00`。
  - 任务 20 已完成：第二次且最终 full `runId=f53af589c12841deb4b4d6138a5e9862`，CST `2026-08-17 00:08:00–01:27:16`，直接执行干净 HEAD `102efca990fb778fa18f7c73d9dcffd1da91347a`、execution rootDigest `776b8262ad350e1eb082a72c8f8faecae339c9afa8500a69a83582658a32e6e8`；通过后的证据只 amend 同一逻辑提交，最终 Git SHA 不冒充 full 直接执行 SHA。权威 `verification/regression-tier-full-continue-latest.json` 的 SHA-256 为 `af1206f90f005a05dd100e174c152089978928e8cf57b77c059f5da59c89d4d3`。`expectedTotal=151 / total=151 / PASS=151 / scriptBadCount=0 / badCount=0 / skipped=0 / ok=true`；A115/A126/A136/A137/A163 五个首轮失败点全部正式 PASS，原先未启动的 31 项也全部执行通过。preflight/postflight 均 `ok=true`，auxiliary fixtures closed，suite lock release prepared/committed，process ownership、Docker lease、execution integrity complete/strict clean，secret scan `ok/residueFree=true`，共享角色矩阵和安全设置前后相等。suite identity cleanup 为 `ok/runtimeAccessCleaned/closed=true`，并按设计以 `cleaned/principalDeleted=false`、`retainedDisabled/reusableTombstone=true` 保留不可登录的禁用墓碑；普通 `regression-tier-full-latest.json` 仍是旧基线。full 额度最终为 `2/2`，没有也不得运行第三次。

## 允许路径

- 当前治理与证据：`docs/09-交接清单.md`、`docs/12-当前批次验收清单.md`、`docs/13-列表API契约.md`、`docs/guides/codex-execution-policy.md`、`scripts/docs-governance-check.mjs`、A186 验收报告/归档、`docs/验收报告/README.md`。治理脚本只允许把用户授权的第二次 full 绑定到首轮 runId、已执行数、窄 scope 和禁止第三次重跑合同，不得放宽其他预算。
- 前端：问题直接涉及的 `frontend/src/modules/system`、`finance`、`master-data`、`sales`、`purchase`、`inventory`、`production`、`outsourcing`；`UAT-A185-001/002` 顶层打开链允许精确修改 `frontend/src/app/App.vue`，`UAT-A185-009` 打印链允许修改 `frontend/src/modules/documents`；`UAT-A186-012/013` 仅允许修改经根因证明丢弃顶层详情字段的 `frontend/src/services/documentApi.ts`。其余共享 `frontend/src/components`、composable 和 service 只有经根因证明才允许修改。
- 后端：上述模块直接对应的 controller/application/domain/query/repository 与测试；`UAT-A185-009` 允许精确修改 reports/document-output 代码与测试。新增三项收窄为 `ProductionTaskAppService` 的母件任务快照落库、`ProductInAppService`、`MaterialIssueAppService` 的母件快照展示口径、`StockTransferAppService`、`StockCountAppService`，以及 `TenantProductionChainIsolationTest`、`TenantInventoryIsolationTest`；共享生命周期、编号、库存或权限代码只有在根因明确且补跨模块合同后允许修改。
- 回归：现有相关测试、已登记的 `scripts/a186-production-output-regression.mjs`、承接最新迁移 upgrade/fresh/replay 的 `scripts/a167-production-plan-multi-line-migration-regression.mjs`、A116/A101 精确夹具兼容，以及首次 full 收口所需的 `scripts/a115-metadata-definition-regression.mjs`、`scripts/a126-sales-daily-usability-regression.mjs`、`scripts/a137-tenant-schema-constraint-regression.mjs`、`scripts/a163-cash-transfer-regression.mjs`、`scripts/helpers/regression-auth.mjs`、`backend/src/test/java/com/jdy/erp/system/api/RolePermissionControllerIntegrationTest.java`、`scripts/regression-child-process-guard-contract.mjs`、`scripts/regression-redis-primary-ownership-contract.mjs`、`scripts/regression-session-invariant-contract.mjs`。`scripts/a136-operation-log-actor-regression.mjs` 仅作为正确的全局 orphan oracle 运行，不允许修改。A115/A126/A137/A163 只允许修上述已证明的测试合同，不得顺带修改产品逻辑；A137 不得对 UAT tenant 执行写式 sync。
- 数据库迁移：允许新增且仅新增 `V121`，为 `production_completion`、`stock_transfer`、`stock_count` 三张表在 public、登记 live tenant 与已有 backup 形状中补 nullable `remark TEXT`；旧 NULL 不伪造历史值。必须向前兼容、可回放、独立 review，禁止改写既有 migration。
- UAT：Chrome 可见界面 `http://127.0.0.1:5173`，只操作 `UAT20260810` 和 `UAT-20260816-*` run-owned 数据。定向/full 自动回归只允许由既有 runner 使用 `BLD-TEST/public`；逐次 run-owned 夹具必须通过 pre/postflight 和精确清理。唯一例外是上项固定 `RM-A116-COMPONENT` 及其配套物料名称档案：它们作为可在 fresh/reset 测试库首次建立、后续只读校验且不按 run 增长的 canonical 回归前置，不得被表述为逐次业务夹具已经清理；不得手工改写 `BLD-TEST`，不得触碰 A126 历史 77 批。

## 明确不做

- 不清空 `BLD-TEST`，不删除 A126 历史 77 批，不改写库存 append-only 历史，不操作其他 tenant 或生产环境。唯一获授权的数据 mutation 是任务 16 的两组固定 UUID/完整 ownership 指纹测试残留恢复；禁止前缀、时间段或全库 orphan 扫除。
- 不扩展总账/凭证、成本核算、跨币种结算、预收预付、审批流、供应商绩效或 JDY 仅参考能力。
- 不用 API、脚本或数据库结果冒充最终真实用户验收；开发期可用代码、测试和只读诊断，但最终 UAT 操作仍严格 Chrome-only。
- 不为了门禁全绿修改范围外旧测试或放宽权限、tenant、审核/反审核、库存、编号和来源数量保护。
- 不执行 `git pull`、merge 或改写远端历史；push 前允许且必须只读 `git fetch origin main` 更新 outbound 基线。本轮仅按用户 2026-08-16 的明确授权，在最终审计和第二次且最终 full 后普通推送本地 `main`。若远端出现非 fast-forward 漂移则停止，不自行 merge/rebase/force push。

## 目标门禁

- 静态：受影响前端类型检查/构建、后端目标测试、脚本语法、`git diff --check`。
- 定向：新增三项运行 production、inventory 后端目标测试、前端 build、V121 public/live/backup 拓扑与旧 NULL 兼容合同、A186 浏览器脚本；`area:production` 与 `area:inventory` 均已消费唯一一次，分别以上述 `32/33 + A116`、`27/28 + A101` 精确定向补跑收口，不重跑整 area（area 已含 smoke，不另跑 smoke）。共享核心生命周期、库存、来源数量或权限变更必须有后端快测。
- 浏览器：原 11 项与 25 项主链证据保持原样；新增三项只复验 5 个直接页面场景（SOUT/SCRK 产品信息，SCRK/DBD/PD 备注），不为已通过的 22 项重复制造业务数据。销售订单—发货通知—出库—退货—应收/收款完整链沿用任务 7 的新单号、数量和余额证据。
- 最终：首次 full 的失败证据保持原样；用户授权的第二次且最终一次 `node scripts/run-regression-tier.mjs full --continue-on-failure` 已 `151/151`、postflight/cleanup 全绿。文档治理、工作树与完整 outbound 范围继续按发布前审计闭合。

## 验收

- 原 11 项与新增 `UAT-A186-012/013/014` 均有根因、自动合同、独立 review 和 Chrome 可见结果；三个新增 defect ID 保持三个独立验收结论，未复现或范围外同构项不得为了扩大修复范围而改代码。
- 销售订单—发货通知—销售出库—销售退货—应收/收款形成完整新单号、来源、库存和往来余额闭环。发货通知必须证明审核增加预留、反审核释放、超可用库存与重复占用被阻断、作为销售出库来源可追溯，且已有下游出库时反审核被阻断；采购、生产、委外、调拨、资金及打印的原失败/阻断点均得到明确结论。
- 权限、tenant、审核/反审核、库存双写、正式编号、来源累计数量和禁用候选保持 fail closed；不存在以放宽保护换取页面可走通的修复。
- 所有新增目标门禁按阶段各运行一次；第二次 full 已如实记录且只再 amend 证据文档。独立 review 无未关闭 P0–P2；文档治理、提交范围与完整 `origin/main..main` outbound 范围在发布前复核，再按用户授权普通推送。

## 交付口径

- 逐问题报告：原 11 项加新增 3 项分别记录根因、改动文件、定向测试、真实操作、相关编码/单号、最终状态与结论；新增 014 是一个跨三页面的缺陷，不拆成三个缺陷，也不泛化为全部标准单据。
- 逐主链报告：销售（含发货通知）、采购、生产、委外、库存、资金的来源、审核/反审核、数量/余额和权限边界。
- 明确区分已修复、未复现、范围外和仍阻断；不能用自动回归替代真实页面结论，也不能把“执行过”写成“通过”。

## 最终完成结论

- A185 原 11 项与修后新增 3 项共 14 个用户可见缺陷均已完成根因、实现、最低充分合同、真实页面复验和独立 review。
- 真实 Chrome 已执行 25 项主链与新增 5 个直接页面场景；销售链显式经过发货通知。
- 首次 full 的 120/151 失败、精确恢复、五条 targeted 与 RolePermission 3/3 证据均按上文保留，没有被后续成功重写。
- 用户授权的第二次且最终 full runId `f53af589c12841deb4b4d6138a5e9862` 完整 `151/151` PASS，bad/skipped `0/0`；postflight、fixture、suite lock、process、Docker lease、execution integrity 与 secret scan 全绿；suite identity runtime access 已关闭并按设计保留不可登录的禁用墓碑，而非删除 principal。
- full 额度最终为 `2/2`，逻辑提交为 `5/10`；没有运行第三次 full。
- 完整验收证据见 `docs/验收报告/验收报告-A186-A185全量UAT缺陷复现修复与主链重验-20260817.md`。
