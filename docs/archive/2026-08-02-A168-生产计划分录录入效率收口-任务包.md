---
archiveStatus: historical
archivedAt: 2026-08-02
taskId: A168
title: 生产计划分录录入效率收口
taskKind: governance
taskState: completed
roadmapItemIds: []
roadmapGateId: null
timeBudgetMinutes: 120
goalTokenBudget: 1000000
maxCommits: 2
maxConcurrentSubagents: 2
maxTotalSubagents: 2
maxFullGateRuns: 0
outOfScopePolicy: record-and-stop
---

## 目标

根据真实页面反馈收口生产计划分录录入效率：把新增/删除行工具放到序号格，输入控件完整占用单元格，并让母件物料编码支持按输入字符联想、键盘选择及既有完整物料选择器兜底。

JDY 只作为录单效率与术语参考；具体交互复用当前 ERP 已有主数据联想和选择器约定，不引入新的业务范围。

## 允许路径

- 生产计划分录表、生产计划表单和直接复用的前端主数据联想/选择器接线。
- A168 专属前端回归、当前任务包、交接快照、验收报告与索引。
- 验收完成后把本地 5173 运行时切换到 A168 worktree；不修改原 worktree 的 A166 文件。

## 明确不做

- 不改 A167 后端、V110、生产下推、库存或生命周期逻辑。
- 不改共享表格的全站尺寸规则。
- 不混入原 `main` 工作区 A166 未提交改动，不 push，不运行 full。

## 验收与结论

- 序号格加减行、整格输入尺寸、输入联想、上下键、Enter/Tab、Esc 和完整选择器兜底均已通过真实浏览器回归。
- 选中母件时即时校验并回填其已审核、启用的当前 BOM；无当前 BOM 的可自制物料不会取得稳定 `productId`，保存保持 fail-closed。
- 两型号计划 `SCJH000146` 保存成功，两行稳定物料主键、BOM、仓库和车间均保持独立。
- 前端最终构建通过，A168 定向回归通过，smoke 10/10；本地 `5173` 已切换到 A168 前端并连接 `18080`。
- 实际收口约 40 分钟；使用 1/2 提交、2/2 并发子代理、0/0 full。首次定向回归在保存时暴露测试母件缺少当前 BOM，按本任务产品合同补齐即时 BOM 校验后复跑通过；无范围外失败。
