---
archiveStatus: historical
archivedAt: 2026-08-10
taskKind: governance
roadmapItemIds: []
roadmapGateId: null
taskState: completed
taskId: A183
title: 资金转账账户候选与盘点系统库存真实主链修复
timeBudgetMinutes: unbounded
goalTokenBudget: 15000000
maxCommits: 3
maxConcurrentSubagents: 3
maxTotalSubagents: unbounded
maxFullGateRuns: 1
outOfScopePolicy: record-and-stop
---

# A183 任务包（已归档）

## 目标与结论

A183 已关闭 A182 真实 Chrome 终验留下的两项现场失败，并收口同一路径的最小候选权限合同。

- 资金转账单现在明确区分加载、失败、空列表和恢复状态；“新增”会重新加载已审核、启用且币种为 CNY/USD 的账户，失败后不会保留旧候选或旧选值。
- `financial-account-settlement-selector` 只为 `finance.settle OR finance.cash_transfer.audit` 提供最小候选；账户资料列表、资料选择器和资料写操作没有随之开放。
- 盘点单的“实盘数量、系统库存、差异”在旧隐藏偏好、损坏 JSON、缺列偏好和页签重挂载后仍为必显列，列设置中不可关闭；盘盈/盘亏语义未受影响。
- BLD-TEST 真实 Chrome 完成资金转账 `ZJZZ000090` 保存、审核、反审核、再审核和最终反审核，最后为草稿 v4、双边资金事实已回滚；盘点表头和列设置均显示并锁定三项关键列。

## 改动与门禁

- 实现候选提交：`b64d9ce9`。
- full 后测试清理修复提交：`4ef4b6b2`。
- 权限定向 Java 测试 10/10 通过；前端构建、脚本语法、diff 与文档治理均通过。
- A88 定向通过：`PD000085 / PY-PD000085`；旧隐藏、损坏和缺列偏好均恢复，两行系统库存/实盘数量/差异通过。
- A163 full 前定向通过：主单 `ZJZZ000082`、仅转账权限单 `ZJZZ000080`、浏览器生命周期单 `ZJZZ000084`、tenant 单 `ZJZZ000023`。
- `area:finance` 21/21、`area:inventory` 25/25 通过，均为 0 失败、0 跳过。
- 共享改动范围是 `DocumentForm.vue`、`EntryTable.vue`、`entry-table/types.ts`、`entry-table/useEntryTableColumns.ts`、`ColumnSettingsDialog.vue`；证据来自 A88 的旧偏好恢复、A97 共享 EntryTable UX 和 inventory area。

## 唯一 full 与针对性闭合

- 唯一 full run `e9db65a6db564e858cfba7ad9ff25a72` 于 2026-08-10 04:56:36–06:28:36 CST 完整执行 147/147：146 通过、A136 1 项失败、0 跳过；preflight、postflight、严格干净工作区、进程归属、构建 fingerprint、权限矩阵、安全设置和共享状态复验均通过。
- A183 直接相关的 A88 盘盈/盘亏/主盘点、A97、A139、A163 在该 full 中全部通过；A163 full 单号为 `ZJZZ000093 / ZJZZ000091 / ZJZZ000095`。
- A136 失败的 14 条 actor 孤儿被精确定位为 full 前两次新版 A163 各留下 7 条；full 后 A163 又留下 7 条，现场合计 21 条。根因是 A163 删除 run-owned transfer-only 用户前未删除其操作日志，不是产品代码或 A136 断言错误。
- 三个 actor UUID、用户名、21 个日志 ID、动作多重集和对应转账单语义经锁内重验后，以显式 allowlist 单事务删除；结果为 allowlisted 日志 0、全局 actor 孤儿 0。
- A163 cleanup 已改为 UUID+用户名双重所有权、有限锁等待、事务内零残留/零孤儿校验，先删 run-owned 日志再删 session scope、授权、用户和角色。修复后 A136 定向通过；A163 批次 `20260809224049-1c6f256b` 通过，删除 7 条 run-owned 日志，八类残留均为 0。
- full 配额已用尽且没有运行第二次 full；历史结果保持 146/147，不改写为全绿。A136 的唯一失败由同一当前 diff 的针对性回归与零残留证据闭合。

## Review 与保留问题

- 本批 diff review 为 P0=0、P1=0、P2=0、P3=1；P3 是 offset 多页候选无法识别页间恰好“删 1、增 1且总数不变”的一致性漂移。后端保存/审核仍会复验并以 409 阻断，不会错账；完整修复需 selector API 提供稳定游标或快照 token，另立任务。
- 项目总残留另有既有 P1：`GET /api/cash-transfers/{billNo}` 没有声明资金转账权限，同 tenant 任意已登录角色可能按单号读取账户、金额和备注。该问题不是 A183 diff 引入，按 `record-and-stop` 登记，未在本批扩大 Controller 修改面。

## 预算与边界

- 可核验执行窗口约为 2026-08-10 03:45–06:45 CST，约 3 小时；其中唯一 full 实际用时 1:32:00.452。
- 提交 3/3（实现、测试清理、归档）；full 1/1；并发只读审查最多 3 条。
- 运行时 Goal token 计量器未启用，实际 token 读数不可用；15,000,000 是治理上限，不能由墙钟时间反推。
- 未修改 migration、金额/库存过账规则、跨币种规则、runner 或 manifest；未操作生产环境，未 push。
- 04:04 网络恢复临时唤醒任务已在收口时删除，不会次日重复执行。

完整证据见 `docs/验收报告/验收报告-A183-资金转账候选与盘点系统库存真实主链修复-20260810.md`。
