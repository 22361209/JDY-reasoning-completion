# 验收报告：G 视觉手感专项

- 日期：2026-06-23
- 范围：B1 列表页、销售订单表单、主导航、模块浮层隐藏状态
- 结论：通过 A1 视觉基础验收；尚未达到金蝶级像素/交互完全贴合

## 本次修正

- 左侧主导航取消“首字放大成图标”的视觉处理，改为纯文字紧凑导航。
- 列表页分页规格已保持为 `200/500/1000`。
- 列头筛选按钮保持在列标题右侧，筛选弹层包含条件、输入框、重置、确定。
- 模块导航浮层关闭时使用 `v-show` 从布局和可访问树测量中隐藏，避免隐藏面板造成负坐标和误判。

## 验收证据

- `frontend npm run build` 通过。
- Playwright 视觉/DOM 检查通过：
  - `navIconCount = 0`
  - `overflowCount = 0`
- 截图：
  - `output/playwright/g-list-filter-1366.png`
  - `output/playwright/g-sales-form-1366.png`
  - `output/playwright/g-sales-form-1920.png`

## 已知差距

- 列顺序拖拽目前可用，但仍不是“点住列标题任意位置即可拖动”的最终手感。
- 表单主数据字段仍以编码输入为主，尚未升级为金蝶式可搜索选择器。
- 当前只做基础视觉无遮挡验收，没有做全量像素差异评分。

## 下一步建议

优先补 C：主数据选择器、采购订单表单范式、页签关闭未保存确认。之后再把 D 的编辑/启停策略和 E/F 的审核、出入库业务闭环接起来。
