# 核心流稳定化追踪（历史入口）

原 2026-06 稳定化队列已经归档到：

- `docs/archive/2026-06-核心流稳定化追踪.md`

本文件不再维护“当前队列”，避免与项目真实状态分叉。

当前状态只看：

- `docs/09-交接清单.md`：唯一当前快照。
- `docs/12-当前批次验收清单.md`：唯一当前任务包。
- `config/effective-feature-scope.json`：当前有效范围。
- `config/feature-delivery-status.json`：A0-A4 交付状态、证据和已知缺口。
- `config/regression-manifest.json`：smoke / area / full 回归清单。

历史中的用户确认、业务口径和批次说明仍保留在归档文件、`docs/验收报告/` 和 Git 历史中；需要形成当前范围差异时，必须经用户确认后写入 `config/implementation-overrides.json`，不能继续以本历史队列作为实施授权。
