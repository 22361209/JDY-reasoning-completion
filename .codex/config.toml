# Project-local Codex guardrail. It is loaded in trusted workspaces.
[agents]
# Cap concurrent agent threads; the root agent must still keep total delegation
# within the task-package budget in AGENTS.md.
max_threads = 3
# Do not allow workers to recursively fan out into additional workers.
max_depth = 1
