# JDY 推理补完：Codex 项目执行约束

## 项目阶段

本项目已从“以 JDY 复刻建立骨架”进入“以当前 ERP 为主体的产品化完善”阶段。

- 用户已确认的业务决策、当前项目的数据一致性/安全/tenant/生命周期规范，优先于 JDY 参考资料。
- JDY 资料用于校准业务术语、信息架构、常用流程、关键交互和视觉基线；不得仅因 JDY 存在某功能而擅自扩大本项目范围。
- 与 JDY 的有意差异必须在当前任务包说明原因，并以产品价值和项目规范验收。

## 必读与边界

1. 先读 `docs/09-交接清单.md`、`docs/12-当前批次验收清单.md` 和本任务直接相关代码；不重读全部历史文档或验收报告。
2. 遵守 `CLAUDE.md` 的业务铁律；范围、交付状态和回归归属分别只认 `config/effective-feature-scope.json`、`config/feature-delivery-status.json`、`config/regression-manifest.json`。
3. 生成物、历史验收报告和已关闭任务包不得为“整理方便”而随意改写；历史不一致应通过索引、状态标签或兼容说明处理。

## 执行预算与停止条件

- 每个任务包必须声明时间预算、Goal token 预算、最大提交数、最大子代理数、允许的 full 门禁次数和明确不做事项；缺失时先补任务包，不开工。
- 默认上限：5 小时、15,000,000 Goal tokens、10 个提交、3 个并发子代理、6 个累计子代理、一次 full 门禁。
- 子代理默认只读；共享文件、迁移、catalog、权限、docs/config 只能由一个明确负责人写入。
- 禁止使用完整历史上下文创建子代理；使用无历史或最近 2–3 轮，并在委派文本中给出必要事实。
- 只修当前 diff 引入的失败。既有或范围外失败必须记录为独立任务，不得在当前任务顺手清仓。
- 到达预算 70%、超过预计时间两倍、出现 3 个范围外失败、3 次上下文压缩或 10 个提交时，停止继续实现，向用户报告并等待决定。
- 每个阶段只运行一次针对性门禁；只在最终验收运行一次 full，未改动时不得重复重跑。

## 交付

- 每个任务独立 review、验收和 commit；push 仅在用户明确要求后执行。
- 交付时报告实际耗时、预算使用、改动范围、执行门禁、已知范围外问题和下一步。
- 详细执行规则见 `docs/guides/codex-execution-policy.md`。
